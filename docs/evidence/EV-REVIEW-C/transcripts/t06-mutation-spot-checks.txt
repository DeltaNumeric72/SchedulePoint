=== REV-C / T06 — probe-quality spot-checks: ONE mutation probe re-run from EACH reviewer ===
2026-08-23T05:14:26Z
HEAD fe0d466

############################################################################
# SPOT-CHECK 1 — REV-A's mutation probe M3 (checker rule), re-run by REV-C #
# REV-A claim: 'All four mutation probes bit.'  M3 = MinimumRestBetween     #
############################################################################
sha256 before : 94483d69aada7902d7630ec9c2143e312bd4145cb98647c181ac5875b91a946b
occurrences of the anchor: 1 (must be exactly 1)
patch applied
sha256 after  : 3ea0d19a28e2b1629b30989cd60dd168fa86bb8ad830edf767c634b3db409db3
--- applied diff ---
diff --git a/packages/domain/src/rules/hard-rule-check.ts b/packages/domain/src/rules/hard-rule-check.ts
index 2697c6c..a27020c 100644
--- a/packages/domain/src/rules/hard-rule-check.ts
+++ b/packages/domain/src/rules/hard-rule-check.ts
@@ -578,7 +578,7 @@ function checkMinimumRestBetween(
       const current = ordered[index];
       if (previous === undefined || current === undefined) continue;
       const rest = current.startsAtMs - previous.endsAtMs;
-      if (rest >= minMs) continue;
+      if (rest >= minMs - 3_600_000) continue;
       findings.push({
         finding: 'breach',
         ruleKey: rule.ruleKey,
--- DETECTOR: the SHIPPED domain test (not REV-A's own probe) ---
$ corepack pnpm exec vitest run --project domain test/rules/hard-rule-check.test.ts

[32m- [[39m
[32m-   "breach:rest_10:s2",[39m
[32m- ][39m
[31m+ [][39m

 ❯ test/rules/hard-rule-check.test.ts:240:39
    238|       ],
    239|     });
    240|     expect(evaluate([rule], subject)).toEqual(['breach:rest_10:s2']);
       |                                       ^
    241|   });
    242| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 39 passed (40)
   Start at  05:14:27
   Duration  1.02s (transform 460ms, setup 0ms, collect 569ms, tests 35ms, environment 0ms, prepare 102ms)

DETECTOR_EXIT=1
--- RESTORE ---
sha256 restored: 94483d69aada7902d7630ec9c2143e312bd4145cb98647c181ac5875b91a946b
RESTORE VERIFIED: byte-identical
(empty above = clean)
MUTATION_DETECTED=YES
2026-08-23T05:14:28Z

############################################################################
# SPOT-CHECK 2 — REV-B's mutation M-07, re-run by REV-C                    #
# REV-B claim: '10/10 sampled assertions load-bearing, 0 vacuous'          #
# M-07 = the CAP-068/T-23 client-host scanner (empty-allowlist enforcement)#
############################################################################
sha256 before : a4ee2025353466fb2c6015ad1b90df775abd73005e36603136639bc8a07f8b65
--- line 49 as it stands (REV-B's anchor) ---
    expect(scan(`fetch("http://localhost:3001/api/health")`).length).toBe(1);
patch applied
--- applied diff ---
diff --git a/scripts/gates/test/network-assertion-guard.test.ts b/scripts/gates/test/network-assertion-guard.test.ts
index 7aae9e3..6fddfcc 100644
--- a/scripts/gates/test/network-assertion-guard.test.ts
+++ b/scripts/gates/test/network-assertion-guard.test.ts
@@ -46,7 +46,7 @@ describe('detects outbound hosts in every shape they arrive in', () => {
   });
 
   it('flags localhost too — a hardcoded dev URL must not ship', () => {
-    expect(scan(`fetch("http://localhost:3001/api/health")`).length).toBe(1);
+    expect(scan(`fetch("http://localhost:3001/api/health")`).length).toBe(0);
   });
 
   it('names the host in the message', () => {
--- DETECTOR ---
$ corepack pnpm exec vitest run --project gates test/network-assertion-guard.test.ts
[31m+ 1[39m

 ❯ test/network-assertion-guard.test.ts:49:70
     47| 
     48|   it('flags localhost too — a hardcoded dev URL must not ship', () => {
     49|     expect(scan(`fetch("http://localhost:3001/api/health")`).length).t…
       |                                                                      ^
     50|   });
     51| 

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯


 Test Files  1 failed (1)
      Tests  1 failed | 25 passed (26)
   Start at  05:14:53
   Duration  511ms (transform 78ms, setup 0ms, collect 84ms, tests 19ms, environment 0ms, prepare 86ms)

DETECTOR_EXIT=1
--- RESTORE ---
sha256 restored: a4ee2025353466fb2c6015ad1b90df775abd73005e36603136639bc8a07f8b65
RESTORE VERIFIED: byte-identical
(empty above = clean)
MUTATION_DETECTED=YES

--- REV-C's reading of REV-B's own count, which looks self-inconsistent and is not ---
REV-B §A t15 says '10/10 load-bearing'; §C-2 says '9 of 9 load-bearing'; §D says
'10 assertions sampled ... 9 run (M-10 separately)'. The transcript resolves it: the driver
was run TWICE — M-01..M-09 in one batch ('9 sampled: 9 load-bearing, 0 vacuous') and M-10
alone in a second ('1 sampled: 1 load-bearing, 0 vacuous'), because M-10 needs the api
project and a database. 9 + 1 = 10. The claim is ACCURATE; the three numbers are three
correctly-scoped views of the same run. No finding.

--- REV-C's assessment of REV-B's mutation DRIVER (code-read of probes/mutation-sample.mjs) ---
 STRONG: sha256 before/after with byte-identical restore asserted per mutation; an anchor-miss
   aborts that mutation as ERRORED rather than silently patching the wrong line; the applied
   diff is printed; the still-green output is dumped when an assertion IS vacuous, so a
   vacuity finding would carry its own evidence.
 ONE WEAKNESS, recorded: 'load-bearing' is decided from the runner's EXIT CODE alone. A
   mutation that broke compilation, or that reddened some OTHER test in the same file, would
   also exit non-zero and be scored load-bearing. In this sample the risk is small — every
   mutation is a value swap on one line and every run reported exactly '1 failed', which is
   the signature of the intended assertion and nothing else — and REV-C's re-run of M-07
   reproduces that same '1 failed' shape. The claim stands; the driver would be stronger if
   it asserted the failing test's NAME. Recorded as a note, not a finding.
2026-08-23T05:14:54Z

############################################################################
# PROBE-QUALITY JUDGEMENTS — three probes sampled from EACH reviewer        #
############################################################################

REV-A, probe 1/3 — transcripts/probe-sources/repro-truth-table.mjs   [RE-RUN by REV-C, t07]
  Claims: drives resultReproducibility over its whole input space, 26 cases.
  Judgement: PROVES WHAT IT CLAIMS. Direct calls into the shipped dist export; the
  promise-sentence column is a regex over the returned detail, not a re-derivation. It
  reproduced row-for-row under REV-C, including both GH-008 residuals and the 8.999999s
  'well inside' sentence. No vacuity: the two constants are printed, so a probe run against
  a build with different thresholds would announce itself.

REV-A, probe 2/3 — transcripts/probe-sources/mutate.sh                [CODE-READ + M3 RE-RUN]
  Claims: apply -> measure -> restore -> verify restore, per mutation.
  Judgement: PROVES WHAT IT CLAIMS, and is the stronger of the two mutation drivers in the
  pair. It ABORTS unless the find-string occurs EXACTLY ONCE (so it cannot silently patch the
  wrong site), prints the applied diff, compares sha256 before/after, and runs git status on
  the file. REV-C re-ran its M3 arm and reproduced REV-A's exact figure (1 failed | 39 passed).

REV-A, probe 3/3 — transcripts/probe-sources/dst-sweep.mjs                     [CODE-READ]
  Claims: 5424 intervals, 23 zones x 3 years x transition dates +/-1 x 16 windows, 0 violations.
  Judgement: PROVES WHAT IT CLAIMS, AND IS NON-VACUOUS BY CONSTRUCTION. It counts and prints
  'gap starts observed: 108' and 'fold starts observed: 93' — a sweep that enumerated the right
  dates but never landed on a transition would report 0 there and be visibly hollow. It exits
  non-zero on any violation (process.exit(failures === 0 ? 0 : 1)), so the exit code is real.

REV-A, probes 4-5 (p2 audit/termination, p3 selection window)   [CODE-READ; RE-RUN in t11]
  ONE DESIGN WEAKNESS WORTH THE ADJUDICATOR'S ATTENTION, common to both: neither arm asserts
  its OUTCOME. p3 ends with expect(movedRows).toBeGreaterThan(0) and expect(before.stale)
  .toBe(false) — non-vacuity only; p2/H ends with expect(typeof accepted).toBe('boolean'),
  which cannot fail. This is a defensible review-probe stance ('REV-A reports; it does not
  decide', stated in both files) and it prevents the probe from failing merely for finding the
  other outcome. But it means REV-A-003 and REV-A-004 rest on READING A PRINTED LINE, not on a
  test result. REV-C therefore re-ran both and read the same lines independently (t11).

REV-B, probe 1/3 — probes/mutation-sample.mjs                 [CODE-READ + M-07 RE-RUN]
  Judgement above (this transcript, spot-check 2). PROVES WHAT IT CLAIMS, with the recorded
  exit-code-only weakness.

REV-B, probe 2/3 — probes/red-case-arms.sh.txt                             [CODE-READ]
  Claims: 13 arms proven in BOTH directions through the runner's own machinery.
  Judgement: PROVES WHAT IT CLAIMS, and its design choice is the right one — it uses the
  SHIPPED shard filter (SP_RED_SHARD=k/65) rather than a bespoke harness, so what it measures
  is what CI measures. It reads PIPESTATUS rather than the tail's exit, and it runs
  git status --porcelain after EVERY arm with the evidence paths excluded, which is what makes
  the NR-14 clean-tree property checkable from outside. REV-C verified the shard arithmetic
  claim independently: with 65 registered arms, index % 65 == k selects exactly index k.

REV-B, probe 3/3 — probes/privacy-log-leak.probe.test.ts.txt                [CODE-READ]
  Claims: a marker driven through six wire positions, hunted in the server's own log, with a
  planted-needle falsifiability control.
  Judgement: PROVES WHAT IT CLAIMS — AND REV-B GRADED ITS OWN RESULT CORRECTLY. The probe
  carries BOTH guards a hunt of this shape needs: a non-vacuity assertion
  (expect(harness.logs.length).toBeGreaterThan(0)) and a separate control arm that plants a
  needle through harness.app.log.info and requires the same hunt to find it. REV-B then
  declined to call the result a pass because only ONE log line was captured across six
  requests — a thin haystack — and filed it as a NOTE with that caveat in the finding text.
  That is the correct grade, and REV-C endorses both the probe and the restraint.

############ REV-B-005 — CONFIRMED, and its root cause is deeper than reported ############

REV-B reported that .ts/.sh under docs/ turns the two doc validators red. CONFIRMED by
REV-C from the validator sources:
  $ sed -n "166,169p" docs/fable/validate.py
        if os.path.splitext(f)[1].lower() in (".ts", ".tsx", ".js", ".sql",
                                              ".tf", ".dockerfile") \
           or (f.endswith(".py") and f != "validate.py"):
            bad_code.append(os.path.relpath(os.path.join(root, f), REPO))
  $ sed -n "456,457p" docs/architecture/validate.py
CODE_EXT = {".ts",".tsx",".js",".jsx",".sql",".py",".yaml",".yml",".tf",".dockerfile",
            ".sh",".rb",".go",".java",".cs",".proto",".prisma"}

WHAT REV-B DID NOT NOTICE. Architecture assertion 52a defines an allowlist that NAMES the
evidence tree, and then never consults it:
  $ sed -n "467,479p" docs/architecture/validate.py
ALLOWED_IMPL_ROOTS = ("spikes", "apps", "packages", "solver", "scripts",
                      ".github", "docs/evidence")
for root, dirs, files in os.walk(REPO):
    dirs[:] = [d for d in dirs if d not in SKIP_DIRS]
    for f in files:
        ext = os.path.splitext(f)[1].lower()
        full = os.path.join(root, f)
        rel = os.path.relpath(full, REPO)
        if ext in CODE_EXT \
           and os.path.basename(full) not in ("validate.py", "validate.sh") \
           and (rel.startswith("docs/") or rel.startswith("schedulepoint-research/")):
            arte.append(rel)
check("52a. Documentation and research trees contain no application code",

  ALLOWED_IMPL_ROOTS includes "docs/evidence" — but 52a's condition is
  'rel.startswith("docs/")' with no reference to ALLOWED_IMPL_ROOTS at all. The variable is
  consulted ONLY by 52b:
  $ grep -n "ALLOWED_IMPL_ROOTS" docs/architecture/validate.py
467:ALLOWED_IMPL_ROOTS = ("spikes", "apps", "packages", "solver", "scripts",
488:    if not any((entry + "/").startswith(a.split("/")[0] + "/") for a in ALLOWED_IMPL_ROOTS):
  and 52b iterates os.listdir(REPO) filtered by os.path.isdir — top-level DIRECTORIES only.

  Two consequences:
   1. A reviewer who reads the allowlist concludes docs/evidence is exempt from 52a. It is
      not. That is exactly the trap REV-B fell into, and the reason REV-B-005 exists.
   2. Because 52b only looks at directories, a stray FILE at the repository root is invisible
      to it — which is why the tracked '=' file (REV-C-007) has survived every run of this
      validator since 2026-08-05.
  Recorded as REV-C-008 (NOTE), attached to REV-B-005 rather than replacing it.
2026-08-23T05:22:53Z
