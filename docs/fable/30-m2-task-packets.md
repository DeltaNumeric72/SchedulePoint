# 30 — M2 Task Packets (finalized)

**Status: finalized 2026-08-03 on the owner's M2 authorization, against frozen M1 checkpoint `b051193`. All three packets NOT ISSUED at authoring time; issuance is recorded per packet below and in the task log.** Every packet inherits the runbook ([17-opus-agent-runbook.md](17-opus-agent-runbook.md)) including the thirteen non-bypass rules, the return-report format, worktree discipline, the standing merge/port-hygiene and standalone-verification disciplines ([control/OPUS-AGENT-RUNBOOK.md](control/OPUS-AGENT-RUNBOOK.md)), [24-execution-standards.md](24-execution-standards.md) §§E–F, and the FAD-7 environment substitutions (embedded-postgres, corepack pnpm, no Docker builds on this machine).

These packets supersede the *proposals* in [29-m1-exit-report.md](29-m1-exit-report.md) §11 by expanding them to full runbook format under the owner's M2 authorization of 2026-08-03, which also governs where it is more specific than this document.

**Execution order:** OPUS-M2-001 first and alone. OPUS-M2-002 and OPUS-M2-003 are **blocked until M2-001 is accepted and merged**, are revalidated against M2-001's actual outcome before issuance, and then run in parallel worktrees with disjoint globs. All three are **critical class: independent second review mandatory.**

---

## OPUS-M2-001 — SBX harness, G-ARCH tenancy subset, NR-13 fixture-isolation decision, MULTI provisioning

| Field | Content |
|---|---|
| **Task ID** | OPUS-M2-001 |
| **Milestone / Slice** | M2 / evidence infrastructure (clears the M1 carried item) |
| **Status** | ISSUED 2026-08-03 (issuance recorded in the task log) |
| **Objective** | Three deliverables, in this order: **(A)** the NR-13 fixture-isolation decision, measured then implemented; **(C→B)** the MULTI multi-organization fixture environment, then the SPEC-16 SBX evidence harness executing the G-ARCH tenancy subset SBX-001/002/004/005/006 against the M1 kernel |
| **User outcome** | none directly (evidence and test infrastructure); indirectly, every later feature ships on provably isolated tenancy with per-scenario evidence |
| **Engineering outcome** | The 0-of-39 SBX execution count starts moving with durable artifacts; the shared-fixture coupling class (NR-13) is structurally closed before feature suites multiply; MULTI exists for every subsequent multi-org test |
| **Dependencies** | M1 complete and merged (checkpoint `b051193`). No other task may run concurrently with this one |
| **Relevant documents** | [SPEC-16](../architecture/specs/SPEC-16-sbx-evidence-contracts.md) (nine contract fields, EVIDENCE_BLOCKED, meta-test); research report [18-targeted-sandbox-test-plan.md](../../schedulepoint-research/reports/18-targeted-sandbox-test-plan.md) §5.1 (SBX-001..006 as authored); [16-testing-and-environments.md](../architecture/16-testing-and-environments.md) §3 (MULTI definition); SPEC-01 (isolation batteries + byte-identity discipline); SPEC-06 (evaluator + entitlement gating); SPEC-11 §Maintenance-plane (FAD-14 sanctioned cross-tenant read); RISK-REGISTER NR-13 entry |
| **Allowed files** | `scripts/sbx/**` (new — harness, MULTI provisioning, contract declarations, reports); `apps/api/test/**` and `packages/*/test/**` and `apps/api/src/testing/**` (fixture-isolation refactor + test-support seams); vitest/test configuration files; root `package.json` (script entries only); `docs/evidence/EV-M2-NR13/**`, `docs/evidence/EV-M2-SBX/**`; `docs/dev-setup.md` (append-only) |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**`, `schedulepoint-research/**`, `spikes/**`, `apps/web/**`, `solver/**`, `migrations/**` (**a needed schema change is an escalation, not an edit**), `scripts/gates/**` and `scripts/red-cases/**` except **adding** new red-case directories (never modifying existing gates), production runtime source outside declared test-support seams (any production-code change beyond a seam is an escalation) |
| **Required implementation — A. NR-13 (first, before anything else lands)** | A **measured** comparison on the real 551-test suite (or a representative sworn subset with the sampling disclosed): (1) per-test transactional rollback; (2) dedicated organizations/fixtures per mutating suite; (3) any hybrid the evidence supports. Measure at minimum: wall-clock full suite, wall-clock standalone worst files, isolation guarantees, compatibility with the embedded-postgres-per-worktree model, compatibility with concurrent agents/developers, migration cost across the 36 existing files. The selected approach must: eliminate test-order dependence; make each test own its state; support standalone AND full-suite execution; prevent hidden dependence on queues, clocks, ports, migrations, processes, or prior suites; define fixture ownership; define cleanup responsibility; work with embedded PostgreSQL and worktrees; support concurrent activity; include **regression tests reproducing the prior coupling class** (the three couplings point-fixed in M1-004 are the reference class — an order-shuffled full run and per-file standalone runs must both pass). The decision is **proposed with measurements in the return report and ratified by Fable as a FAD entry before the refactor merges** |
| **Required implementation — C. MULTI (before B)** | A deterministic, scripted MULTI fixture per doc 16 §3: **≥2 organizations, ≥2 groups each, one user holding different roles per membership**, plus users covering every SPEC-06 role, suspended/revoked membership cases, entitlement-on and entitlement-off organizations. Provisioning is idempotent and deterministic (two provisions from clean produce canonically identical content, timestamps excepted); synthetic data only; provisioning script is the single fixture owner. Must demonstrably support: ≥2 orgs · multiple groups per org · differing memberships · cross-org deny paths · cross-group permission paths · org-scoped administration · group-scoped operations · maintenance-plane testing · deterministic creation · standalone execution |
| **Required implementation — B. SBX harness + G-ARCH tenancy subset** | A harness under `scripts/sbx/` in which **every SBX test declares all nine SPEC-16 contract fields** (owner, fixture provenance, deterministic setup, external dependency, fault controls, objective oracle, retained artifact, environment, earliest execution point) — a test missing any field is not runnable and the harness says so. Scenario states: `PASS` / `FAIL` / `EVIDENCE_BLOCKED(<named dependency>)` — **a blocked scenario is never a pass and never silently skipped** (SPEC-16 §7). The harness must: use synthetic data only; exercise the real tenant tables under the real roles; **detect vacuous tests** (every scenario carries a falsifiability probe — a seeded violation or disabled-control variant that must make it fail; a scenario that cannot be made to fail is reported as vacuous and fails the run); test org and group isolation, reads and writes, direct database access under each application role, missing tenant context, invalid/forged tenant context, context switching, pool reuse across contexts, and the maintenance-plane exception (confirming it is **explicit, narrow, SELECT-only, `app_worker`/`app_migrator` only, exactly as FAD-14 pins it — and that no second cross-tenant path exists**); produce durable evidence artifacts per scenario. **Scenario scoping against the M1 kernel:** **SBX-001** = role × registered-route/surface matrix over MULTI (every route explicit allow or clean deny; denials leak nothing; byte-identity where SPEC-01 requires it). **SBX-002** = its capability mapping (CAP-006/CAP-057): the grant × entitlement × role gating truth table proven server-side — *the picklist-specific arm re-executes at the picklist milestone; this reinterpretation is recorded in the report, and doubt about it is an escalation*. **SBX-004** = the full cross-tenant sweep: every resource type in the current schema × org boundary × group boundary × (API surface AND role-scoped direct SQL); pass = 100% denial with no not-found/not-permitted distinction. **SBX-005/006** = contracts declared in full; sub-scenarios whose subjects exist (e.g. membership suspension/deactivation history retention) execute; sub-scenarios requiring the authn/session/invitation/impersonation subsystem (not yet built — no M1 packet contained it) report `EVIDENCE_BLOCKED(authn milestone)` per SPEC-16 §7. **G-ARCH is NOT closed by this task** (it also needs SBX-011/013/014b/022/023/028 at later milestones); this task clears the tenancy subset and files the evidence |
| **Product requirements** | none (infrastructure task); the report's scenario table is the product |
| **Architecture constraints** | Layering unchanged; harness lives outside `apps/`/`packages/` production graphs; no new runtime dependency in production code; no gate weakened (non-bypass rule 10); FAD-14's maintenance-plane exception is verified, not extended |
| **Tenant-isolation requirements** | SBX-004 sweep zero cross-tenant rows in any probe; any wrong-tenant row = task failure. The harness itself must run inside the unit-of-work discipline when it touches tenant tables as an application role (probing *around* it deliberately, as each role, is the point of the sweep and is done via the harness's declared probe connections, never via production code paths) |
| **Authorization requirements** | SBX-001/002 exercise the SPEC-06 evaluator as shipped; no new routes; no policy changes. Any authorization gap the sweep finds is reported (and is a candidate blocking finding), never patched in this task |
| **Audit requirements** | No new mutations requiring audit (fixture provisioning writes through the normal audited paths where they exist, or is disclosed as direct seeding with rationale); the audit chain must verify clean after every harness run (`0 / 1 / 0` shape) |
| **Migration requirements** | None expected. Any needed schema change = escalation before writing it |
| **UI requirements** | none (no UI in this task) |
| **Accessibility requirements** | not applicable (no UI); existing axe gate must remain green |
| **Tests** | The NR-13 regression class (order-shuffled + standalone); the SBX subset itself; MULTI determinism test (double-provision comparison); meta-test per SPEC-16 §7 (every declared fixture provisions from clean; every declared artifact is produced) |
| **Red-case tests** | (1) With one RLS policy disabled inside a rolled-back probe transaction, SBX-004 demonstrably fails; (2) a deliberately vacuous scenario (assertion that cannot fail) is caught by the vacuity detector and fails the run; (3) an `EVIDENCE_BLOCKED` scenario forced on a gate-required test makes the subset run exit non-zero (blocked ≠ pass); (4) a test written with hidden order-dependence (reference implementation of the old coupling class) fails under the new fixture strategy |
| **Named-condition proofs** | (1) SBX-004 zero-cross-tenant sweep; (2) vacuity-detector efficacy; (3) order-independence of the full suite under the NR-13 strategy; (4) MULTI double-provision determinism. **Each runs standalone, in its package suite, and in the complete battery, and all three results are reported** (standing discipline) |
| **Standalone verification** | Every named-condition proof file above; plus every test file the NR-13 refactor touches must pass standalone (spot-checked ≥10 files by the implementer, full sweep by the orchestrator at acceptance) |
| **Acceptance criteria** | NR-13 measurements captured + decision proposed (and ratified as a FAD before merge) + refactor implemented + regression class green shuffled and standalone · MULTI provisioned, deterministic, all ten support points demonstrated · SBX-001/002/004 **PASS with zero cross-tenant rows and zero unexplained denials-that-leaked**; SBX-005/006 contracts declared with every sub-scenario PASS or EVIDENCE_BLOCKED(named) · report contains: scenarios required / executed / passed / failed / blocked, tables exercised, policies exercised, vacuous assertions detected, evidence paths · `corepack pnpm check` 12/12 · `pnpm red-cases` all proven (including the new ones) · audit chain verifies clean post-run · worktree finishes clean with no lingering cluster |
| **Commands to run** | `corepack pnpm install` · `corepack pnpm check` · `pnpm red-cases` · the NR-13 benchmark command(s) · the MULTI provision command (twice, with the comparison) · the SBX subset command · each named-condition proof standalone — all with captured output |
| **Required evidence** | `docs/evidence/EV-M2-NR13/` (measurements, decision inputs, regression proofs) and `docs/evidence/EV-M2-SBX/` (per-scenario artifacts, the scenario table, MULTI provisioning log), each with INDEX.md |
| **Escalation conditions** | A schema or production-code change (beyond declared test seams) appears necessary · the SBX-002 reinterpretation seems to contradict SPEC-16 or the capability mapping · NR-13 evidence favors an approach requiring architectural change · any SBX scenario's blocked/executable boundary is ambiguous · embedded-postgres per-worktree behaviour diverges under the new fixture strategy · any isolation doubt whatsoever (a suspected leak is a stop-and-report, not a retry) |
| **Rollback instructions** | No migrations, so rollback = `git revert` of the squash-merge. The NR-13 refactor must be revertible as one unit: if reverted, the old fixture behaviour returns intact (no test deleted, none weakened). State this explicitly in the return report |
| **Fable acceptance checklist** | Re-run every command from clean · diff strictly within allowed globs · verify the NR-13 decision matches the measurements (not preference) · re-run the shuffled-order proof with a fresh seed of my choosing · re-run SBX subset and diff the scenario table against the claimed one · verify EVIDENCE_BLOCKED entries name real dependencies and nothing executable was blocked · verify the vacuity detector catches a vacuous case I write myself · verify MULTI determinism · full battery + standalone sweep of every named-condition proof · confirm no gate or existing test was weakened (diff-level check) |
| **Independent-review checklist** | Probe with reviewer-authored SQL and API calls, not by reading tests: attempt cross-tenant reads/writes as every role against MULTI · attempt to make an SBX scenario pass vacuously (delete its subject; does it still pass?) · mutation-test the vacuity detector itself · attempt order-dependence reintroduction (write a coupled pair; is it caught?) · verify the maintenance-plane exception is the only cross-tenant path (search for others independently) · verify fixture ownership/cleanup claims by killing a run mid-flight and inspecting residue · verify standalone claims by running files standalone yourself · check the NR-13 measurement methodology for bias (warm caches, unequal baselines) · confirm no production-code change hides in the test diff |

**Independent second review: MANDATORY** (isolation-evidence critical class).

---

## OPUS-M2-002 — Shift-type catalogue, shift groups, valid combinations (first capability-bearing, first UI-bearing slice)

| Field | Content |
|---|---|
| **Task ID** | OPUS-M2-002 |
| **Milestone / Slice** | M2 / scheduling-structure catalogue |
| **Status** | **ISSUED 2026-08-03** — revalidated against the actual OPUS-M2-001 outcome (§7 below); FAD-16 doc-06 amendment in force |
| **Objective** | The shift-type catalogue, shift groups, staff groups, valid combinations, per-shift-type weekday+holiday demand, and the group holiday calendar — schema, domain, contracts, authorized+audited API, and the product's **first UI surface** — preserving the source product's observed shift-definition concepts through the approved modern domain model, never by copying the legacy schema |
| **User outcome** | A scheduler (or group admin) defines shift types with their full observed semantics, bundles them into shift groups with scoring modes and request-off semantics, constrains legal shift/position pairings, sets per-weekday and holiday demand defaults, and maintains the group holiday calendar — all validated, audited, and tenant-isolated |
| **Engineering outcome** | First rows move on the feature-parity matrix; the UI foundation (form pattern, validation summary, list pattern, SPEC-14 matrix discipline) exists for every later surface; the M4 engine's demand and catalogue inputs exist |
| **Dependencies** | OPUS-M2-001 accepted and merged (fixture strategy + MULTI + SBX harness are its substrate); SP-E UX brief ([28-sp-e-ux-brief.md](28-sp-e-ux-brief.md)) governs; **orchestrator pre-deliverable at issuance: dated additive amendment to doc 06 §3.2 recording the extended `shift_types` field set and the demand/holiday tables, with a FAD entry** |
| **Relevant documents** | [09-domain-model.md](09-domain-model.md) §1 scheduling structure; [06-data-architecture.md](../architecture/06-data-architecture.md) §3.2 (as amended at issuance); [05-independent-product-specification.md](05-independent-product-specification.md) §3; research [01-application-map.md](../../schedulepoint-research/reports/01-application-map.md) ADM-07/08/09 and [05-scheduling-engine.md](../../schedulepoint-research/reports/05-scheduling-engine.md) §1 (observed field inventories; **read-only reference — never copy names of persons/orgs/sites**); SPEC-01, SPEC-06, SPEC-11 (kernel contracts); SPEC-14 + [28-sp-e-ux-brief.md](28-sp-e-ux-brief.md) (UI); glossary report 12 (terminology is exact) |
| **Allowed files** | `apps/api/src/catalogue/**` (new), route registration touchpoints, `packages/domain/src/catalogue/**`, `packages/contracts/src/catalogue/**`, `apps/web/src/**` (catalogue surfaces + shared form/list foundation components it creates), `migrations/**` (new, numbered at merge), tests for each, `docs/evidence/EV-M2-CATALOGUE/**`, SPEC-14 component-matrix update file if it lives in-repo under `apps/web` |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**`, `schedulepoint-research/**`, `spikes/**`, `solver/**`, `scripts/gates/**`, applied migrations (additive only), `apps/api/src/profiles/**` (M2-003's parallel scope), the SBX harness (`scripts/sbx/**`) except adding catalogue scenarios in a clearly separated directory if the packet's tests need them, **`shift_type_qualifications` (integration-packet scope — do not create it)** |
| **Required implementation** | Migrations (reversible, RLS in the same migration, composite tenant FKs, tenant-qualified uniques): **`shift_types`** — code/short name, full name, description, display metadata (**a constrained palette key + text-style enum validated against the curated accessible set — never a free hex value**; SP-E token discipline extends to user-chosen colours), start/end time + `crosses_midnight`, `is_on_call`, `attracts_stipend`, `is_manual_only`, `is_daily_pick`, `include_in_statistics`, `is_leave_of_absence`, `report_order`, `allow_on_request`, `allow_off_request`, `credit_weight`, state active/retired, archive-not-delete while referenced, effective dating per the doc-06 versioning rule; **`shift_groups`** — name, member shift types, `scoring_mode ∈ {hard, weighted}`, weight, `allow_request`, `request_off_label`; **`staff_groups`**; **`valid_groups`** — shift-type set × allowed pick positions, with the **group-level pick-position count as a monotonically-increasing-only setting (trigger/CHECK-enforced)** per the observed constraint (research ADM-07); **`shift_type_weekday_demand`** — per shift type × {Mon..Sun, holiday} default demand counts (the M4 engine input; owner-mandated in this slice); **`group_holidays`** (CAR-011) with minimal authoring. Domain services + typed contracts; routes with SPEC-06 policies (allow AND deny per capability); audit emission on every mutation inside the unit of work (FAD-12 ordering); **a field-mapping table in the return report: every observed concept from the owner's list → its column(s)/mechanism, with every deliberate divergence from the legacy shape stated and justified** (e.g. request-off text living on shift groups per the observed model; "start day" semantics carried by `crosses_midnight` + the demand weekday dimension if that is the chosen mapping). Request-eligibility flags land as enforced data (server-side filters on the surfaces that exist); the full request lifecycle is M5 and the report says so |
| **Product requirements** | The owner's observed-concept list is binding: short+full names · description · colour and typography metadata · start day and time · end time · on-call · stipend · manual-entry · daily-pick · statistics visibility · leave-of-absence · report ordering · ON and OFF request eligibility · request text · optimization flags and weights · weekday and holiday demand · archiving and effective dating. Preserve the functionality through the approved model — mechanical schema copying is a review-rejectable defect |
| **Architecture constraints** | Layering (domain imports nothing; web imports contracts only); no third-party host from the client (allowlist stays empty); I-10 one action one request; new UI dependencies require licence + maintenance note and are the **only** lockfile changes permitted in the parallel pair |
| **Tenant-isolation requirements** | All new tables group-scoped tenant data: RLS + policy in the same migration; every statement through the unit of work; catalogue SBX-style cross-tenant probes green; zero wrong-tenant rows in any probe |
| **Authorization requirements** | New capabilities registered and evaluated through the SPEC-06 evaluator (no parallel gate); deny paths tested for **every** new capability; module/entitlement mapping declared (escalate if the module taxonomy is ambiguous — do not invent modules); I-19 re-evaluation inside the mutation's unit of work |
| **Audit requirements** | Every mutation lands one audit row in the same unit of work: actor, on-behalf-of, before/after, mechanism, correlation id; chain verifies clean after the suite |
| **Migration requirements** | Additive only; reversible with stated down-migrations; up→down→up clean including the new tables; RLS pairing gate green; numbering assigned at merge (second merger rebases) |
| **UI requirements** | First UI-bearing slice, governed by [28-sp-e-ux-brief.md](28-sp-e-ux-brief.md) in full: nothing persists before Save (I-13 — no optimistic creation); forms with label + `aria-describedby`, `role="alert"` validation summary with field links; explicit loading/empty/error/validation/permission-denied states for every surface; server-authoritative verdicts; context-stale interruption honoured; tokens only (a hard-coded colour/size is review-rejectable); shift-type colour rendering never uses colour as sole carrier |
| **Accessibility requirements** | SPEC-14 matrix rows added for every new component with evidence per cell; axe green at desktop + mobile viewports; keyboard-complete (tab order, focus management on dialogs/validation); 320px reflow; ≥44px targets; forced-colors safe; reduced-motion honoured |
| **Tests** | Per-capability allow+deny; RLS probes for every new table (reads and writes, org and group boundary); archive-not-delete enforcement (delete attempt on referenced rows fails); monotonic pick-position proof (decrease rejected at the database); demand upsert semantics; holiday calendar uniqueness; UI states exercised at both viewports; contract round-trips; **fixture discipline per the M2-001 strategy — no order dependence, standalone-clean** |
| **Red-case tests** | New tenant table without RLS fails the gate (exercised for one of this task's tables in a red-case proof); route without policy fails the gate; a hard-coded hex colour in a component fails review tooling if such a lint exists or is added as a red case; a mutation bypassing audit emission fails the emission-coverage check |
| **Named-condition proofs** | (1) Published-referenced shift type cannot be hard-deleted (database-level); (2) pick-position monotonicity under concurrent increase attempts; (3) cross-tenant catalogue sweep zero rows; each standalone + package + full battery, both results reported |
| **Standalone verification** | Every named-condition proof file; every new test file spot-checked standalone |
| **Acceptance criteria** | Field-mapping table complete with zero silently dropped observed concepts · all tests green (`corepack pnpm check` 12/12, red cases proven) · SPEC-14 matrix updated with evidence · axe green both viewports · deny path per capability captured · audit chain clean · parity-matrix rows for CAP-011/CAP-012 (and the CAP-004 holiday slice) move with evidence links · migration cycle clean · zero cross-tenant rows |
| **Commands to run** | `corepack pnpm install` · `corepack pnpm check` · `pnpm red-cases` · migration cycle · catalogue test suites + standalone proofs · axe/e2e at both viewports — all captured |
| **Required evidence** | `docs/evidence/EV-M2-CATALOGUE/` with INDEX.md: harness output, field-mapping table, screenshots of every UI state at both viewports, axe output, deny-path captures |
| **Escalation conditions** | An observed concept cannot be expressed in the approved model without a schema shape not in this packet · the module/entitlement taxonomy for catalogue capabilities is ambiguous · SPEC-14 and the brief conflict on any component · the demand model seems to belong to M4 rather than here · any isolation or authorization doubt · UI foundation choices that would bind M2-003's later surfaces (report, don't guess) |
| **Rollback instructions** | Down-migrations stated per migration; `git revert` of the squash-merge is executable in dev; UI components have no persisted state beyond the schema, so schema rollback is complete rollback. Confirm the down path in the migration-cycle evidence |
| **Fable acceptance checklist** | Re-run everything from clean · diff strictly within globs (especially: no `profiles/**`, no `shift_type_qualifications`) · walk the field-mapping table against the owner's list item by item · exercise the UI myself at both viewports including every error/denied state · attempt a cross-tenant read and a deny-path bypass myself · verify I-13 (no persistence before Save) by network inspection · verify audit rows for each mutation type · verify parity-matrix moves match shipped evidence |
| **Independent-review checklist** | Reviewer-authored DB probes: cross-org and cross-group reads/writes on every new table as every role · attempt hard-delete of referenced catalogue rows at the database · attempt pick-position decrease concurrently · mutation-test: disable the audit emission for one mutation — does a test fail? disable one RLS policy — does the sweep fail? · verify effective-dating reads select the in-force row (the M1 entitlement lesson applies to any effective-dated catalogue read) · probe the UI's server trust (forge a client-side allow; server must deny) · verify no schema mechanical copy (column-by-column against the legacy inventory, checking the divergences are real) · fixture independence and standalone execution of the new suites · axe + keyboard walk yourself, don't trust screenshots |

**Independent second review: MANDATORY** (first tenant-facing mutations on the new kernel + first UI surface).

---

## OPUS-M2-003 — Effective-dated work profiles, FTE targets, qualifications

| Field | Content |
|---|---|
| **Task ID** | OPUS-M2-003 |
| **Milestone / Slice** | M2 / staffing parameters |
| **Status** | **ISSUED 2026-08-03** — revalidated against the actual OPUS-M2-001 outcome (§7 below); runs in parallel with OPUS-M2-002 (disjoint globs re-verified) |
| **Objective** | Effective-dated work profiles with FTE and weekday/holiday targets and maximum assignments (`membership_work_profiles` + `membership_weekday_fte`, CAR-006), and qualifications with expiry and evidence references (`qualifications` + `qualification_holdings`, PO-DEC-12 default) — schema, domain, contracts, authorized+audited API. **API-first: no web UI in this packet** (rationale: the UI foundation is M2-002's parallel deliverable; profile surfaces follow once it exists — recorded as a deliberate sequencing decision, not a dropped capability) |
| **User outcome** | (via API now, UI next) an administrator records each member's work profile — FTE, per-weekday and holiday targets, maximum assignments — effective-dated with full history; grants qualifications with expiry that will gate scheduling eligibility |
| **Engineering outcome** | The M4 engine's people-side inputs exist; the effective-dating pattern (EXCLUDE non-overlap + single in-force loader) becomes the reusable reference implementation; CAP-013 and CAP-058 parity rows move |
| **Dependencies** | OPUS-M2-001 accepted and merged; parallel with OPUS-M2-002 — **`shift_type_qualifications` (the join to M2-002's tables) is explicitly OUT of both packets and lands in the integration packet** |
| **Relevant documents** | [09-domain-model.md](09-domain-model.md) §1; [06-data-architecture.md](../architecture/06-data-architecture.md) §3.2 rows for `membership_work_profiles`-family, `qualifications`, `qualification_holdings`; [05-independent-product-specification.md](05-independent-product-specification.md) §3; PO-DEC-12 (default: administrator-granted, patient-safety adjacent); SPEC-01/06/11; migration 0002's EXCLUDE-constraint pattern (the in-repo precedent); **EV-M1-AUTHZ finding S-01 (the arbitrary-row entitlement read) — the lesson this packet exists to reuse** |
| **Allowed files** | `apps/api/src/profiles/**` (new), route registration touchpoints, `packages/domain/src/profiles/**`, `packages/contracts/src/profiles/**`, `migrations/**` (new, numbered at merge), tests, `docs/evidence/EV-M2-PROFILES/**` |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**`, `schedulepoint-research/**`, `spikes/**`, `apps/web/**`, `solver/**`, `scripts/gates/**`, applied migrations, `apps/api/src/catalogue/**` and `packages/*/src/catalogue/**` (M2-002's parallel scope), `shift_type_qualifications` (integration packet), **no lockfile changes** (a needed dependency is an escalation) |
| **Required implementation** | Migrations (reversible, RLS-in-same-migration, composite tenant FKs): **`membership_work_profiles`** — membership-scoped, effective start/end, work percentage/FTE, maximum assignments, **EXCLUDE constraint guaranteeing non-overlap per membership** (0002's pattern); **`membership_weekday_fte`** — per weekday {Mon..Sun} + holiday targets, keyed to the profile row per CAR-006; **`qualifications`** — key, name, `requires_expiry`, issuing body; **`qualification_holdings`** — membership × qualification × valid_from, `valid_until`, `evidence_ref`, status ∈ {pending, valid, expiring, expired, revoked}, **`SENSITIVE-PII` classification honoured with narrower read access**, retained after expiry for audit. **One shared in-force loader** (single implementation, used by every reader AND every writer's precondition checks): deterministic "row in force at instant T" selection — never an arbitrary row, never `LIMIT 1` without a total order, proven at boundaries (instant of change, future-dated rows present, gaps, first/last row). Writers: future-dated changes supported; historical rows never rewritten (supersede via date-bounded rows); administrative corrections create new rows with audit trail, never UPDATE history; overlap attempts rejected by the database. Typed contracts; SPEC-06 policies with allow+deny per capability; audit emission per mutation inside the unit of work |
| **Product requirements** | Effective start and end dates · one authoritative row in force at a time · historical preservation · future-dated changes · deterministic in-force-now loading · no arbitrary-row selection · overlap prevention · FTE targets · weekday and holiday targets · maximum assignments · qualifications with expiry · group scope · audit history · authorization · safe administrative updates. All owner-mandated |
| **Architecture constraints** | Layering; pure domain logic for the in-force selection (unit-testable without a database, mirrored by SQL-level tests); no client-facing surface (API only, this packet) |
| **Tenant-isolation requirements** | All tables tenant data under RLS in the same migration; membership-scoped rows respect group scope; zero wrong-tenant rows in any probe |
| **Authorization requirements** | New capabilities through the SPEC-06 evaluator; deny path for **every** new capability; qualification holdings' SENSITIVE-PII narrows read grants (who may read another member's holdings is role-gated and tested); I-19 in-transaction re-evaluation |
| **Audit requirements** | Every mutation audited (actor, on-behalf-of, before/after, mechanism, correlation id) in the same unit of work; qualification revocation and administrative corrections are first-class audited events; chain verifies clean |
| **Migration requirements** | Additive, reversible, cycle-clean; EXCLUDE constraints proven by rejection tests; numbering at merge (second merger rebases) |
| **UI requirements** | none (API-first; recorded above) |
| **Accessibility requirements** | not applicable (no UI); axe gate stays green |
| **Tests** | In-force loader boundary battery (instant-of-change, future-dated, gap, first/last, timezone-explicit instants); **loader/writer agreement proof: the row the writer's precondition saw is the row the reader returns, inside one unit of work** (the M1 S-01 lesson as a named test); overlap rejection (database-level); history-rewrite rejection; per-capability allow+deny; RLS probes per table; SENSITIVE-PII read narrowing; round-trip authoring (create → future-date → supersede → read history) via the API; fixture discipline per M2-001 strategy |
| **Red-case tests** | Tenant table without RLS fails the gate; route without policy fails the gate; a loader variant using arbitrary-row selection (the S-01 defect, reintroduced deliberately in a red case) is caught by the boundary battery; an UPDATE to a historical profile row is rejected |
| **Named-condition proofs** | (1) In-force determinism under adjacent/future-dated rows; (2) loader/writer agreement; (3) overlap-rejection under concurrent inserts; each standalone + package + full battery, both reported |
| **Standalone verification** | Every named-condition proof; every new test file spot-checked standalone |
| **Acceptance criteria** | Round-trip authoring proven end-to-end with captured output · deny path per new capability captured · in-force battery green including all boundary cases · overlap and history-rewrite rejections proven at the database · `corepack pnpm check` 12/12 · red cases proven · migration cycle clean · audit chain clean · zero cross-tenant rows · CAP-013/CAP-058 parity rows move with evidence |
| **Commands to run** | `corepack pnpm install` · `corepack pnpm check` · `pnpm red-cases` · migration cycle · profile suites + standalone proofs — all captured |
| **Required evidence** | `docs/evidence/EV-M2-PROFILES/` with INDEX.md |
| **Escalation conditions** | The weekday-FTE shape in doc 06 proves insufficient for the owner's target list · qualification semantics need a decision beyond PO-DEC-12's default · any effective-dating case where determinism cannot be guaranteed · a needed dependency or lockfile change · any isolation/authorization doubt · anything requiring M2-002's tables |
| **Rollback instructions** | Stated down-migrations; `git revert` executable in dev; qualification holdings carry PII — the down-migration's data destruction is acceptable in dev and stated as such |
| **Fable acceptance checklist** | Re-run everything from clean · diff strictly within globs (no catalogue files, no lockfile delta, no `shift_type_qualifications`) · walk the in-force battery cases against the packet's boundary list · attempt the S-01 defect myself (insert adjacent effective-dated rows, read at boundaries) · attempt history rewrite as each role · verify SENSITIVE-PII narrowing by probing as each role · verify audit rows per mutation type · confirm the API-first scope note appears in the parity-matrix update |
| **Independent-review checklist** | Reviewer-authored probes: concurrent overlapping-insert races at the database · boundary reads at exact effective instants (inclusive/exclusive edges) · loader/writer divergence hunt (find any code path selecting profile rows outside the shared loader) · cross-tenant and cross-group sweeps on all four tables as every role · SENSITIVE-PII leakage probes (holdings readable by peers?) · mutation-test the EXCLUDE constraint and the history-rewrite rejection · verify future-dated writes cannot alter the currently-in-force verdict retroactively without audit · fixture independence + standalone runs · confirm no arbitrary-row (`LIMIT 1` sans total order) pattern anywhere in the diff |

**Independent second review: MANDATORY** (effective-dating + kernel-adjacent critical class).

---

## 5. Shared integration surfaces (pre-assigned before any parallel issuance)

| Surface | Ownership |
|---|---|
| **Shared migrations / numbering** | None shared; each task's migrations are additive and numbered at merge — the second merger rebases and renumbers. A migration both tasks need is an escalation, not a shared edit |
| **`shift_type_qualifications`** (M2-002 × M2-003 join) | **Integration packet** (OPUS-M2-004, prepared in advance per the authorization; issued only if composition is needed — the join table needs it by construction, so expect it) |
| **TENANT_TABLES registry** | Each task appends its own tables in its own change; mechanical line adjacency at merge is resolved by the second merger's rebase; anything beyond mechanical = integration packet |
| **Capability registry / SPEC-06 policy wiring** | Each task registers only its own capabilities; the evaluator itself is frozen (a change to it = escalation) |
| **Audit event-name registry** | Same append-only discipline as TENANT_TABLES |
| **Test fixtures** | The M2-001 strategy is binding on both; MULTI is read-only shared substrate — neither task mutates the MULTI provisioning script (changes = escalation) |
| **UI components** | M2-002 owns all of `apps/web` this milestone; M2-003 ships no UI |
| **Lockfile** | M2-002 only (UI dependencies with licence notes); M2-003 none |

## 6. Entry gate, branches, review, evidence

- **M2 entry gate (satisfied 2026-08-03):** M1 exit accepted; NR-13 costing is M2-001's opening step; owner authorization received.
- **Branches/worktrees:** `opus/m2-001-sbx-harness` · `opus/m2-002-catalogue` · `opus/m2-003-profiles`, each in `.worktrees/`, squash-merged to always-green `main`, worktree removed and branch deleted after acceptance. No concurrent embedded-postgres runs in one worktree; ports are per-worktree.
- **Review:** independent second Opus review mandatory for all three; reviewers must author their own database probes and test mutations; acceptance never rests solely on existing tests passing; focused delta review when a revision alters security-critical mechanisms; orchestrator re-runs everything at every round and runs every named-condition proof standalone + in-package + in-battery.
- **Interruption protocol:** per the runbook — incremental commits mandatory; read-only worktree inspection before resuming; resume from commits and diffs, never conversational memory.
- **After M2-001:** revalidate 002/003 against the actual outcome, update packets with dated notes, re-verify disjoint globs, run all validators, then issue in parallel.
- **M2 close:** control-doc sweep, validators, full battery on main, named-condition proofs standalone, complete G-ARCH SBX tenancy subset re-run, clean tree, frozen checkpoint, exit report ([format per the owner's authorization] — including the honest note that roadmap-M2's remaining slices — rule authoring/AST, B-\* corpus, full group settings, site attribute — are **not** in these three packets and carry forward explicitly).

## 6a. OPUS-M2-004 — Integration and hardening (added 2026-08-03, per the authorization's §5.6; ISSUED 2026-08-03)

| Field | Content |
|---|---|
| **Task ID** | OPUS-M2-004 · M2/integration · **Status: ISSUED 2026-08-03** (after M2-002 ff2e67b and M2-003 567964c merged) |
| **Objective** | Compose what the parallel pair deliberately left unowned, and fix the cross-cutting defects their reviews surfaced: **(1)** `shift_type_qualifications` (the M2-002×M2-003 join, doc 06 §3.2) + eligibility read wiring (qualification requirements answerable per shift type; enforcement is M4's consumer); **(2)** consolidate `catalogue-fixture.ts` + `staffing.ts` seeding into `provisionMulti` per the D-1 ruling (multi.ts becomes the single fixture owner again; the per-file modules become thin delegates or disappear); **(3)** the `sequence::text` lexicographic-sort class: three test sites + `apps/api/src/audit/verification.ts:119-124` (production; ordering of the problems array), and **seed 531651 joins FIXED_SEEDS** with its provenance note; **(4)** per-worktree `SP_TEST_PG_PORT` derivation (E-1; derive from worktree path hash unless overridden; document) + Playwright preview-port override (E-2); **(5)** the request-budget ledger entry for the catalogue UI (I-10; the narrow `scripts/gates/**` ledger-data allowance below); **(6)** red-case runner port-release wait (U-04 transient); **(7)** holiday-read ruling (S-10/D-10, product ruling recorded here): holiday READ is granted to holders of the catalogue-authoring capability as well as group administrators — a scheduler authoring per-shift-type holiday demand must see the holiday calendar; writes stay group-settings-gated |
| **Allowed files** | `apps/api/migrations/**` (new, additive), `apps/api/src/catalogue/**`, `apps/api/src/profiles/**`, `apps/api/src/audit/verification.ts` (the ORDER BY fix only), `apps/api/src/http/routes/**` (holiday-read policy + eligibility read), `packages/domain/src/**`, `packages/contracts/src/**`, `apps/api/test/**`, `scripts/sbx/**` (FIXED_SEEDS + port derivation), `scripts/red-cases/run.mjs` (port-wait only, disclosed diff), **`scripts/gates/` request-budget ledger DATA file only** (never gate logic), `apps/web/playwright.config.*`/preview scripts (port override only), `docs/evidence/EV-M2-INTEGRATION/**`, `docs/dev-setup.md` (append) |
| **Prohibited** | Gate LOGIC (`scripts/gates/*.mjs` beyond the ledger data file), `scripts/check.mjs`, applied migrations, `docs/architecture/**`, `docs/fable/**`, research, spikes, UI components beyond the holiday-read surface's policy effect, lockfile |
| **Binding requirements** | Everything in §7 (fixture discipline, TENANT_TABLES + non-vacuous SBX-004 for the new table, fixture-regression green incl. the now-twelve fixed seeds, ProbeFalsified discipline, restore-by-id). `shift_type_qualifications`: group tenant table, RLS-in-same-migration, composite FKs to both parents, archive-not-delete, capability = catalogue-authoring for writes, deny paths tested, audit before/after. The fixture consolidation must leave every existing test green **unchanged in meaning** (no assertion weakened — the consolidation is proven by the full battery plus the shuffled gate). The `verification.ts` fix carries a regression test proving numeric ordering of the problems array. Port derivation proven by two simultaneous worktree batteries in evidence |
| **Named-condition proofs** (3 modes each) | (1) eligibility read answers per shift type with zero cross-tenant leakage; (2) post-consolidation order-independence (full seed set); (3) numeric sequence ordering under a >9-row problems fixture |
| **Acceptance** | `corepack pnpm check` · `red-cases` · `fixture-regression` (12 fixed + rotating) · `sbx` (26-entry registry; new table visible non-vacuously) · migration cycle · named proofs ×3 — all green in the implementer's AND the orchestrator's hands; evidence EV-M2-INTEGRATION with INDEX.md; escalate anything smelling like scope (especially any temptation to touch gate logic) |
| **Review** | **Independent second review MANDATORY** (new tenant table + audit-module production edit + fixture-owner change = critical class). Reviewer probes per §8 of the owner's authorization, incl. attacking the consolidated fixture ownership and the eligibility read's tenancy |

**Amendment 2026-08-03 (deliverable 5 escalation ruled):** the catalogue Save-success path measures **two** requests (POST + server-authoritative list refetch). Ruled: the budget for that interaction is **2, justified** — the refetch is the PO-DEC-18 server-authoritative display posture, not amplification; the justification lands in the ledger entry itself. The packet's allowed globs are widened by exactly: `apps/web/e2e/**` (the request-budget recording only — no assertion elsewhere may change) and the ledger DATA file as already allowed. A one-request alternative (POST returns the row, client-side list update) is recorded as a UI-slice option for a later packet, not this one.

## 7. Revalidation against the actual OPUS-M2-001 outcome (dated note, 2026-08-03 — binding on M2-002 and M2-003)

OPUS-M2-001 merged as `bc61ee7` after a four-round review cycle. The following are now **binding requirements** on both parallel packets, superseding any softer wording above:

1. **Fixture discipline is FAD-15 as corrected (`b9755d1`), concretely:** mutating suites provision owned tenants via `ownedMulti('<slug>')` / `provisionMulti` (`apps/api/test/support/{multi,owned-multi}.ts`); the shared baseline is enforced-immutable (the Layer-1 digest names offending files); non-tenant preconditions (queue, clock, global `users`) are established, never assumed; every test owns its subject; **fixture restores go by row id, never by predicate** (second-review finding E-01).
2. **New tenant tables register in `TENANT_TABLES` in the same change as their migration**, and the task's evidence includes a re-run `pnpm sbx` in which SBX-004 reports the new tables **observed with visible rows** (non-vacuous) and zero wrong-tenant rows. A new table missing from the sweep is an acceptance failure.
3. **`corepack pnpm fixture-regression` (11 fixed seeds + rotating, both shuffles, full standalone sweep, Layer-1 control) must be green at submission and will be re-run at acceptance.** A rotating-seed failure is a defect to fix; the seed joins the fixed set (FAD-15 ruling 3, exercised once already at M2-001 acceptance — coupling seven).
4. **Vacuity discipline:** any new SBX scenario follows the `ProbeFalsified` sentinel contract (perturb → confirm ≥1 row touched → re-execute the shipped oracle → throw only on witnessed rejection); deny/rejection tests carry an ALLOW control first; an assertion that cannot fail when its subject is absent is a review-rejectable defect anywhere in the diff (the class is now pinned by red cases).
5. **For M2-002:** the FAD-16 doc-06 §3.2 amendment defines the authoritative field set (display metadata as CHECK-constrained token-set members; `shift_type_weekday_demand`; monotonic `groups.pick_position_count`); the field-mapping table maps the owner's observed-concept list against it.
6. **Shared-surface ownership (§5) re-confirmed unchanged.** Globs verified disjoint: M2-002 owns `apps/api/src/catalogue/**`, `packages/*/src/catalogue/**`, all of `apps/web/**`, and its migrations; M2-003 owns `apps/api/src/profiles/**`, `packages/*/src/profiles/**`, and its migrations, with **no lockfile changes**. `shift_type_qualifications` remains integration-packet scope. Migration numbering at merge; second merger rebases. Both tasks append (never edit) `TENANT_TABLES`, the capability registry, and audit event names.
