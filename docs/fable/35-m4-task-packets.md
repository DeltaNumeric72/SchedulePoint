# 35 — Milestone M4 task packets

**Status: authored 2026-08-07 on the owner's M4 authorization, against frozen M3 checkpoint `7b579f2` (tag `milestone/M3`). Entry record and prerequisite register: [34-m4-entry-and-prerequisite-register.md](34-m4-entry-and-prerequisite-register.md).** Every packet inherits the runbook ([17-opus-agent-runbook.md](17-opus-agent-runbook.md)) including the thirteen non-bypass rules, [24-execution-standards.md](24-execution-standards.md) §§E–F, the FAD-7 environment substitutions (embedded-postgres, corepack pnpm, no Docker builds on this machine — the solver worker image is authored, its build is a CI condition), the standing merge/port-hygiene/standalone-verification disciplines, FAD-15 fixture isolation, `corepack pnpm fixture-regression` + the SBX battery at every acceptance, and the `ProbeFalsified` falsifiability discipline. **All M4 packets are critical class: independent second review with reviewer-authored falsification probes is mandatory for every one.** M5 is not authorized; nothing in this milestone may implement requests, vacation, notification delivery, marketplace, picklist, reports, documents, or external connectors.

**Execution order.** The owner's mandatory sequence M4-000 → 001 → 002 → 003 → 004 → 005 is preserved. M4-000 is split into three packets for review/rollback clarity (the authorization permits splitting, prohibits merging): **M4-000A** (staffing-input integrity) first and alone, then **M4-000B** (schedule graph, locations, time) and **M4-000C** (rule revisioning, publication handoff, provider gate) in parallel worktrees with disjoint globs, serialized merges. The real solver packets (M4-001 onward) do not issue until every M4-000 exit condition in doc 34 §4 is proven. M4-001..005 are pre-declared in §6 and are finalized in this document before their own issuance.

---

## 1. M4 entry verification

Recorded in [34-m4-entry-and-prerequisite-register.md](34-m4-entry-and-prerequisite-register.md) §2: checkpoint/tag/tree confirmed; validators 95/95 · 36/36 · PASS; check 13/13; red-cases 21/21 on the clean serial re-run (first run's 3 preview-port contention failures diagnosed and recorded); SBX 6/6 falsifiable with the 44-table sweep at 0 wrong-tenant rows; fixture-regression per the doc-34 row. Exit codes checked directly.

## 2. Shared integration surfaces — pre-assigned before any parallel issuance

Binding on M4-000B and M4-000C (M4-000A runs alone). Crossing a line mid-task is an **escalation, never an edit**.

| Surface | Ownership |
|---|---|
| Migrations | 000A → `0012`. 000B → `0013`. 000C → `0014`. Parallel pair merges serialized; the second merger rebases, renumbers to follow the first, and re-runs the full battery before merge. **Amended 2026-08-10 (FAD-30):** 000A's review condition F-1 consumed `0013` (work-profile DELETE capability gate), so **000B → `0014` and 000C → `0015`**; 000C's prohibited migration range reads 0001–0014 accordingly. The §4/§5 packet bodies below retain their original numbers as authored; this row governs |
| `apps/api/src/db/schema.ts` + contracts barrel | Additive-only, each packet under its own namespace; a genuine collision = escalation |
| `packages/domain/src/eligibility/**` | **Created by 000A, frozen to 000B/000C** (they consume it read-only) |
| `apps/api/src/schedule/**` | **000B owns** (graph/locations/time). 000C touches ONLY `publication.ts`, `diff.ts`, and new `review-identity.ts` (publication handoff); a need beyond that = escalation |
| `apps/api/src/rules/**`, `packages/domain/src/rules/**` | **000C owns** |
| `apps/api/src/profiles/**`, `apps/api/src/catalogue/**` demand editor | **000A owns** |
| Unit-of-work / provider guard (`packages/domain/src/ports/**`, `apps/api/src/db/unit-of-work*`) | **000C owns** (the H gate); 000A/000B do not touch |
| Audit event names / action keys | Additive only, proposed in return reports, FAD-ratified at acceptance |
| Test support | `test/support/staffing.ts` extensions → 000A; `test/support/schedule.ts` → 000B; `test/support/rules.ts` → 000C. MULTI baseline enforced-immutable |
| Evidence | `docs/evidence/EV-M4-INPUT-A/`, `EV-M4-INPUT-B/`, `EV-M4-INPUT-C/`; later packets `EV-M4-<NAME>/` |

## 3. OPUS-M4-000A — Staffing-input integrity: weekday demand, qualifications, work profiles, the shared eligibility verdict

| Field | Content |
|---|---|
| **Status** | **ACCEPTED & MERGED 2026-08-10 (94af3c5).** ISSUED 2026-08-07 (worktree `.worktrees/m4-000a`, branch `opus/m4-000a-staffing-input`). Independent review ACCEPT WITH CONDITIONS (F-1 DB capability gap → migration 0013 + red case; F-2/F-3 accuracy/citations; F-4 → FAD-29; F-5 scope crossing → FAD-30); all conditions closed, full battery re-proven (26/26 red, 107/107 fixreg, sweep 45). Evidence EV-M4-INPUT-A. NR-15 intermittent registered. Rulings ratified as FAD-28/29/30 |
| **Objective** | Doc 34 §4-A and §4-B closed: weekday demand (catalogue defaults AND period requirements) as true atomic whole-set aggregates under an `expectedVersion` consistency mechanism; `requires_expiry` enforced at service and database boundaries; retired-qualification behaviour defined and enforced for new holdings, existing holdings, and new shift-type requirements; qualification-set replacement and holding state changes consistency-controlled; an explicit correction/cancellation path for future-effective work-profile rows; deterministic in-force input selection (the row in force at the build instant — the S-03 ruling); **one shared eligibility verdict** (`packages/domain/src/eligibility`) with distinct functional outcomes for expired / future / revoked / retired / missing, consumed by manual scheduling and publication now and by solver input/validation in M4-001/002; shift-type qualification requirements enforced even when no duplicate authored rule exists |
| **Read first** | doc 34 §4-A/B; SPEC-04 §3.4; doc 06 §3.2 (qualifications, holdings, work profiles, weekday demand); FAD-16/17/23/24; `apps/api/src/profiles/**`, `apps/api/src/catalogue/**` current state; M3 exit report §13.8 |
| **Allowed files** | `apps/api/migrations/0012_*.sql` (new); `apps/api/src/profiles/**`; the demand-editor halves of `apps/api/src/catalogue/**` and `apps/api/src/schedule/**` requirements PUT (each edit justified per file); `packages/domain/src/eligibility/**` (new); `packages/contracts/src/{profiles,catalogue,schedule}/**` (additive); `apps/web/src/**` demand/qualification editors ONLY where an expectedVersion or load-before-edit change requires it (minimal, justified); `apps/api/test/**`; `apps/web/e2e/**`; `scripts/red-cases/**` additions; `docs/evidence/EV-M4-INPUT-A/**`; root config script entries |
| **Prohibited** | `docs/architecture/**`, `docs/fable/**`, research, `spikes/**`, `solver/**`, migrations 0001–0011, `apps/api/src/{authn,audit,rules}/**`, unit-of-work internals, `scripts/gates/*` modifications (additions need a red case), existing tests never weakened |
| **Required behaviour** | (1) Demand replacement: saving produces exactly the presented set; omitted entries follow ONE documented canonical rule (delete-absent, stated in the contract and on the page); `expectedVersion` CAS (DB-owned counter, FAD-24 `settings_version` precedent) refuses concurrent interleaving — two concurrent replacements can never combine into a union (proven by a 12-round concurrent test asserting DB state); editors load current values before opening; open-then-save-unchanged is a byte-level no-op (never resets to zero, not audited as a change per FAD-24, with the load-bearing emission control); I-10 one-action-one-request preserved. (2) `requires_expiry`: a holding without expiry for a requires-expiry qualification is refused at the service AND by a database trigger/constraint (red-cased at both layers). (3) Retired qualifications: new holdings refused; existing holdings retained and readable with their verdict reflecting retirement; new `shift_type_qualifications` rows naming a retired qualification refused; each behaviour authored as an explicit ruling in the return report for FAD ratification. (4) Holding/qualification mutations carry expectedVersion. (5) Future-effective work-profile rows get an explicit correction/cancellation operation (audited, guarded: only rows not yet in force; the in-force row is never mutated). (6) The eligibility verdict is a pure domain function over (holdings, qualification states, shift-type requirements, work profile, membership state, date) returning a typed verdict naming its reason; profiles/schedule surfaces that currently compute eligibility ad hoc converge on it; absence semantics preserve FAD-23's three-state display |
| **Tests / red cases** | Concurrency: demand replacement races (both editors, catalogue and period requirements); expectedVersion stale → explicit 409, no merge. DB red cases: requires-expiry trigger; retired-qualification refusals. Eligibility verdict: property tests over the five distinct outcomes; boundary at expiry date vs assignment date; verdict equality between the manual-path consumer and the publication consumer (same fixture, same verdict, asserted). Falsifiability: each new proof file carries a mutation control |
| **Acceptance** | check 13/13 · red-cases all proven (incl. new) · fixture-regression green (fresh rotating seed) · SBX battery green, sweep floor unchanged or raised with the new tables reported · migration 0012 up/down/up clean · named proofs standalone + in-suite + full battery (all three reported) · evidence bundle with INDEX · tree clean, no lingering cluster |
| **Escalate if** | Any behaviour here requires a schema change beyond 0012's declared set · the canonical omitted-entry rule conflicts with any observed-model document · eligibility convergence would change a shipped FAD-23 behaviour · any test passable only by weakening |

**Independent second review: MANDATORY.** Reviewer-authored probes at minimum: race the demand editors with hand-built concurrent clients; attempt the union outcome; attempt a requires-expiry bypass via every write path and role; attempt a retired-qualification holding via raw service calls; mutation-test the verdict-equality proof; verify the no-op save emits nothing while a real save still emits (both directions).

## 4. OPUS-M4-000B — Schedule graph consistency, group-scoped relationships, locations, and time

| Field | Content |
|---|---|
| **Status** | **ACCEPTED & MERGED 2026-08-12 (a614f7e; evidence EV-M4-INPUT-B).** ISSUED 2026-08-11. Independent review ACCEPT WITH CONDITIONS — 2 blocking (B-1 degenerate DST-gap intervals → R-B4a joint translation, 399→0 enumerated; B-2 TIMEZONE_BASIS_STALE answered 404 → 409 wired with HTTP proof + control), both closed and delta-verified (reviewer reproduced 399→0 independently, mutation-tested both fixes); branch-topology defect caught by the delta review and repaired by cherry-pick before merge. Rulings R-B1..R-B7 + R-B4a ratified (FAD-31). Formerly: FINALIZED — NOT ISSUED; blocked on M4-000A acceptance (it consumes the eligibility verdict read-only). **Amended 2026-08-10 (FAD-30):** M4-000A is accepted — this packet may issue. Its migration is **`0014`** (this table's original `0013` line superseded by §2); `apps/api/test/support/schedule.ts` is already-modified state to take as found; `packages/domain/src/eligibility/**` is FROZEN read-only. **Amended 2026-08-11:** the single function `cloneVersion` in `apps/api/src/schedule/service.ts` is CEDED to 000C (credit-preserving clone, its §5 clause G); 000B must not modify `cloneVersion` — everything else in `apps/api/src/schedule/**` remains 000B's. **Amended 2026-08-11 (second ruling, on 000B's escalation):** (1) `cloneVersion` becomes JOINTLY edited with pre-declared compose semantics — 000B may make ONLY the 3-line timezone-basis inheritance (one `readTimezoneBasis` call + two `.values()` keys; 0014's columns exist only in 000B's tree, so 000C cannot author it), 000C owns credit preservation; the second merger composes both and re-runs the full battery, and each packet's proof must survive composition. (2) Route-file grants, region-scoped: `schedule-authoring.route.ts` assignment/authoring endpoints only (locationId plumbing, cellRevision digest, the zonedInstant DST-rule implementation) — the closed-period transition endpoint region is 000C's; `schedule-views.route.ts` wholly 000B's (render under the version's published timezone basis). (3) The held-back grid/location contract additions may proceed (contracts are §2 additive; justify each; prefer backward-compatible shapes). (4) The `packages/domain/src/index.ts` two-line barrel export ACCEPTED (the 000A/000C precedent). (5) The N-5 rewrite (cross-group arm structurally unreachable under the composite FK), the V-17/V-19 dropRealityFor precondition move, and V-18's 13→15 frozen columns are accepted PROVISIONALLY — the independent reviewer is directed to verify no assertion weakened, especially that N-5's replacement tests genuinely cover the original class |
| **Objective** | Doc 34 §4-C, §4-E, §4-F closed: database-proven group consistency for every scheduling relationship (composite FKs / invariant checks, never UI filtering); membership-reference semantics for scheduling defined and enforced (kind, active/inactive, effective dates, ended, functional/placeholder participants); the full §4-E schedule-graph consistency list refused at the database or by guarded transition; shifts connected to real locations by a group-qualified relationship exposed in contracts and authoring inputs; archived-location behaviour defined; group-vs-location timezone ruling; site-scoped eligibility input defined (PO-DEC-01 default preserved — no site entity); true calendar-date validation replacing shape-only checks; explicit DST gap/fold, overnight, UTC+14/−12, leap-year, invalid-date behaviour (documented and test-pinned); the timezone interpretation needed to reproduce a build/published version snapshotted; timezone changes coordinated with build-input/publication creation |
| **Read first** | doc 34 §4-C/E/F; SPEC-05 (graph tables); doc 06 §3.2; migration 0009/0010 sources; FAD-24 (timezone administration); M3-006 overnight/DST rendering evidence |
| **Allowed files** | `apps/api/migrations/0013_*.sql`; `apps/api/src/schedule/**`; `apps/api/src/settings/**` (location/timezone halves, justified); `packages/domain/src/{calendar,time}/**` (new pure date/timezone semantics); `packages/contracts/src/schedule/**`, `src/settings/**` (additive); `apps/web/src/schedule/**` location exposure (minimal, justified); tests/e2e; red-case additions; `docs/evidence/EV-M4-INPUT-B/**` |
| **Prohibited** | Everything else — expressly `packages/domain/src/eligibility/**` (000A's, frozen), `apps/api/src/{rules,profiles,authn,audit}/**`, unit-of-work internals, migrations 0001–0012, `solver/**` |
| **Required behaviour** | Composite tenant+group FKs (or trigger-enforced invariants where an FK cannot express it) for: snapshot↔identity (same group), snapshot↔shift (same version AND same group), credit↔identity/snapshot/version, participant↔group, `current_published_assignments`↔current published version (V-15c equality already proven — add the FK/trigger arm), requirement↔period date range, shift↔period date range, period↔version. Assignment writes and eligibility input define membership semantics: an ended/suspended membership is refused as a NEW assignment target (with the manual-override arm and reason where FAD-23 sanctions it); effective-date behaviour stated; placeholder/functional participants explicitly out of scope for M4 solver input (recorded, owner-visible). `shifts.location_id` gains the group-qualified composite FK to `locations` (nullable stays legal — a shift without a location is a stated, allowed state); archived locations refuse NEW shift references, existing references retained and rendered with the archived marker; location timezone is display metadata only — the GROUP timezone governs schedule semantics (ruling proposed for FAD ratification); calendar-date validation (real dates, rejecting 2027-02-29 and 2027-13-01 at contract AND database); DST gap instants refused or normalized per a stated rule, fold instants disambiguated per a stated rule, both test-pinned; overnight shifts keep M3-006's both-days attribution; UTC+14/−12 and leap-year fixtures; `schedule_versions` (draft-writable column) or `publication_records` snapshot the tz interpretation (zone id + a version identifier of the tzdb rule set in use) so a published version's rendering is reproducible; a group-timezone change while a draft build input exists surfaces the staleness explicitly (CAS class) |
| **Tests / red cases** | For every FK/invariant added: a red case proving the cross-group / cross-version / out-of-range write is refused at the database as the app roles; migration cycle with populated fixtures (up/down/up preserving data); DST gap/fold/overnight/UTC-edge/leap fixtures asserted through the real read surfaces; tz-snapshot reproducibility (published version renders identically after a group tz change) |
| **Acceptance** | Standing battery + migration 0013 cycle + named proofs standalone/in-suite/battery + evidence with INDEX + tree clean |
| **Escalate if** | An FK cannot be added without data repair (existing fixture rows violating it) · any D-15 interaction (adding constraints touching published-version children needs the app_migrator path and must be stated) · the tz-snapshot shape needs a SPEC-05 amendment · any doubt about PO-DEC-01 site scope |

**Independent second review: MANDATORY.** Probes: attempt cross-group snapshot/credit/holding writes as every role; craft a graph inconsistency through legal-looking service sequences; attack the date validator with the full pathological calendar set; attempt to move a published version's rendering by changing the group timezone.

## 5. OPUS-M4-000C — Rule revisioning and registry, publication handoff, provider-outside-transaction control

| Field | Content |
|---|---|
| **Status** | **ACCEPTED & MERGED 2026-08-12 (323d576; evidence EV-M4-INPUT-C).** ISSUED 2026-08-11. Escalated its authored-glob gap before editing (five crossings granted, §2 amendment); independent review ACCEPT WITH CONDITIONS — C-1 (material-input digest: 3/9 classes falsifiable vs a nine-class claim) closed with a nine-class mutation matrix + shrink-guard pin, delta-verified (reviewer re-ran its sweep: 9/9 bite, both pin directions catch). Second merger: rebased onto a614f7e, composed cloneVersion (both halves verified in body) and the region-shared authoring route; 8 composition failures diagnosed to 5 mechanisms (4 = one positional migration-cycle assumption; 2 = 000C fixture defects caught by 000B's new FKs; 1 genuine interaction — timezone-staleness precondition now ordered before the review-digest CAS, 000B's 409 contract untouched); the authorized retired-verdict decorative-arm repair executed with rebuild-between-patch-and-run (both listed tests now observe). Composed battery green (38/38 red cases by the runner's count). Rulings ratified FAD-32/33. Formerly: FINALIZED — NOT ISSUED; issues in parallel with M4-000B after M4-000A merges (disjoint globs per §2). **Amended 2026-08-10 (FAD-30):** M4-000A is accepted — this packet may issue in parallel with 000B. Its migration is **`0015`** and its prohibited migration range is **0001–0014** (this table's original `0014`/`0001–0013` lines superseded by §2); `packages/domain/src/eligibility/**` is FROZEN read-only. **Amended 2026-08-11 (issued-packet escalation ruled — the correct behaviour FAD-30 demands, exercised):** the implementer stopped at five crossings its Required behaviour forces; the orchestrator GRANTS, additive/minimal-and-justified per edit: (1) `apps/api/src/http/routes/rules.route.ts` (the CAS route half, `409 STALE_RULE`, revisions read); (2) `apps/api/src/http/routes/schedule-publication.route.ts` (the review-digest/material-input-digest site); (3) **the single `cloneVersion` credit-preservation edit in `apps/api/src/schedule/service.ts`** — this one function is CEDED from 000B's surface to 000C (000B notified mid-flight: do not touch `cloneVersion`); (4) `apps/api/src/http/routes/schedule-authoring.route.ts` (the audited closed-period transition endpoint only); (5) `scripts/check.mjs` + root `package.json` gate entries (ADDITIONS only — parity with 000A's "root config script entries", an authoring omission here). Three disclosed additive crossings accepted with precedent: `packages/domain/src/index.ts` barrel export, `apps/api/src/db/schema.ts` (§2 shared additive surface), `apps/api/src/db/unit-of-work.ts` (in-glob, runtime provider guard only) |
| **Objective** | Doc 34 §4-D, §4-G, §4-H closed: `expectedVersion` CAS on rule and rule-set mutation (no last-write-wins); immutable `rule_revisions` (every mutation writes a revision row; stable `rule_key` never silently changes historical meaning — historical revisions are reconstructable byte-identically); builds and publications identify exact rule revisions; the rule-kind registry **generated from the closed AST source** proving exactly 30 unique kinds, evaluated/unevaluated disjoint, every kind carrying classification, required input, semantic ruling (or the named pending ruling), evaluation owner, and milestone owner where input arrives later — the step-06 document's counting/classification discrepancies corrected in the generated registry; AST bounds (depth, list sizes, numeric ranges, scope cardinality) enforced at validation with evaluation time/memory bounds on the checker; **publication handoff**: period lifecycle completed (a `closed` period refuses new versions/builds/publications except through an explicit audited transition), publication review identity expanded to every material input (participant, date, time, shift type, location, pin, credit, conflicts, rule revisions, catalogue revisions, profiles, qualifications, demand, timezone — the M3-005 review-digest CAS extended to a full material-input digest), idempotency bound to operation type + semantic request, clone/revert-forward preserves credits, schedule differences and affected-participant calculations share one material-change definition covering time/shift type/location/pins/credits; **provider gate (H)**: the SPEC-12 U-07 rule implemented — a provider/RPC call inside a database unit of work fails a static gate AND a runtime guard, red/mutation-cased, so database snapshot creation and external solver execution are structurally separate phases before any solver RPC exists |
| **Read first** | doc 34 §4-D/G/H; SPEC-04 §3; SPEC-05 §6 + FAD-22/26; SPEC-12; `step-06-node-kinds.md`; `packages/domain/src/rules/**`; `apps/api/src/schedule/{publication,diff}.ts` |
| **Allowed files** | `apps/api/migrations/0014_*.sql`; `apps/api/src/rules/**`; `packages/domain/src/rules/**`; `apps/api/src/schedule/{publication.ts,diff.ts}` + new `apps/api/src/schedule/review-identity.ts` ONLY; `packages/domain/src/ports/**` + `apps/api/src/db/**` (the runtime provider guard ONLY — each edit justified); `packages/contracts/src/{rules,schedule}/**` additive; `apps/web/src/{rules,publication}/**` expectedVersion exposure (minimal, justified); `scripts/gates/**` ADDITIONS ONLY (registry gate + provider-in-transaction gate, each with a red case); `scripts/red-cases/**`; tests/e2e; `docs/evidence/EV-M4-INPUT-C/**` |
| **Prohibited** | Everything else — expressly 000A/000B surfaces, `solver/**` beyond reading the corpus, migrations 0001–0013, the audit module, existing gates |
| **Required behaviour** | Revisions: `rule_revisions` append-only (no UPDATE/DELETE grant for runtime roles), written in the same unit of work as the mutation, carrying the full serialized AST + schema version + revision number per rule; the current row's `revision` is the CAS token; a replayed stale write fails explicitly. Registry: a generator script emits the registry from `RULE_NODE_KINDS` + `EVALUATED_HARD_RULE_KINDS` + `NOT_EVALUABLE_REASONS` + per-kind metadata; a build-failing gate proves the committed registry equals the generated one (red case: edit the committed registry by hand); counts asserted 30 unique, disjoint sets, 6+24=30 (or the corrected split if a ruling moved kinds — the numbers come from code, not prose). Bounds: max AST depth, max list lengths, numeric ranges, scope cardinality enforced in `validate.ts` with explicit errors; checker time/memory bounded by construction (documented bound argument, test with a pathological-but-legal AST). Publication handoff per the objective; the material-change definition is ONE exported function consumed by diff, affected-participants, and the review digest (equality asserted). Provider gate: an ESLint rule (or dedicated gate script) fails any import/call of a provider-port implementation inside unit-of-work callbacks, and the runtime unit-of-work context exposes an `assertOutsideUnitOfWork()` guard that every provider port entry point calls; red case: a probe provider called inside `withUnitOfWork` → gate fails AND runtime throws; mutation case: disable the guard → the red case's detection fails (falsifiability) |
| **Tests / red cases** | CAS races on rules/rule sets (12-round, DB-asserted); revision reconstruction byte-identity; hand-edited registry caught; closed-period refusal matrix (build/version/publication attempts); credits preserved across clone and revert (count + values + keying asserted); material-change single-definition equality; provider-gate red + mutation cases |
| **Acceptance** | Standing battery + migration 0014 cycle + named proofs three-way + evidence with INDEX + tree clean |
| **Escalate if** | The review-digest expansion conflicts with FAD-26's shipped CAS shape · revision storage needs a SPEC-04 amendment · the provider guard cannot be expressed without touching the frozen unit-of-work contract · any registry count disagrees with code in a way a ruling has not sanctioned |

**Independent second review: MANDATORY.** Probes: race rule writers; attempt last-write-wins through every mutation path; attempt to alter a historical revision as every role; hand-tamper the registry; attempt publication on a closed period through every transition; verify clone-credit preservation against a crafted credit set (moved + voided credits included); attempt a provider call inside a transaction through an indirection the lint might miss (dynamic import, callback capture) and verify the RUNTIME guard still fires.

## 6. Pre-declared packets M4-001 .. M4-005 (finalized here before their own issuance)

Per the authorization's minimum sequence; each will be finalized to the full field set in this document before issuance, after the M4-000 exit evidence is in.

- **OPUS-M4-001 — Solver runtime boundary and canonical input.** SPEC-04 §1: separately packaged Python 3.12/OR-Tools worker (own image authored; local execution in the FAD-7 venv; image build recorded as the standing CI condition), no database credential, versioned authenticated RPC, one solve per subprocess, bounded cancellation/timeout/forced termination per the EV-M0-SPC measured mechanisms (watcher-thread StopSearch, never signals), stub solver first, API→worker→API round trip on a synthetic problem, provider execution proven outside every database transaction (the 000C gate consumed), worker image digest/solver version/compiler version/parameters/reproducibility mode recorded. Canonical input: one immutable versioned snapshot (organization/group/version identity, period, timezone snapshot, date range, locations, demand, active effective participants, account/participant type, in-force profiles + FTE, qualifications + expiry, shift-type qualification requirements, pins/protected/manual assignments, applicable rule revisions, configuration revisions) persisted with input hash, constituent revisions, compiler version, schema version, creation time, initiator, idempotency information; ambiguous/stale/cross-group/inactive/invalid-date/missing-required states refused before worker invocation.
- **OPUS-M4-002 — Rule semantics and E1 feasibility.** Every M4-evaluable HARD rule as a solver constraint; independent domain validator (not reusing the solver implementation) rejecting failing candidates; SOFT rules in explicit objective tiers; no rule silently ignored; later-milestone kinds gated with named owners; the M4-dependent semantic rulings resolved (the 11 one-ruling-away kinds; AvoidDate overnight attribution). E1 corpus: small/medium/large feasible, multisite, rule-heavy, progressive/pinned, qualifications, FTE, locum-shaped, fairness-shaped inputs, contradictory rules, missing qualifications, over-demand, fixed-assignment conflict, the documented B-infeasible set; zero independently measured hard violations on every feasible fixture; every infeasible fixture proven infeasible with bounded/minimal explanations; deterministic corpus generation with reproducible hashes; deliberately incorrect worker results rejected.
- **OPUS-M4-003 — Build lifecycle and scheduler experience.** The complete 16-state model with server-authoritative transitions, idempotent submission/retry, honest infeasible/failed/cancelled/timeout/unknown distinction, worker claim ownership and fencing (stale worker cannot overwrite a newer result), no candidate persisted usable before independent validation, candidate selection creating a new M3 draft (never mutating published), progressive builds preserving pins. Scheduler UI: demand/build launch, monitoring, cancellation, retry, infeasible/timeout/worker-failure surfaces, candidate comparison, conflicts, hard/soft results, quality summary, explanation tiers 1–2, progressive controls, selection into a draft — with loading/empty/stale/denied/validation/error/cancellation/timeout/infeasible/recovery states, keyboard/responsive/accessible-table/confirmation behaviour maintained.
- **OPUS-M4-004 — E2 optimization, quality, explanations.** Explicit objective tiers with recorded components and weights; candidate quality metrics; conflict taxonomy (PO-DEC-13 default); fairness measurements appropriate to available inputs; comparison against the same canonical snapshot; progressive optimization around pins; deterministic mode using the M0-measured deterministic portfolio and deterministic-time controls with its cost measured; OPTIMAL/FEASIBLE/INFEASIBLE/TIMEOUT/CANCELLED/FAILED/UNKNOWN never conflated (no optimality claim for a feasible result); solver statistics recorded for reproduction/comparison; explanation tiers 1–2 bounded with honest degraded states; explanation failure never reported as scheduling infeasibility.
- **OPUS-M4-005 — Integration, concurrency/recovery, close.** The real 14-step workflow (period → demand → profiles/qualifications → rules/pins → canonical input → build → worker → independent validation → conflicts/quality review → candidate selection → new draft → review → publish through the existing path → staff view) against the real web app, API, PostgreSQL, and worker, with the M4 feature APIs unmocked in the critical-path suite; the authorization's full concurrency-and-recovery matrix (simultaneous submission, idempotent retry, duplicate/stale worker results, worker crash, API restart, cancellation/timeout during solve, result-after-cancellation, every revision-changed-after-assembly case marked stale and never silently current, participant inactivation before acceptance, double candidate-selection, two builds per period, stale expected current version, location/timezone revision change, invalid worker output, infeasible-explanation timeout, progressive pin conflict); SBX-015/016/017 executed and falsifiable; the complete M1–M3 regression green; exit report (36), control docs, Codex review prompt, tag `milestone/M4`.

## 6a. OPUS-M4-001 — FINALIZED 2026-08-12 (issued against main `323d576`, M4-000 exit conditions proven per doc 34 §4a)

| Field | Content |
|---|---|
| **Status** | FINALIZED — issuance recorded here and in the task log |
| **Objective** | SPEC-04 §1 solver runtime boundary + the canonical input snapshot, per the §6 pre-declaration (which remains binding where this table is lossy). The worker: separately packaged Python 3.12/OR-Tools, its own image authored (local execution in the FAD-7 venv; image build recorded as the standing CI condition), NO database credential, versioned authenticated RPC, one solve per subprocess, bounded cancellation/timeout/forced termination via the EV-M0-SPC measured mechanisms (watcher-thread StopSearch, never signals), **stub solver first**, API→worker→API round trip on a synthetic problem, worker image digest/solver version/compiler version/parameters/reproducibility mode recorded. The canonical input: ONE immutable versioned snapshot (organization/group/version identity, period, timezone snapshot [0014 basis], date range, locations, demand, active effective participants, account/participant type, in-force profiles + FTE [S-03], qualifications + expiry, shift-type qualification requirements, pins/protected/manual assignments, applicable rule revisions [0015], configuration revisions) persisted with canonical input hash, every constituent revision, compiler version, schema version, creation time, initiator, idempotency information; ambiguous/stale/cross-group/inactive/invalid-date/missing-required states refused BEFORE worker invocation |
| **Binding constraints from M4-000 evidence** | (1) The solver RPC is invoked from the caller's context AFTER `run()` returns — never from work scheduled inside a transaction (000C review N-2; the AsyncLocalStorage mark propagates past commit and will refuse). (2) The solver provider module declares `@provider-port` and its entry point opens with `assertOutsideUnitOfWork()` — the 000C static gate + runtime guard are consumed, not re-implemented. (3) Input selection uses the shared eligibility verdict (`packages/domain/src/eligibility`, FROZEN read-only) and the in-force loader — no second spelling of any selection rule (the S-01 scanner class). (4) The snapshot's timezone interpretation uses the 0014 basis columns; a stale basis is refused per R-B7. (5) Migration `0016_*` — additive only; the populated-cycle test asserts by NAME never position (the composed-battery lesson) |
| **Read first** | CLAUDE.md · this doc §6/§7 · doc 34 §4a · SPEC-04 (whole) · SPEC-12 · EV-M0-SPC SPIKE-REPORT (H-0..H-8) · `spikes/` solver spike code · `packages/domain/src/ports/provider-boundary.ts` + `apps/api/src/db/provider-boundary.ts` · `apps/api/src/schedule/review-identity.ts` (the material-input classes the snapshot must cover) · EV-M4-INPUT-A/B/C INDEX files |
| **Allowed files** | `solver/**` (the worker — first packet allowed here); `Dockerfile.solver` (new, root); `apps/api/migrations/0016_*.sql` (new); `apps/api/src/solver/**` (new — RPC client, canonical input assembly, snapshot persistence); `packages/domain/src/ports/solver-*.ts` (new port files only); `packages/contracts/src/solver/**` (new, additive); `apps/api/test/**`; `scripts/red-cases/**` additions; `scripts/gates/**` ADDITIONS only (if a new gate is warranted, with red case); root config script entries; `docs/evidence/EV-M4-BOUNDARY/**` |
| **Prohibited** | Everything else — expressly `packages/domain/src/eligibility/**` (frozen, consume read-only), `apps/api/src/{rules,profiles,authn,audit,schedule,catalogue,settings}/**` (read-only consumption via existing exports), `apps/web/**` (M4-003's), migrations 0001–0015, existing gates, the unit-of-work contract, `docs/{architecture,fable}/**` |
| **Required behaviour** | Worker: image pins Python 3.12 + OR-Tools at the FAD-10 versions; RPC authenticated (shared-secret or mTLS per SPEC-04 — if SPEC-04 under-specifies, escalate, do not invent); request/response versioned schema; the worker process receives the snapshot payload, never a DB handle; one solve per subprocess with the parent enforcing wall-clock timeout and forced termination; cancellation via watcher-thread StopSearch with the outcome reported honestly (a cancelled solve is CANCELLED, never a misreported timeout/failure — the EV-M0-SPC H-6 finding). Stub first: a stub solver returning a fixed valid candidate proves the full round trip, auth, timeout, cancellation, and forced-kill paths before any real constraint model exists. Canonical input: assembly runs inside ONE unit of work (snapshot consistency), persists the snapshot + hash + constituents, then the RPC fires OUTSIDE the transaction (the N-2 pattern); the hash is deterministic (same inputs → same hash, proven); every refusal class (ambiguous, stale [revision moved since read], cross-group, inactive participant, invalid date, missing required input) is a typed refusal with a test; snapshot immutability DB-enforced (no UPDATE/DELETE grant + guard trigger, the 0015 revisions precedent) |
| **Tests / red cases** | Round trip on a synthetic problem (stub solver); timeout kills the subprocess (measured); cancellation mid-solve honest; forced termination leaves no orphan; RPC auth refusal; malformed/oversized worker response rejected; snapshot hash determinism + immutability red case (raw UPDATE refused all roles); each input-refusal class; the provider-gate red case extended to the REAL solver port (an in-transaction solver call refused at runtime AND caught by the static gate); mutation controls per FAD-15 conventions |
| **Acceptance** | Standing battery green (check with any new gate · red-cases all proven incl. new · fixture-regression fresh seed · sbx, sweep floor unchanged-or-raised with new tables reported) · migration 0016 cycle clean (by-name assertions) · named proofs standalone/in-suite/full-battery · worker image builds locally with digest recorded (or the build blocker escalated with the venv fallback proven) · evidence bundle EV-M4-BOUNDARY with INDEX · tree clean, no processes |
| **Escalate if** | SPEC-04 under-specifies the RPC auth mechanism · the snapshot shape needs a SPEC-04/SPEC-05 amendment · any input constituent is unreachable from existing read surfaces without touching a prohibited module · the worker image cannot build in this environment (report, fall back to venv execution, record the CI condition) · Python/OR-Tools versions conflict with FAD-7/FAD-10 · anything passable only by weakening |

**Independent second review: MANDATORY** — reviewer-authored probes at minimum: attack the RPC auth; attempt a DB credential reach from the worker process; race cancellation against completion; attempt snapshot mutation via every role; attempt an in-transaction solver invocation through shapes the static gate cannot see; verify hash determinism against reordered inputs; verify every refusal class with hand-built payloads.

## 7. Entry gate, branches, review, evidence (standing; applies to every packet)

Branches `opus/m4-00X-<slug>` in `.worktrees/m4-00X`, derived DB/preview ports, squash-merge by Fable only, separate commit per accepted task, complete validation (validators + check + red-cases + fixture-regression + SBX battery + migration cycle) after each merge. Independent second review by a separate sub-agent with reviewer-authored probes for every packet; blocking findings repaired then delta-reviewed; the orchestrator re-runs every acceptance command from a fresh checkout before accepting. Escalation is success behaviour. A passing test without a demonstrated red or mutation case is not sufficient evidence for a load-bearing invariant. Nothing here weakens any existing test, invariant, gate, accessibility requirement, tenant control, authorization control, audit requirement, or privacy restriction. Manual scheduling remains override/recovery/fixed-input only. Synthetic data only; no real notification; the source product is never visited.
