# OPUS-AGENT-RUNBOOK (pointer + task log)

Protocol: [../17-opus-agent-runbook.md](../17-opus-agent-runbook.md) + [../24-execution-standards.md](../24-execution-standards.md) §§E–F.

**Standing merge discipline (added 2026-08-02, after recurring twice in OPUS-M1-001; extended 2026-08-03 after a fifth recurrence in OPUS-M1-004):** `scripts/check-output.txt` and `scripts/red-cases/evidence-output.txt` regenerate on every gate/red-case run and live under a glob most packets prohibit. Implementers and the orchestrator alike: never `git add -A`; discard these with `git checkout --` before committing; verbatim captures belong in the task's `docs/evidence/` bundle. **Finish with the worktree clean AND no background test run still holding a cluster** — the embedded-postgres port is derived per worktree, so a lingering `pnpm check` makes the next person's suite fail to start. That collision has a distinctive signature: whole test *files* fail with tests *skipped* and **zero tests failed** (observed 2026-08-03: 18 files failed, 278 skipped, 0 failed). Read it as contention, then re-run serially before concluding anything about the code.

**Standing verification discipline (added 2026-08-03, from OPUS-M1-004):** a suite that passes in the full run may still fail alone. The C-2 lease-recovery proof passed 545/545 in-suite and failed **5 of 5 standalone**. Every task that adds a proof of a named condition must run that file standalone as well as in the battery, and report both.

**Standing notes (added 2026-08-04, M2 close):** (1) ports are now DERIVED per worktree (55500 + hash mod 400; `SP_TEST_PG_PORT` overrides) — the 2026-08-02 note's "port derived per worktree" is finally true rather than assumed; (2) run `pnpm red-cases` through pnpm, never `node scripts/red-cases/run.mjs` directly — four cases lose their `--dir` arguments under direct node and report a false 10/14 (verified M2-003 review); (3) the red-case runner now waits for port release, closing the check-then-red-cases GREEN-arm transient (U-04); (4) `pnpm check`/`pnpm sbx` rewrite tracked evidence files (timestamps, per-scenario ms, RUN-SCOPED counters) — restore with `git checkout --` unless intentionally refreshing the bundle from a coherent run (NR-14).

**Standing acceptance discipline (added 2026-08-03, with FAD-15; the command lands with OPUS-M2-001's merge):** `corepack pnpm fixture-regression` — the fixed seed set + one rotating seed under both file- and test-order shuffle, plus the per-file standalone sweep and the baseline-immutability control — runs at **every task acceptance and every milestone exit**, not on every `pnpm check` (35 suite runs is acceptance-time cost, ruled on an OPUS-M2-001 escalation; the gate runner was deliberately not modified). A rotating-seed failure is a defect to fix, never flake to retry; the exposing seed is recorded and joins the fixed set.

**Standing notes (added 2026-08-13, M4-001S acceptance):** (1) A red-case arm exiting with vitest's **"No test files found"** is an INFRASTRUCTURE failure, not a gate outcome — the runner currently cannot tell them apart on either arm (a green arm false-fails; a red arm false-"passes"), and each such exit orphans that case's cluster daemon. Treat it as a re-run signal; runner hardening (detect the marker, refuse the case as ERRORED) is owned by M4-005 beside NR-16 and review R-2. (2) Check the MACHINE before diagnosing a battery: a wedged system daemon (`mediaanalysisd` at 322% CPU) plus a Spotlight indexing storm from battery file-churn produced three different transient failure sets in three consecutive runs; the authoritative run is the one on a verified-calm machine (1-min load < ~6), and `uptime` belongs in the pre-battery checklist. (3) NEVER chain the next battery behind a result-read in one command — the second command starts regardless of what the first showed; read, verify, then launch. The M4-001S acceptance violated this once (recorded in EV-M4-001S §0) and the resulting parallel-battery window invalidated a fixture-regression run and confused the diagnosis that followed.

**Stage-1 recovery checkpoint (2026-08-13): `18de8aa283ca0cdccf2df9cda316cb7a7c91b9dd`** — M4-001 closeout + recovery battery + FAD-34/35 + OPUS-M4-001R finalized (doc 35 §6b). M4-001R issues from the main tip carrying this record; its exact base commit and branch are recorded in its row below at issuance.

| Task | Slice | Status | Verdict |
|---|---|---|---|
| OPUS-M4-003 build lifecycle + scheduler experience | M4/lifecycle+UX | **ACCEPTED & MERGED 2026-08-16** (squash `8deb4c5`; evidence EV-M4-003; base `d120ae3`, final `1725c53`; migration 0018 — six tenant tables, floor 48→54 registry / 47→53 sweep). FAD-44 pre-review disclosures + FAD-45 conditions (raw-NUL gate MANDATED on third recurrence — now gate 17 with strict path+count baseline; UI retry; five falsifiability arms; D-4a ratified, D-4b→M4-005). Review ACCEPT WITH CONDITIONS → delta ACCEPT (probes `review/m4-003` @ `5689343`; reviewer corrected its own round-1 measurement openly) → micro-repair (D-1 append-only arm de-decorated incl. an empty-table vacuity self-catch). Reviewer 193-illegal-edge sweep, forged-result and race probes all HELD. Orchestrator battery on byte-identical post-merge main: 17/17 (1967) · 53/53 · 143/143 (seed 956607) · SBX 53/53 · cycle 0001–0018. Note: the reviewer worktree left two orphaned cluster daemons mid-cycle (cleaned by orchestrator; hygiene reminder issued and honoured at delta) | **ACCEPTED** |
| OPUS-M4-002 rule semantics + E1 feasibility | M4/solver E1 | **ACCEPTED & MERGED 2026-08-15** (squash `5e0ac4e`; evidence EV-M4-002; base `4223677`, final implementation `102e85f`). Three Opus implementers, four ruled escalations (FAD-38/39/40/41), review **REVISE** (R-1 blocking: adjacent-date-only rest pairing, reviewer-probed) → repair (window widened w/ equivalence proof; NUL repair 14/15 hash-neutral; R-6 pins; 16th gate: Python↔TS kind parity) → **delta ACCEPT, zero new findings** (probes retained `review/m4-002`). Checker 6→22 kinds; real CP-SAT model; snapshot v2; candidate validator + eligibility-gap check; 15-class corpus hash-reproduced by reviewer; boundary byte-identical. Orchestrator battery on byte-identical post-merge main: 16/16 (1899) · 48/48 · 137/137 (seed 617763) · SBX 47/47 · cycle 0001–0017. RK-05/06/10 + R-6 ratified (FAD-43). NR-18/NR-19 registered | **ACCEPTED** |
| OPUS-M4-001R FAD-28 contract-statement correction + race-inertness proof | M4/correction | **ACCEPTED & MERGED 2026-08-13** (squash `efa1ffd`; evidence EV-M4-001R; base `fb4b72b`, implementation `8ef4e33`). Independent review **ACCEPT**, zero blocking — comment-stripped byte-identity proven; inertness unfalsifiable through any consumer or the enforcement read plane; the rebuild proven load-bearing by omission; adjacent interleavings held R1/unique-issue; probes retained (`review/m4-001r` @ `9fc280d`). Findings adjudicated: R-1 MEDIUM out-of-scope → NR-17 + OPUS-M4-001S (FAD-36); R-2 → M4-005 candidate; R-3 → M4-001S; R-4 cosmetic. Orchestrator acceptance battery on post-merge main (tree byte-identical to branch): 15/15 (1796) · 43/43 · 127/127 (seed 249048) · SBX 47/47 0-wrong-tenant · cycle 0001–0016 clean | **ACCEPTED** |
| OPUS-M4-001S requires_expiry flip-vs-grant write-time serialization (review R-1) | M4/correction | **ACCEPTED & MERGED 2026-08-13** (squash `d910af6`; evidence EV-M4-001S; base `9b260eb`, implementation `3ba0b20`; migration 0017). Review **ACCEPT, zero blocking** (probes `review/m4-001s` @ `08c763a`) — both orders refused, forbidden-state hunt 16/16 held, each lock half load-bearing, deadlock hunt clean, retirement race preserved measured, guard bodies un-loosened, down-migration honest. V-1..V-4 recorded (FAD-37). Orchestrator battery green on byte-identical post-merge main; red-cases authoritative 44/44 after three diagnosed transient runs (new "No test files found" signature → M4-005; axe preview class; mediaanalysisd load storm). **NR-17 RETIRED.** Orchestrator process defect at acceptance recorded honestly in EV-M4-001S §0 (a chained battery launch violated the verify-before-next rule; standing note added) | **ACCEPTED** |
| OPUS-M0-001 isolation spike | M0/SP-A | **ACCEPTED & MERGED 2026-08-01** (ea77ac5; evidence EV-M0-SPA). Session-limit interruption recovered: revision found complete-but-uncommitted, independently re-verified (36/36), committed. Second review: APPROVE WITH FOLLOW-UPS, blocking items fixed. Orchestrator applied one-line README count fix during recovery | **ACCEPTED** |
| OPUS-M0-002 scaffold + CI gates | M0/scaffold | **ACCEPTED & MERGED 2026-08-01** (12/12 gates + 14/14 red-cases re-run by orchestrator; evidence EV-M0-SCAFFOLD; 4 deviations accepted: bundle-text host allowlist w/ request-context voiding, hand-rolled secret scan, spike-local credential prefix, root nginx conf; orchestrator fixed one stale CLAUDE.md sentence, disclosed). Follow-ups recorded: prettier-as-gate decision deferred to M1; domain node-builtin rule stays strict until a recorded need | **ACCEPTED** |
| OPUS-M0-003 solver boundary spike | M0/SP-C | **ACCEPTED & MERGED 2026-08-01** (8dad022; evidence EV-M0-SPC; H-6 finding invalidated the seed+worker-count reproducibility clause — spike-contradiction protocol executed: SPEC-04/doc-02/doc-12 amended, FAD-10) | **ACCEPTED** |
| (internal) architecture verification review | planning | commissioned 2026-08-01 | see [../22-readiness-assessment.md](../22-readiness-assessment.md) |
| OPUS-M1-001 tenancy schema + SPEC-01 context middleware | M1/tenancy kernel | **ACCEPTED & MERGED 2026-08-02** (356ddf5; evidence EV-M1-TENANCY). Second review REVISE (3 blocking: T-01 scenario fidelity, unfalsifiable red case, falsified `users`-policy claim) → revision closed all R-findings + self-disclosed 2 defects (owner-exempt policy impossible in PG; harness child run deleting parent DB) → delta re-review APPROVE WITH FOLLOW-UPS → final delta N-01..N-06 (pg_temp pin, roles-aware exemption check, residual record). 3 escalations ruled FAD-11; FAD-12 from review R-04. Orchestrator re-ran full battery at each round. Residuals 1–7 recorded in EV-M1-TENANCY INDEX §5.2 — **OPUS-M1-002 entry conditions** | **ACCEPTED** |
| OPUS-M1-002 authorization evaluator (SPEC-06) | M1/authz kernel | **ACCEPTED 2026-08-03**, landed via OPUS-M1-004 (f547e90; evidence EV-M1-AUTHZ). Fifteen-row truth table (packet's "fourteen" was stale — FAD-13); 39,285,000-case cross-product vs an independent oracle, unsampled, 0 disagreements. Second review REVISE (S-01/S-08 blocking: L1 read an arbitrary effective-dated entitlement row, so one legitimate admin write silently 404'd an entire organization — reproduced end-to-end; plus unrecoverable sole-admin lockout, explicit deny failing OPEN on a malformed instant, three controls proven inert by mutation) → revision closing all → delta re-review APPROVE WITH FOLLOW-UPS → final delta (CHECK constraints added so a false code comment became true, S-21/S-24/S-25). Residuals 1/2/4/5 closed at the database; 1b/6/7 recorded. Session-limit interruption recovered with no loss | **ACCEPTED** |
| OPUS-M1-004 M1 kernel integration (FAD-13) | M1/integration | **ACCEPTED & MERGED 2026-08-03** (f547e90; evidence EV-M1-INTEGRATION). Merged 002 onto 003 by composition at every conflict (FAD-12 ordering: evaluate → deny → mutate → audit in one unit of work); worker moved to the real evaluator and `provisionallyAuthorized` deleted; audit emission for 002's mutations; tenant-table registry extended to migrations 0002+0003 — which surfaced five unprobed gaps, each closed by making an assertion apply. Second review APPROVE WITH FOLLOW-UPS (merge verified as composition by blob-classifying every file against both parents). **Orchestrator verification caught what both agents missed:** the C-2 lease-recovery proof passed 545/545 in-suite and failed 5/5 standalone — diagnosed as a seeded-backlog precondition the test never declared, NOT a reclaim defect (reclaim measured 574–576 ms); FAD-14 C-2 stands. T-01/T-02 (missing red case for the emission gate; deny-ordering test denying a layer too early) fixed before merge | **ACCEPTED** |
| OPUS-M2-001 SBX harness + G-ARCH tenancy subset + NR-13 decision + MULTI | M2/evidence infrastructure | **ACCEPTED & MERGED 2026-08-03** (bc61ee7; evidence EV-M2-NR13 + EV-M2-SBX). Phase A measured NR-13 → ratified FAD-15 (mid-task stop honored). Second review REVISE (R-01..R-12; vacuity detector accepted any throw, five declared artifacts unwritten) → delta REVISE (D-01..D-09; refusal-cascade truncating the five-role sweep with false causes in shipped evidence, absence-based probes reproducing the very vacuity shape the task targets, write arm never reaching RLS) → second delta APPROVE WITH FOLLOW-UPS (E-01..E-06, closed) → rotating seed 65339 caught coupling seven AT ACCEPTANCE (fixed, seed joined the set — FAD-15 ruling 3 exercised on its first outing). One tooling-outage interruption recovered per protocol (read-only inspection; dirty tree preserved and verified, not redone); one orchestrator diagnosis (the "L3.3 kernel-guard mystery" was L4.1's superuser grant INSERT — triggers fire for superusers, the control working). Final: check 12/12 (585 tests), red 14/14, sbx 4 PASS + 1 EVIDENCE_BLOCKED (0 vacuous / 0 probe-error), fixture-regression 39/39; all re-run by orchestrator on the branch AND on main post-merge. FAD-15 corrected once from review evidence (R-06/R-07). Implementer's own tally: of seven couplings found, one by reading, six by shuffled execution or the adversarial reader | **ACCEPTED** |
| OPUS-M2-002 shift-type catalogue + groups + valid combinations (first UI slice) | M2/catalogue | **ACCEPTED & MERGED 2026-08-03** (ff2e67b; evidence EV-M2-CATALOGUE). Second review REVISE (S-01..S-12; blocker: vacuous cross-group proof arm — the reviewer's CAR-001-class policy mutation left the sweep green; also INSERT-blind pick-position gate, cosmetic effective dating, arbitrary-row pick-position read, version-not-a-control) → revision (incl. sweep-mutation red case pinning the reviewer's exact mutation) → delta APPROVE WITH FOLLOW-UPS (U-01..U-07 closed; notable: U-04 transient red-cases GREEN-arm right after check → runbook note below) → second-merger rebase 0004→0005, mechanical composition (one composed test-assertion hunk disclosed: set-equality + shrink-floor), two microsecond-vs-millisecond window defects found by the shuffled gate post-rebase and fixed deterministically. Implementer's own SBX-001 run found authorize-after-body-parse disclosure (fixed, 422 class, both directions asserted). Session-limit interruption recovered. Orchestrator final rerun on rebased branch: 850 tests 12/12, red 14/14, sbx 403-cell matrix / 175 readings / 0 wrong-tenant / 25 of 25 tables, fixture-regression 57/57; battery green on main post-merge. First parity rows move: CAP-011, CAP-012, CAP-004 (holiday slice) | **ACCEPTED** |
| OPUS-M2-003 effective-dated work profiles + FTE + qualifications (API-first) | M2/staffing parameters | **ACCEPTED & MERGED 2026-08-03** (567964c; evidence EV-M2-PROFILES). Second review APPROVE WITH FOLLOW-UPS (T-01..T-08) — the in-force centerpiece survived reviewer boundary/race/rewrite attacks incl. a constructed ambiguity arm and the FAD-17(2) recursion hazard built for real; findings closed incl. forward-only authoring (retroactive 422, future admin capability carried), weekday-target carry-forward on supersession, owner-binding DELETE guards, audit before/after+mechanism. Implementer's own verification caught two gaps beyond the T-list (route 500→422; an order-dependent test its in-flight gate exposed). Review also found the sequence::text class in PRODUCTION verification code (T-07 → micro-packet) and the shared-default-port hazard (E-1 → micro-packet). Session-limit interruption recovered from commits. Orchestrator rerun: 739 tests 12/12, red 14/14, sbx 17 tables clean, fixture-regression 47/47; battery re-run green on main post-merge | **ACCEPTED** |
| OPUS-M2-004 integration + hardening | M2/integration | **ACCEPTED & MERGED 2026-08-04** (fce095c; evidence EV-M2-INTEGRATION). Seven deliverables: shift_type_qualifications (0006) + eligibility reads, D-1 fixture consolidation, sequence::text fix incl. PRODUCTION audit verification + seed 531651 → FIXED_SEEDS (12), per-worktree port derivation (E-1/E-2 closed, proven by simultaneous batteries), request-budget ledger under the budget=2 ruling (green-before-red both directions), red-case port wait (U-04), holiday-read ruling via action key. One escalation ruled mid-task (deliverable 5 glob widening + budget ruling). Second review APPROVE WITH FOLLOW-UPS (V-01..V-08, closed; V-02 fault-masking fix on read AND command paths). Implementer self-caught three defects (incl. reintroducing the S-01 class — caught by M2-003's structural scan) and self-corrected one falsified premise. Orchestrator rerun green on rebased branch (881 tests, 63/63 gate) and on main post-merge | **ACCEPTED** |
| OPUS-M3-008 integration + hardening (final M3 packet) | M3/integration | **ACCEPTED & MERGED 2026-08-05** (dfa717f; evidence EV-M3-INTEGRATION). Step-06 HARD-rule re-validation (checker not solver — complete-file review; 6 EVALUATED incl. CallSpacing after the review falsified its reason, 24 honest NOT-EVALUABLE; FAD-27 fail-closed ruling); qualification expiry vs assignment date; audit READ API + group-scope verification fix; FAD-25 holiday key split; migration 0011 (up-marker self-found defect); **NR-14 redesign — restore discipline RETIRED** (zero-modification tree proven by implementer, reviewer, and orchestrator; writers discovered not hand-kept); preview + nested port derivation (E-2 closed for good; O-1 the recurring 55455 seed now digest-derived, fixture-regression 102/102); composed proofs; QA-SCH 9/6/1; SPEC-14 A-cells done, 10 M-cells honestly unclaimed. Second review REVISE (B-1 raw NUL binary-blinding the central file — the class this same commit fixed elsewhere; B-2 the false CallSpacing reason) + 8 fold-ins → delta closed all; reviewer ran 23 adversarial probes + mutation tests both directions and could not falsify the load-bearing set. Three implementer self-found defects by running (a migration that recorded itself applied while executing nothing; NR-14's largest writer missed; a NUL/space seed divergence deriving different ports invisibly). Delta on fresh checkout: 1384 tests 13/13, red 21/21, fixreg 102/102, sweep 44/44, cycle 0001..0011, NR-14 tree zero | **ACCEPTED** |
| OPUS-M3-005 publication + version-management experience | M3/publication UX | **ACCEPTED & MERGED 2026-08-05** (112b8c4; evidence EV-M3-PUBLICATION-UX). Review with recorded-conflict blockers (no rule execution), graduated friction, grant-only publish/revert (zero-DB-delta denials proven), client-retained idempotency key (publish-once red-cased, 12 concurrent rounds), history/comparison/affected-staff, immutable-history display (DOM-asserted), forward correction, audit-read ESCALATION correctly executed (publication_records display labelled not-the-chain; read API → M3-008). FAD-26: sign-off route + STALE_REVIEW CAS ratified after attack. Self-found serious defect: error mapping inside the unit of work committed the step-04 publishing state on refusal — found by falsifying the period lock's load-bearing-ness; reviewer reproduced both directions. Second review: NO blocking findings (11/11 claims, 18 probes). Mini-delta closed the unbudgeted interaction + version-number rendering. Delta re-verified: 1275 tests 13/13, red 19/19, fixreg 94/94 | **ACCEPTED** |
| OPUS-M3-006 staff-facing schedule views | M3/staff views | **ACCEPTED & MERGED 2026-08-05** (57685ab; evidence EV-M3-STAFF-VIEWS). Personal schedule + daily sheet (doc-07 distinction held), calendar AND tabular first-class, server-side timezone rendering with instant-overlap windows, overnight both-days incl. DST-crossing nights, drafts/superseded invisible at the query level (red-cased). FAD-25: the carried calendar-key ruling — self-scoped schedule.own_published.read + schedule.published.read under CAP-020, write-never-implies-read standing rule, holiday-key split → M3-008. Second review: NO blocking findings — self-scoping survived a viewer granted all 25 group capabilities and a 38-capability evaluator drive. Claim-accuracy delta: N-1 closed by MAKING the per-clause claim true (decoupled states crafted through legal transitions, measured per clause); ledger honesty; citation disambiguation. Contention seeds re-proven serially. Second-merger rebase onto 112b8c4: ONE additive router.tsx conflict disclosed, composed ledger 38 interactions 0 duplicates. Orchestrator battery on rebased branch: 1305 tests 13/13, red 20/20, fixreg 96/96, sweep 44/44 0 wrong-tenant, cycle clean | **ACCEPTED** |
| OPUS-M3-004 scheduler authoring experience | M3/authoring UX | **ACCEPTED & MERGED 2026-08-05** (20c0c13; evidence EV-M3-AUTHORING-UX). Grid + first-class accessible alternative (reviewer proved the STRONGER full-sweep equality), cell editor, 16 thin routes over the frozen M3-003 service, 27 remaining rule editors (30/30 kinds, registry-derived count), FAD-23 eligibility ruling. Second review REVISE, **4 blocking, all from reviewer-authored CONCURRENT probes**: B-1 CAS non-atomic at READ COMMITTED (11/12 rounds both writers won; shipped proof was sequential; docblock claim false), B-2 empty-cell race → 404 to the loser (saved accidentally by a unique constraint, mislabeled), B-3 add+remove silent merge, B-4 decorative override reason. Fixed: pg_advisory_xact_lock INSIDE compareAndSet (frozen service untouched), 409-class mapping, three 12-round race tests asserting DB state, falsifiability-before-wiring, both-sides reason enforcement + both-arms e2e. Fold-ins: 366-day period bound (contract; DB CHECK → future migration, recorded), datesBetween throws, aria parity, grid denial e2e, budget completions. Second-merger rebase onto c3e1baa: ONE additive router.tsx hunk disclosed; composed ledger verified (27 interactions). Orchestrator battery on rebased branch: 1245 tests 13/13, red 18/18, fixreg 91/91, sweep 44/44 0 wrong-tenant, cycle 0001..0010 clean. Standing note: the two earlier not-proven red arms were the fixed-preview-port (4173) collision across concurrent worktrees — E-2 class, permanent fix owned by M3-008; interim: SP_TEST_PREVIEW_PORT override | **ACCEPTED** |
| OPUS-M3-007 group settings + site attribute | M3/settings | **ACCEPTED & MERGED 2026-08-04** (c3e1baa; evidence EV-M3-SETTINGS). Migration 0010 (locations FORCE RLS + typed CHECK-constrained settings columns + settings_version), audited timezone admin (no stored instant moves — proven), request-until/picklist-access stored+validated with enforcement honestly absent (M5/M9), both site directions fixture-proven with RED arms, NO site entity (rule 12 held under independent search). FAD-24 (3 keys role-implied group_admin alone; 6 settings.* names; no-op writes unaudited with control). Second review: **no blocking findings**, 12/12 claims survived incl. a reviewer mutation test of the site fixtures and a full role-matrix attack; NB round closed (no-op audit events; acknowledgement read-not-derived; a shuffle consequence self-caught). Implementer self-found: a decorative CAS (guarded on the wrong counter — replaced with DB-owned settings_version), an order dependence, an SBX fixture asserting the opposite of the model. Delta re-verified: 1128 tests 13/13, red 16/16, fixreg 88/88, sweep 44/44, cycle clean. Honest evidence classification: days_before_period_start flagged SCHEDULEPOINT-REQUIREMENT, not observed | **ACCEPTED** |
| OPUS-M3-002 typed rules + compiler + authoring + B-\* corpus | M3/rules (carried M2 slice) | **ACCEPTED & MERGED 2026-08-04** (7dd37dc; evidence EV-M3-RULES). Mandatory pre-migration escalation honored (implementer STOPPED; ruled FAD-21: rules+rule_sets only, category enum — pattern/staff_rules superseded, field mapping verified complete). 30-node closed AST, deterministic compiler (5 TZ/locale combos), NEW rule-node-mapping gate (13th, reviewer mutation-tested 5 ways), DB round trip all 30 kinds × HARD/SOFT with mutation control, B-\* corpus with construction arguments + minimal certificates, authoring API (no new capability key — doc 08 §6 reading verified) + first UI slice (3/30 editors disclosed on-page, remainder → M3-004). Second review REVISE (B-1 non-minimal overdemand certificate — the M4 grading oracle; + 9 fold-ins incl. unknown-key validator rejection, bidirectional category CHECK, two false comments) → delta: exactly one fixture hash changed, three verified certificates untouched. Orchestrator delta battery on fresh checkout: 1023 tests 13/13, red 16/16, fixreg 79/79, cycle clean, corpus byte-identical. Reviewer could-not-falsify list: closed set at every layer/role, immutability constraints, determinism, gate non-vacuity, authz, audit 1:1, FAD-17(2) non-recursion, I-13 | **ACCEPTED** |
| OPUS-M3-003 schedule periods + identity/snapshot model + publication core | M3/publication spine | **ACCEPTED & MERGED 2026-08-04** (e275597; evidence EV-M3-PUBLICATION). Migration 0009 (11 tables incl. current_published_assignments real-table D-1b, groups.timezone); D-15a/b/c with FAD-22 INSERT coverage; publication transaction (period lock, required CAS, D-17 one-domain idempotency with honest replay, one outbox event, crash recovery); draft-only manual assignment/override; forward-only correction; affected-staff diff + scalar outbox intents; complete V-01..V-19 (23 rows) + SBX-018 falsifiable; I-11 both halves. Second review REVISE, 2 blocking both reviewer-found by probing: **B-1** the SECURITY DEFINER prune was a within-tenant per-version DELETE primitive irreversibly disarming D-1b (now refuses current/unpublished versions, red-cased with the reviewer's exact probe); **B-2** D-15a missed INSERT — published content set could GROW — and moveCredit/voidCredit/recordConflict reached published versions, recordConflict with neither capability nor audit (rules 4/6) — closed under FAD-22, all 7 mutators assertEditable. +6 fold-ins (required CAS after the reviewer raced two different versions into a double-publish; key domain; honest replay; no raw 23505; D-1b tenant-qualified; length-prefixed digests). Reviewer non-vacuity proof: 3 subject mutations each failed the harness naming its row. Second-merger rebase onto 7dd37dc: pure composition disclosed per hunk (schema.ts reconstructed under a mechanical additive-only proof), sweep floor 33→44. Orchestrator re-ran the battery on the rebased branch: 1060 tests 13/13, red 16/16, fixreg 82/82, SBX 301 readings 0 wrong-tenant, cycle 0001..0009 clean. Disclosed: implementer once killed the parallel worktree's postgres (auto-recovered; no data loss) | **ACCEPTED** |
| OPUS-M3-001 authentication + sessions + invitation/activation | M3/authn (carried M1 slice) | **ACCEPTED & MERGED 2026-08-04** (6a01196; evidence EV-M3-AUTHN). First-party email+password + TOTP MFA, server-side sessions (hashed tokens, idle+absolute lifetimes on a clock seam, immediate suspension invalidation, rotation on privilege change), invitation/activation separated from reset (STM-017/018), login-email change (CAR-027), session-epoch freshness (I-19), break-glass session auditing (SPEC-11 §3.2), pre-auth route-policy class. Migration 0007. Second review REVISE (**B-1** raw session secret in two evidence artifacts — the secrecy test scanned logs/payloads/rows but not the evidence files it generates; **O-1**, orchestrator-found, SBX-001 not actually re-run with real sessions and a now-**false** `EVIDENCE_BLOCKED(authn)` reason left in place while the report claimed it cleared) → revision closed both (redaction by construction + evidence-dir secrecy scan with its own falsifiability probe; the 611-cell matrix re-driven through 10 real per-role `__Host-sp_session` sessions, false block removed) + **N-1** raw-NUL→` ` regression against a prior review ruling + **N-3** table-count wording → orchestrator delta re-verified on a fresh checkout: 12/12, 965 tests, 14/14 red, fixture-regression 75/75, SBX 5/5 (0 blocked/0 vacuous), sweep 30 tenant tables +users / 210 readings / 0 wrong-tenant, migration cycle clean. Reviewer could not falsify cross-org RLS, namespace separation, TOTP replay, AES-GCM AAD binding, suspension immediacy, clock-seam integrity, forged-token handling, the pre-auth gate, the unmodified evaluator, or the capability baseline. FADs ratified: **FAD-18** (user_mfa/AES-256-GCM AAD-bound), **FAD-19** (scrypt), **FAD-20** (password_reset_tokens); doc 06 §3.2 amended 2026-08-04. Implementer self-found 5 defects by running (lockout counter rolled back with the failed attempt; vacuously-true activation single-use; negative session window from a DB-clock/injected-clock mismatch). SBX-005's two remaining blocked sub-scenarios are **non-authn** (impersonation, notification-delivery milestones) | **ACCEPTED** |
| OPUS-M4-001 solver runtime boundary + canonical input | M4/solver boundary | **ACCEPTED & MERGED 2026-08-12** (squash `b1aa64e`; evidence EV-M4-BOUNDARY; migration 0016). Independent review **REJECT** — B-1 blocking (the RPC MAC covered a re-derivation of the message, not the wire bytes; ASCII-identical, non-ASCII-divergent) + C-1/C-2/C-3 + N-3/N-5/N-6 → repaired (framed wire, pre-auth structured refusals, honest `crashed` vs `killed`, cycle-`finally`, 32 anchors re-verified) → delta review by the original reviewer on `7f18b22` (`review/m4-001-delta`; probes retained at `a9c5309`). Authoritative battery §2b: 15/15 (1792/139) · 43/43 red · 126/126 fixreg · SBX 47/47 / 0 wrong-tenant. Limitations: stub solver; no OR-Tools behaviour; no benchmark; image authored NOT built (CI condition); CPython 3.9.6 local (3.12 rerun CI condition); tz divergence detectable not re-derivable. **This row, and the three M4-000 rows below, were added 2026-08-13 during Stage-1 recovery — the M4-era acceptances had never been logged here (doc 24 §E gap, recorded honestly)** | **ACCEPTED** |
| OPUS-M4-000C rule revisioning/registry, publication handoff, provider gate | M4/prerequisites | **ACCEPTED & MERGED 2026-08-12** (`323d576` as second merger; evidence EV-M4-INPUT-C; migration 0015). Review ACCEPT WITH CONDITIONS — C-1 (nine-class digest claim, three falsifiable, two structurally unprovable) closed with the both-halves mutation matrix + shrink-guard pin, delta-verified 9/9. Composed battery caught 8 cross-packet failures / 5 mechanisms; FAD-32/33 | **ACCEPTED** |
| OPUS-M4-000B schedule graph, locations, time | M4/prerequisites | **ACCEPTED & MERGED 2026-08-12** (`a614f7e`; evidence EV-M4-INPUT-B; migration 0014). Review ACCEPT WITH CONDITIONS — 2 blocking reviewer-probe finds (B-1 degenerate DST-gap intervals → R-B4a, 399→0; B-2 stale-basis 404→409 with wire proof), both closed + delta-verified; the delta review also caught closure commits on the wrong branch pre-merge. FAD-31 | **ACCEPTED** |
| OPUS-M4-000A staffing-input integrity + shared eligibility verdict | M4/prerequisites | **ACCEPTED & MERGED 2026-08-10** (`94af3c5`; evidence EV-M4-INPUT-A; migrations 0012+0013). Review ACCEPT WITH CONDITIONS — F-1 (DB capability gap → 0013 + red case), F-2/F-3 (accuracy/citations; the granted-while-retiring claim falsified by probe P3e → §8 corrected), F-4 (FAD-29), F-5 (scope crossing → FAD-30); all closed, battery re-proven. NR-15 registered. FAD-28/29/30 | **ACCEPTED** |
| OPUS-M1-003 audit chain + outbox (TDG-04 micro-spike first) | M1/audit+async kernel | **ACCEPTED & MERGED 2026-08-03** (946fc72; evidence EV-M1-AUDIT + spikes/sp-d-worker). **TDG-04 CONFIRMED — GO on 18/18 spike experiments** (transactional enqueue, 4h hard-coded lease, crash recovery exactly-once) with four GO conditions C-1..C-4 carried into production code. Second review REVISE (tail truncation undetected while X-03 claimed satisfied; outbox exactly-once discharged weaker than the spike — double delivery measured in two windows) → revision closing both + production wiring + SPEC-11 periodic jobs + checkpoint-poisoning composite FK + verify-function confinement → delta re-review APPROVE WITH FOLLOW-UPS (blockers re-probed closed by the reviewer's own unchanged probes) → micro-delta N-01..N-04. Session-limit interruption recovered mid-revision with no loss. New: the system's only cross-tenant read (maintenance plane, explicit `TO app_migrator` policies + one `app_worker`-only enumeration function, no tenant content beyond organization ids, not itself audited — EV-M1-AUDIT §5 #16) | **ACCEPTED** |
