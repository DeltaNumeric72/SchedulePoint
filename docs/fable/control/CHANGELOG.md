# CHANGELOG

## 2026-08-21 — MILESTONE M4 COMPLETE: OPUS-M4-005 ACCEPTED & MERGED (480105b); tagged `milestone/M4`
- The final packet, executed across three sessions and two interruptions with zero loss (the checkpoint at `f746a45` reconstructed exactly): the 40-proof concurrency/recovery matrix; D-4b + the `build.solve` queue binding (migration 0019 — policy + SECURITY DEFINER counter, no table) with claim-time per-org capacity and S-16t; absolute input staleness (class-level wire per FAD-50 C-4); SBX-015/016/017 (9/9; SBX-016 22/22); FAD-48 clause (A) BUILT — `GET /me/context`, the client context sender, and the real-stack 14-step critical path at both viewports with no interception and a zero-skip guard; the hardening register discharged (NR-15 diagnosed/repaired/verified · NR-16 · NR-18 with the gate's magic-byte hardening · NR-19 with its load-bearing proof · runner ERRORED + spawn signatures · phantom citations · screenshot currency).
- **The milestone's last two mysteries had one measured cause:** the test-support pinned set carried a 10s wall clock beside the 100-unit deterministic budget; CP-SAT stops at whichever arrives first, and the wall clock always won on `B-fairness-shaped` (`deterministicTimeUnits` 21.76→12.53 under load; pinned at 76.702882 once freed). S-08t now asserts its precondition and pins statistics equality. The product-side half became FAD-49/50: `resultReproducibility` derived on read from parameters + termination fact + statistics — a CANCELLED, killed, wall-truncated, or unrecorded run can never claim `reproducible` on any surface (the B-1 cancellation hole was reviewer-found against real CP-SAT and closed with required parameters that made the compiler audit all call sites).
- Governance: FAD-48 (clause (A) built not waived) · FAD-49 (result-side honesty by derivation; solver/** stayed closed) · FAD-50 (review adjudication: B-1 + 7 conditions incl. the unprovable NR-19 closure, the zero-skip real-stack hole, the staleness disclosure narrowing) · FAD-51 (final round: spawn ERRORED signature, aggregate-before-truncate completeness, the exact step-14 outcome). Four review rounds ending **ACCEPT** (probes `review/m4-005` @ `bdd78dc`).
- **Orchestrator acceptance battery on the final candidate (EV-M4-005 §24): validators 95/95 · 36/36 · research-from-fresh-clone PASS; check 17/17 (2153); red-cases 64/64 — the milestone's first fully-green complete battery; fixture-regression 153/153 (seeds 123456 + 531651 green); SBX 9/9, 371 readings, 0 wrong-tenant, 53/53; migration cycle 0001–0019 clean by name; real-stack 2/2 both viewports.** Post-merge main: byte identity on every branch-touched path, check 17/17 re-proven, all validators green.
- Fable exit hygiene: the research validator now passes from a FRESH checkout (`b370e03` — eight tracked placeholders for directories git cannot carry empty; FAIL reproduced and PASS proven from clean clones); the two reserved control-doc raw NULs repaired and **the NUL-gate baseline is EMPTY**. Parity at exit: 18 verified · 3 in-progress · 37 not-started; no capability dropped. Exit report: doc 36; Codex review prompt FILED (doc 37 — the review itself is owner-initiated and outstanding). **M3R remains PAUSED. No production-readiness or compliance claim. M5 and the prototype-enablement lane NOT begun.**

## 2026-08-18 — OPUS-M4-004 ACCEPTED & MERGED (79e78b6): E2 optimization, quality, and explanations
- The E2 layer on the E1 engine, no migration: one global integer scale (10⁴, doc 08 §3.4 decided) single-sourced across the language boundary and digest-refused on mutation — closing E1's silent truncation of sub-unit weights; the recorded objective profile `e2-default-v1` (weighted sum, no lexicographic claim); T2 minimisation that earns `EXPLAINED_MINIMAL` only within its caps; §4 statistics persisted; §7 metrics with a per-cell demand rate plus an INDEPENDENT demand check; PO-DEC-13's taxonomy; fairness dispersion; the progressive-pin reader; opt-in deterministic mode with its cost measured and stated on the surface (≈1×/≈10×, no band — PO-DEC-23 resolved-not-banded until M6); creditless build drafts; comparability enforced with refusals rendered as refusals.
- Governance: FAD-46 (review adjudication: three surface defects blocking — a refusal rendered as "identical", incomparable measurements side-by-side, an unmatched demand rate; PO-DEC-23 record corrected to RESOLVED; E2-R1's ratio change ratified as a semantic change) and FAD-47 (acceptance: E2-R1..R4 ratified; `demand-not-met`→`hard-breach` ruled correct; the screenshot-currency and runner-leftover items to M4-005). Review REVISE → repair (13 findings; **the raw-NUL gate mandated by FAD-45 caught its FOURTH occurrence in the repair round's own new file** — with path and offsets, on the first check against new code; the F-09 phantom-citation sweep found four, then the reviewer found the sweep itself under-scoped: 9 pre-packet full-path phantoms recorded for M4-005) → delta ACCEPT WITH CONDITIONS (F-14: a sixth rejection reason unplumbed past the validator — an over-staffed candidate would have been refused with no reason shown; fixed with a four-link chain pin asserting EVERY reason round-trips) → micro-repair.
- Battery on byte-identical post-merge main: **17/17 gates (2042 tests) · 59/59 red cases · 146/146 fixture-regression (seed 900437) · SBX 371 / 53 of 53 / 0 wrong-tenant · migrations 0001–0018 clean.** Continuity: a usage-limit termination mid-battery lost nothing — the detached run finished on its own. **The scheduling pipeline is functionally complete on main; ONE packet remains — M4-005 integration/recovery/close.**

## 2026-08-16 — OPUS-M4-003 ACCEPTED & MERGED (8deb4c5): the build lifecycle and scheduler experience
- Migration 0018 (six tenant tables; sweep floor 48→54 registry / 47→53 sweep): the sixteen states with database-guarded transitions (193 illegal edges reviewer-swept, zero acceptances), epoch fencing with durable refusals, the reaper, D-4a/D-4d partial unique indexes, D-17 idempotency. No candidate becomes usable before the independent checker passes it; `approved` requires zero unresolved hard findings; selection creates a new M3 draft through ONE write path (FAD-44's `origin` grant — the write-path S-01 class closed before it opened). Scheduler UI with the full state matrix + retry at both viewports (axe 412; budgets measured: the one assumed budget was corrected DOWN to its measurement).
- Governance: FAD-44 (four pre-review disclosures adjudicated) and FAD-45 (review conditions: the **raw-NUL gate mandated on the class's third recurrence — now CI gate 17** with a strict path+count baseline pinning NR-18's three doc instances; the UI retry gap; five falsifiability arms — the fencing state-clause as a full red case; D-4a's five-in-flight-states reading ratified; D-4b assigned to M4-005 with the queue binding). Review ACCEPT WITH CONDITIONS → delta ACCEPT (the reviewer falsified the NUL gate six ways and openly corrected its own round-1 measurement) → micro-repair (the append-only arm de-decorated — grant refusals are not trigger proofs — with an empty-table FOR-EACH-ROW vacuity self-caught in the act of repairing it).
- Battery on byte-identical post-merge main: **17/17 gates (1967 tests) · 53/53 red cases · 143/143 fixture-regression (seed 956607) · SBX 371 readings / 53 of 53 / 0 wrong-tenant · migrations 0001–0018 clean.** Six+ implementer self-caught defects on the record. **Next: M4-004.**

## 2026-08-15 — OPUS-M4-002 ACCEPTED & MERGED (5e0ac4e): the E1 feasibility engine
- The milestone's centerpiece, and its hardest passage: three Opus implementers, four ruled escalations, one REVISE review cycle. **FAD-38** (snapshot v2 — the pre-code escalation that saved the packet from building on missing inputs) · **FAD-39** (isOnCall) · **FAD-40/41** (the C-2 drain-starvation investigation: the orchestrator's own mis-attribution corrected on the finishing agent's capture; BOTH pre-existing hygiene defects fixed — outbox-dispatch's 20 orphan rows, periodic's dead-pool-locked checkpoint job — plus NR-19, the production `reclaimStalePools` release-vs-lock gap it surfaced) · **FAD-42** (review adjudication) · **FAD-43** (acceptance ratifications: RK-05 min-composition, RK-06 target-never-bound, RK-10 uuid, R-6 empty-scope vacuous).
- The review earned its mandate again: R-1 (blocking, reviewer-probed) — `MinimumRestBetween` posted adjacent-date pairs only, so multi-day rest rules half-compiled; fail-closed (the §3.3 independent checker refused every such candidate — the deliberate redundancy proven load-bearing in production form) but unusable-build class. Repaired with an equivalence proof the delta reviewer sat on with worst-case constructions and could not break; plus the NUL repair (14/15 hash-neutral), the four R-6 pins, and the 16th gate (Python↔TS kind parity, six-way mutation-tested). **Delta ACCEPT, zero new findings.**
- What is now true on main: 22 of 30 rule kinds independently evaluable (8 honestly fail-closed with named owners; 0 open rulings, all eleven RK ids resolved-and-cited); the real CP-SAT model behind the unmoved M4-001 boundary; the pure candidate validator incl. the eligibility-gap check the race-produced fixture forced; the 15-class deterministic corpus (reviewer-reproduced hash-for-hash); S-08t bit-identical reproduction under the full pinned set. Battery on byte-identical post-merge main: **16/16 gates (1899 tests) · 48/48 red cases · 137/137 fixture-regression (seed 617763) · SBX 47/47 0-wrong-tenant · migrations 0001–0017 clean.** NR-18 registered (tree-wide doc NULs, the M3-008 class's third recurrence). E2/UI/lifecycle remain M4-003/004; no benchmark bands; no production-readiness claim. **Next: M4-003 packet expansion.**

## 2026-08-13 (latest) — OPUS-M4-001S ACCEPTED & MERGED (d910af6); NR-17 retired; the M4-001 recovery arc complete
- Migration 0017 serializes the `requires_expiry` flip against in-flight grants at the row-lock level: flip `FOR UPDATE` on `false→true`, holding guard `FOR KEY SHARE` inside its lookup (same-statement acquisition is the correctness), both orders refused 23001, retirement race deliberately preserved (FAD-28/35 measured). Lock-ordering rule vs the 0003 audit advisory lock documented, tested (30+25 crossed rounds 0×40P01), and adopted repo-wide (FAD-37(2)) — binding on M4-002's solver dispatch. The pre-existing raw-SQL deadlock construction (D1) is retained in-suite as the record of what breaking the rule costs.
- Independent review **ACCEPT, zero blocking**: hand-built both-order races; a 16-case forbidden-state hunt that also proved the surface narrower than assumed (INSERT the only creator, no status re-entry edge, `valid_until` not updatable — pinned as FAD-37(4)); each lock half load-bearing one-at-a-time; guard bodies semantically 0012 + only the locks; the populated cycle honest (the D3 admission returns when 0017 is down). V-1..V-4 recorded.
- Orchestrator acceptance battery on byte-identical post-merge main: check 15/15 (1801); red-cases **44/44 authoritative** after three retained transient-failed runs (NEW runner signature "No test files found" → M4-005 hardening; axe preview class; a load storm from a wedged `mediaanalysisd` at 322% CPU, killed, load 96→5); fixreg 129/129 (seed 368944); SBX 47/47; **cycle 0001–0017 clean**. Orchestrator's own chained-battery process defect recorded (EV-M4-001S §0) + three new runbook standing notes. **NR-17 RETIRED. Next: Stage 2 — the M4-002 packet expansion.**

## 2026-08-13 (later) — OPUS-M4-001R ACCEPTED & MERGED (efa1ffd); OPUS-M4-001S finalized on review finding R-1
- The FAD-28 correction is on main: `grantHolding`'s docblock states the admitted-race/verdict-inertness contract (comments-only, byte-identity-proven), the `retired-verdict` case cites FAD-33(1), and `granted-while-retiring-inertness.test.ts` pins the P3e interleaving permanently — deterministic via the 0003 audit advisory lock (the naive ordering is unbuildable; the reversed pair blocks at the grant's audit insert, observed in pg_stat_activity, fail-loud). Inert on manual read, publication gate, and canonical solver input, with a satisfied control; red-cased in both arms of `retired-verdict` (violation without rebuild: passes blind; with the case's rebuild: 3 consumer arms fail — the FAD-33(1) discipline demonstrated).
- Independent review **ACCEPT, zero blocking** — and produced the milestone's next MEDIUM finding anyway: **R-1**, the `requires_expiry` flip-vs-grant interleaving is admitted with no read-side backstop (`requires_expiry` is not a verdict input; `solver-snapshot.ts:154` overclaims). Adjudicated as **NR-17 + FAD-36**: write-time serialization is the bounded fix (retirement had a verdict dimension; this has none), packet **OPUS-M4-001S** finalized (doc 35 §6c, migration 0017, both orders refused, deadlock analysis vs the audit lock, mutation-cased, independent review). R-2 (red-cases build ordering, pre-existing) → M4-005 candidate; R-3 folded into M4-001S; R-4 cosmetic.
- Orchestrator acceptance battery on post-merge main `efa1ffd` (byte-identical to the reviewed branch): validators 95/95 · 36/36 · PASS; check 15/15 (1796); red 43/43; fixreg 127/127 (seed 249048); SBX 6/6, 47/47, 0 wrong-tenant; cycle 0001–0016 clean. **M4-002 planning remains blocked until M4-001S closes.**

## 2026-08-13 — OPUS-M4-001 closeout (accepted & merged 2026-08-12, b1aa64e) + Stage-1 recovery + OPUS-M4-001R finalized
- **M4-001 (solver runtime boundary + canonical input, migration 0016) recorded ACCEPTED & MERGED at `b1aa64e`, evidence EV-M4-BOUNDARY.** The acceptance merge had landed without its control-plane sweep; this closeout repairs that. Review was **REJECT** on B-1 (the RPC MAC signed a re-derivation — platform `canonicalStringify` UTF-8 vs worker `json.dumps(ensure_ascii=True)`, divergent beyond ASCII) → framed-wire repair (the tag travels beside the body bytes; neither side re-serialises to verify) + C-1 pre-auth structured refusals, C-2 honest `crashed`-vs-`killed`, C-3 cycle-`finally`, N-3/N-5/N-6 hardenings, 32 red-case anchors re-verified after re-anchoring → delta review by the original reviewer (`review/m4-001-delta`; probes retained at `a9c5309`). Authoritative battery: 15/15 gates (1792/139), 43/43 red, 126/126 fixreg, SBX 47/47 tables 0 wrong-tenant. Limitations stand: stub solver, no OR-Tools behaviour, no benchmark, image authored NOT built (CI condition + digest), CPython 3.9.6 locally (3.12 rerun = CI condition), tz divergence detectable not re-derivable.
- **Stage-1 recovery verification on main (serial, inner exit codes, EV-M4-BOUNDARY §10):** validators 95/95 · 36/36 · PASS · check 15/15 on the serial re-run (first run's single R-B4a enumeration timeout diagnosed as contention-class, 47/47 standalone, FAILED capture retained) · red 43/43 · fixreg 126/126 (seed 367857) · SBX 6/6 · migrations 0001–0016 clean. Branch-tree identity to main proven; no stranded M4-002 work; task-log gap found (no M4 rows in control/OPUS-AGENT-RUNBOOK.md) and repaired with all four M4 acceptances.
- **FAD-28 reconciliation ruled (FAD-35):** the admitted-race / verdict-fail-closed contract is authoritative; `grantHolding`'s docblock still carries the falsified write-time-exclusion claim (single occurrence, repo-swept). **OPUS-M4-001R finalized in doc 35 §6b** — docblock correction, permanent P3e-interleaving inertness proof (manual + publication + canonical solver input), FAD-33 citation fix; mandatory independent review; issues alone before any M4-002 planning. **FAD-34** records the M4-001 acceptance rulings (framed-wire MAC pattern; kill-attribution vocabulary; sweep = registry − 1).

## 2026-08-12 — M4-000 COMPLETE: OPUS-M4-000B (a614f7e) and OPUS-M4-000C (323d576) ACCEPTED & MERGED
- **000B (schedule graph/locations/time, migration 0014):** 11 composite FKs + 6 invariant triggers all red-cased with FK-alone arms; membership semantics R-B1/2/3 (no override arm — state is a fact, not a judgement); pure calendar/time domain; timezone basis snapshots on versions; R-B4/R-B5/R-B6/R-B7 ratified. Review found 2 BLOCKING via reviewer-authored probes: B-1 — a shift shorter than a DST gap inverted (399 enumerated degenerate pairs; fixed by R-B4a whole-interval translation, re-enumerated to 0, reviewer-reproduced independently); B-2 — the stale-basis refusal answered 404 instead of its declared 409 (never HTTP-tested; fixed with wire proof + restored-zone control). The delta review also caught the closure commits on the wrong branch — repaired by cherry-pick, content-identity proven, before merge.
- **000C (rule revisioning/registry/publication handoff/provider gate, migration 0015):** append-only rule_revisions via SECURITY DEFINER writer (no runtime role can forge — 12/12 refused); one DB counter = CAS token = revision number; generated 30-kind registry gate with three step-06 count corrections and RK-RULING-01..11; period lifecycle; ONE material-change definition (a credit move previously produced NO diff row); SPEC-12 provider boundary — static gate (mutation diagonal clean) + fail-closed runtime guard that refuses captured-closure/dynamic-import/lookup-table and post-commit-scheduled invocations. Review condition C-1 (digest proof claimed 9 classes, 3 falsifiable — 2 unprovable against the fixture) closed with a nine-class both-halves mutation matrix + shrink-guard pin; delta-verified 9/9.
- **Second-merger composition (000C onto post-000B main):** 8 failures / 5 mechanisms, all diagnosed before fixing — 4 from one positional migration-cycle assumption (now by-name), 2 were 000B's new FKs refusing real inconsistencies in 000C fixtures, 1 genuine interaction (timezone staleness now asserted before the review-digest CAS; 000B's 409 contract untouched). The authorized decorative-arm repair of 000A's retired-verdict case executed (rebuild between patch and run; both listed tests now observe). Composed: 38/38 red cases by the runner's count.
- Main at M4-000 close: **1700 tests / 129 files, 15/15 gates, 38/38 red cases, 116/116 fixture-regression, SBX 46/46 tables 0 wrong-tenant, migrations 0001..0015 cycle clean.** FADs 31/32/33; NR-16 (I-15 scanner under-approximation) registered, owner M4-005; NR-15 unreproduced and open. Doc 34 §4a: all ten exit conditions matched to executed evidence. **M4-001 finalized (doc 35 §6a) and issued.**

## 2026-08-10 — OPUS-M4-000A ACCEPTED & MERGED (94af3c5)
- First M4 packet on main: migrations 0012 (staffing-input integrity) + 0013 (work-profile DELETE capability gate, the review's F-1 condition). Atomic whole-set demand/requirement replacement under DB-owned expectedVersion CAS; requires-expiry service+DB enforcement incl. the flip-guard; retired-qualification rulings enforced at both layers; future-work-profile cancellation path; the shared eligibility verdict (`packages/domain/src/eligibility`) with five distinct non-satisfied outcomes, converged on by manual scheduling AND publication; shift-type qualification requirements enforced without duplicate authored rules; enforcement read plane (one purpose-token SELECT policy, four security dimensions review-proven).
- Independent review **ACCEPT WITH CONDITIONS** — union-interleaving, bypass, mutation (6 verdict mutations all caught), boundary (13/13) and security probes all HELD; findings F-1 (DB capability gap, MEDIUM — fixed by 0013 + red case), F-2/F-3 (accuracy/citation — corrected), F-4 (plane residuals — FAD-29), F-5 (unreported scope crossing — FAD-30). All conditions closed and the full battery re-run green.
- Honest record: first fixture-regression run failed 12/14 (three packet-introduced order couplings, fixed as stricter tests; failed runs retained); one non-reproducing intermittent in a pre-existing test registered as **NR-15** (55+ clean whole-suite runs, packet bounded out as cause at schema level, mechanism UNKNOWN — explicitly not a flake ruling). Execution note: implementation and review both ran on Opus 5 under Fable orchestration (owner directive 2026-08-10) after repeated usage-limit terminations on Fable-model executors.
- Main after merge: **1448 tests / 115 files, 13/13 gates, 26/26 red cases, fixture-regression 107/107 (seed 849638), SBX 6/6 with the sweep floor 44→45, migrations 0001..0013 cycle clean.** FADs 28–30. Doc 35 amended: 000B → migration 0014, 000C → 0015; eligibility module frozen for the parallel pair.

## 2026-08-05 — Milestone M3 COMPLETE (8/8 accepted)
- OPUS-M3-008 (dfa717f) closed the milestone: step-06 HARD-rule re-validation live (6 EVALUATED / 24 honestly NOT-EVALUABLE, FAD-27 fail-closed), qualification expiry vs assignment date, the audit READ API, the holiday key split, migration 0011, the NR-14 redesign (restore discipline retired — zero-modification tree proven three ways), full port derivation (E-2 closed; the twice-recurring 55455 seed fixed), composed proofs, QA-SCH 9/6/1, SPEC-14 A-cells with 10 M-cells honestly unclaimed.
- Its review REVISE closed a NUL byte binary-blinding the central file, a factually false not-evaluable reason (CallSpacing now EVALUATED on is_on_call with 13 tests), 8 fold-ins, and the orchestrator's nested-port finding. Reviewer: 23 adversarial probes, mutation tests, could not falsify the load-bearing set.
- Main at milestone close: **1384 tests, 13/13 gates, 21/21 red cases, fixture-regression 102/102, sweep 44/44 0 wrong-tenant, migrations 0001..0011 cycle clean, NR-14 tree zero.** NR-14 retired; NR-4 stays elevated through M4. Exit report: 33-m3-exit-report.md; frozen checkpoint tagged milestone/M3.

## 2026-08-05 — Wave 2 complete: OPUS-M3-005 (112b8c4) and OPUS-M3-006 (57685ab) ACCEPTED & MERGED
- Both reviews returned **zero blocking findings** — a milestone first; every claim survived reviewer-authored probes (publish-once concurrency, zero-DB-delta denials, a 38-capability self-scoping drive, DST-crossing overnight fixtures). Small claim-accuracy deltas closed pre-merge on both (M3-006's N-1 closed by MAKING the per-clause claim true over legally-crafted decoupled states).
- FAD-25 (calendar-key ruling: self-scoped read keys, write-never-implies-read standing rule, holiday-key split → M3-008) and FAD-26 (sign-off route + STALE_REVIEW CAS ratified; audit READ API assigned to M3-008) recorded. M3-005's audit-display escalation executed correctly.
- Main at wave-2 close: **1305 tests, 13/13 gates, 20/20 red cases, fixture-regression 96/96, sweep 44/44 0 wrong-tenant, migrations 0001..0010 cycle clean.** Parity: CAP-014 → verified, CAP-020 → in-progress (14 verified / 3 in-progress / 41 not-started; none dropped). One session-limit and one network interruption recovered with zero loss.
- **Seven of eight M3 packets done. OPUS-M3-008 (integration & hardening) issues next, alone.**

## 2026-08-05 — Wave 1 complete: OPUS-M3-007 (c3e1baa) and OPUS-M3-004 (20c0c13) ACCEPTED & MERGED
- **M3-007 settings/site:** migration 0010, typed settings columns with honest enforcement absence (M5/M9), audited timezone admin (no stored instant moves), both site directions fixture-proven with RED arms, NO site entity (rule 12). Review: zero blocking findings; NB round closed (no-op writes no longer audited as changes). FAD-24.
- **M3-004 authoring UX:** grid + accessible alternative, cell editor, 27 remaining rule editors (30/30), FAD-23 eligibility ruling. Review REVISE on four reviewer-probed blockers — the CAS was NOT atomic (11/12 concurrent both-win; the shipped proof was sequential), two adjacent race shapes wrong/silent, decorative override reason — fixed with an advisory lock inside the CAS, 12-round DB-asserted race tests, falsifiability-before-wiring, both-sides reason enforcement.
- Main at wave-1 close: **1245 tests, 13/13 gates, 18/18 red cases, fixture-regression 91/91, sweep 44/44 tables 0 wrong-tenant, migrations 0001..0010 cycle clean.** Parity: CAP-004/019 → verified (13 verified / 3 in-progress / 42 not-started; none dropped). One session-limit interruption recovered (zero loss). Standing: fixed preview port 4173 is an E-2-class hazard → M3-008; SP_TEST_PREVIEW_PORT interim.

## 2026-08-04 — OPUS-M3-002 (7dd37dc) and OPUS-M3-003 (e275597) ACCEPTED & MERGED
- **M3-002 rules:** 30-node closed AST + deterministic compiler + NEW rule-node-mapping gate (13th) + DB round trip (all kinds, mutation-controlled) + B-\* corpus + authoring API/UI slice. FAD-21 ruled on the implementer's mandatory pre-migration stop (pattern/staff_rules are categories of the typed model — the free-form tables would have re-opened the CAR-006 escape hatch). REVISE: non-minimal infeasibility certificate (the M4 grading oracle) + 9 fold-ins → exactly one fixture hash changed. 1023 tests 13/13.
- **M3-003 publication:** SPEC-05 core — 11 tables, D-15 set-immutability, single-current CAS publication with one-domain idempotency, complete V-01..V-19 + SBX-018, I-11 both halves. REVISE on two reviewer-probed blockers: the SECURITY DEFINER prune was a within-tenant DELETE primitive disarming D-1b, and D-15a missed INSERT (published content could GROW; three mutators unchecked, one unaudited). **FAD-22** amended SPEC-05 (INSERT coverage, required expected_prior_current_version_id, one idempotency-key domain). Second-merger rebase: pure composition disclosed per hunk, sweep floor 33→44. 1060 tests composed, 301 sweep readings 0 wrong-tenant.
- One session-limit interruption (both implementers mid-flight) recovered per protocol with zero loss; one cross-worktree postgres kill disclosed and benign. Parity: CAP-014/016/019 → in-progress (11 verified / 4 in-progress / 43 not-started; none dropped).

## 2026-08-04 — OPUS-M3-001 ACCEPTED & MERGED (6a01196)
- Authentication/sessions/invitation/activation on main: password+TOTP MFA, server-side sessions (hashed tokens, idle+absolute lifetimes on a clock seam, immediate suspension invalidation, rotation on privilege change), invitation/activation separated from reset (STM-017/018), login-email change (CAR-027), session-epoch freshness (I-19), break-glass session auditing (SPEC-11 §3.2), pre-auth route-policy class. Migration 0007.
- Exit obligations met: SBX-005/006 executed in full (0 EVIDENCE_BLOCKED(authn)), SBX-001 re-run on real sessions, SBX-004 sweep 30 tenant tables +users / 210 readings / 0 wrong-tenant, SPEC-11 X-12 filed. 965 tests, 12/12 gates, 14/14 red, fixture-regression 75/75, migration cycle clean — orchestrator delta re-verified on a fresh checkout.
- Second review REVISE, two blocking: **B-1** a raw session-token secret printed into two committed evidence files (the secrecy test scanned logs/payloads/rows but not the evidence files it generates); **O-1** (orchestrator-found) SBX-001 was not actually re-run with real sessions and carried a now-false EVIDENCE_BLOCKED(authn) while the report claimed it cleared. Revision closed both (redaction by construction + evidence-dir secrecy scan with its own falsifiability probe; the 611-cell matrix re-driven through 10 real per-role sessions) plus N-1 (raw NUL→space regression against a prior review ruling) and N-3 (table-count wording). Reviewer could not falsify RLS/namespace/TOTP-replay/AES-GCM-AAD/suspension/clock-seam/forged-token/pre-auth-gate/evaluator/baseline.
- FADs ratified: FAD-18 (user_mfa AES-256-GCM AAD-bound), FAD-19 (scrypt N=2^15,r=8,p=1, with recorded side-channel limitation), FAD-20 (password_reset_tokens separate namespace). Doc 06 §3.2 amended 2026-08-04. Parity: CAP-005/008/009 → verified (11 verified / 1 in-progress / 46 not-started; none dropped).

## 2026-08-04 — Milestone M3 AUTHORIZED; packets finalized
- Owner authorized M3 against `e476573` (M4 not authorized). Entry verification PASSED with exit codes checked (checkpoint/tag/tree/branches/merges confirmed; validators 36/36 · 95/95 · PASS; gates 12/12; red cases all proven; NR-14 regenerated artifacts restored).
- [32-m3-task-packets.md](../32-m3-task-packets.md) finalized: OPUS-M3-001 authn/sessions/invitation (first, alone — clears SBX-005/006 + X-12 + break-glass session auditing), OPUS-M3-002 typed rules/compiler/B-\* corpus, OPUS-M3-003 SPEC-05 publication core (full V-01..V-19, SBX-018); §6 shared-surface pre-assignment; §8 pre-declares M3-004..008 (authoring UX, publication UX, staff views, settings+site, mandatory integration/hardening).
- All fourteen M2 carried items dispositioned with explicit owners and completion points (§2); deferred halves carry milestone/family/reason/dependency/capability/gate/evidence.

## 2026-08-04 — Milestone M2 COMPLETE (4/4 accepted)
- M2-002 (ff2e67b, first UI slice) and M2-003 (567964c) merged after REVISE→delta-APPROVE and APPROVE-WITH-FOLLOW-UPS cycles; M2-004 integration (fce095c) composed the join table, consolidated fixtures per D-1, fixed the sequence::text class incl. production audit verification, closed the port-collision hazard, and landed the request-budget ledger under the budget=2 ruling.
- Review findings of record this half: a vacuous cross-group proof arm (a CAR-001-class policy mutation sailed through — now pinned as a red case), an INSERT-blind capability gate, effective dating that was created_at in disguise, an arbitrary-row read (the S-01 class, twice more — once reintroduced by the person who had just read the warning, caught by the structural scan), a version counter that wasn't a control, reachable retroactive history creation, silent weekday-target loss on supersession, and a fault-masking catch. All closed with regression evidence.
- One session-limit interruption (both agents) recovered per protocol with zero loss. FAD-16/17 recorded; doc 06 amended thrice (dated). NR-13 retired; NR-14 opened low; E-1/E-2 closed. Parity: first 10 rows move (8 verified, 2 in-progress).
- Frozen M2 checkpoint = the exit-report commit, tagged milestone/M2 (milestone/M1 retro-tagged at b051193 per 24 §E — the tag was missed at M1 close, disclosed).

## 2026-08-03 — OPUS-M2-002 and OPUS-M2-003 issued in parallel
- Packets revalidated against the actual M2-001 outcome; binding deltas recorded as 30-m2-task-packets §7 (FAD-15 fixture discipline concretely named, TENANT_TABLES + SBX-004 non-vacuous coverage for every new tenant table, fixture-regression at submission and acceptance, ProbeFalsified vacuity discipline, restore-by-id).
- FAD-16 recorded and doc 06 §3.2 amended (dated, additive): extended shift_types field set with CHECK-constrained display tokens, shift_type_weekday_demand, monotonic groups.pick_position_count.
- Disjoint globs re-verified; shared-surface ownership table §5 unchanged; shift_type_qualifications reserved for the integration packet. Branches opus/m2-002-catalogue and opus/m2-003-profiles.

## 2026-08-03 — OPUS-M2-001 accepted and merged
- OPUS-M2-001 ACCEPTED (bc61ee7) after: phase-A NR-13 measurement with mid-task FAD-15 ratification; a tooling-outage interruption recovered per protocol; an orchestrator diagnosis dissolving a suspected kernel-guard defect (superuser arrangements fire capability triggers — the control working); independent second review REVISE → delta REVISE → APPROVE WITH FOLLOW-UPS across R-01..R-12 / D-01..D-09 / E-01..E-06; and the rotating seed catching a seventh test coupling at acceptance time (FAD-15 ruling 3's first live exercise).
- Landed: fixture-isolation refactor per FAD-15 (as corrected b9755d1), the fixture-regression acceptance gate, the MULTI factory fixture, the SPEC-16 SBX harness with ProbeFalsified-sentinel vacuity detection and per-scenario retained artifacts, and the G-ARCH tenancy subset evidence (SBX-001/002/004 PASS; 005 partial; 006 blocked on authn — named, not hidden). G-ARCH remains open (six non-tenancy SBX tests at later milestones).
- Review findings of record: the vacuity detector originally accepted any probe throw as falsifiability; five declared artifacts were unwritten with nothing checking; the five-role sweep's refusal cascade wrote false causes into shipped evidence; two probes reproduced the absence-vacuity shape inside the detector built to kill it. All closed with red cases.
- M2-002/003 remain NOT ISSUED pending packet revalidation.

## 2026-08-03 — M2 begun; OPUS-M2-001 issued
- M2 authorized by the owner against frozen M1 checkpoint b051193. Three packets finalized in runbook format (docs/fable/30-m2-task-packets.md, commit 73cd212); validators green at authoring (plan 36/36, arch 95/95, research PASS).
- OPUS-M2-001 issued: NR-13 fixture-isolation measured decision first, then MULTI provisioning, then the SPEC-16 SBX harness executing the G-ARCH tenancy subset (SBX-001/002/004/005/006) against the M1 kernel. Branch `opus/m2-001-sbx-harness`.
- Packet rulings recorded in the packet doc: SBX-002 reinterpreted per its capability mapping (CAP-006/CAP-057), picklist arm re-executes at its milestone; authn-dependent SBX-005/006 sub-scenarios report EVIDENCE_BLOCKED per SPEC-16 §7 (the authn subsystem was a roadmap M1 slice that no issued M1 packet contained — carried, not hidden); shift_type_qualifications pre-assigned to a prepared integration packet; M2-003 API-first (UI follows M2-002's foundation).
- OPUS-M2-002/003 NOT ISSUED — blocked on M2-001 acceptance, then revalidation.

## 2026-08-03 — Milestone M1 COMPLETE (4/4 accepted)
- Tenancy kernel (356ddf5), audit chain + outbox with TDG-04 confirmed (946fc72), authorization evaluator + kernel integration (f547e90). Exit report: docs/fable/29-m1-exit-report.md.
- A fourth task (OPUS-M1-004, FAD-13) was added mid-milestone: the two parallel packets left the integration surface unowned, and the merge conflicted exactly where audit emission meets real authorization. Composed rather than hand-resolved, with its own review.
- Three of four tasks returned REVISE from independent second review; every blocking finding came from a reviewer's own database probes (arbitrary-row entitlement read 404'ing an organization; audit chain reporting intact after tail truncation; outbox double-delivering; a flagship isolation test satisfiable by the defect it existed to catch). Orchestrator rerun additionally caught a named-condition proof passing in-suite and failing 5/5 standalone.
- FAD-11/12/13/14 recorded; SPEC-01 §7.1/§4.3 amended; NR-9 retired, NR-13 (shared-fixture coupling) opened, FA-3 discharged.
- M1 exit PASSED WITH ONE CARRIED ITEM (G-ARCH SBX tenancy subset — harness does not exist; opens M2-001). M2 NOT authorized.

## 2026-08-02 — M1 begun; OPUS-M1-001 accepted
- M1 authorized by the owner against checkpoint b49d3dd. OPUS-M1-001 (tenancy schema + SPEC-01 context middleware + production unit-of-work) ACCEPTED and squash-merged (356ddf5) after a three-round review cycle: independent second review REVISE (3 merge-blocking findings, incl. a falsified security claim in the migration) → full revision → delta re-review APPROVE WITH FOLLOW-UPS → final hardening delta (SECURITY DEFINER pg_temp pin, roles-aware owner-exemption assertion).
- FAD-11 (three SPEC-01 escalation rulings: revoked membership → 404 with T-02b added; EX-2 org-scoped DML; capability gate above SQL until M1-002) and FAD-12 (authorization + mutation share one unit of work on every surface) recorded; SPEC-01 §7.1/§4.3 amended with dated notes.
- EV-M0-SPA qualified (embedded-postgres exit-code masking — per-test log unaffected); root eslint now ignores `.worktrees/**` (gate race found during verification); standing merge-discipline note added to the runbook.
- SP-E UX brief + design tokens authored (docs/fable/28) — carried M0 item discharged.
- OPUS-M1-002 and OPUS-M1-003 issued in parallel worktrees; EV-M1-TENANCY residuals 1–7 recorded as M1-002 entry conditions. M2 NOT authorized.

## 2026-08-02 — M0 acceptance review (owner-directed; Fable)
- All exit-report claims independently re-verified against the repository: isolation harness re-run 36/36 (fresh install), CI gates 12/12, red cases 14/14, solver harness H-0..H-8 re-run 0 failed with every load-bearing finding reproduced (H-6 nondeterminism, H-5 SIGKILL cleanliness, H-8 assumption-core cost). docs/fable/26-m0-acceptance-addendum.md.
- One defect found and fixed at acceptance: secret-scan gate walked Python virtualenvs (`.venv` missing from the shared skip list) — false-positive red gate for any developer following the spike README. Fixed in scripts/gates/lib/gate.mjs; red-case battery re-proven 14/14 after the fix. Nothing weakened.
- First three M1 packets finalized in runbook format — docs/fable/27-m1-task-packets.md, marked NOT ISSUED; plan validator extended (+7 assertions, now 36) to guard against silent issuance.
- M0 exit criteria confirmed PASSED (carried items unchanged). M1 recommended, NOT begun.

## 2026-08-01 — Independent planning mandate (Fable)
- Full audit of research corpus (retained, authoritative) and remediated architecture (retained as PROPOSED, re-review required); findings F-01..F-11.
- Created docs/fable/00–20: assessment, audits, product spec, parity matrix (58 rows), workflow catalogue, roles model, domain/state-machine ratifications, architecture adoption with conditions, engine + picklist plans, security and testing strategies, **implementation roadmap M0–M12**, Opus runbook, risk additions NR-1..8, decision register, recommendation with first task packet OPUS-M0-001.
- Created control documents (this directory).
- No application code. No source research. No gate claimed.

## 2026-08-01 (later) — Expanded decision authority mandate (Fable)
- All formerly-blocking decisions resolved under delegated authority: FD-1..4, 20 product decisions, TDG-01..15 technology selections, OI-1..7 / EV-1..8 / LEGAL-1 provisional defaults — docs/fable/21-decision-resolution.md.
- Internal adversarial verification review of the highest-risk remediations commissioned (Opus sub-agent); external re-review remains REQUIRED, repositioned to block beta entry rather than M1 (corrected per internal-verification finding V-04).
- Corpus corrections applied with dated history: CAR-026 count fix (report 18 + remediation record), F-05 stale checkout-context sentence (arch doc 02), banner amendments on the pending-decision registers (report 24 §6, arch 19 §2.2/§3).
- New: 22-readiness-assessment, 23-opus-task-packets (three approved packets, not executed), 24-execution-standards (branch/worktree strategy, quality gates, authorization prompt); roadmap amended (M1 gate, §6a compact sequence); plan validator added (docs/fable/validate.py).
- Implementation still not started; no gate passed; no SBX test executed.

## 2026-08-01 (final) — Internal verification incorporated; plan declared ready
- Internal adversarial verification returned VERIFIED WITH CORRECTIONS NEEDED (29 findings: 16 High, 10 Medium, 3 Low, incl. 5 governance findings against this mandate's own process — all accepted).
- All findings dispositioned in docs/architecture/remediation/internal-verification-corrections.md; five new design decisions (FD-5..FD-9: daily_assignments for picklist selections, keyed-HMAC reference pseudonymization, signed vocabulary snapshots, current_published_assignments for D-1b, vacation as a true subtype); 21 architecture files amended with dated markers.
- External re-review corrected from "advisory" back to REQUIRED, blocking controlled-beta entry (V-04); "the architecture is proposed, not approved" restored.
- Architecture validator strengthened to 95 assertions (absence-assertions 48c/50d/55a-e per the review's meta-finding). All three validators green.
- 22-readiness-assessment.md finalized. Frozen-checkpoint rule adopted (V-01): no amendments while a commissioned review is open.

## 2026-08-01 (M0) — OPUS-M0-001 accepted; session-limit recovery
- Recovery: prior session died mid-revision (uncommitted worktree changes). Assessment: revision complete and trustworthy after independent verification; committed and squash-merged (ea77ac5).
- OPUS-M0-001 ACCEPTED: 36/36 isolation harness, TDG-02/03 confirmed (in-process scope), second review blocking findings fixed. Evidence: docs/evidence/EV-M0-SPA.
- SPEC-01 §4 amended from executed evidence (FAD-9): nullif predicate, all-four read-back, composite tenant FKs, existence-oracle mitigation, startup pooler assertion.
- OPUS-M0-002 and OPUS-M0-003 issued in parallel worktrees.

## 2026-08-01 (M0 close) — Milestone M0 complete
- All three tasks ACCEPTED and squash-merged (ea77ac5, 2880bd2, 8dad022) with evidence filed (EV-M0-SPA/SCAFFOLD/SPC); interruption recovery performed and documented; independent second review on the critical task; one plan-contradicting spike finding handled per protocol (FAD-10, SPEC-04 amended).
- TDG-01/02/03(scoped)/11(scoped)/14 confirmed; TDG-04 carried to OPUS-M1-003; SP-E UX brief carried to M1 entry.
- Exit report: docs/fable/25-m0-exit-report.md. All validators green (95/95, 29/29, research PASS) + CI 12/12 on main. M1 NOT authorized.

## 2026-08-01 (M0 final checkpoint) — Control-document reconciliation
- Owner-directed sweep of all living control documents before closing M0. Updated: PROJECT-STATUS (stale phase line), EVIDENCE-INDEX (three M0 evidence rows replacing 'none yet'), ASSUMPTIONS (FA-3 largely discharged), RISK-REGISTER (NR-4/NR-1 reconciliation), CHANGELOG (this entry). Verified current, no change warranted: ARCHITECTURE-DECISIONS (FAD-1..10 complete), PRODUCT-DECISIONS (no product decision changed in M0), TEST-TRACEABILITY (M0 rows already PASSED+evidence), IMPLEMENTATION-ROADMAP (M0 complete / M1 not authorized), OPUS-AGENT-RUNBOOK log, OPEN-QUESTIONS, ORCHESTRATION, FEATURE-PARITY-MATRIX pointer (no capability reached implemented/verified in M0 — spikes and scaffold only).
- Exit report §12 added: per-document disposition. All validators green. M1 NOT authorized.
