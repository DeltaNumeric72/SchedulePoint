# ARCHITECTURE-DECISIONS (live register)

Canonical: docs/architecture/decisions/ — 23 ADRs, status **PROPOSED**; per-ADR acceptance occurs as their confirming spikes/harnesses pass (M0–M3), external re-review REQUIRED and blocking for beta entry (V-04); architecture remains PROPOSED until it upgrades the verdict.

Orchestrator decisions (FAD series):
| # | Decision | Where | Date |
|---|---|---|---|
| FAD-1 | Single repository: pnpm workspaces + solver/ Python package | [../11-architecture.md](../11-architecture.md) §5 | 2026-08-01 |
| FAD-2 | Entitlements are the product flag system; no runtime flag service | [../11-architecture.md](../11-architecture.md) §5 | 2026-08-01 |
| FAD-3 | Genius merges into Scheduler; no untestable role distinctions | [../08-roles-and-permissions.md](../08-roles-and-permissions.md) §3 | 2026-08-01 |
| FAD-4 | UI = typed-contract React SPA + Fastify JSON API (resolves doc 02 §2.3 posture within TDG-01/14 discretion) | [../21-decision-resolution.md](../21-decision-resolution.md) §3 note | 2026-08-01 |
| FAD-5 | Technology selections TDG-01..15 (Fastify, Kysely+pg, pg.Pool/PgBouncer-txn, graphile-worker, first-party auth + openid-client, provisional providers, Playwright-Chromium renderer, ical-generator, S3+MinIO, ClamAV, AWS ca-central-1 provisional, self-hosted OTel/Grafana, Secrets Manager, React+Radix+Tailwind+TanStack, KMS signing) — decided pending M0 spike confirmation | [../21-decision-resolution.md](../21-decision-resolution.md) §3 | 2026-08-01 |
| FAD-6 | Review strategy: internal adversarial verification gates M1; external re-review required, blocking beta entry (amended per V-04) | [../21-decision-resolution.md](../21-decision-resolution.md) FD-2 | 2026-08-01 |

Corrections applied: F-05 (doc 02 §3.2) — done 2026-08-01 · CAR-026 (report 18 count) — done 2026-08-01.
| FAD-7 | M0 local-environment substitution: no Docker/Homebrew/sudo on this machine — PostgreSQL via `embedded-postgres` (real user-space PG binaries) for spike/dev; pnpm via corepack (no global install); solver spike in a Python venv (ortools 9.15 on system 3.9), solver image authored but built later in CI; docker-compose files authored for CI/platform use. Evidence value preserved: genuine PostgreSQL semantics and genuine subprocess kill semantics either way | this entry | 2026-08-01 |
| FAD-8 | Validator phase-boundary rescope: planning-phase checks 52a/52b (arch) and 10a (plan) retired and replaced with the boundary they protected — no application code in docs//research trees; implementation only in authorized roots (spikes/, apps/, packages/, solver/, scripts/, .github/) | validate.py comments | 2026-08-01 |
| FAD-9 | TDG-02 CLOSED (Kysely+pg confirmed by EV-M0-SPA) and TDG-03 CLOSED for in-process pg.Pool (external pooler explicitly unproven; CI follow-up required before any external pooler is deployed). Three attached conditions adopted as normative: nullif predicate spelling (SPEC-01 §4.3 amended), all-four-settings read-back (SPEC-01 §4.2 amended), pooler-mode startup assertion + SET-statement lint (SPEC-01 §4 amendment block). New sharp edges recorded: composite tenant FKs mandatory; tenant-qualified unique keys + generic 23505 at edge | EV-M0-SPA; SPIKE-REPORT §6-7 | 2026-08-01 |
| FAD-10 | Solver reproducibility mechanism corrected from measured evidence (EV-M0-SPC H-6/H-7/H-8): seed+worker-count pinning is insufficient; reproducible builds pin deterministic portfolio (interleave_search) + deterministic time limits + full parameter set; T1 explanations = separate budgeted failable assumption-solve (blanket reification rejected); cancellation = watcher-thread StopSearch, signals prohibited; production solver image pins Python 3.12; deterministic-mode cost measured on E1 corpus before defaults freeze. TDG-11 solver-runtime aspects CONFIRMED; image-build + 3.12 re-run are CI conditions for E0 closure | SPEC-04 amendment; docs 02/12 | 2026-08-01 |
| FAD-11 | Three OPUS-M1-001 escalations ruled (SPEC-01 amended with dated notes): (1) revoked membership → `404` byte-identical to non-existent id, `409 CONTEXT_STALE` reserved for still-authorized-but-stale contexts — T-02 corrected, T-02b added; (2) §4.3 EX-2 governs cross-group reads; organization-scoped context holds organization-bounded DML on groups/memberships (SPEC-06 §1.1 group creation is real); (3) the organization-role capability gate is enforced at §2.3 step 2 in the application layer until OPUS-M1-002's non-recursive grant relations land (a memberships policy querying memberships recurses) — the organization boundary stays in SQL; M1-002's second review must verify the gate | SPEC-01 §7.1 + §4.3 amendments; EV-M1-TENANCY | 2026-08-02 |
| FAD-12 | Authorization re-evaluation and the mutation it authorizes share ONE unit of work, on every surface (HTTP and jobs alike) — a role read committed in a separate transaction is a stale authorization decision (I-19); the audit write (OPUS-M1-003) joins the same unit of work. Ruled from OPUS-M1-001 second-review finding R-04, which measured the HTTP probe surface splitting authorize/mutate across transactions while the jobs surface did it correctly | OPUS-M1-001 second review R-04; packet audit requirement | 2026-08-02 |
| FAD-13 | Three OPUS-M1-002 escalations ruled: (1) the jobs-surface evaluator gap (worker still authorizes on M1-001 role lists because `apps/api/src/jobs/**` was M1-003's parallel scope) is closed by a post-merge integration task — wire `executeJob` to the SPEC-06 evaluator, add audit emission for M1-002's mutations, extend the TENANT_TABLES registry, reconcile migration numbering — issued after both tasks merge; until then the worker's role gate is a recorded narrower control (deny-by-default in the role dimension). (2) Entitlement administration must not be self-referentially module-gated: revoking the entitlements module locking an organization out of entitlement administration with no recovery surface is an unrecoverable fail-closed state; in M1 entitlement mutation is not a tenant-facing capability (bootstrap/operator path only), and when a tenant-facing surface lands it is role/grant-gated but exempt from the entitlements-module availability check; tenant-vs-platform administration recorded open for the operator-surface milestone. (3) SPEC-06's amended truth table has FIFTEEN rows; the packet's "fourteen-step" wording was stale — the spec governs; packet corrected with a dated note | OPUS-M1-002 return report; SPEC-06 | 2026-08-02 |
| FAD-14 | **TDG-04 CLOSED — graphile-worker CONFIRMED** by executed spike evidence (SP-D, 18/18: transactional enqueue proven by rollback, durable lease, crash recovery exactly-once), with four conditions now normative in production code: C-1 the queue's own tables ship RLS-enabled with ZERO policies and no grants — our schema installer generates policies by loop and throws if any table is left unpoliced; C-2 graceful shutdown does not release in-flight jobs and the 4-hour lease is NOT configurable, so a rolling deploy would strand them — a heartbeat pool registry + `force_unlock_workers` at startup recovers them (565 ms measured on the production path); C-3 a handler ignoring `abortSignal` does not exit on first SIGTERM — the dispatch task threads it; C-4 at-least-once delivery requires DURABLE idempotency (`outbox_effects` PK `(organization_id, idempotency_key)`) — an in-process dedup was measured double-delivering in two windows and was rejected at review. Also adopted: the maintenance-plane cross-tenant read (explicit `TO app_migrator` SELECT policies + one `app_worker`-only `LANGUAGE sql` SECURITY DEFINER enumeration function returning organization ids, counters and timestamps only) as the sanctioned alternative to a BYPASSRLS role or a hidden owner exemption — the only cross-tenant read in the system, pinned by tests, not itself audited | EV-M1-AUDIT; spikes/sp-d-worker/SPIKE-REPORT.md | 2026-08-03 |
| FAD-15 | **NR-13 RATIFIED — fixture-isolation strategy S4** (per OPUS-M2-001 phase A measured evidence, EV-M2-NR13): an **enforced-immutable** shared MULTI baseline (mutation-census control asserts byte-identity to seed at end of run — enforced because eleven files already violate the convention) · **per-file owned tenants** provisioned via the MULTI provisioner-as-**factory** (fixture content owned by the provisioning script, the instance by the file; cleanup by construction — per-run cluster + RLS invisibility) · **declared non-tenant preconditions** (queue drained the production way per M1-004's C-2 pattern, clock, global `users` minted per-slug) · **per-test subject ownership** inside files. **S1 (per-test transactional rollback) REJECTED on structural evidence, not speed:** a nested unit of work may not re-tenant (`NESTED_TENANT_CHANGE`, I-15 — cross-tenant probes are inexpressible inside one transaction; verified in production code by the orchestrator), 20 of 24 files use a second backend that cannot see the outer transaction, so S1 covers ≤4 files and fails *silently* elsewhere. S2 alone insufficient (4 of 6 measured couplings are non-tenant state). S3 (per-file DB clone, 56.2 ms/file measured) recorded as the fallback if S4 proves insufficient. **Regression gate:** fixed seed SET (incl. the six known-exposing seeds) + a rotating seed, both file- and test-order shuffle, per-file standalone sweep, baseline-immutability control — a rotating-seed failure is a defect to fix, never flake to retry (orchestrator's independent rerun of seed 31337 produced a different failure count than the implementer's — interleaving is machine-dependent, which is exactly why a set, not a seed). **Red case 4 (assertion that cannot fail when its subject is absent) IN SCOPE** — NR-13 surfaced a live instance (`outbox-dispatch.test.ts:320`); the refactor fixes it and the class gets a red case. Truncate-and-reseed correctly foreclosed by the append-only audit triggers (rule 6) and stays foreclosed. **Corrected 2026-08-03 (second-review finding R-06):** the original text's "cleanup by construction — per-run cluster + RLS invisibility" overstated two claims the reviewer falsified. (1) RLS does NOT enforce fixture ownership — it scopes to the *declared* context, and a test declares whatever ids it likes; the reviewer wrote into a sibling file's tenant by deriving its ids from the slug. Ownership is enforced by the factory's per-run slug registry (duplicate registration throws) plus a per-run nonce making owned-tenant ids non-derivable — controls added in the R-06 fix, not properties RLS ever provided. (2) Cleanup-by-construction holds for normal termination; after a SIGKILL the orphaned cluster BLOCKS the next run (with a diagnostic naming the orphan, per the R-07 fix) rather than contaminating it — fail-closed, not clean | EV-M2-NR13 (worktree, merges at acceptance); orchestrator verification this entry; second review R-06/R-07 | 2026-08-03 |
| FAD-16 | **Doc 06 §3.2 additive amendment for the M2 catalogue** (M2-002 issuance pre-deliverable): `shift_types` extended with description, constrained display metadata (`display_palette_key`/`display_text_style` — CHECK-constrained token-set members, never free colour), `include_in_statistics`, `is_leave_of_absence`, `report_order`, `allow_on_request`/`allow_off_request`; new `shift_type_weekday_demand` (per shift type × {mon..sun, holiday} default demand — the M4 engine input authored at the catalogue); `groups.pick_position_count` monotonically-increasing-only (trigger-enforced, the observed constraint); "start day" carried by time columns + demand weekday dimension; request-off text stays on `shift_groups` per the observed model. Field mapping to the owner's observed-concept list is a binding M2-002 deliverable; divergences stated, never silent | doc 06 §3.2 dated amendment; 30-m2-task-packets M2-002 | 2026-08-03 |
| FAD-34 | **M4-001 acceptance rulings (OPUS-M4-001), ratified at the Stage-1 closeout.** (1) **Framed-wire authentication is the RPC MAC pattern:** the wire is `<auth line>\n<body bytes>` — the tag travels beside the exact bytes it covers, the auth metadata sits in the MAC prefix, and NEITHER side re-serialises to verify (the review's B-1: a MAC over a re-derivation was ASCII-identical and non-ASCII-divergent between `canonicalStringify` and `json.dumps(ensure_ascii=True)`). The pattern maps to HTTP unchanged and binds M4-002+ RPC evolution. (2) **Kill-attribution vocabulary:** `signal !== null` ⇒ `killed`; spawn failure and exit-without-response ⇒ `crashed`; a cancelled solve is `CANCELLED` — never conflated (SPEC-04 §2 carries both words; EV-M0-SPC H-6 honoured). (3) **Sweep = registry − 1, permanently:** `TENANT_TABLES` counts 48; the SBX-004 sweep reports 47 — `users` (global, through-membership scope) gets a dedicated probe, not a column comparison; the floor assertion is over the REGISTRY. (4) Pre-auth refusals are structured (`exit 2` + typed reason, no traceback), proven for non-ASCII MACs and 60,000-deep JSON. (5) The migration-cycle idiom is ratified repo-wide: roll down BY NAME until the target comes down, re-up in a `finally` (third recurrence of the positional class; FAD-33(4) extended to the down-loop's own throw) | EV-M4-BOUNDARY §2b/§8; M4-001 review + delta review | 2026-08-13 |
| FAD-35 | **FAD-28 reconciliation (Stage-1 recovery): the fail-closed/read-time contract is AUTHORITATIVE; OPUS-M4-001R authorized.** The granted-while-retiring READ COMMITTED interleaving remains ADMITTED at the 0012 trigger (measured, review P3e); safety is the shared verdict's lifecycle-first `retired` outcome (FAD-28(2) stands unmodified — this entry does not revise it, it resolves a source contradiction against it). `grantHolding`'s docblock still claims trigger-level write-time exclusion — the exact falsified claim; repo-swept: ONE source occurrence. Ruling: (a) NO write-time exclusion is claimed or implemented; (b) the docblock is corrected by a bounded Opus packet (doc 35 §6b), never silently; (c) a permanent falsifiable concurrency proof must pin the race-produced holding INERT for manual scheduling, publication, and canonical solver input, with the M4-002 carry-forward that the independent candidate validator consumes the same verdict and its corpus includes a race-produced retired-holding fixture; (d) the `retired-verdict` red-case comment's citation is corrected `FAD-30` → `FAD-33(1)` (the decorative-arm standing rule's true number; EV-M4-INPUT-C §8's same wording is retained as accurate-at-time-of-writing history) | EV-M4-INPUT-A §8/§10 C3; FAD-28; qualifications.ts:298 sweep | 2026-08-13 |
| FAD-39 | **M4-002 second escalation ruled: `isOnCall` joins snapshot v2; RK-RULING-05's `min` departure endorsed.** (1) `CallSpacing` cannot be modelled worker-side without `shift_types.is_on_call`; the adapter's REFUSAL (never a rule that silently matches nothing) is the correct posture and is retained as the unknown-input backstop. GRANT: one additive `isOnCall: boolean` on the snapshot's shift-type entry, read from 0005's `shift_types.is_on_call`; it folds into the SAME v2 bump (v2 has never shipped — no v3). The FAD-38 conditions apply unchanged (additive-only, proofs re-anchored, review-verified). (2) The implementer's RK-RULING-05 departure — `min(node bound, profile bound, profile max)` rather than "profile overrides" — is ENDORSED ahead of ratification: an override that can only tighten is the sole direction SPEC-04 §3.3 admits for a HARD limit. Final ratification of RK-05/06/10-as-authored occurs at acceptance with the review's verification | EV-M4-002 (continuation report); FAD-38 | 2026-08-14 |
| FAD-53 | **Post-M4 internal review (doc 38): all 28 consolidated findings adjudicated; the repair phase opened (2026-08-23).** Three fresh-Opus reviewers filed (REV-A `review/rev-a@6cac92f`, REV-B blind `review/rev-b@e3220a5`, REV-C consolidation `review/rev-c@c52c2ab`); REV-C recommends REVISE; no reviewer, across ~100 probes, found a functional product defect reachable through a shipped surface. **Every finding is ACCEPTED at its filed severity** (none rejected, downgraded, or combined away; A-001=B-003 is one defect found blind by both — the strongest possible confirmation). Repair packets, serial: **R-1** (BLOCKING REV-B-001+REV-C-001, absorbing GH-011/GH-009-open-side): the shared `recordRequests` window made deterministic — opens only after page-load network quiescence and closes on quiescence; class-wide (18 zero-budget interactions + the `i13-schedule-authoring` arm inherits); regression = REV-C's t14 ordering instrumentation + repeat-under-load; unblocks §9.8. **R-2** (A-001=B-003): measured per-test ceilings on the two single-solve e2-objective arms (~130s measured → 400s; R-B4a/GH-005 precedent). **R-6** (B-002): the signer-less `drainQueue` at outbox-dispatch's `afterAll` fixed per the module's own R-10 docblock; NR-15's register entry re-opened and re-worded honestly at acceptance (its "verified clean across the complete seed set" did not hold). **R-4** (A-003, product): the selection window closed — staleness re-evaluated under the same ordering that made the window constructible (the org audit advisory lock), so doc 35 §6g ruling 4's ABSOLUTE wording becomes true rather than re-worded; regression built from REV-A's committed probe. **R-5** (A-004, defence-in-depth): migration 0020 freezes `termination_reason`/`solver_status` against same-state UPDATE, closing the guard's early-return hole; populated-cycle test + red-case arm. **R-3** (records batch): doc 38 §7's battery row renamed to what it executes ("schema cycle, empty database") + an explicit populated-cycle item scoped to the five existing populated tests; the tracked root file `=` (C-007) removed; doc 06's CAP-061 rename annotated and its four broken table rows repaired (A-009/A-010). **R-7** (honesty batch = GH-008 executed + A-005/006/007 + B-006/B-008-pin): mid-band "well inside" sentence accuracy, `solver_status` fail-closed, the vitest zero-match guard, the hard-rule-check docblock count (24→8), a pin on `disableRequestLogging`, the typed web label record, the sentence-sweep enumeration. **R-9** (plan): doc 38 §3 amended — probe sources carry `.txt` suffixes (B-005; all three reviewers converged) and probes must be composed-run-safe (C-011). [Corrected 2026-08-24 at R-9 acceptance: "all three reviewers converged" overstated — REV-B established the suffix rule, REV-C adopted it as stated convention, and REV-A filed BEFORE the finding existed (its bundle's `.ts`/`.sh` probe sources fail the validators 94/95 · 35/36 — the demonstration of the cost, on a never-merged branch, left as the record). R-9 as executed also carries a third rule routed at R-6's acceptance: composed validation runs use --no-cache (vitest's results cache schedules previously-failed files first, making composed-run greens path-dependent).] **R-10** (corrigendum): the REV-A agent repairs its own report's ID instability and coverage-table mis-citation (C-003/C-010/C-006), verified at registration. **R-11** (CI homes, C-004): `sbx` added to the per-push gate battery; `fixture-regression` as a scheduled nightly on main (cost recorded in owner issue #4); doc 38 §7 unchanged (serial local battery still required at the gate). [As executed, R-11 also measured the locked-backlog candidate and EXCLUDED it — NR-15's cause stays UNESTABLISHED, register updated honestly — and its seed-1 validation run captured the fixed-seed failure that became R-12.] **R-12** (the seed-1 fixed-seed failure R-11 captured, routed at R-11's acceptance): diagnosed to mechanism — R-03's fixed 30 s deadline vs graphile-worker's FIFO claim over the composed run's inherited `outbox.dispatch` backlog (893 × ~35.5 ms = 31.7 s; latent since M1, proven failing at `d28f669`, R-6 exonerated; deterministic reproduction at depth 3011) — and repaired by a named-precondition drain before R-03's runner starts (`Math.max(45_000, inherited × 150)`), with the deadline, assertion and enqueue path untouched and a no-regression proof that the repair cannot turn a green run red at any depth or machine speed. Carries NR-22 (new, reviewer-authored): a vitest seed pins the shuffle only up to collection order, which is unstable — a reproduction must pin the observed precondition (for a queue-timing defect, the inherited backlog depth), not the seed; retirement (a canonical-sort sequencer, or rewording the printed operator line) is same-milestone, owner NR-22. **Ruled record-only, no retro-edits (rule-13 posture, as FAD-52):** doc 36 and EV-M4-005 stay frozen — B-004's missing supersession pointers are carried by THIS entry (doc 36 §2's `76.702882` is machine-specific per FAD-52; §6's "64/64" superseded by the 65-arm battery; §6 row 5 means the empty-database schema cycle; §10.4's window IS reachable per A-003); C-009's internal disagreement (tracked request-count json = 1 vs transcript + two re-executions = 2) is recorded with the transcript ruled the better-evidenced figure; A-008's two accuracy notes recorded; B-007 recorded (same-caller 4xx echo, not a log path); C-008 recorded. **M3R ruled resolved-by-substitution:** no M3R findings register exists anywhere (C's t01 — §2.7 was untestable as written); per the owner's §C ("perform a fresh M1–M4 cross-boundary review if no durable M3R report exists"), THIS three-packet review IS that fresh cross-boundary review, and M3R's closure rides the doc 38 gate. Gate criteria re-checked after R-1..R-12 + delta verifications + the §7 serial battery. [Docket closed 2026-08-24: all twelve repair packets ACCEPTED and landed; the §7 serial battery and the §9 gate decision are what remain.] |
| FAD-54 | **Doc 38 §7 red-case leg: adjudicated SATISFIED IN ITS PRIMARY FORM (sharded CI + union guard, green twice on the final candidate's tree); the serial local run is environment-capped, recorded, not required.** §7's own wording names the forms — "sharded in CI, with the union guard green; a serial local run is equivalent evidence" — so the sharded form is the item's primary form and the serial run the permitted equivalent. The serial attempt was made first and terminated EXTERNALLY twice with ZERO arm failures (15 arms proven both directions across the attempts; per-arm restore held through both uncatchable kills; the executor escalated rather than improvising — FAD-15 ruling 3 honoured): a session-environment cap kills a background task at ~60 min and a monitor-supervised task at ~30 min, and detached processes are refused, while the 67-arm serial run needs ~5.2 h. The CI form executed the complete battery on this exact tree content twice — runs 32716033514 (head `85efa2b`) and 32724748023 (head `007dfef`), 13 shards + `red-case shard completeness` all success, attempt 1 both — and the completeness guard derives its census from the runner itself, so a dropped arm fails the run (the OPUS-GH-010 design). Full job-level citation: `docs/evidence/EV-DOC38-GATE/leg5-adjudication.txt`; the terminated attempts: `leg5-red-cases.txt`, kept unedited. Riding rulings: (1) doc 38 §7's "65/65" census is amended in place (dated) to 67 — R-5 added arm 66, R-8 arm 67; the runner is authoritative. (2) The environment cap is recorded for battery planning: long serial legs in this environment decompose into sub-hour invocations — group C (fixture-regression) runs per-seed for exactly this reason, using the nightly matrix's argument-for-argument vitest command. (3) Nothing is weakened: no arm skipped, no filter used on the serial attempts, the same runner and census executed in full in CI, and the sharded form was doc 38's own named primary all along. |
| FAD-52 | **The FAD-49 derivation completed: `resultReproducibility` now reads deterministic units (packet OPUS-GH-007, 2026-08-22).** Post-M4 CI enablement exposed, on ground truth twice independently measured (15+ runs; deterministic-budget raise 36× byte-inert), that the implemented derivation was narrower than FAD-49(1)'s own description: it never read `deterministicTimeUnits`, so `WALL_CLOCK_BINDING_FRACTION=0.9` let a genuinely wall-truncated 10s run (wall 8.6–9.1s, 8.076904 of 100 units — 92% unspent — FEASIBLE, no proof) read `reproducible` with the promise sentence; knife-edge intermittent, load-sensitive in the WRONG direction (load pushes the broken check into passing). **Ruling (implemented and independently reviewed ACCEPT):** (1) new verdict `stopped-early`, claims-less — fires only for pinned + `completed` + status exactly FEASIBLE + units recorded + units ≤ `DETERMINISTIC_BUDGET_UNSPENT_FRACTION=0.5` × budget; names NO cause (the non-budget stop set is open), says "may produce a different schedule", never the promise; the ≥0.9 wall rule keeps precedence; between 0.5× and the budget NO new claim (falls through verbatim — the residual mid-band overclaim is registered follow-up GH-008 M-1, not silently absorbed); null units on this path → `unrecorded` (B-1's hole-shape one field along). (2) Vocabulary carried end-to-end: contracts enum 5→6, route lifts units from statistics, web label added; proven by construction (both sides parse through `buildRunDetailSchema`). (3) The §20c falsifiability control RE-FOUNDED on a fresh in-arm OPTIMAL solve (83.130356/100 units here vs 76.702882 on the machine of record — units are same-machine-stable, NOT cross-machine-portable; support docblock corrected); the tripwire arm renamed to assert non-reproducibility on BOTH sides of the knife edge with per-branch sentence assertions; the two remaining `bound===false` S-08t preconditions converted to `reproducible===true` (strictly stronger; B-1's `bound` retained where the clock IS the question). (4) New red-case arm `result-reproducibility-units-branch-removed` (battery 64→65; compile-clean patch — the `&& false` spelling fails tsc and would blind the arm), 15 new test arms incl. the INFEASIBLE-at-0-units counterexample. (5) Frozen milestone records (doc 36, EV-M4-005) are NOT retro-edited; this entry carries the supersession; dev-setup's living count corrected 64→65 per FAD-50 C-5. Registered follow-ups: GH-008 (mid-band sentence M-1 · `solver_status` fail-open M-2 · wire assertion, typed label record, sentence-sweep enumeration, vitest zero-match-exits-0 guard per FAD-50 C-3's precedent). |
| FAD-51 | **M4-005 delta-review adjudication (ACCEPT WITH CONDITIONS accepted; final condition round ruled).** The delta review verified B-1 and C-1..C-7 CLOSED by the reviewer's own round-1 instruments (the RP-1/RP-2 defect reproductions no longer compile — the required-parameter design auditing its own call sites; the NR-19 mutation now kills the new proof; the forced selection-refusal now fails the critical path; the forced-skip real-stack run now exits 1; no id or revision survives on the staleness wire). Three new conditions ruled for a FINAL bounded round, none blocking: **D-1** — N-1(ii) is not met as FAD-50 worded it: a spawned-command failure emits the diagnostic but the synthesised text matches no ERRORED signature, so an asymmetric spawn failure (green spawns, red does not) would score PROVEN with the violation never tested → add a spawn-failure signature to the ERRORED set with text the runner's synthesised diagnostic actually matches, and extend the runner-signature falsifiability arm to pin it; **D-2** — the C-4 wire's completeness claim is FALSE in three places: `stalenessWire` aggregates over the ALREADY-TRUNCATED changes list, so per-class counts inherit `CHANGE_LIMIT` while `changeCount` passes through complete (measured: total 25, class sum 20; the shipped arm's title claims completeness its body does not assert) → aggregate BEFORE truncating so the per-class counts are complete over all changes (the bounded list stays bounded), make the arm assert class-sum == changeCount on an over-limit fixture, and correct the docblock + INDEX §22e with a dated note — the C-7 class, repaired the same way; **D-3** — critical-path step 14 asserts A state, not THE correct state (the four-way disjunction admits empty and non-empty; the viewport-independent `schedule-status` line carries the exact correct outcome and is asserted only attached) → assert `schedule-status` text matches the no-shifts statement at both viewports; **D-4 (note)** — the corrected C-7 assertion still logs "the queue is empty" two lines below → carry the log line with the message. Verification for the final round: named tests standalone, both halves of touched arms, the real-stack suite both viewports, full `check`; then delta-verify by the ORIGINAL reviewer confined to D-1..D-4; the full 64-arm battery + fixture-regression + complete acceptance battery run at orchestrator acceptance | M4-005 delta-review record (`review/m4-005` @ `db781f8`) | 2026-08-21 |
| FAD-50 | **M4-005 independent-review adjudication (verdict REVISE accepted).** Blocking: **B-1** — `resultReproducibility` decides from parameters + wall time only, so a CANCELLED deterministic run (reviewer-reproduced against real CP-SAT and through the shipped route: 2.35s wall, 5.6 of 100 units, a person ended the search) renders `reproducible` with the exact promise sentence FAD-49 exists to delete → the verdict MUST also read the termination fact already persisted (`termination_reason`/`solver_status` — still a derivation, no schema, no worker change, FAD-49(3) globs): only a `completed` termination may reach `reproducible`; an interrupted run gets a FIFTH honest verdict naming the interruption (never `reproducible`, never conflated with `unrecorded` whose facts are absent), both directions pinned with the reviewer's RP-1/RP-2 shapes as permanent implementer-authored tests. Conditions ruled: **C-1** NR-19's release-path unlock gains its own permanent proof (reviewer proved the mutation kills nothing shipped — the packet's "each hardening item lands with its own proof" was not met for this item); **C-2** critical-path steps 10–14 become GENUINELY asserted (the reviewer diagnosed the cause: the server applies and the UI renders `build-applied`; the spec looks before React settles — so await the testid and assert the applied draft and the step-12–14 content; the silently-pass guard is removed); **C-3** the real-stack config's own setup SETS the `SP_REAL_STACK` handshake (today nothing does — the marquee suite passes with 2 skipped) AND the config fails loudly when zero tests ran; the two false docblocks corrected; **C-4 RULED**: the enforcement-read plane MAY compute staleness on the detail read (the FAD-29 rationale — the gate computes on truth), but the WIRE discloses input class + direction + counts ONLY — constituent ids/revisions are withheld at least for the `qualificationHolding` class from callers lacking `staffing.qualification_holding.read` (uniformly class-level if no surface consumes ids — measure first, disclose); the per-GET full-assembly cost is RECORDED as a known cost with an owner, not optimized now; **C-5** dev-setup's arm count corrected (63→64); **C-6** INDEX §19b/§18d/§19c amended with dated notes (the reproducibility "did NOT prove" line is now FALSE — §21 built it; "63-arm"→64; the research-validator condition closed on main `b370e03`); **C-7** the crash-restart message states the narrowed predicate it actually asserts. Note-fixes GRANTED in the same round: N-1(i) the runner's inline lines annotate `invertPolarity` arms so the artifact stops self-contradicting, N-1(ii) `run()` surfaces a spawn `result.error` as ERRORED instead of silent empty output; N-3 the stale 10.05× "THIS packet measured" number corrected; N-4/N-5 the two stale NR-19-adjacent docblocks; N-7 `/me/context`'s counter map NARROWED to the caller's own membership groups; N-8 `apiRequest` header-spread order fixed so the context header cannot be silently overridden. Recorded, not repaired: N-6 (systemActor idiom, consistent), N-9 (M-22 gap stays — rule 13, never renumber; the gap is recorded), N-10 (SBX-017's single-identity fixture recorded as a doc-36 limitation), N-11 (the un-falsified read-then-write window in `applyCandidateToNewDraft` recorded as an admitted, undemonstrated limitation with the derived-on-read staleness as compensating visibility; hardening owner M5+), N-1's remaining runner latencies (prepareFailed×invertPolarity, preflight-warning) recorded with the runner backlog. Delta review by the ORIGINAL reviewer mandatory; the full 64-arm battery + fixture-regression run at orchestrator acceptance (N-12) | M4-005 review record (`review/m4-005` @ `c5fcd4d`) | 2026-08-21 |
| FAD-49 | **M4-005 continuation escalation ruled: the RESULT-side reproducibility record is made honest — derived, not stored; solver/** stays closed.** The diagnosed S-08t root cause (EV-M4-005 §20: `maxTimeInSeconds: 10` racing `maxDeterministicTime: 100`, CP-SAT stopping at whichever limit arrives first, `deterministicTimeUnits` moving 21.76→12.53 under load while the wall clock binds and pinning at 76.702882 across a 1.7×-slower machine once it does not) exposes a PRODUCT defect beyond the repaired test: `reproducibilityMode` (`packages/domain/src/ports/solver-port.ts`) is computed from the REQUEST, `builds/service.ts` defaults `max_time_seconds` to **10**, so a deterministic-mode build under the default wall clock can be wall-truncated and persist a record claiming `deterministic` — the confident-wrong-claim class SPEC-04 §4 (amended) exists to refuse ("deterministic time limits, never wall-clock, on any reproducibility-claiming build"). **Ruling:** (1) The honest verdict is DERIVED result-side from facts already persisted (parameters + result statistics: wall time vs wall budget, deterministic units vs deterministic budget) — NO schema change, NO worker change; the statistics provably suffice because the repaired test derives exactly this. `solver/**` remains prohibited: `cpsat_adapter.py`'s `TERMINATION_COMPLETED` wording stands, with the platform-side derivation carrying the honesty; if the implementer proves statistics insufficient, that is a new escalation, not a grant. (2) ONE predicate, single-sourced in the domain beside `reproducibilityMode` (request-side mode stays as the dispatch statement, its docs corrected to say so); every surface that reports a RESULT as reproducible consumes the result-side verdict; the test-support `wallClockVerdict` CONVERGES on the domain function (the S-01 second-spelling class) and the §20c(3) tripwire arm is updated DELIBERATELY as its own comment requires. (3) Allowed-file extension for the bounded corrective round (each edit minimal and justified): `packages/domain/src/ports/solver-port.ts` (additive verdict + doc correction), `apps/api/src/solver/**` and `apps/api/src/builds/**` (attach/report the verdict at read/response time), `packages/contracts/src/{solver,builds}/**` additive, `apps/web/src/builds/**` ONLY where a surface currently renders result reproducibility from the request-side mode, tests/red-case arm updates, EV-M4-005 evidence. NOT granted: `solver/**`, ANY migration, everything else. (4) The 10-second default wall clock racing an opted-in deterministic budget is recorded as an OWNER question (a config-time refusal would fake machine-independent knowledge of units-per-second; the honest control is the result-side record) — not changed. (5) The deterministic-cost docblock's wrong attribution (EV-M4-004 territory, named in §20d) is granted for the same round as a comment-only correction with the measurement cited. (6) Battery economics: the corrective round proves its named tests standalone + `check` + affected red-case arms both halves; the FULL batteries run once on the final candidate at orchestrator acceptance — never skipped, run where they decide | EV-M4-005 INDEX §20a/§20d; SPEC-04 §4; FAD-34/47 | 2026-08-21 |
| FAD-48 | **M4-005 clause-(A) escalation ruled: the browser↔server join is BUILT, not waived.** The measured obstacle: `apps/web/src/api/client.ts` sends no SPEC-01 §2.2 declared-context header, every capability route (112) refuses without it, and no response returns the freshness counters (`contextVersion.{organizationVersion, groupVersion}`) a client would need — so every e2e spec intercepts and the real join has never been exercised. This is precisely the class the critical-path requirement exists to expose; a mocked critical path is REFUSED. **GRANT (minimal, additive):** (1) ONE authenticated context-read endpoint (e.g. `GET /me/context` or per-organization) returning the caller's memberships with the two counters — policy-declared (I-02), pre-auth-class-appropriate, deny-tested, no new capability key (reading one's own context is session authority, the FAD-25 self-scoped precedent), I-19-consistent (counters read in the same unit of work as the membership); (2) the client sender: `client.ts` fetches the context once per session/organization, caches it, sends the header on every capability request, and on `409 CONTEXT_STALE` re-reads and retries ONCE (I-10 budgets recorded — the interaction gains one request only where the header must be established); (3) the critical-path e2e then runs against the REAL stack. Allowed-file extension for M4-005: `apps/api/src/http/routes/` (the one new route file), `apps/web/src/api/client.ts` (+ a context module). SPEC-01 §2.2's declaration model is UNCHANGED (client-declared, server-verified — the client now actually declares). **Also ruled:** (4) NR-15 is a REAL DEFECT, now diagnosed (drainQueue deadlock-by-construction on a handler-less `build.solve` job) and repaired at the shared resource — it moves from "unresolved intermittent" to "diagnosed and repaired, verification pending the four unverified seeds"; the widened capture is what found it in one line after ~200 shuffled runs could not — retained permanently. (5) The three not-proven red-case arms are repaired as scoring/runner defects (two the packet's own; `solver-progressive-pin-unfixed` diagnosed first) and the 63-arm battery re-run on the final tree. (6) The research validator PASSES from the repository root (verified by the orchestrator today); the agent's FAIL is to be re-verified from the root — a cwd artifact, not a corpus defect, unless proven otherwise. (7) `EV-M2-SBX/scenario-report.txt` (the SBX harness's tracked report naming six scenarios) is GRANTED for the nine-scenario refresh — it is the harness's own output file, the SBX floor record | EV-M4-005 INDEX §12 (the obstacle measured); SPEC-01 §2.2; FAD-25 | 2026-08-18 |
| FAD-47 | **OPUS-M4-004 acceptance ratifications.** (1) **E2-R1 RATIFIED as a SEMANTIC CHANGE:** the objective profile `e2-default-v1` — fairness ×100 > work-percentage ×10 > preference ×1 — preserves E1's tier ORDER but changes its ratios (E1 30/20/10 → E2 100/10/1); ten preference violations outweighed three fairness violations under E1 and cannot under E2. Recorded, digest-enforced (results are comparable only across the same profile), never described as "preserved". (2) **E2-R2 RATIFIED:** the objective is a weighted sum; no surface claims lexicographic tier priority (a lexicographic objective is a different construction — staged solves — and promising it while shipping a sum would be the confident-wrong claim SPEC-04 §5 refuses). (3) **E2-R3 RATIFIED:** fairness semantics — `weekend_load` start-date attribution; `call_load` REFUSES on absent `isOnCall`; `CreditDistribution` normalises `per_fte`; `per_eligible_day` floors at 1. (4) **E2-R4 RATIFIED:** `fairness-outlier` conflicts name measured extremes with NO threshold (`blocksApproval:false`, `banded:false`) — no `fairness_dispersion` band exists. (5) **`demand-not-met` → `hard-breach` RULED correct** (the implementer flagged rather than re-mapped): the reason string covers both shortfall AND overfill; overfill is not unmet demand; the shortfall case independently raises PO-DEC-13's `unmet-demand` conflict via the rate — one reason, one class, no double-classification. (6) **`OBJECTIVE_SCALE = 10_000` is the doc 08 §3.4 global decision**, single-sourced across TS/Python with digest refusal; `scaleWeight` ROUNDS (the E1 truncation defect that zeroed sub-unit weights is closed and pinned). (7) Evidence-currency note: the retained cross-snapshot screenshot predates the F-01 repair; the e2e assertion is the proof; scoped screenshot regeneration (which rewrites every bundle) is an M4-005 sweep item alongside the dist-artifact runner hardening (third observation) and the nine pre-packet full-path phantom citations | EV-M4-004; M4-004 review + delta records (`review/m4-004` @ `1d898d6`) | 2026-08-18 |
| FAD-46 | **M4-004 review adjudication (verdict REVISE accepted).** Blocking: **F-01** a comparability REFUSAL rendered as an affirmative "identical" claim (the differences section unguarded by the verdict — the retained screenshot showed the contradiction and AC-35 walked past it) → guard on `comparability.comparable`, assert the identical-claim ABSENT on refusal; **F-02** incomparable measurements tabulated side-by-side under "cannot be compared" → the quality table is gated on the same verdict (per-candidate measurements may render SEPARATELY, never in one column); **F-03** `demand_satisfaction_rate` counts assignments capped at requirement without matching to required cells, and its shortfall conflict is thereby suppressed → implement SPEC-04 §7's definition (filled REQUIRED slots ÷ required, matched per date×shift-type cell per RK-RULING-01), the shortfall conflict raised when < 1, semantics PINNED (the reviewer's probe-04 shape: assignments on a no-demand date must score 0), and — the D-4 lesson — a demand-coverage check enters the INDEPENDENT checker/validator so it no longer rests on the model posting demand as hard. Conditions: **F-04** the deterministic-cost disclosure states the PACKET's measurement (≈1× startup-dominated; ≈10× on the searching class; H-7's faster-half NOT reproduced), never the M0 toy figures; **F-05** `scale_weight`/`scaleWeight` negative-half divergence closed (one rounding rule, pinned) or the "identical" comment corrected + the positivity contract cited; **F-06 RULED:** PO-DEC-23 is RESOLVED per doc 21 (targets adopted; bands set at M6 ≤ targets) — the nine "pending" surfaces say "no band exists until the M6 benchmark" instead; **F-07 RULED:** E2-R1's ratio change 30/20/10 → 100/10/1 is RATIFIED as the E2 profile (`e2-default-v1`, digest-enforced) but must be RECORDED as a semantic change from E1's ratios (limitation + FAD note), not described as "preserved"; **F-08** T2's clock excludes T1's elapsed (or `budgetSeconds` records the real allowance); F-09..F-13 recorded (citation fix; `explanationBudgetSeconds` plumbed or its absence documented; the load-average claim removed or transcripted; solverStatus on list/comparison recorded as an M4-005 UX item; the explanation-time/T2 record contradiction fixed). E2-R2/R3/R4 RATIFIED as authored. Delta review by the original reviewer mandatory | M4-004 review record (`review/m4-004` @ `91813df`) | 2026-08-17 |
| FAD-45 | **M4-003 review adjudication (ACCEPT WITH CONDITIONS) + the NUL gate mandate.** (1) **R-1:** four raw NULs in `readiness.ts` — the THIRD recurrence of the M3-008/FAD-42(R-2)/NR-18 class in two packets — repaired hash-neutrally, and **a raw-NUL gate is now MANDATED** (scripts/gates addition with red case, scanning tracked source+docs for `U+0000`, allowlisting only genuinely-binary formats): a class that recurs three times against a quoted lesson gets an automated control, not a fourth lesson. NR-18's doc instances become the gate's known-violations baseline or are repaired in the same round (implementer's choice, disclosed). (2) **R-2:** the missing UI retry control is a packet-objective gap — repaired (retry from failed/timeout states via `retryOfBuildRunId`, e2e + budget recording per I-10). (3) **R-3:** the five unproven DB controls gain falsifiability per FAD-33(1) — the fencing state-clause (`IF v_state <> 'running'`) as a red-case arm (it is the sole refusal for settled-run candidate injection), the other four as in-test mutation controls at minimum. (4) **R-4 ratifications:** D-4a's "non-terminal = the five in-flight states" RATIFIED; **D-4b (per-organization concurrency cap) assigned to M4-005** with the worker-queue binding where it naturally lives (disclosed in the INDEX, not silently absent); `protected_assignment_identities` write-only is CORRECT for M4-003 — its readers are M4-004 (progressive optimization) and M4-005 (matrix) — recorded. (5) The six `build.*` audit names + `build_run` subject RATIFIED as proposed (incl. the reaped-not-audited split — a worker fact belongs on the timeline; the state change it causes is chained). Delta review by the original reviewer mandatory | M4-003 review record (`review/m4-003` @ `2c0a860`) | 2026-08-16 |
| FAD-44 | **M4-003 disclosures adjudicated (pre-review repair round).** (1) **`origin` grant:** `selection.ts` writing assignment rows directly (because `addManualAssignment` hardcodes `'manual'`) would be a second spelling of the assignment write path — the S-01 class, for writes. GRANT, minimal: one optional `origin?: 'manual' | 'solver'` on `AddAssignmentInput` (default `'manual'`), the schedule service passing it through, and `selection.ts` REFACTORED to call the service — one write path, correct origin, rule 7's manual/solver distinction preserved in data. (2) **Audit names:** six additive `build.*` event names + one subject type GRANTED (the 000A additive-name precedent); exact names proposed in the repair report, ratified at acceptance. (3) **Capability key:** `schedule.version.edit` RETAINED for M4 — building toward a draft is version-editing authority; a dedicated `schedule.build.administer` key is a recorded owner question (the FAD-27 `audit.read` precedent shows the shape), NOT implemented now — no baseline change either way (rule 11 untouched). (4) **Credits into a build-produced draft:** honest non-carry stands for M4-003; the ruling belongs to M4-004 (E2/quality owns the credits interplay; note FAD-32(6) governs CLONES — a build-produced draft is a new version, a genuinely open question). (5) The two report-21 edge-table departures (`infeasible→draft_configuration` absent — retries are new rows; `queued→cancelled` present) are RATIFIED as the packet's own SPEC-04 rulings, correctly disclosed in three places | M4-003 return report; doc 35 §6e | 2026-08-15 |
| FAD-43 | **OPUS-M4-002 acceptance ratifications.** (1) **RK-RULING-05 RATIFIED as authored:** WeekdayFteLimit per weekday over the accounting window with **min-composition** — `min(node bound, profile-derived bound, profile max_assignments)`; the implementer's departure from the packet's "profile overrides" proposal is the ruling of record (an override may only tighten a HARD limit, SPEC-04 §3.3); `holiday` not-evaluable (no calendar constituent). (2) **RK-RULING-06 RATIFIED:** WorkPercentageTarget is a target, never a bound — SOFT objective term in integral assignment units `|count − round(target% × basis)|`; HARD authoring is a classification contradiction and blocks. (3) **RK-RULING-10 CONFIRMED:** `assignment_identities.id` (uuid) is the FixedAssignment identifier domain; no schema change; the human-meaningful cross-period key stays a recorded future owner question. (4) The **R-6 pins** (known-empty scope vacuous both sides; unknown id blocks) verified by the delta review's four-cell check — FAD-42(3)'s ruling is fully in force. (5) Delta-review observations recorded: the `chronological()` sort is load-bearing for the model/checker equivalence (a one-line comment marking it → next packet touching that file); `max_day_span` is O(days²·types²) worst-case at large minHours — immaterial at current period bounds, revisit if periods lengthen. (6) The registry now reads **22 evaluated / 8 not-evaluable / 0 open rulings, 11 resolved-and-cited** — every stable RK id retained with its resolution (rule 13) | EV-M4-002; M4-002 review + delta-review records | 2026-08-15 |
| FAD-42 | **M4-002 review adjudication (verdict REVISE accepted) + the R-6 empty-scope ruling.** (1) **R-1 (blocking)** — `cpsat_adapter.py` pairs each date with the next date only, so `MinimumRestBetween` beyond a one-day window is never constrained model-side; fail-closed (the checker refuses every such candidate — §3.3's deliberate redundancy proven load-bearing in production form) but the engine cannot produce a usable build for a legal rule class and reports OPTIMAL-then-rejected instead of honest infeasibility. Repair: pairing widened to the rule's real window (or an explicit modellability refusal for `minHours` the model cannot express — no silent half-compile), both-direction pin, a corpus fixture whose `minHours` exceeds one day. (2) **R-2** — nine raw NUL bytes (three files, incl. the corpus generator binary-blinded to diff) repaired hash-neutrally (proven `sha256`-identical spelling exists). (3) **R-6 RULED: a KNOWN but EMPTY `scope.staffGroups` on a HARD rule satisfies VACUOUSLY** — the rule binds members of the named group; an empty group leaves nothing to bind (the RK-RULING-04 class: an empty scope makes no statement); an UNKNOWN id still blocks via the covering vocabulary. At base the block was an accident of the kind being wholly not-evaluable, not a semantic. Pinned BOTH ways (known-empty passes; unknown blocks). (4) **R-7** — §6d's "compile-time/CI error" clause is honoured properly: a static parity gate (scripts/gates ADDITION, red-cased) binds Python `HARD_KINDS` to TS `EVALUATED_HARD_RULE_KINDS`. (5) R-3/R-4/R-5 disclosure corrections in the INDEX; R-9 dead branch removed; R-8 recorded as RK-04-consistent with a note for M4-003's pin handling; R-10 recorded (pre-existing raw DELETE outside every grant — a future hygiene item beside the FAD-41 §25b `reclaimStalePools` gap); R-11 the venv path scrubbed from evidence prose (placeholder), never present in code. Delta review by the ORIGINAL reviewer mandatory | M4-002 review record (`review/m4-002`, probes committed) | 2026-08-15 |
| FAD-41 | **FAD-40's mechanism DISPROVEN by capture; the true producer named; second one-file grant.** The finishing agent built the external capture FAD-40's diagnosis lacked and caught the surviving job: `audit.checkpoint` from `periodic.test.ts` — attempts 1, `run_at` in the PAST, locked by a DEAD pool, `is_available false`. C-2's drainer can NEVER take it (it registers `outbox.dispatch` and nothing else; `staleAfterMs` is an hour by design), so `count(*)` starves for the full 45s. Every clause of FAD-40's recorded mechanism (outbox failing-sink job, backoff, future `run_at`) is contradicted — with the queue provably empty at position 34, C-2 still failed 4-of-7. **Corrections, recorded not hidden:** (1) FAD-40's causal claim is WITHDRAWN; its granted fix STANDS on its own merit — `outbox-dispatch.test.ts` was leaving 20 measured orphan rows, now finalized through the production drain with a zero-rows end-of-file assertion (a real FAD-15 hygiene defect, found and fixed). (2) The orchestrator's "proven end-to-end" wording in FAD-40 overclaimed: the orphan's existence was proven; its identity as C-2's starving job was inferred, and the inference was wrong. The capture-before-conclusion rule the reviewers keep teaching applies to the orchestrator too. **GRANT #2:** `apps/api/test/audit/periodic.test.ts` (own-your-state: the file finalizes/unlocks-and-drains its checkpoint/verify jobs through a production-visible path before it ends, with a zero-rows-it-created assertion) and, ONLY if the fix genuinely cannot be expressed there, `apps/api/test/support/queue.ts` (e.g. the drain registering the real task names + the production `force_unlock_workers` recovery from M1-004's C-2 pattern — justify; never weaken C-2's precondition, `crash-restart.test.ts` stays prohibited). Proof bar unchanged: exposing seed green twice + the full gate | EV-M4-002 §§20–24, the step-19+ capture | 2026-08-15 |
| FAD-40 | **The C-2 drain-starvation coupling: diagnosed pre-existing, one-file fix granted to M4-002.** The M4-002 battery's seed-123456 failures (C-2 arms, 3-of-5 on branch) are a PRE-EXISTING coupling, mechanism proven by the orchestrator's own reproduction + code reading: `outbox-dispatch.test.ts`'s I-11 arm leaves its `AlwaysFailingSink` job in `graphile_worker._private_jobs` (its `afterAll` destroys the runtime but never finalizes the failing job, which graphile-worker reschedules with exponential backoff); `crash-restart.test.ts`'s `drainQueue` counts ALL rows while the drain worker cannot take a future-`run_at` job → the 45s precondition timeout whenever shuffle geometry and the backoff window align. Both files predate M4-002 (the packet's census: zero added enqueue paths); the packet's ~40s added runtime and new files re-exposed the alignment. **GRANT (the FAD-30 discipline, orchestrator-initiated):** `apps/api/test/audit/outbox-dispatch.test.ts` ONLY, for a stricter-test fix — the I-11 arm (and any sibling leaving a failing job) finalizes its job through a production-visible path before the file ends; C-2's own file remains untouched and its precondition unweakened; the fix is proven on the exposing seed and by the full gate. The evidence chain (agent census + orchestrator order-extraction + code reading) is retained in EV-M4-002 | orch-seeded-run.log; queue-table semantics; EV-M4-002 | 2026-08-15 |
| FAD-38 | **M4-002 escalation ruled: canonical snapshot v2 (Option A, full).** The implementer stopped pre-code (correct FAD-30 behaviour): the v1 `SolverInputSnapshotDocument` cannot carry `staff_groups`/`staff_group_members`, `valid_groups`/`valid_group_shift_types`, or a qualification key→id mapping — inputs RK-RULING-02/03 and CP-SAT modelling of `RequiresQualification` require; `RuleScope.staffGroups` makes ANY rule not-evaluable, blocking `B-ruleheavy`. Option B (fail 3 kinds closed) REJECTED: it narrows "every M4-evaluable HARD rule" and unbuilds `B-infeasible-missing-qualification` — a rule-11-class narrowing the authorization forbids. **GRANTED, additive-only:** (1) `packages/domain/src/ports/solver-snapshot.ts` gains staffGroups(+members), validGroups(+shiftTypes), qualification `{id,key,status}`; `SOLVER_SNAPSHOT_SCHEMA_VERSION` 1→2; two new constituent kinds; NOTHING removed or reshaped — existing consumers compile unchanged. (2) Assembly/contracts follow additively (already in-glob). (3) M4-001's determinism/immutability proofs are RE-ANCHORED, never weakened; per-file disclosure required; the independent review verifies no assertion lost. (4) RK-RULING-05/06/10 authored against the v2 shape. (5) The proposed 22-evaluated/8-not partition and the EXECUTION of the registry's existing `solver-and-validator` owner for `PatternRule`/`AlternatingWeek`/`ProtectedRange` are accepted as clarification (their M3 not-evaluable-against-content reasons stand unmodified; candidate-content implication is the new, different question) — reviewer verifies. (6) The `candidateFacts` optional seam on `CheckedVersion` accepted (publication caller byte-unchanged; FAD-27 preserved, asserted). (7) The four disclosed pin rewrites (hard-rule-check.test.ts ×3, step-06-hard-rules.test.ts ×1) are authorized as semantics-rewrites under the ruled semantics — reviewer verifies the original assertion classes survive (the N-5 precedent). OR-Tools venv outside the worktree + verified CP-SAT assumption support (T1 `EXPLAINED_SUBSET`) recorded from the escalation's probes | EV-M4-002 (escalation record `6f3fb0d`, probes 01/02) | 2026-08-13 |
| FAD-37 | **OPUS-M4-001S acceptance rulings.** (1) The 0017 mechanism is ratified: flip guard `FOR UPDATE` on `false→true`; holding guard `FOR KEY SHARE` **inside the lookup statement** (locking beside the read would re-read the stale snapshot — the correctness lives in the same-statement acquisition); the retirement race deliberately does NOT serialize (`FOR NO KEY UPDATE` vs `KEY SHARE`), preserving FAD-28/35 measured. (2) **Lock-ordering rule adopted repo-wide:** qualification-row locks are acquired before the 0003 per-organization audit advisory lock; every staffing writer audits LAST; any future writer that audits earlier, or any new advisory lock ordered against these rows (V-2 names `acquireStaffingSet`), must restate the analysis — M4-002's solver dispatch must not introduce new lock orders against these rows. (3) Review observations recorded: V-1 (the FK-privilege nuance on SELECT-only roles — harmless under current grants), V-2 (one-way staffing-set lock, measured clean), V-3 (order (A) rests on the VOLATILE fresh-snapshot rule), V-4 (0017 prevents creation; no backfill CHECK; sweep found zero pre-existing violations). (4) The reviewer's narrower-surface finding is pinned as fact: INSERT is the only holding-creator path; the status machine has no re-entry edge from `{revoked, expired}` into the live set; `valid_until` is not updatable by any runtime role | EV-M4-001S; M4-001S review record (`review/m4-001s` @ `08c763a`) | 2026-08-13 |
| FAD-36 | **OPUS-M4-001R acceptance + the R-1 ruling: write-time exclusion for the `requires_expiry` flip race.** (1) M4-001R ACCEPTED (review ACCEPT, zero blocking; the corrected docblock, the P3e inertness proof across all three consumers, and the FAD-33(1) citation fix are on main at `efa1ffd`). The proof's synchronisation mechanism is ratified as harness precedent: the 0003 per-organization audit advisory lock serialises the shipped services, so deterministic interleavings are built by reversing the pair and observing the block — causal, fail-loud, never timing-based. (2) **R-1 ruling:** the `requires_expiry` flip-while-granting interleaving is admitted TODAY with no read-side backstop (`requires_expiry` is not a verdict input; a race-admitted open-ended holding reads `satisfied` indefinitely on every consumer incl. the canonical solver input). Unlike retirement (FAD-28/35: read-time inertness), the bounded fix HERE is **database-level write-time serialization** — adding a verdict dimension would change the frozen eligibility module and FAD-23 semantics. Corrective packet OPUS-M4-001S (doc 35 §6c, migration 0017) is authorized and MUST close before M4-002 issues; `solver-snapshot.ts:154`'s write-time overclaim is corrected in the same packet; NR-17 registered. (3) Review R-2 (red-cases build-ordering on a fresh worktree, pre-existing) → M4-005 hardening candidate beside NR-16 | EV-M4-001R INDEX + review record (`review/m4-001r` @ `9fc280d`) | 2026-08-13 |
| FAD-31 | **Schedule-graph, membership, and time rulings (OPUS-M4-000B), ratified with the review's verification.** (1) **R-B1/R-B2/R-B3 membership semantics:** only an `active` membership is a NEW assignment target; the assignment falls inside the membership's effective window (`tstzrange [)` semantics — `ends_at <= valid_to` is correct); EXISTING assignments retained on later state change; **no manual-override arm** — FAD-23's override answers an access-control absence, membership state is a fully visible fact. (2) **R-B4 DST gap:** resolves FORWARD by the transition's OWN gap width (30 min/1 h/2 h pinned; add-an-hour mutants fail); **R-B4a (from review blocking finding B-1):** when the START is gap-normalized the WHOLE interval translates forward by the gap width — endpoints resolved independently produced 399 enumerated degenerate pairs, now 0; a typed `DegenerateShiftIntervalError` remains a backstop proven unreachable in 843k probes. (3) **R-B5 fold:** START takes the earlier occurrence, END the later — exactly-once coverage; its original "never degenerates" overclaim is WITHDRAWN (non-degeneracy is R-B4a's property) and the withdrawal is recorded in the module doc. (4) **R-B6:** location timezone is DISPLAY METADATA; the GROUP timezone governs every schedule semantic. (5) **R-B7:** a published version renders under its published basis; a stale draft is refused (`TIMEZONE_BASIS_STALE`, HTTP **409** with both zones — the review's B-2 fixed a 404 fall-through), never reinterpreted; aggregating views cut the calendar axis in the group's current zone with per-entry provenance. Honest limit: `tzdb_version` makes rule-set divergence detectable, not reproducible (one tzdb exists — the runtime's). (6) Additive audit: `schedule.version.published` gains scalar `timezone`. (7) N-5's cross-group D-1b arm is STRUCTURALLY UNREACHABLE under the participant FKs (reviewer attempted reconstruction by four paths, all refused) — its two replacement tests are the accepted coverage; V-18 enumerates 15 frozen columns from information_schema | EV-M4-INPUT-B; M4-000B review + delta review | 2026-08-12 |
| FAD-32 | **Rule-revision, registry, and publication-handoff rulings (OPUS-M4-000C).** (1) **Three step-06 count corrections, now derived from code:** `semantics-not-pinned` 7→8; `constrains-the-search-not-the-content` 7→6 (`StaffOverLocumPriority` is `data-not-modelled-at-m3`, double-listed in prose); one-ruling-away 10→11. Eleven stable pending-ruling ids **RK-RULING-01..11** (resolution owned by M4-002). (2) `rules.version` IS the revision number — one DB-owned counter is both CAS token and revision identity; zero-rows discriminated `not-found` vs `stale`. (3) The `amended` change kind and ONE exported material-change definition (time/shift type/location/pins/credits) consumed by diff, affected-participants, and the review digest — before it, a credit move produced NO difference row. (4) Period lifecycle transition table; `closed → published` deliberately absent; closed-period writes refused at the DB even via raw SQL. (5) Idempotency refusal codes `IDEMPOTENCY_KEY_REUSED_FOR_DIFFERENT_OPERATION`/`_REQUEST`. (6) **Voided credits are cloned** — a withdrawn credit is a record, not an absence. (7) The material-INPUT digest covers nine classes, each with a falsifiable mutation arm asserting the class digest AND the composite move, pinned to `MATERIAL_INPUT_CLASSES` so the set cannot silently shrink or grow unfalsified (review condition C-1's closure). Recorded residuals: a superuser can forge a revision row (append-only guard covers UPDATE/DELETE; no runtime role can — 12/12 refused); the closed-period guards rest on the composite FK for cross-tenant defence (defence-in-depth holds); the B-2-style CONFLICT_CODES backstop is real defence-in-depth but unfalsifiable alone (the typed branch runs first) | EV-M4-INPUT-C; M4-000C review + delta review | 2026-08-12 |
| FAD-33 | **Composition and process rulings from the M4-000 parallel-pair merge.** (1) **Decorative-arm standing rule ADOPTED** (000B's systemic finding, measured): a red-case arm that patches package SOURCE but asserts only through a DIST consumer proves nothing — every red case must name at least one test importing the patched file source-relative within its own package, OR rebuild between patch and run. 000A's `retired-verdict` arm repaired under this rule during merge integration (rebuild chosen over shrinking the assertion set; the naive rebuild broke compilation — TS6133 — so the violation is spelled compile-clean; both listed tests now observe, verified green/red standalone and in the composed battery). (2) **Precondition ordering at publication:** timezone-basis freshness is asserted BEFORE the review-digest CAS — a group-timezone change moves both, and `STALE_REVIEW` first would cost a useless re-confirm round trip before naming the real cause; the in-transaction service assertion remains the control. (3) `cloneVersion` composed from both packets' edits (000B's 3-line basis inheritance in the versions insert; 000C's `cloneCredits` at the tail) — verified in the function body, both proofs passing composed. (4) Migration-cycle tests assert applied migrations BY NAME, never by position (`count: 1` reversing "the last migration" poisoned the shared cluster when 0015 landed above 0014 — four downstream failures from one stale positional assumption; M4-001 adds 0016 and this must not recur). (5) **I-15 scanner under-approximation registered as NR-16**: the tenant-table detector requires a quoted literal earlier on the same line; bare-line raw SQL naming a tenant table passes today (in `publication.ts` as much as anywhere). Follow-up owned by M4-005 hardening. (6) Disclosed dead export `setRuleState` in `apps/web/src/api/rules.ts` — removal owned by M4-003 (the web packet) | M4-000B/C reports and reviews; composed-battery captures in EV-M4-INPUT-C §8 | 2026-08-12 |
| FAD-28 | **Staffing-input rulings (OPUS-M4-000A), ratified from the implementer's authored rulings + the independent review's probe evidence.** (1) **Retired qualifications:** R1 — a NEW holding naming a retired qualification is refused at BOTH layers (service `QUALIFICATION_RETIRED` 422; DB trigger `qualification_holdings_guard_expiry_retirement` 23001). R2 — EXISTING holdings are retained, readable, windows/status unchanged; the status machine keeps working (revoking a holding of a retired qualification stays possible). R3 — a retired qualification ceases to confer eligibility: the shared verdict evaluates lifecycle FIRST and returns the distinct outcome `retired` even over an in-window `valid` holding; an unresolvable lifecycle is `retired`, never `satisfied` (fail-closed). R4 — a NEW `shift_type_qualifications` row naming a retired qualification is refused in both "new" shapes (fresh INSERT and reactivation of an archived row); an already-active row stays active — retirement never rewrites existing requirements. R5 — `requires_expiry` cannot be flipped ON while open-ended live holdings exist (`QUALIFICATION_REQUIRES_EXPIRY_CONFLICT`), closing the two-statement bypass. (2) **Verdict outcome set pinned in code:** `satisfied | missing | expired | future | revoked | retired`; aggregate precedence revoked > expired > future. The granted-while-retiring READ COMMITTED interleaving is ADMITTED at the trigger (measured, review P3e) — safety is the verdict's fail-safe outcome, not write-prevention, and the evidence INDEX says so. (3) One additive audit event name `staffing.work_profile.cancelled`. (4) **DB capability gate for the future-row work-profile DELETE** (review F-1, migration 0013): token `staffing.work_profile.administer` — the same capability the HTTP route enforces; guard ordering is retention-check BEFORE capability-check, so in-force retention stays absolute and capability-independent (the owner remains bound by 23001, the 0012 proof preserved) | EV-M4-INPUT-A; M4-000A review record (ACCEPT WITH CONDITIONS, all closed) | 2026-08-10 |
| FAD-29 | **Enforcement read plane ratified with recorded residuals (OPUS-M4-000A, review F-4).** Migration 0012's one additive SELECT policy on `qualification_holdings`, gated on a transaction-local purpose token set via `set_config(name, value, true)` and cleared in `finally` — RLS stays ENABLED+FORCEd, tenant predicate unchanged. Review proved all four security dimensions: no survival across COMMIT/ROLLBACK on a pooled backend; cross-tenant read under the token = 0 rows; near-miss token = 0 rows (not a wildcard); `app_readonly_support` still 42501 with and without the token (the 0004 SENSITIVE-PII narrowing survives). **Residuals recorded, not dismissed:** (a) confinement is a module convention, not a structural control — any server code inside a unit of work can set the token; candidate structural hardening owned by M4-001's canonical-input work or later. (b) The `QUALIFICATION_REQUIREMENT_BREACH` refusal discloses a qualification id, outcome token and date to a publisher who may lack `read_any` — accepted for now as the price of an actionable refusal; revisit if the disclosure surface widens | EV-M4-INPUT-A INDEX §10; M4-000A review record | 2026-08-10 |
| FAD-30 | **M4-000A scope-crossing ruling + migration renumbering.** The packet edited `apps/api/test/support/schedule.ts` (a surface §2 of doc 35 assigns to 000B) and the schedule-service/publication files needed for verdict convergence, beyond its literal allowed glob, WITHOUT the required escalation (review F-5). Ruling: the work is in-objective (packet clause 6 requires the publication consumer to converge on the shared verdict), 000B had not issued so no merge risk existed, and the edits are accepted — but the unreported crossing is a process defect recorded here; the standing rule stays "a crossing is an escalation, never an edit," and every M4 packet brief now restates it. Consequences for the parallel pair: `packages/domain/src/eligibility/**` is FROZEN read-only for 000B/000C; `test/support/schedule.ts` is already-modified state 000B must take as found. **Migration renumbering (doc 35 §2 amended):** the F-1 fix consumed 0013, so 000B → `0014` and 000C → `0015`; 000C's prohibited range becomes 0001–0014 | M4-000A review record F-5; doc 35 dated amendment 2026-08-10 | 2026-08-10 |
| FAD-27 | **Step-06 rulings (OPUS-M3-008).** (1) **An unevaluable HARD rule BLOCKS publication** — fail-closed per SPEC-04 §3.3 ("a HARD rule is never skipped by any code path"); the explanation names the rule and its NOT-EVALUABLE status; the audited disable-then-publish path is the explicit escape; M4 shrinks the unevaluable set. Ratified after the reviewer verified the ruling "genuinely bites" (it forced a fixture change taken honestly rather than dodged). (2) The **6 EVALUATED / 24 NOT-EVALUABLE kind table** (step-06-node-kinds.md) is the scope of record — each reason honest and pinned after the review falsified CallSpacing's (now EVALUATED on `is_on_call`); eleven kinds are one ruling away each, listed for M4. (3) `AvoidDate` start-date-equality semantics stated and test-pinned; the overnight-attribution question recorded for the owner/M4. (4) New grant-only `audit.read` key (held by zero roles by default) + `group.holiday_calendar.read` (the FAD-25 split executed). (5) Settings-page consolidation NOT DONE — disclosed, carried with reasons | EV-M3-INTEGRATION; M3-008 review record | 2026-08-05 |
| FAD-25 | **The scheduler-vs-calendar-key ruling + staff read keys (OPUS-M3-006; closes carried item §2.10).** Reading one's own published schedule is SELF-SCOPED and independent of every scheduler/administrator key: new `schedule.own_published.read` (ownership-required, NO override capability — proven by a 38-capability evaluator drive) and `schedule.published.read`, both group-scoped under CAP-020, role-implied for member/viewer/scheduler/group_admin, withheld from telecom (docs/fable/08-roles-and-permissions.md row), org-role exclusion per P-10 (the FAD-24 precedent). **Standing rule adopted: write never implies read, read never implies write** — the D-10 coupling (one key gating holiday-calendar reads AND writes) is what produced the original edge; its split into read/write keys is assigned to OPUS-M3-008 which owns that surface. No baseline expansion | EV-M3-STAFF-VIEWS §3; M3-006 review (no blocking findings) | 2026-08-05 |
| FAD-26 | **Two OPUS-M3-005 disclosed additions ratified.** (1) The sign-off route (`POST …/versions/:id/state` over the frozen `transitionVersion`, existing `schedule.version.edit` key) — without it no version could reach `approved` over HTTP; reviewer-attacked: cannot skip states, cannot touch published/superseded (rows byte-identical after refusal), audited. (2) The review-digest CAS (`409 STALE_REVIEW`) — a second compare-and-set answering "did the draft change between review and confirmation?", which the prior-current CAS cannot; omission is 422, never a bypass. Also of record: the audit READ surface does not exist (publication audit display ships on `publication_records`, labelled as not-the-chain) — the audit read API is an OPUS-M3-008 deliverable | EV-M3-PUBLICATION-UX; M3-005 review (no blocking findings) | 2026-08-05 |
| FAD-23 | **Eligibility absent-vs-empty ruling (OPUS-M3-004).** Absent is a third value and must never render as ineligible: `staffing.qualification_holding.read` is grant-only, so a default scheduler's eligibility read reports what the CALLER can see, not a verdict about the member — rendering it as "not qualified" would manufacture a false patient-safety-adjacent statement from an access-control artefact. Three-state display: eligibilityKnown:false / row-absent → "Eligibility not shown", all candidates offered; eligible:false (genuinely missing to a caller who can see) → offered WITH required override reason (enforced contract+route+client on that arm only — B-4 closed the decorative-reason defect). Empty does not block: this surface IS the override/recovery mechanism (rule 7); enforcement lives at publication (step-06, M3-008) and M4. Reversal confined to two named functions | EV-M3-AUTHORING-UX; M3-004 review record | 2026-08-05 |
| FAD-24 | **Settings authorization + audit vocabulary (OPUS-M3-007).** Three group-scoped action keys — `group.settings.administer`, `group.timezone.administer` (own key by blast radius, the one settings change reaching published history), `group.location.administer` — role-implied for `group_admin` ALONE per doc 08 §6 (`✓` = role-implied, scheduler `—`; the row's org_admin `✓` is satisfied at L3: an org admin without a group membership is denied, the accepted M2-002 precedent). Six `settings.*` audit names + `location` subject type; `groups.settings_version` DB-owned CAS counter deliberately separate from the authorization freshness counter (both directions proven); the cross-product.ts additive deviation accepted (the role seed lives there). No-op writes emit NO audit event, with a load-bearing control proving emission still happens when something moves. No new CAP-### — the 58 baseline unchanged | EV-M3-SETTINGS; M3-007 review record (no blocking findings) | 2026-08-05 |
| FAD-22 | **SPEC-05 §4/§6 strengthened from OPUS-M3-003's independent second review** (dated amendment in the spec): (1) **D-15a extends to `INSERT`** — the review proved a published version's child set could GROW (B-2a: a new snapshot attached to a published version as `app_runtime`), which violates I-18's set-immutability reading; mechanism now `BEFORE INSERT OR UPDATE OR DELETE` with `COALESCE(NEW.version_id, OLD.version_id)`, and every service mutator of version-scoped content (incl. `moveCredit`/`voidCredit`/`recordConflict`) refuses non-draft versions — post-publication credit corrections are expressed on the next draft; (2) **`expected_prior_current_version_id` is REQUIRED** in the §6 signature (the review raced two different approved versions to a double-publish under the optional form — V-12's explicit-loser guarantee cannot hold otherwise); (3) **one idempotency-key domain** `^[A-Za-z0-9_.-]{1,64}$` contract+CHECK enforced, replay returns the recorded outcome. Also from the same review: the SECURITY DEFINER prune must be bound to the publication path (refuse a version that is still `is_current`) — it was a within-tenant per-version DELETE primitive that irreversibly disarmed D-1b | SPEC-05 dated amendment 2026-08-04; M3-003 review record | 2026-08-04 |
| FAD-21 | **`pattern_rules`/`staff_rules` are categories of the typed `rules` model, not separate tables (OPUS-M3-002 pre-migration ruling).** SPEC-04 §3.1 mandates typed-AST storage "not as free-form constraint objects"; separate free-form `segments`/`conditions`/`action` tables would re-open the CAR-006 escape hatch. Migration `0008` creates `rules` + `rule_sets` only, with an additive CHECK-constrained `category ∈ {general, pattern, staff}` discriminator. **No capability dropped (rule 11)** — every column of both superseded rows maps to a typed node/scope (mapping recorded in the escalation doc + doc 06 §3.2 amendment); `staff_rules` INTERNAL sensitivity derived from `scope.memberships` non-empty. `pattern_rules`/`staff_rules` rows retained as historical, not created. Ruled on OPUS-M3-002's mandatory pre-migration escalation (`EV-M3-RULES/ESCALATION-pattern-staff-rules.md`) after Fable verified the field mapping is complete and SPEC-04 §3.1 is the authority | doc 06 §3.2 dated amendment 2026-08-04; EV-M3-RULES | 2026-08-04 |
| FAD-18 | **`user_mfa` table + AES-256-GCM MFA-secret encryption (OPUS-M3-001).** A TOTP secret cannot be hashed (the verifier recomputes HMAC over it), so it is encrypted at rest, AES-256-GCM, key `SP_AUTHN_MFA_KEY_V<n>`, associated data binding `(organizationId, userId)` so a ciphertext relocated to another row fails to open (independent-review PROBE D could not falsify). `keyIsIsolated` returns **`false`** and discloses it — the same honest posture as the audit checkpoint signer; genuine key isolation is a deployment property this repo does not yet provide (recorded limitation). `last_accepted_time_step` monotonic (trigger `MFA_TIME_STEP_REPLAY`); recovery codes hashed; row FORCE-RLS and excluded from the support/break-glass read grant (SBX-004: `app_readonly_support/user_mfa = 42501`); MFA reset deletes it | doc 06 §3.2 dated amendment 2026-08-04; EV-M3-AUTHN | 2026-08-04 |
| FAD-19 | **scrypt as the password hash (OPUS-M3-001).** `scrypt$N$r$p$salt$digest`, `N=2^15, r=8, p=1`, `node:crypto` (measured hash 38.9 ms / verify 34.7 ms / ~32 MiB). Ratified over the packet's "argon2id preferred" because every argon2 binding is a native addon and the no-new-runtime-dependency + script-entries-only constraints forbid it. **Recorded limitation, not hidden:** scrypt lacks argon2id's side-channel hardening and its separate time/memory parameters — revisit if a native argon2 dependency later becomes acceptable | EV-M3-AUTHN; 32-m3-task-packets §3 FAD line | 2026-08-04 |
| FAD-20 | **`password_reset_tokens` — a fifth authn table (OPUS-M3-001 disclosed deviation).** SPEC-11's separate-flow/separate-namespace requirement for reset vs invitation (STM-017/018) cannot hold if the two share a namespace, which a `purpose` column on `invitations` would be. Hence a dedicated table, token stored hashed only, single-use `consumed_at`. `hashToken` domain-separates session/invitation/password_reset so a token minted for one is opaquely rejected by the others (independent-review PROBE B could not cross the namespaces). Additive, RLS-forced in migration 0007 | doc 06 §3.2 dated amendment 2026-08-04; EV-M3-AUTHN | 2026-08-04 |
| FAD-17 | Two OPUS-M2-003 escalations ruled: (1) **`membership_weekday_fte.day` is `{mon..sun, holiday}`** (doc 06 §3.2 amended with a dated note) — one day vocabulary across demand (FAD-16) and staffing targets. (2) **The `schema.test.ts` "no policy calls `app_acting_membership_holds`" assertion is correctly narrowed to its own rationale**: the prohibition exists because a policy ON a table that helper READS (`memberships`, `capability_grants`) recurses; on any other table the call is safe. `qualification_holdings`' SENSITIVE-PII SELECT policy may therefore carry the capability predicate (grant-only capabilities cannot be trigger-gated for reads). Conditions: the forbidden set is derived from the helper's own body (never a hand-kept list); the check asserts the helper IS called somewhere so the narrowing cannot pass vacuously; the M1 migration comment saying "never in an RLS policy" is historical overstatement in an applied migration — superseded by this ruling, and M2-003's independent review must probe for actual recursion as every role | OPUS-M2-003 return report D-2; EV-M2-PROFILES INDEX §3 | 2026-08-03 |
