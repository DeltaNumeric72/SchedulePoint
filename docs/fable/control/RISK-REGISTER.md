# RISK-REGISTER (live)

Seeded from docs/architecture/19 §1 (RISK-01..31, ratified) + [../18-risk-register.md](../18-risk-register.md) (NR-1..NR-8). Reviewed at every milestone exit.

**Updates 2026-08-01 (delegated-authority mandate):**
- RISK-22 (no independent review) — **partially retired:** internal adversarial verification commissioned and dispositioned ([../22-readiness-assessment.md](../22-readiness-assessment.md)); residual (external perspective) carried until the required pre-beta external review (blocking beta entry, V-04). NR-5 re-scoped to that review's outcome.
- RISK-19 / NR-8 — **retired as blocking:** decisions resolved with recorded reversibility; benchmark-band guard (bands must be at or under report-21 targets) is now a rule, not a pending ratification.
- RISK-25 (residency) — **mitigated by design:** provisional ca-central-1 posture adopted (OI-3/4); residual until procurement.
- NR-3 (stale doc-02 sentence) — **retired:** F-05 corrected.
- **NR-9 (new):** a provisional technology selection (TDG row) fails its M0 confirmation spike — affected row reopens; bounded churn. L: low-med, I: med. Retired by: OPUS-M0-001/003 + M0 wiring evidence.
- **NR-10 (new):** the internal review's shared model family under-finds defects that the executable harnesses also miss. L: low, I: high. Mitigation: T/P/V harnesses are the decisive evidence; required external review blocks beta entry. Retired by: harness executions + external review.

Currently elevated (watch list): EV-1 vendor specs (long lead, G-CONN) · NR-4 sub-agent quality variance (**stays elevated; M3-close data in the 2026-08-05 update**) · NR-10 · **NR-15 (RE-OPENED 2026-08-23; still open — the 2026-08-24 update immediately below MEASURES the last remaining candidate and excludes it, so the cause stays UNESTABLISHED, and the 2026-08-21 retirement remains superseded)** · **NR-20 (new 2026-08-23 — the selection audit-lock hold; figures are test-fixture only, realistic size UNMEASURED)**.

- **Update 2026-09-01 (OPUS-M5-F32 acceptance) — the order-dependence class gains its replay key, and two environment diagnostics.** **(1) Within-file order alone is FU-32's mechanism, and a SINGLE-FILE `--sequence.shuffle.tests --sequence.seed=N` run is the ~12-second replay key for the whole defect class:** the five historical seeds reproduce their recorded COMPOSED-run failure counts EXACTLY in single-file runs (measured by the implementer, independently reproduced at fresh seeds by the reviewer). Standing spelling: when a composed seeded run fails inside one file, try the single-file shuffled run at the same seed FIRST — it replays the class in seconds and separates within-file order dependence from cross-file interaction before any composed-run diagnosis is spent. Canonical order stays green on defective trees, which is why `pnpm check` never sees this class and only shuffled runs do. **(2) Two environment diagnostics from this packet:** a fresh worktree's missing `chmod 777 apps/api` surfaces as a FALSE "STALE CLUSTER? A previous run may have been killed before teardown" message (a permission bit masquerading as an orphan — check the mode before hunting a postmaster); and a single-file vitest filter must be the API-project-relative path (`requests/...`), never `apps/api/test/...`, whose no-match exit 1 reads like a failing run.
- **Update 2026-08-31 (OPUS-M5-004 acceptance) — a schema class rule, two watch items, and a run that survived a mid-packet suspension.** **(1) A widened CHECK domain is a ONE-WAY DOOR (class rule, binding on every future migration):** narrowing a domain back over data the wider domain admitted has exactly three outcomes — it FAILS while any admitted row exists (observed: M5-004's first down migration threw `assignment_snapshots_origin_domain … violated by some row` and half-reversed the chain, 18 files down); it succeeds by DELETING rows a published version may carry (I-18 / rule 5 forbid it); or it succeeds as `NOT VALID`, restoring the constraint's NAME and not its STATE. Standing spelling: **every migration that widens a domain states its reversibility in the header AT AUTHORING TIME**, so the property is priced when the value is taken, not discovered by a cascade; the down migration says plainly what it does not reverse, and the cycle test asserts the surviving definition via `pg_get_constraintdef`. **(2) R-05 watch item (M5-002 surface):** one observed, non-reproducing `expected 'OVERRIDE_REQUIRED' to be 'QUOTA_EXHAUSTED'` at M5-004's seed-20260901 composed run — passes 14/14 in isolation; no approval-path file in the diff; plausible mechanism (the loser observing the exhausted grant at the pre-check and taking §5.5's over-quota branch instead of consuming-then-classifying) NAMED AS PLAUSIBLE, not asserted; ruled record-not-base-run because a timing race replays file order, not interleaving, so a green base run carries no information. A recurrence of the signature in CI or the nightly matrix promotes it to its own diagnosis packet. **(3) Environment:** the pgrep/pkill self-match trap's FIFTH instance (two waiters matching their own `pgrep -f "tsc -b"`); the pause-debris class's FIFTH sighting and first across a MULTI-DAY pause (`/tmp/claude-0` reverted to 700 over the 3.5-day usage-limit suspension — modes are re-verified on every resume, never assumed held); a SIGTERMed red-case arm whose restore HELD through the kill (three md5s byte-identical — the first clean counter-datum for the FU-25/FU-30 debris class); and a NEW standing rule from the M5-004 reviewer's own false negative: **a `packages/domain` source mutation is INVISIBLE to consumers until `tsc -b` rebuilds — the package resolves to `dist/src/index.js`; verify a mutation took effect against the dist, never against a green run.** The mid-packet suspension itself is precedent: work preserved to a NOT-FOR-MERGE branch with evidence, the resumed tree regenerating the preservation md5 byte-identically after 3.5 days, and the packet completing its full review cycle — a usage-limit stall costs time, never integrity.
- **Update 2026-08-27 (OPUS-M5-00C acceptance) — a proof standard, a review-class lesson, and a clean environment run.** **(1) The base-tree replication standard, adopted:** a claim that a composed-run failure is pre-existing rather than the packet's is proven by re-running the SAME file/seed/flags on the UNMODIFIED base tree and observing byte-identical assertion failures — done at M5-00C for both seeded attempts (seeds 20260920/20260921, 3 and 4 failures, all in M5-003's `vacation-selection-http.test.ts` → FU-32). This supersedes diff-doesn't-name-the-file arguments, which show only non-causation-by-edit, not pre-existence. **(2) The wrong-policy-right-conclusion error class (C-5):** a docblock correction written at review named the WRONG RLS arm for a TRUE conclusion — the worse kind of error, because the conclusion still looked sound; caught because the delta CONFIRM re-read 0023's SQL instead of trusting the sentence. Standing spelling (extends the FU-28 rule): any prose that cites a policy, constraint, or mechanism is checked against the FILE at the round that writes it, never against either agent's message; the M5-00C INDEX carries the four policy readings verbatim. The corrected mechanism is itself register-worthy: permissive RLS arms combine with OR, so an administration arm with no ownership predicate means the own-arm's ownership question is NEVER REACHED for a capability holder — the M5-003 composition class restated at the policy layer. **(3) Environment:** zero kills, zero pauses across ~17 h of agent runs including a 2 026 s unit gate and an 866 s axe gate; FU-06's tree-hygiene gate recorded its FIRST live production catch (four untracked new test files, 16/17 → `git add -N` → 17/17); `python .egg-info` build debris is not in `.gitignore` and a bare `git add -N .` intent-adds it (excluded by hand twice; observation in doc 42 §5g). **[Amended at the M5-004 finalization, same day:** the header-splice edit class recorded twice above claimed a THIRD instance at this very landing — the §5g ACCEPTED-record edit consumed doc 42's "## 6. Standing acceptance battery" heading, no validator asserts headings, and it shipped to main in the landing commit before the next edit found it; restored in the M5-004 finalize commit. The standing rule tightens: when an Edit's anchor spans a boundary (the next section's heading, the next entry's header), the replacement must END with that boundary text verbatim, and the edit is verified by re-grepping the boundary line immediately after.**]**
- **Update 2026-08-27 (OPUS-M5-003 acceptance) — one product class, one git trap, one unexplained observation.** **(1) The by-id-write ownership composition class (doc 42 §5f):** on a self-scoped surface, SPEC-06 L5.1 passes BY CONSTRUCTION (the acting membership is its own target) and RLS admits an administrator's write BY DESIGN (rows, never operations) — so §4-class "requester-initiated only" rules can live ONLY in an operation-layer predicate on the verified context's membershipId. Two live instances found and cured at M5-003 (a scheduler could withdraw a member's selection AND a member's M5-001 request — both measured before fixing); the class is closed by enumeration with a structural test over the registered route table (all write verbs). Every future by-id write on an own-scoped surface meets this rule at declaration. **(2) `git checkout -- <file>` TRUNCATES intent-to-add files to EMPTY** — their index entry is empty, and every file a patch-delivered packet ADDS is intent-to-add, so the M5-002-era restore advice is unsafe for exactly those files. Observed live (a 20 739-byte page truncated to zero; caught by a pre-recorded md5; repaired from the patch's own addition hunk and verified three independent ways at review). Standing spelling: restore ADDED files by `cp` from an md5-recorded copy or from the patch's addition hunk; record the md5 BEFORE mutating; `checkout --` remains correct for files that exist in the index. **(3) Phantom vitest process trees, mechanism UNKNOWN:** during M5-003's seeded-run attempts, two orphan vitest trees re-parented to init were observed — started minutes after the run began, launched by nothing the implementer ran, each with its own cluster daemon and postmaster on the SAME port. Recorded as observed (elapsed times in the packet INDEX), cross-referenced as a PLAUSIBLE, unestablished mechanism for the same session's non-reproducing deadlock/missing-relation occurrence and for the kill class's stall presentations. Cleanup rule hardened: kill orphan postmasters by pid chain BEFORE removing `.pgdata-test-*` (removing the directory first crashes the next cluster start). The in-test red-leg restore gap is FU-30.
- **Update 2026-08-26 (OPUS-M5-002 acceptance) — the container's kill class is now MEASURED, and three process rules harden.** **(1) The environment kill class (doc 42 §5d; FU-25):** long vitest legs under the serial red-case runner are killed by **userspace-delivered SIGKILLs** — kernel OOM excluded by `/proc/vmstat` `oom_kill 0`; eight empty `sp-vitest-must-run-*` dirs spanning 2026-08-23→08-26 (five predating the packet's worktree) under the inference rule *a surviving must-run dir IS a killed wrapper*; non-deterministic (a 1 466 s standalone `gate:unit` survived the same window shape); a tool-call-boundary correlation recorded as a lead WITH its counter-example, not a mechanism. Consequence, registered as FU-25: the runner scores a signal-killed gate as a VERDICT (on RED, as PROOF). Until FU-25's fix lands, **serial-battery RED tallies carry that caveat, and FAD-54's sharded-CI form is the primary proof it always was — now for this additional reason.** **(2) The working tree is not a source of truth while the battery runs** (adopted at M5-002 after a mid-battery patch generation captured a live mutation, caught only by the file-count gate): no diffs, md5s, or status reads during arms; this SUPERSEDES the M5-001 spelling of the post-kill re-count as the operative discipline — the re-count remains, AFTER the run, joined by FU-27's `find . -name "__red_case__*"` companion (the diff re-count is structurally blind to ADDED gitignored artifacts; proven empirically by the M5-002 reviewer against its own run). Three disarmed controls were caught in-tree by the file-count assertion in this one packet (a neutered runtime provider-boundary guard twice; the schedule-views published-only predicate — draft visibility to staff). **(3) The pkill/pgrep self-match trap is an ENVIRONMENT property, not an agent habit:** four instances across two independent agents in one packet (dead waiters ×2, self-killed shells ×2 — one victim was the agent that first reported the class). Standing spelling: completion waiters match exact `ps -eo cmd=` text for the child's command, never a substring that appears in the waiter's own command line; `pkill` targets by pid chain, never by a pattern the killing shell itself carries.
- **Update 2026-08-26 (OPUS-M5-001 acceptance) — a process rule from the packet's provenance incident, recorded here because it binds every future packet's adjudication:** **the receiver-side record of cross-session agent messages is unreliable; the rulings of record live in the ORCHESTRATOR's session log and are re-affirmed at adjudication.** At M5-001 the implementer's reports first asserted ratifications, then asserted the opposite (zero inbound messages, four decisions "fabricated"); the orchestrator's log held every ruling as sent, and the implementer's own contemporaneous interims and transcript INDEX contradicted the zero-replies claim. Both reports are retained; the full analysis is doc 42 §5c's provenance item. The same acceptance record carries the amended environment discipline (the FOUR-level scratchpad traversal chain; the post-kill diff RE-COUNT as a standing preflight step after a killed red-case arm left a runtime security guard neutered in a worktree — debris kind 3's sharpest instance; the composed-run invocation of record with the sequencer's stderr line as proof of engagement).
- **Update 2026-08-25 — NR-22 RETIRED (M5-000a, FU-01; reviewer-authored at the delta, landed verbatim):**
  > **`--sequence.seed=N` is a true replay key for `api` file order again.** What landed: `CanonicalFileSequencer` in `apps/api/vitest.config.ts` — a `BaseSequencer` subclass whose `sort` orders the collected specifications canonically (module id, then project name, then pool, by UTF-16 code-unit comparison, deliberately never `localeCompare`) and then applies the seeded Fisher–Yates — installed by the ROOT `vitest.config.ts`, and installed there **only when the invocation asks for a file shuffle**. Collection order is discarded before the shuffle sees anything, so the landed permutation is a function of **(the file SET, the seed)** alone. The retirement condition this register wrote on 2026-08-24 is met exactly as written.
  >
  > **Measured at the review, at a seed the implementer never ran.** A full 142-file `--project api` run at seed 20260825 executed in an order **byte-identical to the order predicted OFFLINE from the shipped helpers before the run started** (141 passed | 1 skipped; 1 461 passed | 14 skipped; 1 382 s; exit 0), and a second run at that seed under very different machine load executed the same order again. The reviewer's own offline predictions at seeds 1 and 123456 reproduce, byte for byte, the orders the implementer's runs executed. Three seeds give three distinct orders and none is the canonical order — **detection power is unchanged**. Five domain-project runs at one seed were byte-identical, two at another likewise and different. The restated shuffle is the library's: the SHIPPED `shuffleBySeed`, imported from the emitted artifact, agrees with `@vitest/utils` 3.2.4's `shuffle` on all 144 (size, seed) pairs tested, with a live negative control; the library's `random(seed++)` increments a local and is therefore identical to the restated `Math.sin(seed)`.
  >
  > **Shuffle OFF is untouched, and that is structural rather than argued.** With no file shuffle requested the root config carries no `sequence` key at all, and vitest's RESOLVED config is byte-equal on the two trees — `{BaseSequencer, shuffle: null, seed: null}` with no flags, `{BaseSequencer, shuffle: true, seed: null}` for `--sequence.shuffle.tests`. Within-file order is decided in the worker from `sequence.shuffle`, which this class neither reads nor sets. The one behavioural addition is a config-level seed default, which **restores** what vitest applies only to its own `RandomSequencer` (`resolved.sequence.seed ??= Date.now()`, gated on that identity): without it a bare `--sequence.shuffle` run would have left each worker to draw its own seed. A CLI `--sequence.seed` still wins.
  >
  > **Two bounds, unchanged.** The file SET is part of the key, so a seed replays an order only against the tree it was drawn on; and pinning the observed PRECONDITION remains the stronger reproduction for a state-dependent defect — R-12 reproduces at a fixed backlog depth of 3 011 in about a minute, where a seed replays 142 files to reach the same state.
  >
  > **What the retirement costs, stated because it is in NR-22's own subject.** Vitest prints its `Running tests with seed "N"` banner only when the sequencer is its own `RandomSequencer`, so a custom sequencer suppresses it. The sequencer therefore announces the **resolved** seed itself, from `sort` — measured: an unseeded `--sequence.shuffle.files` run announces the value the config drew and the resolved `sequence.seed` is that same number; `--sequence.seed=42` announces 42; a shuffle-OFF or tests-only run announces nothing. Every automated invocation — the nightly fixed-seed matrix, the nightly rotating-seed job and both halves of `scripts/sbx/fixture-regression.mjs` — passes `--sequence.seed=N` explicitly as well.
  >
  > **A FIND against this register's own entry, recorded rather than quietly dropped.** NR-22's measured basis — `vitest list --project api --filesOnly --sequence.shuffle.files --sequence.seed=1`, five runs, three distinct orders — used an instrument that **never applies the sequencer**, so those flags were inert and the probe measured collection-order instability rather than the shuffle. Re-measured at this review: on the baseline the same command WITHOUT any shuffle flag returns three distinct orders in three runs, one of them byte-identical to an order the flagged command produced; on the retired tree the flagged command still returns three distinct orders in three runs; the sorted file SET was byte-identical across all nine. **NR-22's conclusion is untouched** — rider (a) established it structurally by reading `RandomSequencer.sort`, and the retirement is proved by EXECUTED order, not by `list`. What must not be misread: `vitest list --filesOnly` output remains unstable after the retirement, and doc 42 §5a's proof (1), which asks for byte-identical `list` output per seed, is unsatisfiable by any sequencer fix and was correctly discharged by executed-order identity instead.
  >
  > **Truth restoration landed with it:** `scripts/sbx/fixture-regression.mjs`'s NR-22 header and the `nightly.yml` rotating-seed comment carry dated notes recording that the printed `Reproduce with --sequence.seed=N` line is true again; the line itself and the script's whole executable text are byte-identical (4 098 = 4 098 stripped bytes via the TypeScript parser, with a live negative control). NR-22 rider (c)'s follow-up **FU-01 takes its first exit and closes.**
  >
  > **Residue, bound here so it cannot drift:** the installation predicate reads argv because vitest folds `sequence.shuffle: {files, tests}` to `sequence.shuffle = tests` before a sequencer could ask. It models cac's actual parsing — both `=value` forms, both space-separated value forms and both negations — and over 16 argv shapes measured against vitest 3.2.4's own resolution it agrees on every shape vitest accepts, **including the space-separated value forms `--sequence.shuffle.files false` and `--sequence.shuffle false`, which an earlier exact-token spelling read in the harmful direction (the review's finding C-1)**. The table is pinned by `apps/api/test/architecture/file-sequencer.test.ts`, which fails on exactly those two shapes when the earlier predicate is restored. False positives remain possible only for argv shapes in which one of these tokens is another option's value; no invocation in this repository produces one. False negatives fall back to stock `RandomSequencer`, losing nothing the repository had.
- **Update 2026-08-25 (M5-000a acceptance) — the citation-sweep collision class is generalized here, because its third instance was just found the same way its first two were: by CI, after a local pass.** The OPUS-M4-005 citation sweep reads path-like tokens out of prose and asserts they resolve in the repository; three distinct trigger conditions have now each turned a gate red once: **(1)** path-like tokens in a docblock's PROSE (R-4's regression file citing a probe filename whose tail parsed as a repo path — the NR-20 entry's datum, 2026-08-23); **(2)** a NEW file's prose is unswept until the commit that adds it, because the sweep enumerates `git ls-files` — invisible locally while untracked (`git add -N` before the local gate run is the mitigation, recorded at R-4); **(3)** fixture DATA read as citations (M5-000a's `file-sequencer.test.ts`: synthetic argv/path fixtures parsed as citable paths — and the first repair's explanatory comment itself spelled the dead path, needing a second round; both failing transcripts retained in the packet evidence). The class rule for every future executor: any new or newly-tracked file whose prose or data can contain path-like tokens gets `git add -N` + the citation gate run locally BEFORE review handoff; fixtures use synthetic roots and non-test suffixes. Not a numbered risk — the gate works (three catches, zero escapes); this entry exists so the class stops being rediscovered one instance at a time.
- **Update 2026-08-24 (doc 38 §7 battery, group C): a SECOND order-dependence class surfaced and stopped the battery — seed 123456's T-15 two-organization concurrency storm (`test/tenancy/unit-of-work.test.ts:898`, SPEC-01 §7.2) timed out at 120 000 ms at file position 111 of 142.** Not the R-12 queue class — zero `outbox.dispatch` completions occurred in the failing file's window (the worker counter sat at 1015 and never advanced). Mechanism, extracted by the battery executor from the eight completed runs rather than inferred: the storm's own workload is FIXED in every run (900 iterations · 3 600 units of work · 3 087 committed · 513 injected faults · 1 800 probe passes × 54 tables = 145 800 probe reads), but those probes scan INHERITED state — the organizations, groups and rows preceding files left in the 54 shared tenant tables — and measured storm time is linear in the inherited live (organization, group) partition count: `storm_ms ≈ 20 451 + 78.7 × partitions`, **R² = 0.9892** over the seven passing seeds (23.1 s at position 3 with 41 partitions → 85.6 s at position 82 with 824; the failing point is CENSORED at the ceiling, not measured). **Correctness held everywhere:** every completed run reports `WRONG-TENANT ROWS: 0` and 0 impossible census rows — this is a cost-vs-ceiling defect, not a tenancy defect. The test's authored comment ("900 iterations still runs in about a second") is off by more than an order of magnitude even at the best observed case. Under FAD-15 ruling 3 this is a **defect to fix, never a flake to retry** — routed as repair packet **R-13**; the fix decision (a storm cost independent of inherited state, or a measured precondition-scaled ceiling per the R-2/R-12 precedent — never a bare timeout bump, never a weakened assertion) is R-13's to make against SPEC-01 §7.2's intent. Battery state at the stop: seeds 1 / 7 / 42 / 424242 / 20260803 / 31337 / 99991 PASS with identical counts (141 files, 1 460 tests each, the known 14 `deterministic-cost` env skips); 123456 FAIL as above; five fixed seeds (20250101, 8675309, 65339, 531651, 740673) and the rotating seed NOT RUN — **the fixture-regression item re-runs in full on the repaired tip.** Evidence: `docs/evidence/EV-DOC38-GATE/leg6-seed-*.txt`, full uncut output, eight files. Owner: the R-13 packet. **[Corrected and CLOSED 2026-08-24 at R-13 acceptance — the mechanism sentence above is FALSIFIED; the failure itself was real and is repaired.]** The claim "storm time is linear in the inherited live (organization, group) partition count (R² = 0.9892)" does not survive a controlled test: the battery's two candidate regressors — inherited partitions and file position — are collinear at **r = 0.9917** across its seven completed runs (a position-only fit scores R² = 0.9803 on the same points, and both models predict the censored point within 0.2% of each other — re-derived independently by the reviewer AND by the finder, who accepted the correction: "I published the first and did not fit the second, which was my error"), so the battery's data could never have identified a mechanism, and the R² was asserted as one. Decoupling the regressors settles it: **1 227 partitions synthesized through the production write paths cost 12.3–15.8 ms per 54-table probe pass and a 22.2 s storm, while the composed run's 1 190 partitions at position 111 cost 68.58 ms per pass and a 136.2 s storm** — same depth, 4.4× the unit cost; a sweep to 2 412 partitions / 34 998 rows stays flat (12.7–18.6 ms, the maximum at the SHALLOWEST database), and +1.5 GB of retained JS heap moves the pass 8%. This original text stays above, unedited, per the NR-20 precedent: the provenance of the error must remain readable. **The DEFECT IS CLOSED by R-13:** the storm now measures its own probe-pass cost before it starts (4 warm + 12 timed passes on a dedicated runtime, outside every asserted counter) and scales its deadline — `min(840_000, max(120_000, passMs × 1800 × 4))`, enforced per iteration with a named error, the vitest timeout demoted to a 900 s hang-only backstop held 60 s above the largest possible ceiling. Nothing is weakened — iterations, fault schedule, 54-table coverage, probe count and all five assertions byte-identical, verified at byte level by the review — and the no-regression property is STRUCTURAL, not sampled: the `max` binds from below, so the ceiling can never return less than the old fixed 120 000 ms (pinned by a new pure-function test; held over 19 adversarial inputs at review; the two patch revisions proven value-identical over 144 714 inputs at the delta). Both the implementer's and the reviewer's independent composed seed-123456 runs landed at position 111, EXCEEDED the old deadline (136 154 / 140 541 ms), and went green under measured ceilings (493 800 / 432 600 ms) with identical counters and `WRONG-TENANT ROWS: 0`. Two recorded trades, named by the finder at its delta: this is a test-harness repair for an UNESTABLISHED root cause — it makes T-15 robust to the run-wide slowdown, it does not remove it (that risk now sits in NR-23 below rather than in a red test); and the calibration self-scales because it is measured in the same run, so a slower machine or deeper position gets proportionally more budget — the property that makes recurrence-for-the-observed-reason implausible rather than merely unobserved. What the failure actually exposed is a different, larger phenomenon → **NR-23**.
- **Update 2026-08-24 — NR-23 (new, from R-13's controlled falsification): composed `api` runs exhibit a RUN-WIDE, position-correlated per-round-trip slowdown; the cause is UNRESOLVED.** `OBSERVED`: over the 141 files with printed durations in all eight tracked battery transcripts (`docs/evidence/EV-DOC38-GATE/leg6-seed-*.txt`), the median correlation between a file's duration and its position in the run is **r = +0.95** (78.7% of files above r = 0.8) — independently re-derived by the R-13 reviewer from the same transcripts; the files that do not correlate are predominantly the ones not dominated by database round trips (CP-SAT solver tests, file-scanning architecture tests, subprocess-spawning red-case arms — plus a few that do touch the database but are too short for the drift to register). The unit cost is per round trip: one 54-table probe pass costs ~12–16 ms early in a run and 68–78 ms at position 111, on the same database depth. `UNRESOLVED`: the cause. Three hypotheses are EXCLUDED **by measurement, not inference** (R-13's controlled experiments): inherited row volume and live (organization, group) partition count (sweep to 2 412 partitions / 34 998 rows: flat 12.7–18.6 ms, maximum at minimum depth), and worker JS heap (+1.5 GB retained ballast: +8%). It must NOT be written up as a mechanism until a measurement identifies one. **Currently benign, twice over:** correctness held in every completed run (`WRONG-TENANT ROWS: 0` throughout), and R-13's measured ceiling absorbs the drift for the one test whose round-trip count made it cross a fixed deadline. **Why it stays open:** it affects ~79% of the suite's files, it is a property of composed runs rather than of any test, and it bounds how far a composed `api` run can grow before other fixed deadlines start crossing — the next candidate fails the way T-15 did. The slowdown also drifts WITHIN the storm (observed/calibrated 1.10 and 1.30 in the two composed runs; the ×4 safety absorbs both at 0.28/0.32 of budget). L: low today. I: med (a suite-growth ceiling; false defect signals of exactly the class FAD-15 ruling 3 governs). Retired by: a measurement that identifies the cause (candidate starting points, named not claimed: embedded-postgres statement/plan-cache or WAL behaviour over a long-lived cluster; connection-pool aging; vitest worker-side accumulation other than raw heap size — the excluded three are OFF the list). Owner: post-gate, M5-era, alongside NR-20's realistic-size measurement. **[Rider 2026-08-25 — the full 14-run fixture-regression re-run on the repaired tip `149edee` supplies the entry's first systematic series, and a finding.]** All 13 fixed seeds plus rotating seed 467407 GREEN, identical counts every run. The T-15 calibration measured per run: calibrated pass cost **14.83–54.83 ms (3.7×)** and storm **21 250–132 961 ms (6.3×)** on one unchanged tip; budget fraction 0.17–0.35, no run near its ceiling; calibration error −33% to +39% (single-pass sampling variance), every case absorbed by the ×4 safety; the 120 000 ms floor bound on exactly one seed (31337, calibrated product 106 776 ms — the can-only-add-budget branch exercised live). Near-identical positions calibrated 31% apart (seeds 65339/8675309 at positions 46/47), confirming the driver is per-process load, which position only loosely proxies. **The finding: seed 740673's storm ran 128 256 ms — a SECOND fixed-set ordering above the old fixed deadline**, never previously observed because the stopped attempt halted at seed 123456 (the 8th); the pre-R-13 tip therefore carried at least two latent T-15 failures among thirteen fixed orderings, both late placements (108/111 of 142) with the two highest calibrated costs. R-13 repaired a class, not a seed. Evidence: `docs/evidence/EV-DOC38-GATE/leg6b-*.txt` (14 uncut transcripts). **[Rider 2026-08-27 (M5-00C) — the first growth-ceiling crossing CANDIDATE, named while still amber.]** M5-00C's second seeded attempt (seed 20260921) crossed a fixed deadline for exactly the reason this entry predicted ("the next candidate fails the way T-15 did"): the R-03/R-12 drain precondition inherited **198 jobs and exceeded its 45 s floor** — the drain's `timeoutMs: max(45_000, inherited × 150)` yields 45 000 ms at that depth while the drift-inflated per-job cost late in a composed run pushed the actual drain past it. One occurrence, second attempt green on the same tree at the other seed, so recorded as a CANDIDATE, not a defect: the fix shape on file is the R-2/R-13 pattern (a measured-margin drain budget — calibrate per-job cost in-run and scale, never a bare constant bump), owed to the packet that observes it twice or the M5-006 sweep, whichever first. The nightly 13-seed matrix on GitHub runners is the systematic recurrence series; a matrix recurrence at this site promotes the candidate to a defect under FAD-15 ruling 3. **[Datum 2026-08-31 (M5-004):** T-15's own instrumentation in a composed run calibrated **210.92 ms per 67-table probe pass against 20.83 ms for the same probe isolated the same day — 10×**, the storm's ceiling scaled to its 840 000 ms cap, storm green. The phenomenon re-measured, not a new one; the cause remains UNRESOLVED per this entry.**]**
- **Update 2026-08-24 (post-M4 internal review, FAD-53 repair R-12 — the docket's last packet):** **The seed-1 fixed-seed failure recorded at the end of the R-11 entry below now has an established mechanism, a repair, and a reproduction — "Owner: the R-12 diagnosis" is discharged.** Mechanism, verified by controlled experiments and linear in queue depth: `periodic.test.ts` R-03 starts a concurrency-1 `startOutboxRunner`, enqueues `audit.checkpoint` through `app_enqueue_job` (which deliberately sets no priority), and polls a fixed 30 s deadline — but graphile-worker claims strictly by `(priority, run_at, id)`, i.e. FIFO, so R-03's own job sits behind every `outbox.dispatch` job the composed run accumulated before the file started. The seed-1 order R-11 captured inherited **893** jobs at ~35.5 ms each = **31.7 s against a 30.0 s budget** — 6% over. The defect is **latent since M1**: it was proven failing at `d28f669` (pre-R-6), which **exonerates R-6**; controlled depths bracket it (900 green, 2000/3000 red) and it reproduces **deterministically at a fixed depth of 3011 in about a minute**, after the seed order itself proved unreproducible (see NR-22 below). Repair, adjudicated and delta-verified: R-03 now establishes its precondition — `drainableJobCount` → a signer-bearing `drainQueue` with `timeoutMs: Math.max(45_000, inherited × 150)` → assert the backlog is 0 with a named-precondition message — before its runner starts; the 30 s deadline, the `toBe(before + 1)` assertion, and the enqueue path are untouched (the last byte-identical). **No-regression theorem, reviewer-restated at the delta:** for any depth *D* and per-job cost *c*, if the unrepaired test passes then *D·c* < 30 000 < 45 000 ≤ max(45 000, 150·*D*), so the repair cannot turn a green run red at any depth or machine speed. The reviewer independently reproduced both legs (red: `expected 2 to be 3` at 30 152 ms, depth 3011; green: 7/7, 3 022 jobs drained, R-03's runner executing exactly one job), and at the close verified the seed-1 position pairing from primary evidence (`periodic.test.ts` ran 87th of 142 in R-12's own seed-1 transcript, the run that inherited 232 jobs). **What this does NOT establish:** REV-B's 336-job occurrence is a different failure (outbox-dispatch's `afterAll`, not R-03's deadline) and its cause **remains UNESTABLISHED** exactly as the R-11 entry below states. Two records that ride this closure: **(1)** the composed seed-1 suite runs skipped `test/solver/deterministic-cost.test.ts` (14 tests — no solver venv in that environment); environmental, named so the 142-file figures read correctly. **(2)** An R-11 evidence-posture correction, recorded rather than left in a transcript: R-11's claim that the scratchpad ancestor-directory modes were "restored to 700" after the embedded-postgres `o+x` requirement measured FALSE on inspection — the mode observed afterwards was 0701, with a single chmod issued after the writes, indistinguishable from the harness re-asserting it; the restoration discipline stands, the *claim of having verified it* is withdrawn as unverified.
- **Update 2026-08-24 — NR-22 (new, reviewer-authored at the R-12 delta, landed verbatim):**
  > **NR-22 (new 2026-08-24, FAD-53 repair R-12 review): `--sequence.seed=N` pins the shuffle permutation only *relative to collection order*, and collection order is not stable.** Measured, not inferred: `vitest list --project api --filesOnly --sequence.shuffle.files --sequence.seed=1`, run five times in one unchanged checkout at `b9909eb`, returned **three distinct file orders**; the file SET was byte-identical every time (142 files, identical sorted digest). Clearing `apps/api/node_modules/.vite/vitest` between four further runs produced four more distinct orders, which **excludes the Vitest results cache as the cause**. The mechanism is in the library and is read off its source rather than inferred: `RandomSequencer.sort(files)` is `return shuffle(files, sequence.seed)` (vitest 3.2.4) — a seeded Fisher-Yates applied to the collected array **with no canonical pre-sort** — so the permutation is a function of (collection order, seed), and collection order is glob-walk order. `--sequence.shuffle.tests` is unaffected: tests within a file are collected in source order, which is stable. **Detection power is NOT affected** — any landed order is a valid random order over the same set, so the fixed-seed battery and the nightly matrix sample order space exactly as intended and no claim either makes is invalidated. **Reproduction precision IS affected, and it is a process hazard rather than a documentation nit.** `scripts/sbx/fixture-regression.mjs` asserts reproducibility twice — "The failing seed is printed so it is reproducible" and "reproducible from the log alone" — and **FAD-15 ruling 3 leans on them**: an executor told to reproduce with `--sequence.seed=N` may fail to and conclude *flake*, the one conclusion ruling 3 forbids. This has already occurred: R-12's own seed-1 re-run placed `periodic.test.ts` behind a **232**-job backlog where R-11's placed it behind **893**, which is precisely why R-12's full-suite green is a composed-safety proof and not a same-order red→green. **Consequently, both sentences in the script are corrected in this change** to say that the seed pins the permutation only up to collection order, and that **a reproduction must pin the observed precondition — for a queue-timing defect, the inherited backlog depth — not the seed alone.** L: low (nothing under-detected). I: med (a real defect mis-filed as a flake, against a binding ruling). Retired by: a sequencer that sorts the collected files canonically before shuffling (a `sequence.sequencer` override in `apps/api/vitest.config.ts` would do it), at which point seed-N becomes a true replay key.

  Three riders the delta round added, landed with the entry: **(a) The structural cache exclusion, stronger than the empirical one above:** `RandomSequencer.sort` *overrides* `BaseSequencer.sort` outright, so the cache-consulting sort path never runs when shuffle is on — the cache is excluded by construction, not only by the four-run measurement. **(b) A method caveat for anyone re-deriving R-12's comment-only identity proof:** a naive scanner-only comparison is NOT sound for this file — a bare `ts.createScanner` loop mis-lexed a template literal and produced a false DIFFER; use a real parser (acorn with `sourceType: 'module'`, corroborated by TypeScript's parser + `removeComments` emit, 4 098 = 4 098 bytes, byte-identical), and prove the stripper actually detects change with a negative control (the C-1a round altered one numeric literal, `1_000_000` → `1_000_001`, and the token streams correctly DIFFERed — a null result is only evidence when the instrument is shown able to report a non-null one). Stripped byte counts are method-dependent (acorn-based stripping preserves layout at 3 910 bytes; TS re-prints at 4 098); **equality is the invariant, the byte count is not** — a future reader must not read the 3 910 / 4 098 mismatch as a contradiction. **(c) The printed-line residue, bound here so it cannot drift:** the operator line `Reproduce with --sequence.seed=N` printed by `fixture-regression.mjs` still carries the over-claim; changing it is an executable change and was correctly routed OUT of the comment-only round. It is **one follow-up with two exits — fix the sequencer (at which point the line becomes true again), or reword the line — owned by whoever takes NR-22's retirement, in the same milestone**, because the line is read by an operator at the exact moment the flake/defect misjudgement FAD-15 ruling 3 forbids gets made.
- **Update 2026-08-24 (post-M4 internal review, FAD-53 repair R-11):** **NR-15 — the locked-backlog candidate is MEASURED, and it is EXCLUDED too. The sentence "the only one the arithmetic admits" no longer holds and is superseded here.** The 2026-08-23 R-6 entry below listed three shapes for REV-B's 336-job remainder, excluded two arithmetically, and left the third — "jobs locked by dead worker pools" — unmeasured and named as the best fit. R-11 measured it, in a probe outside the repository tree that drove graphile-worker 0.17.3's real claim mechanics against a real PostgreSQL 17.10 with the shipped predicates copied byte-for-byte (`drainableJobCount` from `apps/api/test/support/queue.ts`, `reclaimStalePools`' SELECT from `apps/api/src/db/queue-pools.ts`). **Evidence posture, stated so a future reader is not sent looking:** doc 38 §3's R-9.2 rules require a probe to live outside any collected root and to be removed afterwards, so this probe and its run transcripts were SESSION-EPHEMERAL working files and no longer exist. **This entry is the durable record** — every figure below is quoted inline, with the command shape and the exact drain message it produced, precisely so the conclusion does not rest on an artifact nobody can open. Re-deriving it needs only the two predicates named above, a graphile-worker `run()` with a handler that never settles, and a `SIGKILL`. **Every half of the mechanism reproduces exactly as described — and that is what excludes it.** With a 400-job `outbox.dispatch` backlog and a child worker pool whose handler never settles, then `SIGKILL`ed: the pool strands rows, the rows survive its death, `drainableJobCount` keeps counting them, `reclaimStalePools` at `drainQueue`'s `staleAfterMs: 3_600_000` returns **0 stale pools** for a pool that died a second ago, and a fresh drainer clears everything it can and then burns the rest of its 45 000 ms on rows it cannot claim — throwing the NR-15 message verbatim. **The decisive figure is the count.** Two variants: at **concurrency 8** the peak simultaneously-locked row count is **8** and the drain reports **"the queue still holds 8 drainable job(s) after 45000 ms"** (392 of 400 drained, in 627 ms); at **concurrency 1** the peak is **1** and the message reads **1** (399 drained). **The stranded count equals the dead pool's concurrency, exactly, and never exceeds it** — graphile-worker 0.17.3's batch fetch (`localQueue.size`) defaults to `-1`, i.e. off, so a worker holds one job at a time. **And every runner in `apps/api` uses concurrency 1:** `startOutboxRunner` and `startBuildQueueRunner` both default to `options.concurrency ?? 1`, `drainBuildSolveQueue` passes `1` explicitly, `src/index.ts` reads `SP_BUILD_WORKER_CONCURRENCY` defaulting to `1`, and no call site anywhere in `apps/api` passes a larger value. **So one dead pool strands ONE row, and a 336-row remainder by this shape needs 336 pools dead, unreleased and each holding a job at the same instant, inside a single composed run.** For scale, attributed and bounded: R-11's own whole-workspace seed-1 run names **8 distinct graphile-worker workers** across its 142 files — a LOWER bound, since a pool that completes no job never logs one — against **11** `startOutboxRunner`/`startBuildQueueRunner` INVOCATION sites in `apps/api` (13 if the two exported definitions are counted) [figure corrected in place 2026-08-24 at R-11's condition round, on the reviewer's own count: the first draft said 14, which was the raw `git grep` row count — it included the two `export async function` definitions and one docblock line in `periodic.test.ts` that quotes a call. The conclusion is untouched, and the correction is recorded rather than silently applied, per this register's own NR-20 precedent]. Whatever the true instantaneous figure is, it is not 336, and the run is `singleFork`/`fileParallelism: false`, so the pools are not even alive at the same time. **The arithmetic does not admit it either.** All three named candidates are now excluded and **the cause of REV-B's 336-job occurrence remains UNESTABLISHED** — the honest position is that the list was incomplete, not that the answer is on it. Two shapes the probe makes visible and did NOT measure, recorded because they are the ones the exclusions point at: (a) **a drainer whose own single worker claims a job whose handler HANGS** — throughput falls to zero and the entire remaining backlog is the remainder, with no bound from pool concurrency at all; (b) **a failing `outbox.dispatch` handler** — graphile-worker records the failure and schedules a retry with exponential backoff, `drainableJobCount` has no `run_at` filter, so a backed-off job stays counted while being unclaimable (the probe's survivors show `attempts` moving, so the claim counter is live). Both scale with the BACKLOG rather than with worker slots, which is the property the excluded three lack. Neither is claimed; both are named so the next measurement starts from a list that has not already been falsified. **Retirement conditions are unchanged** — a composed `pnpm check` green at the doc 38 §7 gate, a `fixture-regression` run whose figures are reported rather than assumed, and a measurement that identifies the cause — except that the clause pointing at the locked-backlog candidate first is now spent. **One further datum, from R-11's own measurement rather than from a claim:** a single fixed-seed shuffled `api` suite (seed 1, the exact command `scripts/sbx/fixture-regression.mjs` issues) ran **1 513.91 s (25.2 min), 142 files, and failed ONE test** — `test/audit/periodic.test.ts` R-03, "enqueuing audit.checkpoint by name makes the real runner write a checkpoint", `expected 2 to be 3` after 30 139 ms. That is a fixed-seed failure on the current tree, in the `audit.checkpoint`/queue-timing neighbourhood R-6 has just been working in, and under FAD-15 ruling 3 it is a defect to fix rather than a flake to retry. **R-11 did not diagnose it and does not claim a mechanism** — repairing it is outside a CI-homes packet — but it is recorded here rather than left in a scratch transcript, and it is the first thing the new nightly battery would have reported. **Owner: the R-12 diagnosis.** One thing that is NOT a second signal for it, recorded because the transcript invites the mistake: R-11 also started `corepack pnpm fixture-regression --quick` to validate the nightly's invocation, and that run ended after ~43 s having printed only its banner and "1. FIXED SEED SET". **Its cause IS established and it is not a product signal — the operator killed it** (`pkill -f "fixture-regression --quick"`, to free a 4-vCPU box for the locked-backlog probe's re-run); `[ELIFECYCLE] Command failed.` is pnpm reporting the signal, and the missing exit trailer is the same pattern having matched the wrapping shell. It validated the invocation and the `--quick` argv reaching the script, and nothing else. It is named here only so a future reader does not count one defect twice.
- **Update 2026-08-24 (post-M4 internal review, FAD-53 repair R-11):** **NR-21 (new) — the request-recorder's two bounds, moved out of the task ledger and into a register.** R-1 closed REV-B-001/REV-C-001 by replacing `waitForLoadState('networkidle')` with a recorder-internal quiescence wait on both sides of every recording window (`apps/web/e2e/support/request-budget.ts`), and its acceptance recorded two residuals that have lived only in the acceptance record since. **Neither is a defect in the repair.** Each is a bound on what the repair can see, and R-11 files them because a bound recorded only in a commit message is a bound the next reader will assume away — which is REV-C-004's shape (a requirement whose evidence has no home) applied to a limitation instead of to a battery. **(a) The critical-path real-stack spec's own `recordStep` recorder carries HARD I-13 assertions that CI cannot reach today.** `apps/web/e2e/critical-path.spec.ts` keeps a private recorder with the DOM-signal shape R-1 removed from the shared one, and two of its sites are `expect(...requests...).toEqual([])` — so that recorder CAN fail a run. It cannot fail CI **only** because the real-stack configuration (`apps/web/e2e/real-stack.config.ts`) is not wired into `.github/workflows/ci.yml`, and doc 38 §7 puts the real-stack e2e in the required final battery, so the reprieve is temporary rather than structural. **What actually holds it up is placement, and it is one line thick:** both sites bracket with an explicit `waitForLoadState('networkidle')` that is the FIRST such call after their `page.goto`, and a fresh navigation clears the lifecycle — so that one position is where the call is not yet sticky and genuinely waits. Move either assertion, add a second recording to those tests, or let anything else fire the state first, and the mitigation is gone with no test turning red to say so. **(b) The shared recorder's contract is bounded to the requests one call has observed since it began**, so a request already open when the wait starts is tracked only if it SETTLES inside the 500 ms quiet period: a fast straggler re-arms the timer and is duly waited for, while **one that outlives the whole 500 ms is invisible at that layer.** This is a deliberate trade, taken with its alternative named — Playwright's own `networkidle` has no such hole because it tracks from page creation, and it is unusable here precisely because it goes permanently stale after its first firing (the sticky-lifecycle defect R-1 exists to have removed). In this suite the trade costs nothing today, because every caller reaches the open side after its own page-load awaits and the close side after `action` has resolved; that is a property of the CALLERS, not of the primitive, and it is what a future caller can break without noticing. L: low for both halves. I: (a) med — a silent loss of an I-13 proof, which is the invariant with a real incident behind it; (b) low-med — an under-count in a budget assertion, in the direction that PASSES. **Deliberately NOT added to the watch list above:** neither half is an elevated risk, and putting a recorded bound beside NR-4 and NR-15 would dilute a list whose whole value is that its members are few. Retired by: (a) doc 38 §7's real-stack battery acquiring an automated home — at which point the I-13 assertions become live and the placement mitigation stops being load-bearing; (b) a recorder that tracks requests already in flight when a wait begins, or a recorded ruling that the caller-side property in the docblock is the guarantee this suite wants. Owner: post-gate, M5-era.
- **Update 2026-08-23 (post-M4 internal review, FAD-53 repairs R-4 / R-4b):** **NR-20 (new) — the selection transaction now holds the per-organization audit advisory lock across a whole canonical-input assembly, and every audited same-organization write blocks for that span.** This is an operational property the two repairs INTRODUCED, recorded here because it was bought deliberately and its cost is not yet known at a realistic size. R-4 closed REV-A-003 by acquiring `schedule_periods … FOR KEY SHARE` and then the migration-0003 audit advisory lock BEFORE `buildStaleness` (`apps/api/src/builds/selection.ts`); the lock is therefore held while `buildStaleness` re-runs the entire `assembleCanonicalInput` assembly, and is not released until the transaction commits. R-4b lengthens the same hold by re-evaluating FAD-26(2)'s source-digest compare-and-set under those locks (one further `materialInputDigest`), reached only when the staleness verdict passed. **Figures, attributed and bounded — and the span named (corrected 2026-08-23, R4b-REV-1, MINOR, record-accuracy).** On the test fixture the **selection transaction** measures **49–71 ms** across both variants — **R-4 alone 60/49/57 ms; R-4 plus the R4-REV-2 under-lock digest re-read 71/64/66 ms, a mean delta of ≈11.7 ms** — from the **R-4b review's own measurement**. An independent R-4b probe timed one `materialInputDigest` at **6.1–11.0 ms over six samples in one transaction**, which corroborates that delta. **The earlier ~310–380 ms figure is SUPERSEDED:** it originated in the R-4 review's round-1 sentence, was restated by R-4's code comment and restated again here, and it measures the CONTROL **test case** (fixture build, read-backs, apply) rather than the transaction. The direction was conservative, but a control document must not attribute a measurement of one span to another; it is corrected in place rather than deleted so the provenance of the error stays readable. **The LOCK HOLD itself is a SUBSET of the transaction and has NOT been isolated as its own measurement** — the capability check, the run load and the pre-lock digest CAS all sit outside it, so the hold is shorter than these figures by an unmeasured amount. With the right denominator the R-4b delta is **≈17% of the transaction** — the ≈11.7 ms mean delta against the **≈67 ms** mean of the variant that carries it (71/64/66) — while the independent per-call probe's 6.1–11.0 ms is a **9–16% per-call band** against that same denominator. **The two are not interchangeable and the earlier single "~10–17%" clause conflated them** (corrected 2026-08-23, FAD-53 repair packet R-3, on the R-4b reviewer's own arithmetic; the same correction is applied to the code comment in `apps/api/src/builds/selection.ts` that restated it): ≈17% is the mean cost the re-read adds, 9–16% is the spread of one call. Neither is a rounding error, though `buildStaleness` still dominates the hold. **All of these are test-fixture figures. The assembly's cost at a realistic tenant size is UNMEASURED, the hold grows with it, and so does the R-4b delta** (the digest reads assignments, credits, conflicts, the rule set and the whole catalogue). L: low today (selection is a deliberate, low-frequency human action), I: unknown — it is a per-organization serialization point on the audit chain, so the blast radius at scale is every audited writer in that organization, not only schedulers. Retired by: a measurement of `buildStaleness` + `materialInputDigest` at realistic tenant volumes, with a recorded ceiling; if that ceiling is unacceptable, the answer is a cheaper verdict under the lock, never a shorter hold that reopens REV-A-003 or R4-REV-2. **One further datum for the REV-B-005 / R-9 class (path-like tokens in prose turning a gate red), recorded because it appeared in a NEW place:** R-4's regression file cited REV-A's probe by its full filename in a docblock, and because `transcripts/` ends in the literal `scripts/`, the OPUS-M4-005 citation sweep read the tail as a citable path in this repository and CI went red on a0458a4. It was invisible locally while the file was untracked — the sweep enumerates `git ls-files` — so the class now has a second trigger condition worth knowing: a new test file's own prose is unswept until the commit that adds it.
- **Update 2026-08-23 (post-M4 internal review, FAD-53 repair R-6):** **NR-15 RE-OPENED. The 2026-08-21 retirement below claimed "verified clean across the COMPLETE seed set" and that claim did not hold** — REV-B's composed `corepack pnpm check` on the baseline tree, the first one it executed, failed the unit gate with the NR-15 message verbatim (REV-B-002, MAJOR, accepted at filed severity; `docs/evidence/EV-REVIEW-B/INDEX.md`):
  `outbox-dispatch-finalize: the queue still holds 336 drainable job(s) after 45000 ms — the precondition could not be established`, thrown by `drainQueue` (`apps/api/test/support/queue.ts:145`) from `apps/api/test/audit/outbox-dispatch.test.ts`. All twelve tests in that file passed; the `afterAll` finalization hook is what failed. **The rate is REV-C's, not REV-B's sample:** across the reviewers' five VALID composed runs (REV-C t12's tabulation, one of REV-C's own runs excluded for probe contamination) the failure appears **once — 1 in 5 composed, 1 in 4 whole-workspace**; REV-B's own two-run "1 in 2" is its sample, not the class's. A non-reproduction does not falsify a captured failure, and this register already applies that rule to itself (NR-15 was "explicitly NOT ruled a flake" after 55+ clean runs).
  **What R-6 established — a mechanism, reproduced deterministically rather than waited for.** `drainableJobCount` counts `outbox.%` **and** `audit.%`, while `startOutboxRunner` registers the `audit.*` periodic tasks **only when it is given a signer**; `DrainOptions.signer`'s own R-10 docblock states it and states the consequence — "a signer-less drainer has no handler for an `audit.checkpoint` job and therefore cannot empty a queue holding one". The failing call site passed no signer. Planting ONE `audit.checkpoint` job through the production producer (`app_enqueue_job` under real tenant context — never an INSERT into graphile-worker's tables) and handing it to that hook reproduces the failure on demand: the drain consumes the whole `outbox.%` backlog and then spends its full 45 000 ms on a job it has no handler for, exit 1, `Tests 13 passed | Test Files 1 failed`. The surviving row was captured **unlocked** (`identifier: audit.checkpoint`, `attempts: 0`, `locked_at: null`, `locked_by: null`), which separates this from the EV-M4-002 §21 stale-lease shape: it is a missing-handler failure, not an unreclaimable lease.
  **What the repair does, and what it is careful not to do.** The call site now supplies a signer, exactly as that docblock prescribes and as `periodic.test.ts`'s R-10 drain already did. **Nothing is weakened:** the 45 s cap is unchanged, `drainableJobCount`'s predicate is unchanged, no hook is skipped or softened, and the planted jobs are EXECUTED by a real runner (`Completed task 34 (audit.checkpoint, 3.78ms) with success` / `Completed task 35 (audit.checkpoint, 2.83ms) with success`) rather than deleted — §20b's rule that a queue must not be emptied by a statement the system cannot issue is intact. The hook is also self-sufficient: it **plants its own** `audit.checkpoint` job through the production producer immediately before draining and asserts the backlog holds one, so it proves it can drain the `audit.%` half of its own predicate on every run rather than only when the file order happens to ask. (The first shape had a test do the planting; the R-6 review reproduced the defect in that — a name-filtered run skipped the planting test, the pre-assert failed, the drain never ran and ~20 `outbox.*` jobs leaked, a teardown failure that reads like an NR-15 regression. A teardown must not depend on which tests a run selected.)
  **What is NOT claimed, said plainly.** (1) The repair closes the named seam at that call site; it is **not** shown to be what produced REV-B's 336-job occurrence — **no candidate on this list is established, and two of the three are arithmetically excluded.** Three shapes, measured rather than asserted:
  · **the missing handler** — proven on demand and repaired, but it predicts a remainder equal to the number of UNHANDLEABLE jobs, and R-6's own reproduction left exactly **1**, not 336;
  · **throughput against the 45 s cap** — R-6 first recorded this hook finalizing **629 jobs in 23.5 s (~27/s)**, from a **single run**; the R-6 review counter-measured **257 jobs drained inside a file whose entire duration was 5,675 ms** — **≥45/s, ~52/s realistic** once the same 13 tests plus a 21-job drain are known to cost 1,103 ms in isolation. At 27/s the cap clears ~1,215 jobs; at the measured rate ~2,000–2,300. A **remainder** of 336 therefore needs a starting backlog of roughly **1,550–2,700** — two-and-a-half to four times the largest backlog anyone has measured (629). Throughput does not fit either;
  · **jobs locked by dead worker pools** — unlisted until now and the best fit for the arithmetic: `drainableJobCount` counts locked rows, and `drainQueue` sets `staleAfterMs: 3_600_000` so it never reclaims them, so an arbitrary number of locked rows can sit in the count for the full 45 s with no throughput or handler problem at all. This is the EV-M4-002 §21 / NR-19 shape. **It is not repaired here**, and R-6 did not measure it.
  Neither REV-B nor R-6 isolated which file left that backlog. **The missing handler is proven to be A cause, on demand, and is fixed; the specific cause of REV-B's 336-job occurrence is UNESTABLISHED.** Whatever settles it, the answer is a faster, concurrent or reclaim-capable drain — never a longer cap. (2) **No seed-set claim is restated.** `pnpm fixture-regression` was not run by R-6, so nothing here re-establishes "clean across the complete seed set" — the wording that failed. (3) **Residue, recorded rather than closed:** two signer-less drains remain by design — `global-setup.ts`'s (it runs before any test file, so no `audit.*` job can yet exist) and `crash-restart.test.ts`'s own private drain (still unable to execute an `audit.*` job; the suite's only protection there is the convention that no file leaves one behind, enforced per-file by `periodic.test.ts`'s and now `outbox-dispatch.test.ts`'s finalization hooks, not by the shared helper). Retired by: a composed `pnpm check` battery green on this tree at the doc 38 §7 gate, plus a `fixture-regression` run whose figures are reported rather than assumed, **and** a measurement that identifies the cause of REV-B's 336-job occurrence — the locked-backlog candidate in (1) first, since it is the only one the arithmetic admits.
- **Update 2026-08-21 (M4 close):** **NR-15 RETIRED-AS-DIAGNOSED-AND-REPAIRED** — [**superseded 2026-08-23 by the update above: this retirement's "verified clean across the COMPLETE seed set" wording did not hold (REV-B-002); NR-15 is RE-OPENED**] — the five-packet intermittent had a mechanism after all: a `drainQueue` deadlock-by-construction on a handler-less `build.solve` job plus a drain/assertion predicate mismatch; repaired at the shared resource (`drainableJobCount`, namespace predicate) and verified clean across the COMPLETE seed set (fixture-regression 153/153 at close); the widened capture that found it in one line is permanent (the truncation was what kept it unexplained for five packets). **NR-16 CLOSED** (bare-line scanner + red case). **NR-18 RETIRED** — the baseline is EMPTY: the two reserved control-doc bytes repaired by Fable at close (each was the literal byte inside a "raw-NUL→` `" phrase; now the word `space`), the gate's magic-byte hardening closed the rename bypass, and any future NUL fails the build with no amnesty. **NR-19 CLOSED with a load-bearing proof** — the M4-005 review proved the closure claim unproven (deleting the repair broke nothing shipped, FAD-50 C-1); the proof now fails under exactly that mutation. **NR-4 stays elevated into M5-era work — the M4-005 data is the thesis at its sharpest:** four review rounds produced one blocking finding IN A REPAIR ROUND the orchestrator had just adjudicated (a CANCELLED deterministic run rendered `reproducible` — reviewer-probed against real CP-SAT), an unprovable hardening closure, a marquee suite passable with zero tests executed, and a truncation bound claimed complete in three places; every one reviewer-authored, every green battery blind to all of them. **NR-1 exercised twice in M4-005 alone** (sustained Opus unavailability checkpoint; a reviewer session-limit kill at delta-review start) — zero loss both times. New recorded limitations (doc 36 §10, owners named): the 10s default wall clock racing deterministic budgets (owner question, FAD-49(4)); the per-detail-GET assembly cost (M6); the un-falsified selection window (M5+); SBX-017 fixture thinness; the M-22 numbering gap (stays, rule 13).
- **Update 2026-08-18 (M4-004 acceptance):** **NR-18 (raw NULs) — the gate's value is now MEASURED, not argued:** the FAD-45 gate caught the class's fourth occurrence in M4-004's own new file (`demand-coverage.ts`, two bytes) on the first check that ran against new code, with path and offsets in the message; the baseline stays at exactly three pre-existing doc instances (repair → M4-005). **NR-4 (M4-004 data):** REVISE on three surface defects invisible to every green battery — a refusal rendered as an affirmative claim was sitting in the packet's OWN retained screenshot and its e2e walked past it — plus a delta-round reviewer-found MEDIUM (an unplumbed rejection reason that would have refused a candidate with no reason shown). Two consecutive milestones' data now say the same thing: the blocking defects live where batteries don't look — surface rendering, unmatched semantics, cross-layer plumbing — and only reviewer-authored probes find them. **NR-1 (context/continuity) exercised hard and HELD:** three agent terminations across M4-002/003/004 (budget stops ×2, a usage-limit kill mid-battery) and one orchestrator session interruption — every one recovered from committed state + detached batteries with the EXIT marker inside the redirected block; zero lost work. **New hygiene items for M4-005 (accumulating):** red-case runner leaves gitignored `dist/__red_case__*` after SUCCESSFUL runs (three observations; `git status` blind to it — the `find` check is load-bearing); "No test files found" arm signature; nine pre-packet full-path phantom source citations; scoped screenshot regeneration; solverStatus on list/comparison.
- **Update 2026-08-16 (M4-003 acceptance):** **NR-18 now GATE-PINNED** — the raw-NUL class (third recurrence: M3-008, M4-002 R-2, M4-003 R-1) has its automated control: CI gate 17 scans all tracked text files with a strict path+count baseline carrying exactly the three known doc instances; any NEW NUL fails the build, and repairing a baselined one also fails until the baseline shrinks deliberately. Repairing the three baselined instances (evidence files needing byte-exact care) plus the gate's extension-rename hardening (magic-byte sniffing, reviewer-measured bypass) are M4-005 items. NR-18 stays open until the baseline is empty. **NR-4 (M4-003 data):** the pattern held again — the review's blocking-class findings (the NUL recurrence, the decorative append-only arm where the GRANT answered for the trigger) were reviewer-authored probes invisible to green batteries; and this cycle adds a new datum: the reviewer publicly corrected its own round-1 measurement (an AND/OR precedence flaw in its mutation) — the adversarial discipline applied to the adversary. **Process note:** the reviewer's round-1 session left two orphaned cluster daemons it had claimed were cleaned; the orchestrator cleaned them and the delta round verified hygiene with ps/lsof before its final message — the finish-clean claim now carries its verification.
- **Update 2026-08-15 (M4-002 acceptance):** **NR-18 (new):** raw NUL bytes in tracked documentation — `EV-M3-AUTHN/INDEX.md` (binary to git diff), `control/CHANGELOG.md`, `control/OPUS-AGENT-RUNBOOK.md` — the M3-008 class, third recurrence (M4-002 introduced nine more in code, found by review R-2 and repaired hash-neutrally; the doc instances predate the packet and are outside its grants). L: low, I: low-med (an evidence INDEX that git will not text-diff defeats diff-based review). Owner: M4-005 hygiene sweep, with a candidate NUL gate. **NR-19 (new, FAD-41 §25b):** `reclaimStalePools` considers only pools `where released_at is null`, so a pool that releases cleanly while still holding a locked job leaves an orphan NO production sweep reclaims (the exact shape the C-2 investigation surfaced in test form). L: low, I: med (a stuck job invisible to recovery). Owner: M4-003/M4-005 (production `src/db/queue-pools.ts` — needs its own packet scope; the R-10 pre-existing raw DELETE in periodic.test.ts is recorded beside it). **NR-4 (M4-002 data — the milestone thesis confirmed):** the largest packet drew the only REVISE of M4 so far, and the blocking finding was again reviewer-authored — a truncated constraint window invisible to every green battery because the corpus never exercised rest beyond one day; the delta review then independently sat on the repair's bound and equivalence argument before accepting. Four ruled escalations (two implementer-initiated, one orchestrator-diagnosis corrected by implementer capture, one review-driven) all resolved without a single silent improvisation. Mandatory review and escalate-not-improvise stay load-bearing through M4-003..005.
- **Update 2026-08-13 (M4-001S acceptance):** **NR-17 RETIRED** — OPUS-M4-001S accepted & merged (`d910af6`, migration 0017): both flip-vs-grant orders refuse 23001 at the database; the reviewer's 16-case forbidden-state hunt held and additionally pinned the surface narrower than assumed (INSERT is the only holding-creator; no status re-entry edge; `valid_until` not updatable by runtime roles — FAD-37(4)); a permanent concurrent proof + mutation arm are in the battery; the whole-database sweep found zero pre-existing violating rows. **New transient signature registered (owner M4-005, beside NR-16/R-2):** a red-case arm exiting with vitest's "No test files found" is an infrastructure failure the runner cannot distinguish from a gate outcome, on either arm — observed twice on one run, reproducibly benign (both files pass standalone), and each occurrence orphans a cluster daemon. **Environment hazard recorded:** a wedged `mediaanalysisd` (322% CPU) + Spotlight indexing storm from battery file-churn produced three disjoint transient failure sets across three red-cases runs; pre-battery `uptime` check is now a standing note. **NR-4 (M4-001S data):** review ACCEPT with zero blocking for the second consecutive corrective packet — but reviewer probes again added material facts (the narrower surface, the per-half load-bearing proof, V-1..V-4); the discipline stays mandatory.
- **Update 2026-08-13 (M4-001R acceptance):** **NR-17 (new, FAD-36):** the `requires_expiry` flip-while-granting READ COMMITTED interleaving is ADMITTED — final state `requires_expiry=true` with an open-ended live holding, the state R5's sequential trigger refuses — and unlike the retirement race there is NO read-side backstop (`requires_expiry` is not a verdict input; the race-admitted holding reads `satisfied` indefinitely, including in the canonical solver input). Reviewer-reproduced (`review/m4-001r` probe D3). L: low (narrow window, admin-only flip), I: med-high (silent indefinite eligibility over-grant feeding M4 scheduling). Retired by: OPUS-M4-001S acceptance (doc 35 §6c, migration 0017 write-time serialization) — REQUIRED before M4-002. **NR-4 (M4-001R data):** review verdict ACCEPT with zero blocking — but the review still produced the only MEDIUM finding (R-1, an admitted race invisible to every green battery) plus the rebuild-is-load-bearing verification the implementer asserted and the reviewer PROVED by omission. The pattern holds: reviewer-authored probes remain the top defect source.
- **Update 2026-08-13 (M4-001 closeout + Stage-1 recovery):** **NR-15:** zero reproductions across every M4-001 battery (126/126 fixture-regression twice on the branch, once in recovery, all with fresh rotating seeds; `loader-writer-agreement` green throughout). Stays open, mechanism unknown; capture-widening remains the recorded improvement, owner M4-005. **NR-16:** unchanged, owner M4-005. **NR-4 (M4-001 data point — the strongest yet):** the independent review returned the milestone's first outright **REJECT**, and the blocking finding was again reviewer-authored — a wire-level probe (non-ASCII payloads) showing the RPC MAC signed a *re-derivation* of the message rather than the bytes sent, a defect invisible to every green battery because every fixture was ASCII. The same review cycle also surfaced two pre-auth crash paths and a kill-attribution conflation. Mandatory independent review stays load-bearing through M4-002..005. **Process risk surfaced and repaired:** the M4-era acceptances (000A/B/C/001) were never logged in the control task log (doc 24 §E), and M4-001's control closeout did not happen at merge time — both repaired in this closeout; the Stage-1 continuity rules (durable commit at every packet-close boundary) are the standing mitigation. **Transcript-quality note:** EV-M4-BOUNDARY `step-09` lacked its inner EXIT marker; superseded by the recovery battery's captured-exit cycle run (§10 row 08). **NR-16 (new, FAD-33):** the I-15 no-tenant-access-outside-unit-of-work scanner's tenant-table detector requires a quoted literal earlier on the same line, so bare-line raw SQL naming a tenant table passes undetected today (live instances exist in `publication.ts`; a false positive on 000C's discriminator literals is what surfaced the shape). L: low (the unit-of-work is still the only connection path; the connection detectors are unaffected), I: med (a future bare-line direct query would evade the tenant-table arm). Owner: M4-005 hardening; tightening ripples across existing modules so it needs its own packet scope. **NR-15:** zero reproductions across all M4-000 batteries (~90+ further whole-suite shuffled runs incl. three fresh rotating seeds on composed trees); stays open, mechanism unknown, capture-widening still the recorded improvement. **NR-4 (M4-000 full data):** three packets, three independent reviews, three ACCEPT WITH CONDITIONS — every blocking/conditional finding again reviewer-authored (a DB capability gap invisible to a green battery; a degenerate-interval class masked by the packet's own too-long fixture; a 404 masquerading as a declared 409 that no test crossed the wire to see; a nine-class evidence claim with three falsifiable classes, two structurally unprovable). Two NEW review-value classes this milestone: the delta review catching closure commits on the wrong branch (a merge that would have shipped both blockers), and the composed second-merger battery catching 8 cross-packet failures invisible to both packets alone. The mandatory-review + serialized-merge disciplines stay load-bearing through M4-001..005.
- **Update 2026-08-10 (M4-000A acceptance):** **NR-15 (new):** one non-reproducing failure of `apps/api/test/profiles/loader-writer-agreement.test.ts` ("an actual supersession closes the row the precondition named, and no other") on fixed seed 20250101 during the packet's fixture-regression run 2. The file is untouched by the packet and `authorWorkProfile` is byte-identical to the pre-packet tree; the reviewer bounded the packet out as a cause at the schema/data-visibility level (trigger census, RLS policy census, org-scoped loader reads) and refuted the one candidate mechanism (same-millisecond supersession collision — 600 measured supersessions, min gap 2 ms, 0 zero-gap pairs); 55+ whole-suite shuffled runs plus 40 targeted repeats produced 0 reproductions; the gate truncates assertion output so the actual failure was never captured. **Mechanism UNKNOWN — explicitly NOT ruled a flake (FAD-15) and NOT ruled explained.** L: unknown-low, I: low-med (suite reliability, false signals at the worst milestone for them). Watch: every subsequent fixture-regression run; candidate improvement — widen the gate's failure capture (scripts/sbx/fixture-regression.mjs is not gate-protected; authorize in an M4 packet, likely M4-005). Retired by: reproduction + diagnosis, or M4 close with 0 further occurrences and the capture widened. **NR-4 note:** M4-000A adds two data points — the implementer's battery caught three self-introduced test couplings (the FAD-15 gate again doing its job), and the independent review again produced the only MEDIUM defect (F-1, a DB capability gap invisible to the packet's own green run) plus an unreported scope crossing (F-5, FAD-30). The mandatory review stays load-bearing through M4 as planned.
- **Update 2026-08-05 (M3 close):** **NR-14 RETIRED** — the M3-008 redesign makes a plain battery run leave `git status --porcelain` empty (evidence writes go to untracked scratch; `--refresh` updates tracked artifacts intentionally; writers DISCOVERED from git ls-files, not hand-kept; red-cased by breaking the redirect). Proven independently by implementer, reviewer, and orchestrator; the restore-with-`git checkout --` standing discipline is retired. **NR-4 stays elevated with the milestone's richest data yet:** eight M3 tasks — REVISE ×5 (M3-001/002/003/004/008) and zero-blocking ×3 (M3-005/006/007, the milestone's last-issued UX packets); every blocking finding again came from reviewer-authored probes (a raw session secret in shipped evidence; a non-atomic CAS where 11/12 concurrent rounds both-won under a sequential-only proof; a SECURITY DEFINER prune that was a within-tenant DELETE primitive; D-15a's missing INSERT arm; a factually false not-evaluable reason in an owner-ruling document; a NUL byte binary-blinding the central file of the packet that had just fixed that exact class elsewhere). The zero-blocking trend on later packets suggests the disciplines transfer, but three of five earliest-issued packets carried blockers — the mandatory independent review remains load-bearing and stays through M4 (the highest-risk milestone: solver correctness). **E-2 fully closed** (preview + nested ports derived; the recurring 55455 seed fixed at M3-008 after recurring twice).
- **NR-11 (new, FD-6):** compromise of a connector-scoped enclave HMAC key re-identifies external-reference pseudonyms. L: low, I: high. Mitigation: key custody in the enclave's separate trust domain (E-14), rotation with dual-pseudonym window; raw references never stored. Retired by: key-custody verification in SBX-029 sweeps.
- **Internal verification (2026-08-01):** 29 findings dispositioned, corrections applied ([../../architecture/remediation/internal-verification-corrections.md](../../architecture/remediation/internal-verification-corrections.md)); corrections are design-only until harnesses run.
- **Update 2026-08-01 (M0 execution):** NR-9 largely retired — TDG-02/03 (in-process scope), TDG-01/14 (by scaffold wiring), TDG-11 (solver runtime) confirmed by executed evidence; **TDG-04 (graphile-worker leases) remains selected-unconfirmed** — its confirmation micro-spike is folded into proposed OPUS-M1-003. **NR-12 (new, EV-M0-SPC):** deterministic-portfolio cost at production scale unknown (instance-dependent sign measured on toys). L: med, I: med. Retired by: E1 corpus measurement before defaults freeze.
- **Update 2026-08-03 (M1 close):** **NR-9 retired** — TDG-04 confirmed (FAD-14), no selected technology remains unconfirmed. **NR-4 stays elevated with much stronger data:** across four M1 tasks the independent second review returned REVISE on three of them, and every blocking finding came from a reviewer writing its own database probes rather than reading tests — an arbitrary-row entitlement read that 404'd a whole organization, an audit chain reporting "intact" after a tail truncation, an outbox double-delivering in two windows, a flagship isolation test satisfiable by the defect it existed to catch. Self-review did not substitute: two implementers ran their own adversarial passes and the independent reviewer still found blocking defects afterward. **A further class the reviews did not catch** — a proof passing in-suite and failing 5/5 standalone — was caught only by orchestrator rerun, and is now a standing runbook requirement. Mitigation holding; cost is real (three revision rounds, two delta re-reviews). **NR-13 (new):** shared-fixture coupling — 551 tests across 36 files share one cluster and one fixture; three order/state couplings surfaced and were point-fixed in M1-004 alone. L: high, I: low-med (test-suite reliability, false signals). Retired by: costing per-test transactional rollback or a dedicated organization for mutating suites at M2 entry.
- **Update 2026-08-04 (M2 close):** **NR-13 RETIRED** — the FAD-15 strategy (as corrected) is implemented and enforced: enforced-immutable baseline, per-file owned tenants (slug registry + per-run nonce), declared non-tenant preconditions, per-test subject ownership, and the fixture-regression acceptance gate (twelve fixed seeds + rotating, both shuffles, full standalone sweep, Layer-1 control — 63/63 on main at close). The gate caught real couplings four separate times during M2 itself (coupling seven at M2-001 acceptance; M2-002's 12/47 and its S-04 control; M2-003's T-05; M2-004's FK proof) — retirement rests on the mechanism catching the class, not on the class being extinct. **NR-4 stays elevated, richer data:** all four M2 tasks drew independent-review findings (REVISE ×2, APPROVE-WITH-FOLLOW-UPS ×2); blockers again came from reviewer-authored probes (a vacuous cross-group proof arm passing a CAR-001-class mutation; a vacuity detector laundering any throw; a refusal cascade writing false causes into shipped evidence; an absence-based probe reproducing the vacuity shape inside the detector built to kill it); three implementer self-catches and one self-corrected falsified premise show self-review improving but still insufficient alone. **E-1/E-2 (shared default ports across concurrent worktrees, found M2-003) CLOSED by M2-004** (derived ports, proven by simultaneous batteries). New watch item **NR-14:** evidence artifacts regenerated by battery runs (check/sbx rewrite tracked files; RUN-SCOPED counters differ by process shape) — managed by standing discipline, candidate for generated-artifact redesign at M3 entry. L: low, I: low.
- **Watch-list reconciliation (M0 close):** NR-4 (sub-agent quality variance) now has data — one REVISE round on OPUS-M0-001 in which the independent second review caught 3 merge-blocking defects the first pass missed; the gate worked. NR-4 stays elevated through M1 (first production-code milestone). NR-1 (context loss) exercised in practice: one session-limit interruption recovered from control docs + worktree state with zero loss.
