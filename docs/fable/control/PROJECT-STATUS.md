# PROJECT-STATUS

**As of 2026-08-25 — THE REPLACEMENT-REVIEW GATE PASSES ([39](../39-replacement-gate-decision.md)); M3R CLOSED by substitution; the prototype-enablement checkpoint and M5 are next.**

- **The doc 38 internal review is complete end to end.** Three fresh-Opus reviewers (REV-A · REV-B blind · REV-C consolidation) filed; all 28 consolidated findings ACCEPTED at filed severity (FAD-53); thirteen serial repair packets R-1..R-13 landed, each with a fresh implementer, an independent fresh reviewer, and delta verification by the finding's original reviewer or a recorded surrogate. Codex review: NOT PERFORMED · Codex-specific requirement: SUPERSEDED BY OWNER DECISION · replacement gate: FABLE-PLANNED, MULTI-OPUS-EXECUTED INTERNAL REVIEW. This was an internal review, not an external one.
- **The §7 validation battery passed in full** (EV-DOC38-GATE): validators 36/36 · 95/95 · research PASS; `pnpm check` 17/17; sbx 9/9 with exact recorded figures; migration schema cycle 0001–0020 + six populated cycles; the 67-arm red-case battery in §7's primary sharded-CI form (three complete green executions, union guard); fixture-regression **14/14** (13 fixed seeds + rotating) on the repaired tip; real-stack critical-path e2e 2/2 at both viewports, 14 steps, 12/12 axe sites. Fresh-clone validation of `origin/main` green (install · validators · 17/17). **CI on `main` is fully green for the first time in the repository's history** (run 32798497944, 15/15 jobs, attempt 1).
- **Two functional test-suite defects the battery itself found were repaired without weakening** (R-12 queue-FIFO deadline; R-13 storm ceiling — which the full re-run then proved had a second latent failing ordering). New registered risks: NR-22 (a vitest seed pins the shuffle only up to collection order) and NR-23 (a run-wide position-correlated per-round-trip slowdown, cause UNRESOLVED, correctness intact). REV-C's final ACCEPT was issued by a recorded continuation surrogate after nine in-tree spot-checks.
- **PR #6 MERGED at `be7399b` with all 15 checks green.** Post-gate follow-ups moved to the durable numbered register [40](../40-post-gate-follow-ups.md) (FU-01..FU-17; FU-01 is same-milestone). No compliance or readiness claim is made or implied; owner-reserved actions remain owner-reserved.

**As of 2026-08-22 — GitHub is now the authoritative durable home ([github.com/DeltaNumeric72/SchedulePoint](https://github.com/DeltaNumeric72/SchedulePoint), default branch `main`), reconciled under the continuous GitHub master authorization. Post-M4 independent Codex review: EVIDENCE_BLOCKED (owner action [#2](https://github.com/DeltaNumeric72/SchedulePoint/issues/2)). M3R remains open pending it; prototype checkpoint and M5 NOT begun.**

- **GitHub reconciliation (run `RUN-20260822-GHRECON-01`, continuation record [AUTO-RUN-STATE](AUTO-RUN-STATE.md)):** origin verified exact; `origin/main` = `1593b06` (the M4 CLOSE commit `cc9f3f9` plus the owner-directed CLAUDE.md delegation record — both legitimately accepted). All four milestone checkpoints verified against full history (exist · ancestors of `origin/main` · match their exit-report checkpoints): M1 `b051193` · M2 `e476573` · M3 `7b579f2` · M4 `cc9f3f9`. **GitHub holds zero tags, and this session's credential cannot push tags (HTTP 403, branch-scoped; no tag/release tooling either) — publication is owner action [#1](https://github.com/DeltaNumeric72/SchedulePoint/issues/1).** Until the tags land, the milestones are verified-but-not-durably-frozen on GitHub. Single-orchestrator check clean (no other branches/PRs/issues/leases/runs before this run's writes). CI (`ci.yml`): two build-failing jobs on every PR and push to main, no `continue-on-error` — cannot silently skip; branch-protection settings unreadable with session tooling, recorded unverified.
- **The independent post-M4 Codex review (doc 37) is outstanding — prompt filed at M4 close, no report, no disposition. Marked EVIDENCE_BLOCKED:** no authorized independent Codex runner is available to this session, and a separate Opus reviewer does not satisfy the requirement. The exact verbatim instructions are dispatched to the owner as issue [#2](https://github.com/DeltaNumeric72/SchedulePoint/issues/2), pinned to `cc9f3f9`. **Until the review is received and dispositioned: M3R is not closed, the prototype-enablement checkpoint is not begun, M5 is not begun** — only work that cannot prejudice the review proceeds (reconciliation records, owner-action and external-evidence packages). This restates, unchanged, the M4-exit condition; the 2026-08-22 authorization unpauses M3R **after** the review's findings are available, not before.
- No implementation packet is in flight; no schema, code, or evidence claim changed in this entry. No production-readiness or compliance claim.

**As of 2026-08-21 — MILESTONE M4 COMPLETE. OPUS-M4-005 ACCEPTED & MERGED (`480105b`); exit report [../36-m4-exit-report.md](../36-m4-exit-report.md); main tagged `milestone/M4`. M5 NOT authorized; the prototype-enablement lane NOT begun; M3R remains PAUSED.**

- **OPUS-M4-005 ACCEPTED & MERGED 2026-08-21 (squash `480105b`; evidence EV-M4-005; migration 0019).** The full integration proof: the real-stack 14-step critical path (both viewports, no interception, zero-skip guarded, every step outcome-asserted), the 40-proof concurrency/recovery matrix, SBX-015/016/017 (SBX-016 at 22/22 + eight reviewer injections 8/8), D-4b + the queue binding with S-16t, absolute class-level staleness, and the hardening register discharged (NR-15 diagnosed/repaired/verified; NR-16; NR-18 gate baseline now EMPTY; NR-19 with a load-bearing proof; runner ERRORED + spawn signatures). The milestone's two final undiagnosed failures shared one measured cause — a wall clock racing the pinned deterministic budget — repaired test-side with the precondition asserted, and product-side as the FAD-49/50 derived result-reproducibility verdict (`reproducible` only for a completed termination; interrupted/wall-truncated/unrecorded distinct and rendered honestly). Four independent-review rounds ending **ACCEPT** (probes `review/m4-005` @ `bdd78dc`).
- **Close battery (EV-M4-005 §24):** validators 95/95 · 36/36 · research validator PASS from a genuinely fresh clone; check **17/17** (unit 2153); red-cases **64/64**; fixture-regression **153/153**; SBX **9/9** (371 readings, 53/53 tables, 0 wrong-tenant); migration cycle **0001–0019** clean by name; real-stack **2/2**. Post-merge main: byte-identical on every branch-touched path; check 17/17 re-proven.
- **Parity at exit: 18 verified · 3 in-progress · 37 not-started; no capability dropped.** CAP-015/016/017/059 verified; CAP-018 (partial-schedule circulation) in-progress via SBX-017's circulation half. FADs 48–51 recorded. Standing conditions: solver image build/digest + Python 3.12 rerun (CI); benchmark bands M6 (PO-DEC-23); external architecture re-review still blocks beta (V-04); G-ARCH/G-BETA/G-PROD open. **No production-readiness or compliance claim. Work stopped before M5.**

**As of 2026-08-18 (M4 in progress — M4-004 ACCEPTED & MERGED at `79e78b6`; E2 is real; ONE packet remains: M4-005 integration/recovery/close).**

- **OPUS-M4-004 ACCEPTED & MERGED 2026-08-18 (squash `79e78b6`; evidence EV-M4-004; no migration).** E2 on the E1 engine: `OBJECTIVE_SCALE=10000` single-sourced across the TS/Python boundary with a SHA-256 profile digest refused on any one-sided mutation (and E1's silent truncation of sub-unit weights closed); the recorded tiered objective `e2-default-v1` (weighted sum, no lexicographic claim — E2-R1 ratified as a SEMANTIC ratio change from E1); T2 minimisation under hard caps with `EXPLAINED_MINIMAL` earned only within budget and independently downgraded; SPEC-04 §4 statistics and reproducibility record actually persisted; §7 metrics with per-cell `demand_satisfaction_rate` AND an independent demand check (the D-4 lesson applied — the checker no longer rests on the model posting demand as hard); PO-DEC-13's four-class taxonomy; fairness dispersion from snapshot-carried history; the progressive-pin reader; opt-in deterministic mode with its cost MEASURED and disclosed on the surface (≈1× startup-dominated / ≈10× on the searching class, reviewer-reproduced; no band anywhere — PO-DEC-23 resolved-not-banded until M6); build-produced drafts start creditless (pinned both ways); comparability enforced (same snapshot + same weights) with refusals rendered as refusals. Review REVISE → repair → delta ACCEPT WITH CONDITIONS → micro-repair; the engine core was unfalsifiable under four one-sided scale mutations, an independent T2 minimality re-verification, S-08t bit-identity, and a reproduced cost measurement. **The FAD-45 raw-NUL gate caught the class's fourth occurrence in this packet's own new file, on the first check that ran against new code** — the mandate paid for itself immediately. Battery on byte-identical post-merge main: 17/17 gates (2042 tests + 14 opt-in skipped), 59/59 red cases, 146/146 fixture-regression (seed 900437), SBX 371 readings / 53 of 53 / 0 wrong-tenant, migrations 0001–0018 clean. FAD-46/47 recorded. **Process note:** the finishing implementer was terminated by a usage limit while waiting on the last battery step; because it had launched detached with the marker inside the redirected block, the run completed on its own and nothing was lost — the continuity design working as intended.
- **The automated-scheduling pipeline is now functionally complete on main: canonical snapshot → CP-SAT solve with E2 objective → independent validation → fenced 16-state lifecycle → candidate comparison/quality/explanations → selection into an M3 draft.** What remains is M4-005: the real 14-step end-to-end workflow proof against the live stack, the full concurrency/recovery matrix, SBX-015/016/017, D-4b + the queue binding, the accumulated hardening items (NR-15 capture-widening, NR-16 scanner, NR-18 NUL baseline repair, NR-19 `reclaimStalePools`, red-case runner dist/no-test-files hardening, phantom full-path citations, screenshot-currency sweep, solverStatus on list/comparison), current-state documentation, doc 36, the control sweep, the Codex review prompt, and — only if every exit condition proves — `milestone/M4`. M3R remains PAUSED; no production-readiness claim.

**As of 2026-08-16 (M4 in progress — M4-003 ACCEPTED & MERGED at `8deb4c5`; the build lifecycle and scheduler experience are real; NEXT: M4-004 packet expansion).**

- **OPUS-M4-003 ACCEPTED & MERGED 2026-08-16 (squash `8deb4c5`; evidence EV-M4-003; migration 0018).** The complete 16-state server-authoritative build lifecycle (report 21 §4's states under SPEC-04's D-4a..d/cancellation/timeout supremacy, both departures ratified): six RLS'd tenant tables with transition/fencing/no-delete/append-only guards; epoch-fenced worker claims with durably-recorded stale-result refusals; the reaper; validation-gated usability (no candidate usable before M4-002's independent checker passes it; `approved` requires zero unresolved hard findings at the database too); selection into a NEW M3 draft through one write path (the FAD-44 `origin` grant — solver rows carry `'solver'`); six ratified `build.*` audit names; the scheduler UI with the full state matrix incl. retry, at both viewports, axe 412, budgets measured-not-assumed. **The raw-NUL gate is now CI gate 17** (mandated by FAD-45 on the class's third recurrence; strict path+count baseline carrying NR-18's three doc instances). Review ACCEPT WITH CONDITIONS → condition round → delta ACCEPT → micro-repair; the reviewer's 193-illegal-edge sweep, forged results, and hand-built races all held; six+ implementer self-caught defects recorded (the FAD-15/red-arm disciplines repeatedly doing their job). Battery on byte-identical post-merge main: 17/17 gates (1967), 53/53 red cases, 143/143 fixture-regression, SBX 371 readings / 53 of 53 / 0 wrong-tenant, migrations 0001–0018 clean. FADs 44/45 recorded.
- **Remaining to milestone close: M4-004 (E2 optimization/quality/explanations — also owns the credits-into-draft ruling and the `protected_assignment_identities` reader) and M4-005 (integration/recovery/close — owns D-4b, the queue binding, NR-15/16/18-hardening, SBX-015/016/017, the exit report).** M3R remains PAUSED; no production-readiness claim.

**As of 2026-08-15 (M4 in progress — M4-002 ACCEPTED & MERGED at `5e0ac4e`; the E1 feasibility engine is real; NEXT: M4-003 packet expansion).**

- **OPUS-M4-002 ACCEPTED & MERGED 2026-08-15 (squash `5e0ac4e`; evidence EV-M4-002)** — the milestone's largest packet (89 files, ~77k insertions). The E1 feasibility engine is REAL: every M4-evaluable HARD rule is a CP-SAT constraint in the credential-less worker AND an independently-implemented check in the domain checker (6→22 kinds; zero shared evaluation code — a translation bug in one cannot hide behind a matching bug in the other, proven by three red arms plus the reviewer's own uncovered-family mutation); all eleven RK-RULINGs resolved and pinned both directions (registry: 22/8/0 open, every stable id retained with its resolution); canonical snapshot v2 (FAD-38/39, additive, covering-vocabulary refusals); the pure candidate validator rejects incorrect worker output in four classes plus the eligibility-gap check the race-produced corpus fixture forced; the deterministic 15-class E1 corpus reproduced hash-for-hash by the reviewer in a fresh worktree; honest statuses end-to-end (OPTIMAL/FEASIBLE/INFEASIBLE/T0-T1 explanation states; S-08t bit-identical under the full pinned set). Review REVISE → repair → **delta ACCEPT, zero new findings**. Battery on byte-identical post-merge main: 16/16 gates (1899 tests, incl. the new Python↔TS parity gate), 48/48 red cases, 137/137 fixture-regression, SBX 47/47 0-wrong-tenant, migrations 0001–0017 clean. FAD-38..43 recorded; NR-18 (doc NULs) and NR-19 (`reclaimStalePools` release-vs-lock orphan) registered; the C-2 drain-starvation investigation closed with BOTH pre-existing test-hygiene defects fixed (outbox-dispatch's 20 orphan rows; periodic's dead-pool-locked checkpoint job).
- **Limitations stand:** E1 only — objective tiers exist but tuning/weights/quality/T2+ explanations are M4-004; build lifecycle and scheduler UI are M4-003; local evidence on CPython 3.9.6 (3.12/image rerun = standing CI condition); no benchmark bands (PO-DEC-23 pending); M3R remains PAUSED; no production-readiness claim. **Next: Fable expands OPUS-M4-003 (build lifecycle + scheduler experience, incl. FAD-33(6)'s `setRuleState` removal and R-8's pinned-row note) into the binding packet, commits it, then issues it alone.**

**As of 2026-08-13 latest (M4 in progress — M4-001S ACCEPTED & MERGED at `d910af6`; NR-17 retired; corrective-packet chain complete; NEXT: Stage 2, the M4-002 packet expansion).**

- **OPUS-M4-001S ACCEPTED & MERGED 2026-08-13 (squash `d910af6`; evidence EV-M4-001S; migration 0017).** The `requires_expiry` flip-vs-grant interleaving is refused at the database in BOTH orders (flip guard `FOR UPDATE` on `false→true`; holding guard `FOR KEY SHARE` inside its lookup; each half proven load-bearing by one-at-a-time mutation), the retirement race deliberately preserved (FAD-28/35 measured intact), the lock-ordering rule vs the audit advisory lock documented and hunted clean, and the solver-snapshot overclaim corrected. Independent review **ACCEPT, zero blocking**; the reviewer's forbidden-state hunt pinned the surface narrower than assumed (FAD-37(4)). **NR-17 RETIRED.** Orchestrator battery on byte-identical post-merge main: 15/15 gates (1801 tests), red-cases **44/44** on the authoritative calm-machine run (three transient-failed runs retained and diagnosed — a NEW "No test files found" runner signature assigned to M4-005, the axe preview class, and a machine-load storm from a wedged `mediaanalysisd`), fixture-regression **129/129** (seed 368944), SBX 6/6 47/47 0-wrong-tenant, **migrations 0001–0017 cycle clean**. FAD-37 records the acceptance rulings; the orchestrator's own chained-battery process defect at this acceptance is recorded honestly (EV-M4-001S §0) with a new runbook standing note.
- **The M4-001 recovery arc is complete: M4-001 closeout → M4-001R (FAD-28 contract truth + permanent race proof) → M4-001S (the one genuinely unbacked race closed at write time).** Every stated concurrency contract in the staffing-input surface now matches measured behaviour. **Next: Stage 2 — Fable expands OPUS-M4-002 (rule semantics + E1 feasibility) into the complete binding packet in doc 35, commits it, then issues it alone.** M4-002 carry-forwards already recorded for its packet: the independent validator consumes the shared verdict; the E1 corpus includes a race-produced retired-holding fixture; no new lock orderings against qualification rows (FAD-37(2)); V-2's staffing-set advisory lock noted.

**As of 2026-08-13 later (M4 in progress — M4-001R ACCEPTED & MERGED at `efa1ffd`; OPUS-M4-001S finalized, next to issue; then M4-002 planning).**

- **OPUS-M4-001R ACCEPTED & MERGED 2026-08-13 (squash `efa1ffd`; evidence EV-M4-001R).** The FAD-28 contract statement is now correct everywhere (comments-only docblock fix, proven by comment-stripped byte-identity), the FAD-33 citation fixed, and the P3e granted-while-retiring interleaving is a permanent, falsifiable repo test: race reproduced deterministically via the 0003 audit advisory lock (causal, fail-loud), holding admitted, and INERT on all three consumers — manual read, publication gate, canonical solver input — wired into the `retired-verdict` red case both arms. Independent review **ACCEPT, zero blocking** (probes retained `review/m4-001r` @ `9fc280d`); the reviewer proved the rebuild load-bearing by omission and could not falsify inertness through any path incl. the enforcement read plane. Orchestrator acceptance battery on post-merge main (tree byte-identical to the reviewed branch): 15/15 gates (1796 tests), 43/43 red cases, 127/127 fixture-regression (seed 249048), SBX 6/6 47/47 tables 0 wrong-tenant, migrations 0001–0016 cycle clean.
- **Review finding R-1 adjudicated (FAD-36, NR-17): the `requires_expiry` flip-vs-grant interleaving is admitted with NO read-side backstop** — the race-produced open-ended holding would read `satisfied` indefinitely, including in the canonical solver input, and `solver-snapshot.ts:154` overclaims write-time enforcement. **OPUS-M4-001S finalized (doc 35 §6c): migration 0017 write-time serialization, both orders refused, deadlock analysis vs the audit advisory lock, mutation-cased; mandatory independent review. Issues alone; M4-002 is NOT planned until M4-001S closes.**

**As of 2026-08-13 (M4 in progress — M4-001 ACCEPTED & MERGED; Stage-1 recovery closeout complete; OPUS-M4-001R finalized, next to issue; then M4-002 planning).**

- **OPUS-M4-001 ACCEPTED & MERGED 2026-08-12 (squash `b1aa64e`; evidence EV-M4-BOUNDARY) — control closeout completed 2026-08-13 during Stage-1 recovery** (the acceptance merge landed without its control-plane sweep; recorded honestly, repaired now). The solver runtime boundary and canonical input: separately packaged Python worker (stub solver first, by design), NO database credential (egress-guarded, `_socket` incl.), mutual **raw-byte framed-wire HMAC** RPC (review blocking finding B-1: the original MAC covered a re-derivation, ASCII-identical/non-ASCII-divergent — repaired so neither side re-serialises to verify), one solve per subprocess with watcher-thread StopSearch cancellation and honest `killed` vs `crashed` vs `CANCELLED` attribution (C-2), immutable `solver_input_snapshots` (migration 0016, deterministic hash, no runtime UPDATE/DELETE), provider dispatch proven outside every transaction, every input-refusal class typed and tested. Review: **REJECT → repair → delta-ACCEPT** by the original reviewer (probes retained in git, `review/m4-001` branches). Authoritative battery EV-M4-BOUNDARY §2b: 15/15 gates (1792 tests/139 files) · 43/43 red cases · 126/126 fixture-regression · SBX 329 readings / **47 of 47 tables** / 0 wrong-tenant. **Limitations stand:** stub solver — no OR-Tools behaviour implemented or verified, no benchmark; `Dockerfile.solver` authored, image NOT built locally (build + digest = standing CI condition); local evidence on CPython 3.9.6 (Python 3.12 image rerun = CI condition); tz-rule divergence detectable but old interpretations not re-derivable; M3R remains PAUSED; no production-readiness claim.
- **Stage-1 recovery verification 2026-08-13 (main `b1aa64e`, serial, inner exit codes):** validators 95/95 · 36/36 · PASS; check first run 14/15 (ONE contention-class 5000ms timeout in the R-B4a exhaustive enumeration — diagnosed via 47/47 standalone in 3.7s, retained FAILED capture) → serial re-run **15/15, 1792/1792**; red-cases **43/43**; fixture-regression **126/126** (fresh seed 367857; NR-15 zero reproductions again); SBX **6/6, 47/47 tables, 0 wrong-tenant**; migrations **0001–0016 cycle clean**. Record: EV-M4-BOUNDARY §10. M4-001 task-branch tree verified byte-identical to main; no stranded M4-002 work; retained worktrees clean.
- **FAD-28 grant-vs-retirement reconciliation (FAD-35):** the fail-closed/read-time contract is authoritative (the review's P3e probe measured the admitted interleaving; the C3 evidence correction stands). ONE stale contrary claim survives in `grantHolding`'s docblock; the reviewer's interleaving probe was never retained as a runnable test. **OPUS-M4-001R finalized (doc 35 §6b): docblock correction + permanent falsifiable inertness proof across manual/publication/solver-input consumers + the FAD-33 citation fix in `run.mjs`. Issues alone, before M4-002.** M4-002 is NOT planned until M4-001R closes.

**As of 2026-08-12 (M4 in progress — M4-000 COMPLETE, all eight register entries closed; M4-001 issued).**

- **M4-000 COMPLETE 2026-08-12.** OPUS-M4-000B ACCEPTED & MERGED (`a614f7e`, EV-M4-INPUT-B, migration 0014) and OPUS-M4-000C ACCEPTED & MERGED as second merger (`323d576`, EV-M4-INPUT-C, migration 0015). Both reviews ACCEPT WITH CONDITIONS; every condition closed and delta-verified by the original reviewer with executed probes (000B: R-B4a joint gap translation closing 399 enumerated degenerate intervals + the 409 stale-basis wire fix, both mutation-tested; 000C: the nine-class material-input digest matrix with shrink-guard pin, 9/9 biting). The second-merger composed battery caught **8 integration failures neither packet's own battery could see** (one positional migration-cycle assumption cascading into four; two 000C fixtures refused by 000B's new FKs — the invariants working; one genuine precondition-ordering interaction, resolved without touching either packet's contracts). Doc 34 §4a records the ten M4-000 exit conditions each matched to executed evidence. FADs 31/32/33 recorded (incl. the decorative-arm standing rule and the NR-16 scanner gap). **Main after the final merge: 1700 tests / 129 files, 15/15 gates (two new: rule-kind registry, provider-boundary), 38/38 red cases, fixture-regression 116/116, SBX 6/6 with 46/46 tenant tables 0 wrong-tenant, migrations 0001..0015 cycle clean.** No capability dropped; parity unchanged (prerequisite work under tracked capabilities). NR-15: zero reproductions across all M4-000 batteries (≈90+ additional whole-suite shuffled runs); stays open, mechanism unknown. **OPUS-M4-001 (solver runtime boundary + canonical input) finalized in doc 35 §6a and ISSUED — the M4-000 gate held: no solver packet before input readiness was proven.**

**As of 2026-08-10 (M4 in progress — M4-000A accepted; 000B/000C next).**

- **OPUS-M4-000A ACCEPTED & MERGED 2026-08-10 (94af3c5; evidence EV-M4-INPUT-A).** Staffing-input integrity: atomic whole-set demand/requirement aggregates under DB-owned expectedVersion CAS (delete-absent canonical rule; union proven unreachable including reviewer-authored interleavings the packet's own 12-round races did not cover); `requires_expiry` enforced service + DB with the flip-guard closing the two-statement bypass; retired-qualification behaviour ruled and enforced (FAD-28 R1–R5); future-effective work-profile correction/cancellation path (audited, in-force rows untouchable, DB capability gate added by migration 0013 after review finding F-1); the **shared eligibility verdict** (`packages/domain/src/eligibility`, outcome set `satisfied|missing|expired|future|revoked|retired`) consumed by manual scheduling and publication, FROZEN for 000B/000C, awaiting solver consumers in M4-001/002; shift-type qualification requirements enforced without duplicate authored rules; enforcement read plane ratified with recorded residuals (FAD-29). Independent review: **ACCEPT WITH CONDITIONS — all four conditions closed and re-proven** (26/26 red cases incl. the new capability arm; clean end-to-end battery). Implementation initially failed fixture-regression (three self-introduced order couplings — fixed as stricter tests, failed runs retained in evidence); one **UNRESOLVED intermittent** recorded as NR-15 (pre-existing test, packet bounded out as cause, mechanism unknown — not called a flake, not called explained). Scope-crossing process defect recorded and ruled (FAD-30). Main after merge: **1448 tests / 115 files, 13/13 gates, 26/26 red cases, fixture-regression 107/107 (rotating seed 849638), SBX 6/6 falsifiable 0 vacuous with the sweep floor raised 44→45 (`staffing_set_versions` observed non-vacuously), migrations 0001..0013 cycle clean.** Parity unchanged by design — 000A is prerequisite/integrity work under already-tracked capabilities; no capability dropped. FADs 28–30 recorded. **Next: post-merge battery on main, then issue OPUS-M4-000B (schedule graph/locations/time, migration 0014) ∥ OPUS-M4-000C (rule revisioning/publication handoff/provider gate, migration 0015) in parallel worktrees with disjoint globs, serialized merges.**

**As of 2026-08-07 (M4 AUTHORIZED and in progress).**

- **M4 AUTHORIZED by the owner 2026-08-07** against frozen M3 checkpoint `7b579f2` (tag `milestone/M3`). Entry verification PASSED with exit codes checked (validators 95/95 · 36/36 · PASS; check 13/13; red-cases 21/21 on the clean serial re-run — a first run's 3 preview-port contention failures diagnosed and recorded; SBX 6/6 falsifiable, 44-table sweep 0 wrong-tenant; fixture-regression 102/102 with rotating seed 417227). Entry record and prerequisite register: [../34-m4-entry-and-prerequisite-register.md](../34-m4-entry-and-prerequisite-register.md); packets: [../35-m4-task-packets.md](../35-m4-task-packets.md). **M3R is PAUSED by owner direction — not completed, not cancelled, not superseded, not absorbed into M4; its outstanding findings remain in the project register with their existing production gates.** M4 scope: automated scheduling + the functional prerequisites the solver pipeline directly consumes (M4-000 register); requests/vacation/notifications/marketplace/picklist/reports/documents/connectors and all M5+ scope NOT begun. Mandatory sequence M4-000 → 005; M4-000 split into 000A/000B/000C. No production-readiness claim follows from M4. **Work stops before M5.**

- **Milestone M3 COMPLETE — 8/8 tasks ACCEPTED and merged** (authorized 2026-08-04 against `e476573`): 001 authn (6a01196) · 002 rules (7dd37dc) · 003 publication core (e275597) · 007 settings/site (c3e1baa) · 004 authoring UX (20c0c13) · 005 publication UX (112b8c4) · 006 staff views (57685ab) · 008 integration (dfa717f). Exit report: [../33-m3-exit-report.md](../33-m3-exit-report.md). Frozen M3 checkpoint = the exit report's commit, tagged `milestone/M3`.
- On main at close: **1384 tests / 109 files, 13/13 gates, 21/21 red cases, fixture-regression 102/102, SBX 6/6 with the 44-table sweep at 0 wrong-tenant, migrations 0001..0011 cycle clean, NR-14 zero-modification tree.** Parity: 14 verified · 3 in-progress · 41 not-started; none dropped. FADs 18–27 recorded. V-01..V-19 green; SBX-018 filed; QA-SCH 9 PASS / 6 PARTIAL (owned) / 1 N-A; SPEC-14 A-cells executed, 10 M-cells honestly unclaimed (EV-8 → M6).
- **NR-14 retired** (the redesign proves itself); NR-4 stays elevated through M4; E-2 fully closed.
- **M4 NOT authorized — awaiting the owner.** Proposed M4 packets and the exact authorization prompt are in the exit report. Manual scheduling remains override/recovery/fixed-input/development-stage only; M4 automated scheduling remains mandatory for production.

- **M3 AUTHORIZED by the owner 2026-08-04** against frozen M2 checkpoint `e476573` (tag `milestone/M2`). Entry verification PASSED (checkpoint/tag/tree/branches confirmed; validators 36/36 · 95/95 · PASS; gates 12/12; red cases all proven — exit codes checked; record in [../32-m3-task-packets.md](../32-m3-task-packets.md) §1). Packets finalized: OPUS-M3-001 (authn/sessions/invitation — issued alone first), OPUS-M3-002 (rules/AST/corpus), OPUS-M3-003 (periods/publication SPEC-05 core); further families M3-004..008 pre-declared in §8 and finalized before their own issuance. All M2 carried items dispositioned with owners in §2. **M4 NOT authorized — M3 ends with a frozen checkpoint and exit report; work stops there.**
- Sequencing: M3-001 first and alone; 002/003 issue only after M3-001 acceptance and the §7 revalidation note; serialized merges; OPUS-M3-008 integration packet mandatory.
- **Wave 2 complete 2026-08-05: OPUS-M3-005 (112b8c4) and OPUS-M3-006 (57685ab) ACCEPTED & MERGED** — both reviews zero-blocking. FAD-25/26 recorded. Main: 1305 tests, 13/13, red 20/20, fixreg 96/96, sweep 44/44, cycle 0001..0010. Parity 14/3/41. **Seven of eight packets done. Next: OPUS-M3-008 integration & hardening, alone — then the M3 exit sequence. M4 NOT authorized.**
- **Wave 1 complete 2026-08-05: OPUS-M3-007 (c3e1baa) and OPUS-M3-004 (20c0c13) ACCEPTED & MERGED.** FAD-23 (eligibility three-state) and FAD-24 (settings authz/audit vocabulary) recorded. Main: 1245 tests, 13/13 gates, 18/18 red, fixreg 91/91, sweep 44/44 0 wrong-tenant, cycle 0001..0010. Parity 13 verified / 3 in-progress / 42 not-started. **Next: issue wave 2 — OPUS-M3-005 (publication UX) ∥ OPUS-M3-006 (staff views) per doc 32 §10d/§10e — then OPUS-M3-008 alone, then the M3 exit sequence. M4 NOT authorized.**
- **OPUS-M3-002 and OPUS-M3-003 ACCEPTED & MERGED 2026-08-04** (7dd37dc rules · e275597 publication; evidence EV-M3-RULES + EV-M3-PUBLICATION). FAD-21 (typed-rules table shape, ruled on the implementer's mandatory pre-migration stop) and FAD-22 (SPEC-05 strengthened from the M3-003 review: D-15a INSERT coverage, required CAS, one idempotency-key domain) recorded. Both REVISE rounds closed with reviewer-probe red cases; serialized merge with disclosed pure composition; main at close of the first three packets: **1060 tests, 13/13 gates, 16/16 red cases, fixture-regression 82/82, SBX sweep 44-table floor 0 wrong-tenant, migrations 0001..0009 cycle clean.** Parity 11 verified / 4 in-progress / 43 not-started. **Next: finalize §8 packets in doc 32 (M3-004 authoring UX incl. the 27 remaining rule editors · M3-005 publication UX · M3-006 staff views · M3-007 settings+site · M3-008 integration/hardening — mandatory) and issue per their dependencies. M4 NOT authorized.**
- **OPUS-M3-001 ACCEPTED & MERGED 2026-08-04** (6a01196; evidence EV-M3-AUTHN). Authentication/sessions/invitation/activation on main. Second review REVISE (B-1 secret-in-evidence, O-1 SBX-001-not-real-sessions) → revision closed both + N-1/N-3 → orchestrator delta re-verified green on a fresh checkout: 12/12, 965 tests, 14/14 red, fixture-regression 75/75, SBX 5/5 with **0 EVIDENCE_BLOCKED(authn)**, sweep 30 tenant tables +users / 210 readings / 0 wrong-tenant, migration cycle clean; SPEC-11 X-12 filed; SBX-005/006 executed in full. FAD-18/19/20 ratified. **Next: §7 revalidation note in doc 32, then issue OPUS-M3-002 (rules) and OPUS-M3-003 (publication) in parallel.**

- **Milestone M2 COMPLETE — 4/4 tasks ACCEPTED and merged** (authorized 2026-08-03 against b051193): OPUS-M2-001 SBX harness + FAD-15 fixture isolation + MULTI (bc61ee7) · OPUS-M2-002 catalogue, first UI slice (ff2e67b) · OPUS-M2-003 effective-dated profiles/qualifications (567964c) · OPUS-M2-004 integration + hardening (fce095c). Exit report: [../31-m2-exit-report.md](../31-m2-exit-report.md). Frozen M2 checkpoint = the exit report's commit, tagged `milestone/M2`.
- On main at close: **881 tests / 66 files, 12/12 gates, 14/14 red cases, fixture-regression 63/63 (twelve fixed seeds + rotating), sbx 26/26 tables 0 wrong-tenant, six migrations, cycle clean.** Parity: 8 verified · 2 in-progress · 48 not-started.
- **NR-13 retired** (FAD-15 implemented + the gate catching the class four times in M2 itself); NR-4 stays elevated; NR-14 (regenerated evidence artifacts) opened low.
- Proposed M3 packets and the authorization prompt were in the exit report; the owner authorized M3 on 2026-08-04 (header block above).

- **M2 AUTHORIZED by the owner 2026-08-03** against frozen M1 checkpoint b051193. Packets finalized: [../30-m2-task-packets.md](../30-m2-task-packets.md) (validators green at authoring: plan 36/36, arch 95/95, research PASS).
- **OPUS-M2-001 ACCEPTED & MERGED 2026-08-03** (bc61ee7): FAD-15 fixture isolation implemented (seven couplings closed), fixture-regression acceptance gate 39/39, MULTI factory + canonical fixture, SPEC-16 SBX harness — SBX-001/002/004 PASS all falsifiable, SBX-005 partial, SBX-006 EVIDENCE_BLOCKED(authn). NR-13 structurally addressed (formal retirement at M2 exit risk review). G-ARCH tenancy subset executed and filed; G-ARCH itself remains open by design. Evidence EV-M2-NR13 + EV-M2-SBX. 585 tests, 12 gates, 14 red cases on main.
- **OPUS-M2-002 and OPUS-M2-003 ISSUED 2026-08-03 in parallel** after packet revalidation against the actual M2-001 outcome (30-m2-task-packets §7) and the FAD-16 doc-06 catalogue amendment. Branches `opus/m2-002-catalogue` (worktree `.worktrees/m2-002`) and `opus/m2-003-profiles` (worktree `.worktrees/m2-003`); disjoint globs re-verified; `shift_type_qualifications` reserved for the integration packet. Independent second review mandatory for both.
- (Historical, M2-era: M3 was not authorized until 2026-08-04; M2 ended at its frozen checkpoint as required.)
- Next action: issue OPUS-M3-001 → delegate → orchestrator rerun → independent second review → merge → §7 revalidation → issue 002/003 in parallel.

- Phase: **implementation — Milestone M0 complete and ACCEPTED** (planning ratified and frozen at 2b64a7b; M0 authorized 2026-08-01 and executed; reconciliation checkpoint dc9fd63; final M0 checkpoint = the commit introducing [../26-m0-acceptance-addendum.md](../26-m0-acceptance-addendum.md) — independent re-verification of all M0 claims, 2026-08-02).
- Research: COMPLETE, permanently closed against the source.
- Architecture: PROPOSED + remediated (27/27 findings dispositioned); **internal adversarial verification COMPLETE 2026-08-01** — verdict `VERIFIED WITH CORRECTIONS NEEDED`, 29 findings, all dispositioned and corrections applied ([corrections record](../../architecture/remediation/internal-verification-corrections.md), summarized in [../22-readiness-assessment.md](../22-readiness-assessment.md)); external re-review = REQUIRED, blocking beta entry (V-04); architecture remains PROPOSED until it upgrades the verdict.
- Decisions: **all formerly-blocking decisions resolved** under delegated authority — [../21-decision-resolution.md](../21-decision-resolution.md). Reserved matters (purchases, production accounts, real data/sends, legal) surface when they arise.
- Implementation: **M1 COMPLETE — 4/4 tasks ACCEPTED and merged** (M1 authorized 2026-08-02 against checkpoint b49d3dd; packets [../27-m1-task-packets.md](../27-m1-task-packets.md) + the FAD-13 integration task). Merges: 356ddf5 (M1-001) · 946fc72 (M1-003) · f547e90 (M1-002 + M1-004). Exit report: [../29-m1-exit-report.md](../29-m1-exit-report.md). M2 authorized 2026-08-03 (see header block above).
- Kernel state on main: tenancy + context middleware + production unit-of-work · SPEC-06 fifteen-row evaluator with roles/grants/entitlements · audit hash chain with signed checkpoints + transactional outbox · one evaluator on every surface. 551 tests, 12 gates, 14 red cases, three migrations.
- Carried out of M1: TDG-04 CLOSED (FAD-14) · SP-E UX brief DONE ([../28-sp-e-ux-brief.md](../28-sp-e-ux-brief.md)). Open residuals and named follow-ups are listed in the exit report §8.
