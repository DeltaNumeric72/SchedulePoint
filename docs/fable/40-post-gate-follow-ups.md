# 40 — Post-gate follow-ups register (durable, numbered)

Opened 2026-08-25 at the replacement-gate decision ([39](39-replacement-gate-decision.md)),
discharging the REV-C surrogate's condition C5: a follow-up that lives only in a commit
message or a rolling run-state field is not registered. **FU numbers are stable IDs
(non-bypass rule 13: never removed or renumbered).** Status moves only with evidence;
closing an entry cites the commit or record that closed it. Milestone bindings are
sequencing, not optionality — no entry may be dropped (CLAUDE.md rule 3 posture).

| ID | Item | Origin | Binding | Status |
| --- | --- | --- | --- | --- |
| FU-01 | **NR-22 retirement, two exits:** a canonical-sort sequencer in `apps/api/vitest.config.ts` (seed-N becomes a true replay key), or reword the printed `Reproduce with --sequence.seed=` operator line. One follow-up, two exits, whichever lands first. | R-12 (RISK-REGISTER NR-22 rider (c)) | **SAME MILESTONE** (M5-era, by the rider's own binding) | **CLOSED 2026-08-25 (M5-000a, first exit)** — `CanonicalFileSequencer` landed; the printed operator line is TRUE again, byte-identical; NR-22 RETIRED (RISK-REGISTER note, reviewer-authored, landed verbatim at the M5-000a commit) |
| FU-02 | **NR-23 cause measurement:** the run-wide position-correlated per-round-trip slowdown (median r=+0.95 over 141 files). Excluded by measurement already: row volume, partition count, worker heap. Named starting points: embedded-postgres plan-cache/WAL over a long-lived cluster; connection-pool aging; worker-side accumulation other than raw heap. | R-13 (NR-23) | M5-era, beside NR-20's realistic-size measurement | OPEN |
| FU-03 | **R-13 enforcement red-case arm:** the storm-ceiling throw branch was proven to fire once at review (`SP_STORM_ITERATIONS=6000`, named error 37 ms past the ceiling); making it a standing gate adds arm 68 (census ripple: shards, §7 note). | R-13 review C7 | post-gate packet | **CLOSED 2026-08-25 (M5-000a)** — arm 68 `storm-ceiling-enforcement` standing at `SP_STORM_ITERATIONS=12000` (the review measured N=6000's margin fragile: two of four observed ratios sat below its crossing threshold), named-error + red-gate asserted, restore leg green, census 67→68 with the doc 38 §7 dated amendment. Residue → FU-18 |
| FU-04 | **Solver-command diagnosability:** production `solverWorkerCommand()` is `SP_SOLVER_WORKER_COMMAND ?? 'python3'` with no discovery rung — a from-checkout real-stack run without the variable yields a silent `solver status FAILED` (fail-closed held: "candidate REFUSED"). Decide: a named `NO_SOLVER_COMMAND` refusal (or startup validation) vs the documented-invocation status quo. Evidence: `EV-DOC38-GATE/leg7-attempt1-*.txt`. **CLOSED 2026-08-26 (OPUS-M5-H) — the named refusal, ruled under the delegated mandate.** `solverWorkerCommand()` now reads `SP_SOLVER_WORKER_COMMAND` through the same `requiredEnv(name, why)` the RPC secret two lines above it uses; the `?? 'python3'` default is gone and the message leads with `NO_SOLVER_COMMAND`. **Why startup validation rather than a spawn-time `ENOENT` branch, from this entry's own evidence:** in `leg7-attempt1-*` `python3` was PRESENT and OR-Tools absent, so the worker STARTED and exited without writing — correctly attributed `crashed`/`exit-N-no-response`. Nothing failed to spawn, so an `ENOENT` branch would not have caught the observed failure at all. Fail-closed is unchanged (the system already could not solve; it could not SAY so); additive (no wire enum gained a member, nothing renamed). Trade recorded: a deployment relying on a bare `python3` on PATH must now set the variable — nothing in this repository did (ci.yml ×2, nightly.yml ×2, `applySolverEnv` always, `real-stack-daemon` forwards it, dev-setup §8 documents it). | §7 leg 7 attempt 1 | M5-era (touches production error taxonomy) | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-05 | **`errors.ts` `{ err }` on the 5xx path:** the fixed generic message holds (rule 9's response half), but the logger is handed the whole error object; adjacent to non-bypass rule 9's log half. Bound the logged shape or record the ruling that pino serialization is the boundary. | REV-B-008 residue | M5-era | OPEN |
| FU-06 | **Untracked-probe backstop:** NR-14's `dirtyEvidencePaths` covers tracked files + `docs/evidence/` only, so an untracked probe under a collected root has no automated backstop; the R-9.2 discipline rests on convention. **CLOSED 2026-08-26 (OPUS-M5-H).** `apps/api/test/architecture/tree-hygiene.test.ts` fails on any UNTRACKED, unignored file under a collected root (the five vitest project roots plus `apps/web/e2e`), with a non-vacuity arm requiring each named root to hold a tracked file so the scan cannot silently cover nothing. `git add -N` is the remedy the message names, and it is a discipline this repository already requires before a local gate run (the RISK-REGISTER's citation-sweep class rule), so a new test written the way the repository asks for it passes. Mutation-proven: an untracked file under `apps/api/test/` fails the arm by name. | REV-C-011 residue | post-gate packet | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-07 | **Root-file hygiene gate:** nothing catches a stray file at the repository root (architecture assertion 52b sees directories; `ALLOWED_IMPL_ROOTS` is dead — REV-C-008). The tracked `=` (C-007) was removed by hand; the class is unguarded. **CLOSED 2026-08-26 (OPUS-M5-H).** Home: **build-failing via `gate:unit`, `apps/api/test/architecture/tree-hygiene.test.ts`** (the same file as FU-06). The root's contents are ENUMERATED (20 names) and any other tracked root file fails, with the `=` incident quoted in the message; a second arm fails on an untracked, unignored root file — the same file one step before it gets committed. **Implemented as an assertion inside `gate:unit` rather than as an 18th `pnpm check` entry** (declared judgment: the packet's own acceptance line pins `pnpm check` at 17/17, and a build-failing check is what "gate" means here). Mutation-proven in both arms. | REV-C-007/008 | post-gate packet | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-08 | **Direct-vitest arms unwrapped:** four red-case arms invoke vitest directly rather than through `vitest-must-run.mjs`; the zero-execution boundary is documented, not enforced, at those sites. **AMENDMENT 2026-08-26 (OPUS-M5-H): the count was FOUR when this entry was written and is TWENTY-SEVEN.** Enumerated at `0df0c74` by walking `run.mjs`'s `CASES` and matching a bare `vitest` binary in any command slot: 27 arms, 54 legs. The entry is REV-B-era (M4-005); OPUS-M4-000A/000B/001 added twenty-three more arms in the same shape after it was written. The original text stands above; this row records the measured figure rather than being rewritten. **CLOSED:** all 54 legs routed through `scripts/gates/vitest-must-run.mjs`, and — the durable half — `scripts/red-cases/vitest-invocation.mjs` + `assertEveryVitestInvocationIsWrapped()` at `run.mjs`'s module scope REFUSE TO START a battery containing an unwrapped invocation (exit 2, the `assertMigrationAnchorsAreLive` posture), so the 28th arm written in the old shape fails loudly. Predicate proved both directions on synthetic cases in `runner-signature/check.mjs`; registry control mutation-proven (one arm unwrapped by hand → exit 2 naming the arm and the leg). | REV-B-006 residue | post-gate packet | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-09 | **R-5 INSERT-side guard:** migration 0020 freezes termination facts against same-state UPDATE; the INSERT side of the same honesty property is unguarded. | R-5 residue | post-gate packet | OPEN |
| FU-10 | **R-4b audit premise:** the selection window's closure presumes every audited same-organization writer takes the migration-0003 advisory lock; a future writer that skips the audit recorder reopens the FAD-26(2) axis silently. A gate or a recorded ruling. | R-4b review | M5-era | OPEN |
| FU-11 | **NR-20 at realistic size:** the selection transaction's audit-lock hold (49–71 ms on the test fixture) is unmeasured at realistic tenant volume; retirement condition recorded in NR-20. | R-4/R-4b (NR-20) | M6-era (per NR-20) | OPEN |
| FU-12 | **NR-15's next measurement start-list:** the two backlog shapes the exclusions point at — a drainer whose single worker claims a hanging handler; retry-backoff unclaimable jobs (`drainableJobCount` has no `run_at` filter). | R-6/R-11 (NR-15) | with FU-02's instrumentation | OPEN |
| FU-13 | **Small accepted residues, batched:** R-7 C-3 (`BuildsLayout` label records untyped) · C-4 (`e2-quality` inline verdict list → `schema.options`) · N-1 (three comments overstate what signature pins protect) · R-8 F3 (no assertion that `run.mjs` calls the anchor preflight) · F4 (`stale-edit-cas` green leg fragile under load) · R-8 NIT (refusal message wording) · dev-setup fixture-isolation stale prose · the 142-file sentence-sweep's CI home · the `.mjs`-under-`docs/` gap (convention only). **AMENDMENT 2026-08-26 (OPUS-M5-H): SEVEN of nine discharged; TWO stay OPEN as unactionable, on the record.** Done: **R-7 C-3** — all four untyped label records in `apps/web/src/builds/BuildsLayout.tsx` (`TERMINATION_LABELS`, `REPRODUCIBILITY_LABELS`, `REPRODUCIBILITY_DETAIL`, `STALENESS_DIRECTION_LABELS`) keyed by their wire enums, two of which were extracted into named exported schemas in `packages/contracts` (`reproducibilityModeSchema`, `constituentDirectionSchema` — values unchanged, declarations placed ABOVE their first use so module evaluation does not hit a TDZ); the GH-008 failure mode is now a compile error rather than a label falling back to a raw enum name. **R-7 C-4** — `e2-quality-and-credits.test.ts`'s six hand-typed verdict literals replaced by `resultReproducibilityVerdictSchema.options` (the old comment conceded the hazard in as many words), with a >1 non-vacuity check. **R-8 F3** — `runner-signature/check.mjs` now asserts that `run.mjs` CALLS both module-scope preflights, mutation-proven by commenting out the anchor call. **R-8 F4** — `stale-edit-cas`'s whole-gate legs narrowed to the two files its own comment names, through the wrapper: the shape doc 38 §7's arm-69 amendment adopted while naming this very arm as the counter-example; nothing weakened, globalSetup builds the same migration chain for a two-file run. **dev-setup fixture-isolation stale prose** — it said "Eleven … ten fixed seeds" and "35 suite runs"; `FIXED_SEEDS` holds THIRTEEN, corrected with a dated note recording that the set GROWS by FAD-15 ruling 3 (so the prose was guaranteed to rot) and naming the script as the authority. **The `.mjs`-under-`docs/` gap** — converted from convention to an enumerated allowlist in `tree-hygiene.test.ts`; the one instance, `docs/evidence/EV-M4-INPUT-B/b1-repro.mjs`, is allowlisted with its reason (retained evidence must stay byte-frozen and therefore unlinted). **The 142-file sentence-sweep's CI home** — assessed ALREADY DISCHARGED by FAD-53 R-11: `.github/workflows/nightly.yml` runs the fixed seeds as a matrix plus the rotating seed, with a `seed-set-completeness` job that reads `FIXED_SEEDS` out of the script so the matrix cannot drift; recorded in `docs/dev-setup.md`, whose paragraph claimed only "not in `pnpm check`" and never mentioned the nightly. **STILL OPEN, and why:** *R-7's N-1 ("three comments overstate what signature pins protect")* and *R-8's NIT (refusal message wording)*. Both are packet-REVIEW findings whose text was a session-workspace artifact and was never landed — `docs/evidence` holds no R-7/R-8 review bundle, and FAD-53's entry summarizes R-7's scope without carrying either wording. Searched, and the search is the record: `signature pins`/`pins the signature`/`SIGNATURE` across `scripts/` and every test root; `signature`/`signed` intersected with `pins`/`protect`/`cover`/`guarantee`/`prove` across `apps/api/src/solver` and `packages/domain/src/ports`; `pins` intersected with `signature` across all four test roots; the audit-chain modules; `disableRequestLogging` (R-7's B-008 pin) and its neighbourhood. No comment is identifiable as one of the three and no refusal message as the NIT's subject without guessing. Editing three self-selected comments would be repairing against unknown findings, which is the improvisation the escalation rules forbid. **Process lesson, adopted for this register: a finding routed into an FU must land its TEXT or its file/line pointers in the row at routing time — these two items are the demonstration of the cost.** If either text resurfaces, the items reactivate on this row. | R-7/R-8 reviews; REV-C | one M5-era hygiene packet | **OPEN — 7 of 9 discharged 2026-08-26 (OPUS-M5-H); two items unactionable, see the amendment** |
| FU-14 | **GH-004:** latent scroll wrappers outside the catalogue + a direct focusability assertion (axe class from OPUS-GH-003). | GH-004 | M5-era UI packet | OPEN |
| FU-15 | **GH-009:** the enabled-oscillation refetch class + recorder quiescence default. | GH-009 | M5-era UI packet | OPEN |
| FU-16 | **REV-A coverage table:** the per-scope-area table doc 38 §3 required of REV-A's bundle does not exist; R-10 declared it could not be manufactured retroactively. Standing record of the gap (criterion-2 residue); any future re-review closes it or re-declares it. | REV-C-003 / R-10 | record; revisit at the next review cycle | OPEN |
| FU-17 | **Owner action, issue #4:** billed-minutes posture and marking `red-case shard completeness` + `gate battery` as required checks. Owner-reserved. | GH reconciliation | owner | OPEN (owner) |
| FU-18 | **Arm 68's iteration count should be derived, not pinned:** the enforcement arm pins `SP_STORM_ITERATIONS=12000` against a threshold that scales with the run's own calibration; the four observed sustained/calibrated ratios span 0.50–0.84 on one box and trend down, and a run whose ratio falls below 0.30 would make the arm a false red. Derive N from the violation leg's own calibration (e.g. run the probe first, choose N so the crossing sits at a fixed multiple of the threshold) so the margin is structural rather than sampled. | M5-000a review (delta) | M5-era; with FU-02's instrumentation if convenient | OPEN |
| FU-19 | **X-11's PK exemption is blind to caller-named primary keys:** the arm in `apps/api/test/tenancy/roles-and-schema.test.ts` exempts any index matching `/_pkey$/`, on the stated premise that a primary key is "on `id`, a server-generated UUIDv4 the caller does not choose". Migrations 0021/0022 are the first tables whose PK leads with a CALLER-NAMED column (`request_id`), and the exemption does not distinguish the two cases. Proven empirically at review, not argued: with `request_availability_pkey` reduced to `(request_id)` the oracle reopens (a foreign request returns 23505, a nonexistent uuid 23503 — measured), the X-11 arm still PASSES, and it prints `request_availability_pkey: primary key on a server-generated id` — asserting the opposite of the truth for this class. The packet closed the hole by construction; the CONTROL did not catch it. Narrow the predicate to PKs whose column list is exactly `(id)` (or `(id, organization_id)`), so any other PK must satisfy the tenant-qualification rule. **Amendment 2026-08-26 (M5-002, review C-2):** the inverse face of the same blindness — 0024's `approvals_pk` deliberately does NOT match `/_pkey$/`, so the control genuinely evaluates `approvals` and passes it on `carriesOrganization`; that coverage exists only while the constraint name avoids the exemption pattern, and a rename to `approvals_pkey` would delete it silently. Recorded in 0024's header §6 (the C-2 discharge); the predicate fix above retires both faces at once. **CLOSED 2026-08-26 (OPUS-M5-H).** The exemption is now NAME-INDEPENDENT and column-list based: `isServerGeneratedIdentityKey(columns)` admits exactly `(id)` or `(id, organization_id)`, and `/_pkey$/` is gone from the file. Columns are read by a balanced-paren parser rather than a regex, because both obvious regexes are wrong — a last-group regex reads a PARTIAL index's `WHERE (…)` as the key, and a first-close-paren regex stops inside `lower(` — and the parser is pinned by a pure-function test covering both traps. **Face one proven empirically, on the entry's own case:** with `request_availability_pkey` reduced to `(request_id)`, the narrowed control FAILS (`1 failed \| 38 passed`) where it previously passed while printing the opposite of the truth. **Face two proven at unit level:** the exemption function cannot see a name, so `approvals_pk` and `approvals_pkey` get the same answer — a rename can no longer add or delete coverage. Migration 0024's header §6 carries a dated amendment saying so; `approvals_pk` is deliberately NOT renamed (a shipped identifier, and the paragraph must stay readable as the reason it was chosen). No coverage lost on the shipped schema: the file is 38/38 green unmutated. | M5-000b review + implementer escalation (both finders); amended at M5-002 review | M5-era; with M5-001's route work or M5-H | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-20 | **Referenced-row properties can drift after the referencing row is written:** two BEFORE-ROW guards enforce a condition on a row in ANOTHER table at write time only, and nothing re-checks when that other row later changes. `app_guard_shift_group_off_target` refuses a request naming a shift group whose `allow_request` is false, but 0005's `shift_groups.allow_request` may be flipped false afterwards, stranding accepted requests against a group that no longer admits them. `app_guard_vacation_week_in_period` refuses a `week_start` outside its period, but 0022 grants UPDATE on `vacation_periods.start_date`/`end_date`, so shrinking a period strands existing selections and weekly-capacity grants outside it. Same class as §5.5's mode-stability guard, which IS enforced from the period side — the pattern exists, it is simply not applied to these two. Decide per case: a period-side guard, or a recorded ruling that drift is acceptable and the readers tolerate it. **Amendment 2026-08-27 (OPUS-M5-003): the period-shrink half is CLOSED, branch (b) by measurement.** Migration 0025's period-side bounds guard covers `pending`/`approved`/`committed` selections and allocating weekly-capacity grants; the five irremediable statuses are EXCLUDED BY NAME because the measurement found no remediation path (no DELETE grant for any runtime role; `week_start` outside the UPDATE grant; no outgoing §2 edge) — an unconditional guard would have made periods permanently unshrinkable against inert debris. BOTH measurement premises are asserted as tests so a later grant cannot silently invalidate the exclusion. The `allow_request` half closed at M5-001; the entry is now fully closed. | M5-000b review | M5-003 (vacation) / M5-001 | **CLOSED 2026-08-27 (both halves; period-shrink at OPUS-M5-003)** |
| FU-21 | **The serial red-case runner's shared-port cluster race:** every db-backed arm starts its own embedded cluster on ONE deterministic port (55898) reusing ONE shared `.pgdata-test-55898` directory that persists between arms (observed at M5-000b: two different postmasters on the same pgdata across polls), with no wait-for-release or startup retry — a slow teardown ERRORs the next arm's start (`vitest matched no test file`), intermittently, and a SIGTERMed arm leaves an ownerless pgdata that blocks the next start outright. Sighted twice in one serial battery (`stale-edit-cas` both directions; the isolated `SP_RED_SHARD` re-run of the same arm passed both directions — the only changed variable being predecessors on the port). CI's per-shard fresh containers are immune, which is why it has never bitten there. Fix: make the harness's cluster startup robust (wait-for-port-release and/or one startup retry with a distinct message) or give arms distinct ports. An ERRORED arm is "did not run", never a verdict — the runner already refuses to count it as proven. | M5-000b battery (implementer observation, orchestrator-registered) | M5-era; M5-H or with FU-02's instrumentation | OPEN |
| FU-22 | **The NR-16 bare-line detector under-covers by keyword position.** `bareLineTenantTableDetectors` matches `^\s*(from\|into\|update\|join\|delete\s+from)\s+<table>` on a quote-free line. Two extremely common spellings therefore pass it: `select … from <table>` where the select and the from share a line, and `insert into <table>` at the start of a line — because the anchored alternation requires `into` to be *first*, and in `insert into` it is second. A probe over `apps/api/src/requests/store.ts` alone finds **11** such sites (five `select ${ROOT_COLUMNS} from requests`, six `insert into …`), none of them detected and none of them baselined. **I-15 is not violated** — every one goes through `uow.query` — so this is a gap in the control's reach, not in the rule's enforcement. It **pre-dates OPUS-M5-001**, which neither created nor widened it; M5-001 is simply the packet that made the blind spot large enough to be worth naming. The fix is to add `select\s+.*\bfrom\s+<table>` and `insert\s+into\s+<table>` to the detector and then reconcile the baseline in the same change — which is a ripple across existing modules, and therefore its own packet scope, exactly as NR-16's original entry said of its own tightening. **Nothing is relaxed by recording it; a detector that is honest about its reach is worth more than one that is assumed complete.** | M5-001 review | M5-era; M5-H or its own packet | OPEN |
| FU-23 | **`findByIdempotencyKey` answers `null` for a request whose subtype record this store does not read.** `PgRequestStore.findByIdempotencyKey` returns `record === null ? null : …`, and `loadRecord` returns `null` for `vacation-selection` by design ("a caller that loads a vacation root gets its root and a null record, which is honest about what this store knows"). The composition is less honest than either half: a member who already holds a vacation request under key K, submitting a non-vacation request under the same K, gets `null` from the R-11 replay read, proceeds to the insert, and is refused by `UNIQUE (membership_id, idempotency_key, organization_id)` — a 409 rather than a replay. **The outcome is safe and the failure is loud**, and it is unreachable today (the creation union excludes vacation at compile time and the route refuses it at 422). It becomes reachable the moment M5-003 writes vacation roots, so it is M5-003's seam to close — either by splitting the root read from the record read on the replay path, or by scoping the idempotency namespace per subtype. Recorded now because the reasoning is in front of us now. **CLOSED 2026-08-27 (OPUS-M5-003):** the root read split from the record read (`findRootByIdempotencyKey` + `classifyIdempotencyKey`, decided BEFORE any write); three answers — fresh / replay / a named `IDEMPOTENCY_KEY_REUSED` 409 — proven over HTTP in both subtype directions with 'a bare CONFLICT is the 409 posing as a replay' as the surviving-defect assertion. Per-subtype namespacing rejected: D-7 is 0021's constraint (non-additive) and one key meaning two commands is the opposite of an idempotency key. | M5-001 review | M5-003 | **CLOSED 2026-08-27 (OPUS-M5-003)** |
| FU-24 | **Two catalogue keys have no non-comment reader: `picklist.intervene` and `audit.export`.** Both pre-date M5-002 (`audit.route.ts` declares `audit.read`; every `audit.export` occurrence outside its declaration is comment/SQL prose). A key with no evaluator is a grant that lies (the M5-001 ruling); M5-002 cured the class's other two members (`requests.batch_approve`, `vacation.override_quota`) by giving them evaluators. **Detection rule, adopted from the M5-002 survey defect: prose ≠ reader** — a reader survey must drop the declaration and every line whose body begins `*`, `//`, `/*` or `--`, because a key discussed at length in comments looks well-read while nothing consults it (the FU-19 class: a control that cannot tell being talked about from being used). Dispose per key: an evaluator with a route/surface that reads it, or a recorded removal decision (rule 13 permits neither silent removal nor renumbering). **CLOSED 2026-08-26 (OPUS-M5-H) as a DATED RETENTION for each key — never a silent removal, never an invented surface.** Survey re-run under this entry's own prose≠reader rule (drop the declaration and every line whose body begins `*`, `//`, `/*` or `--`): both keys have ZERO occurrences left in `apps/` and `packages/`. Neither exit is available. `picklist.intervene`: the picklist module does not exist — no route, no service, no table — so an evaluator would mean inventing the surface. `audit.export`: `audit.route.ts` registers one route, a GROUP-scoped subject-filtered read under `audit.read`, and re-pointing it at an ORGANIZATION-scoped export key would widen a grant to give a key a reader (the mistake `group.holiday_calendar.read` exists to undo). Removal refused under rule 13 — both are cited by doc 08 §4 and §6, and the citations would still look correct. Each key now carries a dated disposition docblock in `catalogue.ts` naming the awaited surface, recording that it is held by NO role (so a grant is inert rather than dangerous), and transferring the obligation to the packet that lands its surface — the `vacation.override_quota` precedent, an M1 orphan that got its evaluator at M5-002. | M5-002 report + review (both finders; implementer's survey conceded defective) | M5-era; M5-H or the packet that touches each surface | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-25 | **The red-case runner cannot distinguish a signal-killed gate from a failing gate.** `vitest-must-run.mjs` maps a signal-killed child (`result.status === null`) to `result.status ?? 1` → exit 1 with no message; `run.mjs`'s ERRORED matcher recognizes only the could-not-be-spawned text (FAD-50 N-1(ii)'s ENOENT class), so signal death scores as a VERDICT. On GREEN a killed gate reads `GATE FAILED` (an arm reported unproven that was never tested); **on RED a killed gate scores as "gate failed as required" — a kill counts as PROOF**, the decorative-red-case hazard inside the detector itself. Evidence: the isolated arm-13 re-run at M5-002 (`/tmp/sp-vitest-must-run-OsC0Kq/` empty at 14:18:59 — vitest never wrote `run.json` and the wrapper's own `rmSync` never ran, so child and wrapper were both killed), plus EIGHT empty `sp-vitest-must-run-*` dirs spanning 2026-08-23→08-26 (five predating the M5-002 worktree) under the inference rule **a surviving must-run dir IS a killed wrapper** (the wrapper removes it on every completed run). Kernel OOM excluded (`/proc/vmstat` `oom_kill 0`); the kills are userspace, non-deterministic, environmental. **Fix ratified and DEFERRED** (a packet must not edit the control that reports on that packet): `run.mjs` treats `result.signal !== null` as ERRORED at the FAD-50 N-1(ii) site — narrower than a wrapper-side signature and closes both directions at the source. **CLOSED 2026-08-26 (OPUS-M5-H) FOR THREE CASES, with the fix EXTENDED on a measurement the original ruling predates.** The ratified runner-side branch landed (`result.signal !== null` → ERRORED, at the FAD-50 N-1(ii) site, via `scripts/red-cases/spawn-outcome.mjs` — extracted so a checker can import the classifier, exactly as `errored-signatures.mjs` was). **The measurement:** `run.mjs` never spawns vitest, it spawns the PACKAGE MANAGER, and pnpm converts a descendant's signal death into `status=1, signal=null` — probed for both a direct child and a grandchild. So the runner-side branch fires only when the package-manager process ITSELF is killed. Measurement supersedes argument, and the orchestrator ratified two additions: a wrapper-side branch in `vitest-must-run.mjs` (its vitest child killed → the SAME wording), and the M5-002 inference rule MECHANIZED — the wrapper announces its report directory and removes it on every completed path, so a non-zero gate whose announced directory SURVIVES is a killed wrapper. One signature, three emitters, one matcher. Proven by a harness arm that SIGKILLs a real child and by a real temp directory created then removed; four mutations each killed by their own named assertion. **RESIDUAL, named rather than assumed closed:** a kill that lands on `run.mjs` itself scores nothing at all, because the process doing the scoring is the one that died. | M5-002 (orchestrator wrapper-source reading + implementer's environment record) | M5-era; M5-H or its own corrective packet, BEFORE the next serial battery is relied on | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-26 | **`S-05t` asserts a wall clock against a CP-SAT subprocess in a suite that runs under variable load.** In M5-002's composed seeded run attempt 1 (seed 20260902) it was the single failure in 1 638 tests: the solver-native 3 s deadline (layer 1) lost to the parent kill (layer 3) under shuffle-induced concurrent load; the same file passed three times on the same tree in unshuffled runs, and no solver file was in the packet's diff. Its own docblock concedes the hardware dependency. Fix designation: the measured-margin house pattern (R-2's precedent — measure the environment, set the bound from measurement plus margin, never weaken the assertion's meaning). | M5-002 composed run, class (b) triage | M5-era; the next packet that touches the solver test surface, or M5-H | OPEN |
| FU-27 | **The standing post-kill preflight is structurally blind to ADDED red-case artifacts.** `.gitignore` carries `**/__red_case__*`, so a killed arm's leftover mutation FILE (as opposed to a mutation of a tracked file) is invisible to `git status` and to the diff re-count — proven empirically at M5-002's review: a `timeout`-killed cheap arm left `packages/contracts/src/__red_case__type.ts` behind; `git status` reported zero; a regenerated-diff md5 integrity check passed BYTE-IDENTICAL with five red-case artifacts in the tree; the debris then failed the next typecheck gate 16/17. Both halves of the debris class now have a check: the diff re-count (modifications) and, adopted here, **`find . -name "__red_case__*" -not -path "*/node_modules/*"`** (additions) run beside it. The M5-001 preflight entry is amended by this row, not rewritten. **CLOSED 2026-08-26 (OPUS-M5-H): the check has an executable home.** `scripts/red-cases/debris.mjs` sweeps by NAME over the worktree (skipping `node_modules`, `.git`, `.venv`, `.evidence-scratch`), runs standalone (`--sweep` removes) — the form an operator needs after a kill, when starting another battery is precisely what they must not do to find out what the last one left — and is wired into the runner's own preflight so a battery never inherits the previous one's debris. `run.mjs`'s docblock now carries the two-check post-kill procedure in writing, naming why each check is blind to the other kind of debris, together with the tree-is-not-a-source-of-truth rule. **This entry's premise was reproduced live while closing it:** with a planted `packages/contracts/src/__red_case__probe.ts` in the tree, `git status --porcelain \| grep -c __red_case__` printed `0` and the new check printed the path and exited 1. | M5-002 review (finding produced by the reviewer against itself) | immediate — the check is procedural and binds every subsequent battery/kill recovery | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-28 | **Member-submit role implication is unresolved:** doc 08 §6 marks "Submit requests/vacation" `✓` for Member, yet `requests.own.create/read/withdraw` appear in no role's implication set — member access today rests on explicit grants in fixtures. Either the ✓ means role-implication (then the cross-product gains a Member row) or it means grantable (then the doc's ✓ legend needs the `(G)` marker there). Held deliberately at M5-001 and M5-002 (narrower-never-wider); decide at M5-H with the doc and the cross-product in the same change. **CLOSED 2026-08-26 (OPUS-M5-H): the `✓` means ROLE-IMPLICATION, ruled under the delegated mandate and verified against the document first.** Doc 08 §6's legend reads verbatim "✓ = allowed by role · G = requires named grant · — = denied", and the "Submit requests/vacation" row is `\| ✓ \| — \| — \| ✓ \| — \| — \|`. So the three self-scoped keys join `member` AND `scheduler` (the row's second ✓) and no other role. **KEY-NAME CORRECTION, dated:** this entry's original text names `requests.own.create`; the catalogue's key is **`requests.own.submit`** (`packages/domain/src/authz/catalogue.ts`), verified against the file rather than against any message. The keys are `requests.own.submit` / `.own.withdraw` / `.own.read`. Both layers proven in `apps/api/test/requests/member-submit-role-implication.test.ts`: the constant (positive for the two roles, NEGATIVE for every other, plus an assertion that implication widened nothing — a member still lacks `read_any`/`administer`/`approve`/`deny`), and the real routes over HTTP driven by a member holding the role and no grants, with a viewer refused and the DENIAL'S LAYER checked (`NO_CAPABILITY`). A non-vacuity control reads `capability_grants` and requires no grant of the three keys to the acting membership, so no success below it can be proving the fixture. Fixtures' explicit grants stay valid — implication adds, never removes. | M5-001 observation, held; re-recorded at M5-002 | M5-H | **CLOSED 2026-08-26 (OPUS-M5-H)** |
| FU-29 | **Coverage class: routes proven only below the HTTP layer.** The class that let M5-001's 500-on-success projection defect ship: `route-policy` builds the server with NO database, service tests sit below the layers, so no test drove a request route over HTTP end-to-end — a route can be structurally policied, behaviorally correct in its service, and still fail every real request. M5-002 closed it for the requests surface (`decision-authority.test.ts` and the C-1 queue test drive routes over HTTP). The register item: every packet that adds routes owes at least one HTTP-driven test per route family (success shape AND at least one refusal), and existing route families not yet driven over HTTP (pre-M5 surfaces) are swept once, M5-era. | M5-002 findings (projection defect + fixture gap, one class) | M5-era; sweep at M5-H or M5-006 | OPEN |
| FU-30 | **The unit gate's in-test red-case leg can leave its mutation applied to production source.** `gates-fail-on-violations.test.ts`'s RED leg mutates tracked source files in place during `gate:unit` and, on an exit path that skips its restore, leaves the violation in the working tree — observed at OPUS-M5-003: `provider-boundary.ts` carried the neutered runtime guard after a completed check run, poisoning two subsequent runs (disjoint failure sets on an unchanged tree) before `git diff` on the file found it. The FOURTH recorded in-tree disarmed-control instance and the FIRST from inside the unit gate rather than the battery. `debris.mjs` structurally cannot see it (added-artifacts only); the two-check discipline (`git status --porcelain` + per-file `git diff`) is the current cover. Fix designation: the in-test leg's restore must survive EVERY exit (finally/afterEach, never straight-line code after an assertion); diagnosis starts from the M5-003 run-11 transcript, where the red section reported no failure yet something exited without restoring. Not fixed in M5-003 (a shared control; the packet reports through the gate that runs it — the M5-H precedent). | M5-003 (implementer diagnosis, orchestrator-routed) | M5-era; M5-006 or its own corrective packet | OPEN |
| FU-31 | **The domain ports barrel omits three store ports.** `packages/domain/src/ports/index.ts` exports neither `RequestStore` (M5-001) nor `VacationStore` (M5-003) nor `RequestCommentStore` (M5-00C) — consumers import each from its module path instead, so the registry the barrel exists to be is silently partial and a reader auditing "every store port" from the barrel misses three. Not a behaviour defect (imports resolve; the unit-of-work rule is enforced elsewhere); it is a registry-integrity gap of the FU-08 class. Registered rather than fixed at M5-00C: the barrel is a shared control and shared-control changes route through their own packet (M5-H precedent). | M5-00C landing (orchestrator, from the implementer's observation at M5-00C round 1) | M5-era; M5-006 or the next hygiene batch | OPEN |
| FU-32 | **Order dependence in M5-003's `vacation-selection-http.test.ts`.** Under the composed-run invocation of record, seed 20260920 fails 3 of its tests and seed 20260921 fails 4 ("expected 'withdrawn' to be 'pending'", "the setup selection must exist: expected undefined to be defined", …) — and BOTH failure sets replicate BYTE-IDENTICALLY on the unmodified base tree at `f85510f` (same seeds, same flags), so this is pre-existing, not M5-00C content: the failing set grows with the permutation, which is what an order dependence does and is not what a content defect does. The file's tests share fixture state across `it()` blocks in an order the canonical sequencer can break. NOT cured by removing `--sequence.shuffle.tests` (that would weaken the discipline, rule 10); the fix is to make the file's cases order-independent (own-fixture-per-case, the R-13/T-15 pattern). The nightly 13-seed matrix is the recurrence monitor meanwhile. **[Amended 2026-08-31 — the matrix is now UNIFORMLY red on this defect and therefore blind as a monitor:** nightly runs 3–7 (2026-08-27..31, runs 33087065429 / 33191181018 / 33249459567 / 33305335974 / 33384560069, all on main at `6769319`) fail in ALL 14 seed jobs (13 fixed + rotating), every one on the SAME single test — this file's "a member cannot withdraw a COLLEAGUE's selection" at line 1036, `expected undefined to be defined` — 163/165 files green beside it. M5-00C's 19-file set change moved EVERY seed's canonical permutation into the failing order (the file SET is part of the shuffle key, as NR-22's retirement record states). Ruled per FAD-15 ruling 3: the binding sharpens to **its own small corrective packet (own-fixture-per-case, the R-13/T-15 pattern) immediately after M5-004 lands, before M5-005** — five consecutive blind nightlies is the cost ceiling. Until it lands, a composed seeded run failing ONLY in this file at line-1036-class assertions is cited against this row, not re-diagnosed. Observed again at three further seeds during M5-004 (20260831 ×4 failures, 20260901 ×2, and the reviewer's final-tree run at 20260902 ×1 — the file green 19/19 in isolation every time), all cited under this rule; and the CLASS claimed two fresh instances in M5-004's own new test files, both found by the seeded runs and cured own-fixture-per-case before landing — the register's remedy proven on live subjects twice.**]** | M5-00C (both seeded attempts; base-tree replication by the implementer, verified at review) | M5-era; its own small corrective packet or M5-006, before the nightly matrix is declared clean | OPEN |
| FU-33 | **The CP-SAT objective builds no term from `SoftPreference`.** FAD-60's projection row carries `low|medium|high` verbatim and the weight table ships tested (total, strictly monotone, positive) — but the worker's objective is assembled from SOFT RULES only, so a member's shift preference influences no solve. Wiring the request-derived term is a solver-model + Python-mirror change (it touches `E2_OBJECTIVE_PROFILE` and its cross-language digest) that M5-004 correctly did not take. Routed forward stated-not-hidden in the projection module's header. | M5-004 (FAD-60's own routing; doc 42 §5h) | M5-era; the M5 exit sweep or a dedicated solver packet | OPEN |
| FU-34 | **`HardOn`'s binding source does not exist.** SPEC-08 §6's HardOn row reads "where group policy makes it binding" and no such group policy exists anywhere in the schema or settings (verified against the files at M5-004): `groups` carries request_until/deadline fields and nothing about binding availability. The projection ships the row shape and a TOTAL gate with both branches tested; availability projects no HardOn row today. Candidate SPEC-08/doc 08 clarification or a group-settings schema addition — a recorded decision either way. | M5-004 interim 3 (measured absence; orchestrator-approved narrower-never-wider handling) | M5-era; the M5 exit sweep | OPEN |
| FU-35 | **"Outstanding revision requests" is a column query for five subtypes and an event query for reversed vacation weeks.** M5-004's reversal raises the revision request as audit + outbox events and does NOT write `requests.revision_requested`, because 0023's guard gives that flag exactly one meaning (§4's withdrawal from `reflected_in_version` — FAD-55's vacation exclusion) and widening shipped enforcement for a consumer that does not exist was refused (FAD-61 sub-ruling). When a surface actually consumes "outstanding revision requests" (M5-005's UI or M5-006's sweep), IT decides the uniform shape: a read-side union/view over both sources, or — only then, as a recorded FAD with the consumer beside it — the guard widening. | M5-004 escalation 2 (found by test; option (a) ruled) | M5-005 or M5-006, with the consuming surface | OPEN |
| FU-36 | **`is_override` carries two facts under one name.** 0022:405's frozen equality `is_override = (override_reason IS NOT NULL)` plus §5.6's mandatory reversal reason force `reverseSelection` to set the flag for within-allowance weeks, while the wire comment defines it as "approved over the allowance"; the two facts are told apart today only by the selection's status (`reversed`). Owned at both code sites (M5-004 C4). Separating them (a `reversal_reason` column or an `is_reversal` flag) is a future recorded decision, deliberately not taken while no consumer needs the distinction — a field with no reader lies about being needed (the M5-001 rule, mirrored). | M5-004 review C4 (the reviewer's F1) | A recorded decision when a consumer needs the distinction; review at M5-006 | OPEN |
