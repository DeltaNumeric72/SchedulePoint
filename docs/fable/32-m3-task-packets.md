# 32 — Milestone M3 task packets

**Status: finalized 2026-08-04 on the owner's M3 authorization, against frozen M2 checkpoint `e476573` (tag `milestone/M2`). OPUS-M3-001/002/003 are finalized here and NOT ISSUED at authoring time; issuance is recorded per packet and in the task log. The remaining M3 packet families (§8) are pre-declared in outline and MUST be finalized in this document before their own issuance — M3 does not close on the first three packets alone.** Every packet inherits the runbook ([17-opus-agent-runbook.md](17-opus-agent-runbook.md)) including the thirteen non-bypass rules, the return-report format, worktree discipline, the standing merge/port-hygiene, standalone-verification, red-case-invocation, and NR-14 regenerated-artifact disciplines ([control/OPUS-AGENT-RUNBOOK.md](control/OPUS-AGENT-RUNBOOK.md)), [24-execution-standards.md](24-execution-standards.md) §§E–F, the FAD-7 environment substitutions (embedded-postgres, corepack pnpm, no Docker builds on this machine), and the M2 evidence machinery as the standing floor: FAD-15 fixture isolation (`ownedMulti`, enforced-immutable MULTI baseline), `corepack pnpm fixture-regression` at acceptance, the SBX battery at acceptance, and the `ProbeFalsified` falsifiability discipline (SPEC-16).

**Execution order:** OPUS-M3-001 first and alone. OPUS-M3-002 and OPUS-M3-003 are **blocked until M3-001 is accepted and merged**, are revalidated against M3-001's actual outcome before issuance (a dated §7 note, as in M2), and run in parallel only if their allowed globs are re-verified disjoint at that revalidation. All packets in this milestone are **critical class: independent second review with reviewer-authored adversarial probes is mandatory for every one.** M4 is not authorized; nothing in this document may implement the automated solver.

---

## 1. M3 entry verification record (2026-08-04)

| Condition | Result |
|---|---|
| HEAD is `e476573` | **CONFIRMED** (`git rev-parse HEAD`) |
| Tag `milestone/M2` → `e476573` | **CONFIRMED** (`git rev-parse milestone/M2^{commit}`) |
| Working tree clean | **CONFIRMED** (`git status --porcelain` empty; NR-14 regenerated artifacts restored after the battery run per standing discipline) |
| No M2 branch/worktree with unmerged or uncommitted work | **CONFIRMED** — `git branch -a` shows only `main`; `git worktree list` shows only the primary checkout |
| All four M2 tasks accepted and merged | **CONFIRMED** — `bc61ee7`, `ff2e67b`, `567964c`, `fce095c` all ancestors of HEAD; acceptance records in the task log |
| Validators | plan **36/36** (exit 0) · architecture **95/95** (exit 0) · research **PASS** (exit 0) — exit codes checked directly |
| Quality gates | `corepack pnpm check` **12/12 PASS** (exit 0) on `e476573` |
| Red cases | `corepack pnpm red-cases` **all proven** (exit 0), invoked via pnpm per standing discipline |
| M2 exit verdict | **PASSED** ([31-m2-exit-report.md](31-m2-exit-report.md) §15) |
| M2 carried items recorded here | **§2 below** — every item has an explicit owner and completion point |

## 2. M2 carried-scope disposition — explicit owner and completion point for every item

No item below is carried silently. Items whose completion genuinely belongs to a later milestone carry the seven required fields (milestone, owning family, reason, dependency, capability mapping, production gate, evidence).

| # | Carried item | Owner | Completion point |
|---|---|---|---|
| 1 | SBX-005 and SBX-006 authentication arms + SPEC-11 X-12 | **OPUS-M3-001** | M3-001 acceptance: both executed in full, no `EVIDENCE_BLOCKED(authn)` remaining |
| 2 | Rule authoring + typed AST + compiler | **OPUS-M3-002** | M3-002 acceptance: round-trip proven |
| 3 | B-\* benchmark corpus | **OPUS-M3-002** | M3-002 acceptance: generator + first feasible and infeasible fixtures committed |
| 4 | Timezone configuration | **OPUS-M3-003** (schema: `groups.timezone` NOT NULL + period/publication timezone semantics) + **OPUS-M3-007** (administration surface) | M3-003 and M3-007 acceptance |
| 5 | Remaining group settings — request-until configuration | **OPUS-M3-007** (authoring + storage). Enforcement is **M5**: family = requests (SPEC-08); reason = the gating semantics are defined by the request lifecycle, which is M5 scope; dependency = request aggregate; capabilities = CAP-021..023; production gate = R-01..14 + QA-REQ battery; evidence = EV-M5 | Setting authored/stored/validated at M3-007 acceptance; enforcement evidenced at M5 exit |
| 6 | Remaining group settings — picklist-access configuration | **OPUS-M3-007** (authoring + storage). Enforcement is **M9**: family = picklist (SPEC-02/03); reason = the consuming turn transaction is M9 scope; dependency = picklist module; capabilities = CAP-030, CAP-060; production gate = SBX-020/022 + LIVE-SIM meta-test; evidence = EV-M9 | Setting authored/stored/validated at M3-007 acceptance; enforcement evidenced at M9 exit |
| 7 | Site attribute under the PO-DEC-01 pending default | **OPUS-M3-007** — `locations` + free-form `site_label` per CAR-021 §3.2a, both migration directions modelled with fixtures; **no site administration surface, no site-scoped API** (that would select the branch by stealth — non-bypass rule 12) | M3-007 acceptance |
| 8 | Break-glass session auditing (SPEC-11 §3.2) | **OPUS-M3-001** | M3-001 acceptance |
| 9 | NR-14 regenerated-artifact hygiene redesign | **OPUS-M3-008** (integration & hardening). Interim control: the standing restore-with-`git checkout --` discipline stays in force | M3-008 acceptance |
| 10 | Scheduler-vs-calendar-key grant edge (write-not-read) | **OPUS-M3-006** — the packet must contain the ruling as an explicit deliverable; the ruling is surfaced in the M3 exit report for owner confirmation | M3-006 acceptance |
| 11 | Eligibility-read absent-vs-empty semantics | **OPUS-M3-004** — pinned when the cell editor consumes the eligibility read | M3-004 acceptance |
| 12 | SPEC-14 `M` cells (manual accessibility evidence) | Automatable portions: **OPUS-M3-008**. Real-AT-session cells: **M6**; family = a11y evidence; reason = requires assistive-technology sessions only the owner can provide (EV-8); dependency = EV-8; capability = CAP-066 / SP-HR-3..6; production gate = alpha exit a11y evidence review; evidence = retained AT session records. Until then the cells are **honestly marked unclaimed** — never converted to claims | M3-008 (automatable) / M6 exit (AT sessions) |
| 13 | Retroactive-history authoring (T-01 ruling) | **Product decision required.** Proposed resolution point: the M3 exit report's recommendation section puts the decision to the owner. If approved: milestone = M6; family = administrative completeness; reason = an authoring convenience over effective-dated history, not required by the schedule spine; dependency = the owner's decision; capability mapping = the work-profile/qualification administration rows of report 19 (no capability is dropped — the underlying history model shipped at M2); production gate = profiles-administration QA battery at the owning milestone; evidence = effective-dating test extension | Decision surfaced at M3 exit; implementation at its decided milestone |
| 14 | In-force catalogue reads (S-03 ruling) | **M4**; family = solver inputs; reason = the consumer is the build pipeline; dependency = M4 build submission; capabilities = CAP-015/017; production gate = SBX-015..017 + corpus oracles; evidence = EV-M4 | M4 exit |

Standing conditions unchanged by this document: external architecture re-review blocks beta entry (V-04); FAD-7 CI conditions; EV-1 vendor specifications; G-ARCH remains open until SBX-011/013/014b/022/023/028 execute at their milestones.

---

## 3. OPUS-M3-001 — Authentication, sessions, invitation and activation (the carried M1 slice)

| Field | Content |
|---|---|
| **Task ID** | OPUS-M3-001 |
| **Milestone / Slice** | M3 / authentication, sessions, invitation/activation (carried roadmap-M1 slice; unblocks real-session evidence for every later packet) |
| **Status** | ISSUED 2026-08-04 (branch `opus/m3-001-authn`, worktree `.worktrees/m3-001`; issuance recorded in the task log) |
| **Objective** | First-party email+password authentication with TOTP MFA, server-side sessions with idle and absolute lifetimes, invitation/activation separated from password reset, login-email change (CAR-027), session-epoch integration with the SPEC-06 freshness counters, break-glass session auditing (SPEC-11 §3.2), and the minimal accessible UI for sign-in, MFA enrolment/challenge, activation, and reset. Synthetic accounts only; no real notification destination |
| **User outcome** | An invited synthetic user activates an account (single-use expiring token), sets a password, enrols TOTP, signs in and out, is signed out by idle/absolute expiry, and is immediately invalidated on suspension; an administrator changes a login email (audited, sessions invalidated, invitations reconciled) and resets MFA (X-12: audited, notified, sessions invalidated) |
| **Engineering outcome** | SBX-005 and SBX-006 execute **in full** (no `EVIDENCE_BLOCKED(authn)` remains); SBX-001 re-runs with real sessions; every subsequent packet tests against real authenticated sessions |
| **Dependencies** | M2 complete (`e476573`). No other task may run concurrently |
| **Relevant documents** | [14-security-and-privacy.md](../architecture/14-security-and-privacy.md) §§2 (T-06/T-07/T-22), 6 (MFA TOTP, session posture); [SPEC-11](../architecture/specs/SPEC-11-audit-assurance-and-security-boundaries.md) §3.2 + §10 X-12; [SPEC-06](../architecture/specs/SPEC-06-authorization-truth-table.md) freshness counters (`session_epoch`); [06-data-architecture.md](../architecture/06-data-architecture.md) §3.2 `users` (CAR-027), `sessions`, `invitations`; [10-state-machines.md](10-state-machines.md) STM-017/018 (invitation/activation); research report 18 §5.1 SBX-005/006 as authored + the M2 contract declarations in `apps/api/test/sbx/scenarios.ts`; SPEC-07 §3 (no delivery material in logs); [28-sp-e-ux-brief.md](28-sp-e-ux-brief.md) for the UI slice |
| **Allowed files** | `apps/api/migrations/0007_*.sql` (new, this packet only); `apps/api/src/authn/**` (new); `apps/api/src/http/**` (route registration, context wiring — minimal diffs, each justified in the report); `apps/api/src/db/schema.ts` (additive); `packages/contracts/src/authn/**` (new); `apps/web/src/authn/**` (new) + app shell routing touchpoints (minimal, justified); `apps/api/test/**` (incl. `test/support/authn.ts` new, and the SBX scenario/contract files for 001/005/006); `apps/web/e2e/**` (authn journeys + axe); `scripts/red-cases/**` (additions only); `docs/evidence/EV-M3-AUTHN/**`; root/workspace config only for script entries |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**` (Fable writes doc amendments, never the implementer), `schedulepoint-research/**`, `spikes/**`, `solver/**`, migrations `0001`–`0006` (immutable), `apps/api/src/{catalogue,profiles,audit}/**` except read-only use via existing APIs (audit emission uses the existing audit module API — non-bypass rule 6), `scripts/gates/**` (modifying existing gates prohibited; if the route-policy gate cannot express an explicit pre-auth policy class, that is an **escalation**, not an edit), existing tests may not be weakened or deleted |
| **Required implementation — schema (migration 0007)** | Per doc 06 §3.2: `sessions` (org tenant, token **stored hashed only**, `expires_at` idle + `absolute_expires_at`, active/revoked, purge-on-expiry with revoked-token audit retention) and `invitations` (org tenant, `token_hash`, single-use `consumed_at` set atomically, pending/accepted/expired/revoked); `users` additions: `password_hash` (SECRET), failed-attempt lockout state. **Pre-declared additive doc-06 amendment (ratified as a FAD at acceptance, doc amendment written by Fable):** a `user_mfa` table (org tenant, SENSITIVE/SECRET) holding the TOTP secret **encrypted at rest** (a TOTP secret cannot be hashed), enrolment confirmation, last-accepted time step (replay defence), and **hashed** recovery codes. RLS ENABLE+FORCE+policy in the same migration (gate-enforced); composite tenant FKs; every table CREATE follows the migration-RLS pairing gate |
| **Required implementation — authentication** | Password verification with a strong adaptive hash (argon2id preferred; the selected library and parameters are recorded for FAD ratification at acceptance); lockout and rate limiting per T-22; TOTP MFA — enrolment (secret provisioned, confirmed by a valid code before activation), challenge on sign-in, **required for elevated roles** per doc 14 §6, step-replay rejection; MFA reset per **X-12** (audited, notified via outbox, all sessions invalidated); password reset via single-use expiring token, **separate flow and separate token namespace from invitation/activation** (STM-017/018) |
| **Required implementation — sessions** | Server-side sessions; cookie posture `HttpOnly`+`Secure`+`SameSite`; bounded idle AND absolute lifetimes enforced server-side against a controllable clock seam (deterministic expiry tests); rotation on privilege change; suspension/deactivation invalidates live sessions **immediately** (mid-request denial proven); sign-out revokes; `session_epoch` bumped on authentication events, privilege change, MFA reset, login-email change — wired into the existing SPEC-06 freshness evaluation so a stale session is re-evaluated against current state (I-19) |
| **Required implementation — invitation/activation and login-email change** | Invitation issue (admin capability, audited, outbox intent, synthetic destination only), single-use atomic consumption, expiry, revocation; activation transitions the user per STM-017/018; **login-email change (CAR-027)**: admin capability `identity.change_login_email`, audited, all sessions invalidated, pending invitations reconciled; `login_email` never self-editable |
| **Required implementation — break-glass session auditing (SPEC-11 §3.2)** | Privileged (`app_breakglass` / maintenance-plane) access is itself audited: each use produces chain-linked audit evidence identifying actor, scope, and time; a red case proves an unaudited use is detectable |
| **Required implementation — pre-auth route policy** | Sign-in/activation/reset routes declare an **explicit** pre-auth policy class registered with the route-policy gate — deny-by-default is preserved (I-02): an undeclared route still fails the gate (red case). No entitlement or capability check is skipped on any post-auth surface (non-bypass rule 4) |
| **Required implementation — UI slice** | Accessible sign-in, MFA challenge, MFA enrolment, activation, reset request/complete pages: axe green at both standing viewports, keyboard-completable, programmatic status/error announcements, loading/empty/error/denial states, I-10 request budgets recorded in the ledger, I-13 respected (nothing persists before explicit submission of a completed form) |
| **Secrecy constraints** | No token, password, hash, TOTP secret, or recovery code in logs, errors, traces, queue payloads, audit payloads, or evidence artifacts (non-bypass rule 9; SPEC-07 §3). Evidence redacts by construction, not by post-processing |
| **Tenant-isolation requirements** | All new tables org-tenant with FORCE RLS; sessions/invitations unreachable cross-org (SBX-004 sweep extends to the new tables — sweep table count grows and is reported); unit-of-work discipline for every tenant-table statement (non-bypass rule 1); `set_config(...,true)` only (rule 2) |
| **Authorization requirements** | Invitation issue/revoke, MFA reset, login-email change, and session administration are capability-gated through the SPEC-06 evaluator; every protected operation re-evaluates against current state (I-19); authn events integrate with, never bypass, the fifteen-row evaluator |
| **Audit requirements** | Sign-in success/failure, lockout, MFA enrol/reset, session revocation, suspension-driven invalidation, invitation lifecycle, login-email change, break-glass use — all through the existing audit chain API in the same unit of work as the mutation (FAD-12 ordering); chain verifies clean after every suite run |
| **Outbox requirements** | Credential-change and invitation notifications are **outbox intents only** (I-11); synthetic controlled destinations; no delivery adapter to any real channel; payload bodies never logged |
| **Migration requirements** | `0007` up/down/up cycle clean; down documented; any schema need beyond the declared set = escalation before writing it |
| **Accessibility requirements** | axe both viewports on every new page; keyboard journey e2e for sign-in and activation; SP-HR-3..6 for every interactive element (I-12) |
| **Tests** | Unit + integration on real embedded postgres via `ownedMulti`; every lifetime/expiry path against the clock seam; suspension mid-session; concurrent session revocation; invitation double-consumption race (exactly one wins); lockout thresholds; MFA step replay; email-change reconciliation; UI e2e journeys |
| **Red-case tests** | (1) forged/garbage session token → clean deny, nothing leaked; (2) expired-idle and (3) expired-absolute sessions rejected; (4) suspended user's live session denied mid-flight; (5) reused invitation token rejected (second consumer); (6) TOTP replay (same step twice) rejected; (7) an unregistered route without a policy declaration fails the gate (pre-auth class did not weaken deny-by-default); (8) a token value planted in a log-capture harness is caught (secrecy probe) |
| **SBX obligations** | **SBX-005 executed in full** and **SBX-006 executed in full** — every formerly `EVIDENCE_BLOCKED(authn)` sub-scenario now executes with the falsifiability probe discipline (`ProbeFalsified` sentinel; a probe that cannot fail is vacuous and fails the run); **SBX-001 re-run with real sessions** replacing the M2 harness session surrogate, matrix re-filed; SBX-004 sweep re-run including the new tables; SPEC-11 **X-12** executed and filed |
| **Named-condition proofs** | (1) idle+absolute lifetime enforcement; (2) suspension-invalidates-immediately; (3) invitation single-use atomicity; (4) X-12 full contract; (5) session-epoch freshness integration (stale grant re-evaluated). Each runs standalone, in its package suite, and in the complete battery; all three results reported |
| **Standalone verification** | Every named-condition proof file and every SBX scenario file touched; full-battery + standalone results in the report |
| **Acceptance criteria** | `corepack pnpm check` 12/12 · `corepack pnpm red-cases` all proven (incl. new) · `corepack pnpm fixture-regression` green (rotating-seed failure = defect, seed joins the set) · SBX battery: 005/006 PASS-with-falsifiability in full, 001 re-run PASS, 004 sweep PASS with zero wrong-tenant rows · X-12 filed · audit chain verifies clean · migration 0007 up/down/up clean · UI axe + keyboard green · evidence bundle complete with INDEX.md · working tree clean, no lingering cluster, derived ports only |
| **Commands to run** | `corepack pnpm install` · `corepack pnpm check` · `corepack pnpm red-cases` · `corepack pnpm fixture-regression` · the SBX battery command · migration cycle command · each named-condition proof standalone — all with captured output |
| **Required evidence** | `docs/evidence/EV-M3-AUTHN/` — scenario table (required/executed/passed/failed/blocked), SBX-005/006 artifacts, SBX-001 re-run matrix, X-12 record, lifetime/invalidation proofs, redaction demonstration, migration cycle log, INDEX.md |
| **Escalation conditions** | The route-policy gate cannot express the pre-auth class without modification · any schema need beyond the declared set · the SPEC-06 evaluator needs changes (it must not — integration only) · MFA-secret encryption needs key infrastructure the repo lacks · any test passable only by weakening it · any security or privacy doubt (stop-and-report, never retry) · SBX-005/006 sub-scenarios that still cannot execute (nothing may remain blocked without a named, ruled dependency) |
| **Rollback instructions** | Single squash-merge revert; migration 0007 down documented; no earlier migration touched. State revertibility explicitly in the return report |
| **Fable acceptance checklist** | Re-run every command from clean · diff strictly within allowed globs · verify no gate/test weakened (diff-level) · re-run SBX-005/006 and diff scenario tables against claims · verify zero formerly-blocked sub-scenarios remain unexecuted without a ruling · write my own secrecy probe (plant a fake token, grep all logs/artifacts) · verify session invalidation with my own suspension sequence · verify X-12 evidence · fixture-regression with a fresh rotating seed · ratify or reject the `user_mfa` FAD + hashing-library FAD; write the doc-06 amendment if ratified · standalone sweep of named proofs |
| **Independent-review checklist** | Reviewer-authored probes, not test-reading: attempt session fixation, token forgery, cookie downgrade; attempt invitation replay and cross-org invitation consumption; attempt TOTP replay and brute-force pacing; attempt MFA reset without the capability; verify suspension race (suspend while a request is in flight); search the entire diff and artifacts for secret material independently; attempt a pre-auth route that reaches a tenant table; mutation-test the falsifiability probes of SBX-005/006; verify the clock seam cannot be manipulated from production paths; attempt to make an expiry test pass with the enforcement disabled (must fail) |

**Independent second review: MANDATORY** (authentication/session critical class).

---

## 4. OPUS-M3-002 — Typed scheduling rules, compiler, authoring, benchmark corpus (the carried M2 slice)

| Field | Content |
|---|---|
| **Task ID** | OPUS-M3-002 |
| **Milestone / Slice** | M3 / rule model + B-\* corpus (solver **inputs** — the solver itself is M4 and prohibited) |
| **Status** | ACCEPTED & MERGED 2026-08-04 (7dd37dc; REVISE→delta-accept; FAD-21 ruled on the mandatory pre-migration escalation; record in the task log) |
| **Objective** | The typed rule AST with a closed node set, the compiler with validation and an unmapped-node CI failure, the authoring API and accessible authoring UI with the author → compile → revalidate round trip, deterministic serialization, stable rule identifiers, audit + authorization + tenant/group isolation for every rule mutation, and the B-\* benchmark-corpus generator with the first committed feasible and infeasible fixtures |
| **User outcome** | A scheduler authors versioned rules (hard/soft with the CAR-006 weight discipline), sees validation results, and edits round-trip losslessly; rule sets are composable and stably identified for later builds |
| **Engineering outcome** | M4's solver has its rule input and its fixture half (E1); an unexpressible rule is a schema change with a migration, never an escape hatch |
| **Dependencies** | M3-001 accepted and merged; runs in parallel with M3-003 only if the §6 disjointness re-verification passes |
| **Relevant documents** | [SPEC-04](../architecture/specs/SPEC-04-solver-runtime-and-rule-model.md) §§3 (AST, compiler, hard/soft invariant, canonical inputs), 8 (benchmark corpus), 9 (test contract rows applicable to the rule model); [06-data-architecture.md](../architecture/06-data-architecture.md) §3.2 `rules` (CAR-006 CHECK), `rule_sets`, and the `pattern_rules`/`staff_rules` rows; [12-scheduling-engine-plan.md](12-scheduling-engine-plan.md); doc 06 sensitivity: `staff_rules`-class content names individuals → INTERNAL, narrower access; [28-sp-e-ux-brief.md](28-sp-e-ux-brief.md) |
| **Allowed files** | `apps/api/migrations/0008_*.sql` (new; renumbered by the second merger if M3-003 merges first); `apps/api/src/rules/**` (new); `apps/api/src/db/schema.ts` (additive); `packages/contracts/src/rules/**` (new); `packages/domain/src/rules/**` (new — the AST types and pure validation live in the dependency-free domain package); `apps/web/src/rules/**` (new) + minimal justified shell routing; `apps/api/test/**` (incl. `test/support/rules.ts` new); `apps/web/e2e/**` (rules journeys); `solver/corpus/**` (new — generator + committed fixtures; **no solver code**); `scripts/gates/**` **additions only** for the unmapped-node CI failure (a new gate with a red case; no existing gate modified); `scripts/red-cases/**` (additions); `docs/evidence/EV-M3-RULES/**`; root config script entries |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**`, `schedulepoint-research/**`, `spikes/**`, `apps/api/src/{authn,catalogue,profiles,audit,schedule}/**` (rules may **read** catalogue/profile data via existing APIs only), migrations `0001`–`0007`, `solver/**` outside `solver/corpus/` (**no solver implementation, no optimization code, no assignment generation — non-bypass rule 7 and the M4 boundary**), `apps/api/test/support/{multi,owned-multi,staffing}.ts` (staffing extensions are owned by M3-003; needing one here is an escalation), existing tests/gates never weakened |
| **Required implementation — AST + tables** | `rules` and `rule_sets` per doc 06 §3.2: group tenant, `rule_key` stable identifier (never renumbered — non-bypass rule 13 discipline applies to rule keys), `rule_schema_version`, `classification ∈ {HARD, SOFT}` with the CAR-006 CHECK (`HARD ⇒ weight IS NULL`, `SOFT ⇒ weight NOT NULL`) as the database half of the hard/soft invariant (SPEC-04 §3.3), typed `predicate` AST with a **closed node set**. **Pre-declared ambiguity to resolve by escalation before migrating:** the relationship between the typed `rules` table and doc 06's `pattern_rules`/`staff_rules` rows (whether they are expressed as scopes/categories of the typed model or as separate tables) — propose with rationale, Fable rules before the migration is written |
| **Required implementation — compiler** | Deterministic compile of the AST to the canonical solver-input form (SPEC-04 §3.2): same input → byte-identical output (proven); versioned (`compiler_version`); validation with precise, field-addressed errors; **an AST node type without a compiler mapping fails CI** — implemented as a new build-failing gate with a red case proving it fires on a deliberately unmapped node |
| **Required implementation — authoring** | API: create/edit/disable/archive rules and rule sets, versioned, audited, capability-gated; **round trip proven**: author → persist → compile → load → re-validate → re-serialize with canonical-form equality; deterministic serialization (stable key order, no environment-dependent content). UI: accessible authoring over the typed model on the M2-002 foundation — axe both viewports, keyboard-completable, I-13 explicit Save, I-10 budgets, loading/empty/error/denial states |
| **Required implementation — B-\* corpus** | A deterministic generator under `solver/corpus/` producing known-by-construction fixtures: first committed `B-feasible-*` set (satisfiable by construction, with the construction argument recorded) and `B-infeasible-*` set (each with a minimal certificate of infeasibility recorded); fixtures use synthetic names only, reference the typed rule model, and are committed with a regeneration command whose re-run is byte-identical |
| **Tenant-isolation requirements** | Group-tenant tables, FORCE RLS, composite FKs; cross-group rule reads/writes denied (proven); the SBX-004 sweep extends to the new tables |
| **Authorization requirements** | Every rule mutation capability-gated through the SPEC-06 evaluator; INTERNAL sensitivity honoured (rule content that names individuals gets the narrower access doc 06 assigns); allow AND deny proven per surface |
| **Audit requirements** | Every rule/rule-set mutation audited through the existing chain API, same unit of work; chain verifies clean |
| **Migration requirements** | `0008` up/down/up clean; renumber-on-second-merge discipline |
| **Accessibility requirements** | axe both viewports; keyboard journey e2e for the authoring round trip |
| **Tests** | AST property tests (every node type constructed, serialized, parsed back); compiler determinism; CHECK-constraint red pair (HARD+weight rejected, SOFT−weight rejected); round-trip equality; corpus generator determinism; UI e2e |
| **Red-case tests** | (1) unmapped AST node fails the new CI gate; (2) a rule the AST cannot express has no escape hatch (free-text or JSON-blob bypass attempt is rejected by validation); (3) HARD/SOFT CHECK violations rejected at the database; (4) cross-group rule access denied; (5) corpus fixture tampering detected by the regeneration comparison |
| **Named-condition proofs** | (1) round-trip equality; (2) compiler determinism (byte-identical); (3) unmapped-node CI failure; (4) hard/soft database invariant; (5) corpus regeneration byte-identity. Standalone + package + full battery, all three reported |
| **Standalone verification** | Every named proof and every new test file |
| **Acceptance criteria** | check 12/12 · red-cases all proven (incl. new gate's) · fixture-regression green · SBX battery green with new tables swept · round trip + determinism + corpus proofs filed · audit chain clean · migration cycle clean · UI axe/keyboard green · evidence complete · tree clean |
| **Commands to run** | install · check · red-cases · fixture-regression · SBX battery · migration cycle · corpus generate-twice-and-compare · named proofs standalone |
| **Required evidence** | `docs/evidence/EV-M3-RULES/` — round-trip and determinism records, corpus construction arguments and certificates, gate red-case output, scenario/sweep table, INDEX.md |
| **Escalation conditions** | The `pattern_rules`/`staff_rules` relationship (mandatory pre-migration escalation, above) · any AST node whose semantics a SPEC leaves ambiguous · any need to touch solver runtime · any need for M3-003's tables or staffing fixtures · a corpus fixture that cannot be given a construction argument or certificate · any test passable only by weakening |
| **Rollback instructions** | Single squash-merge revert; 0008 down documented; the new gate reverts with the merge (no other gate touched) |
| **Fable acceptance checklist** | Re-run everything from clean · diff within globs · verify the closed node set is closed (attempt to add an unmapped node; gate fires) · verify determinism on a machine-state variation (fresh clone) · verify corpus certificates actually certify (spot-check the math) · verify no solver logic crept in (diff-level review of `solver/corpus/`) · ratify the pattern/staff-rules ruling into the record · fixture-regression fresh seed · standalone sweep |
| **Independent-review checklist** | Author adversarial rules (deep nesting, unicode names, boundary weights, empty scopes) and verify validation; mutation-test the round-trip proof (corrupt one serialized field — caught?); attempt an escape hatch (raw JSON predicate); attempt cross-group and cross-org rule operations as every role; verify the CI gate cannot be satisfied vacuously (delete all mappings — does it still pass?); verify corpus infeasibility certificates independently; check compiler determinism under locale/timezone variation; verify stable-ID discipline (rename a rule_key — is history preserved?) |

**Independent second review: MANDATORY** (solver-input correctness critical class).

---

## 5. OPUS-M3-003 — Schedule periods, identity/snapshot assignment model, publication core (SPEC-05)

| Field | Content |
|---|---|
| **Task ID** | OPUS-M3-003 |
| **Milestone / Slice** | M3 / the publication spine (SPEC-05 core; API + database + harness — the UI surfaces are M3-004/005/006) |
| **Status** | ACCEPTED & MERGED 2026-08-04 (e275597; REVISE→delta-accept; FAD-22 SPEC-05 amendment from the review's two blocking findings; second-merger rebase with disclosed composition; record in the task log) |
| **Objective** | Schedule periods, schedule requirements, draft schedule versions, assignment identities and snapshots, credits independent of assignments, provenance, pins, manual assignments and manual overrides with reasons, D-15 database-enforced published immutability, D-16 single-current publication, D-17 idempotency, the **complete SPEC-05 §8 proof harness V-01..V-19** (the authorization's V-01..16 plus the dated V-15b/c and V-17..19 additions — the fuller set is binding; running a subset would weaken the spec's own proof), SBX-018, publication audit events, affected-staff difference generation, transactional-outbox notification intents, forward-only correction, version preservation |
| **User outcome** | (API-level this packet) a scheduler creates a period, manually assigns staff with reasons/pins/credits/provenance, publishes an immutable version, corrects forward, reverts by publishing forward; affected staff differences generate notification intents |
| **Engineering outcome** | Reality (`current_published_assignments`) is database-enforced; every published child row provably immutable at the database layer; the publication transaction is idempotent, single-current, crash-safe |
| **Dependencies** | M3-001 accepted and merged (real sessions for the authz surfaces); parallel with M3-002 only if §6 disjointness re-verified |
| **Relevant documents** | [SPEC-05](../architecture/specs/SPEC-05-schedule-version-identity-and-publication.md) in full (identity vs snapshot §1; D-1a/D-1b §2 + §2.1; states + lock §3; D-15a/b/c §4 + §4.1; current selection §5; publication transaction §6; amendment/revert §6.1; cloning §7; **proof table §8 V-01..V-19**; downstream reconstruction §9); [06-data-architecture.md](../architecture/06-data-architecture.md) §3.2 schedule tables incl. V-21/FD-8 `current_published_assignments`, V-23 `credits` rekeying, CAR-020 (no `assignment_audit` table — history is snapshots joined by identity + audit events); [07-schedule-and-publication.md](../architecture/07-schedule-and-publication.md) (ten distinct schedule concepts — do not collapse); SPEC-07 (notification intents); SPEC-12 (cross-module unit of work); research report 18 SBX-018 |
| **Allowed files** | `apps/api/migrations/0009_*.sql` (+`0010` if a split is cleaner; renumbered by the second merger if M3-002 merges first); `apps/api/src/schedule/**` (new); `apps/api/src/db/schema.ts` (additive); `packages/contracts/src/schedule/**` (new); `apps/api/test/**` (incl. `test/support/schedule.ts` new and extensions to `test/support/staffing.ts` — **this packet owns staffing.ts for the parallel window**); `scripts/red-cases/**` (additions); `docs/evidence/EV-M3-PUBLICATION/**`; root config script entries |
| **Prohibited files** | `docs/architecture/**`, `docs/fable/**`, `schedulepoint-research/**`, `spikes/**`, `solver/**` (**no build/solver code — manual content only; manual scheduling is override, recovery, fixed-assignment input, and development-stage functionality, never described or positioned as the production mechanism — non-bypass rule 7**), `apps/web/**` (no UI this packet), `apps/api/src/{authn,rules,catalogue,profiles,audit}/**` except via existing public APIs, migrations `0001`–`0008`, `scripts/gates/**`, existing tests never weakened |
| **Required implementation — schema (migration 0009/0010)** | Per doc 06 §3.2 exactly: `schedule_periods` (group tenant, **no-overlap exclusion per group**), `schedule_versions` (full state set draft/in_review/approved/publishing/published/superseded/cancelled; `lock_state` single editability concept, CHECK-rejected on published; `is_current` partial unique **(D-16)**; never hard-deleted **(D-15c)**), `assignment_identities` (stable, spans versions, never deleted), `assignment_snapshots` (one version each, **(version_id, assignment_identity_id)** D-14, version-scoped overlap exclusion **D-1a** including across midnight, `is_pinned`, origin/provenance), `credits` (**V-23 keying**: identity + source_version + source_snapshot; credited membership may differ from assignee), `current_published_assignments` (**a real table, RLS FORCE**, `EXCLUDE` overlap constraint = **D-1b**, maintained inside the publication transaction), `version_supersessions` (append-only), schedule requirements (period-scoped demand rows consistent with doc 06 and FAD-16's weekday-demand defaults — requirements here are period instances, catalogue defaults stay M2's), **`groups.timezone` NOT NULL** (backfill for existing synthetic fixtures with a declared default, recorded in the migration comment; shift-local semantics resolve against it). D-15a/b/c triggers: child-row immutability under published/superseded parents, the **§4.1 permitted-column set** with transition gating, delete prohibition |
| **Required implementation — publication transaction (SPEC-05 §6)** | Period-scoped lock; approve → publishing → published; `is_current` swap; `current_published_assignments` delete-outgoing/insert-incoming inside the transaction (cross-period double-booking aborts publication — D-1b); **exactly one outbox event per publication** (D-17 idempotency key: replay publishes once); supersession recorded; crash recovery: a killed mid-publication process leaves no partial state and the reconciler returns the version to `approved` (V-16); simultaneous publications: exactly one winner, loser fails explicitly (V-12), different-membership different-period publications do not contend (V-15b) |
| **Required implementation — manual assignment + correction** | Manual assignment/removal/reassignment on **draft** versions only, capability-gated, audited, with origin/provenance and pins; manual overrides carry required reasons; published content is corrected **forward only**: clone → amend → publish (§6.1); revert = clone the target version and publish forward; no mutation path to any published row exists in the API (and the database would reject it regardless) |
| **Required implementation — affected-staff diff + notifications** | On publication, compute the difference against the previously current version per affected membership; write notification **intents** through the transactional outbox in the same unit of work; **a notification failure can never roll back a publication (I-11 — proven by a fault-injected red case)**; payload bodies never logged; synthetic destinations only |
| **Required implementation — the V harness** | The complete SPEC-05 §8 table **V-01..V-19** as an executable harness: every row implemented, byte-identity checks where the row demands them (sha256 of every published row, report and calendar-feed regeneration by `version_id`), the D-15b per-column matrix (V-17/18: **every column tested, none skipped**), transition-gating (V-19), each row's result individually reported. **SBX-018** declared with all nine contract fields and executed with falsifiability probes |
| **Tenant-isolation requirements** | All tables group (or org+group) tenant, FORCE RLS, composite FKs; cross-tenant/cross-group denial proven; SBX-004 sweep extends to all new tables (the count is now substantial — reported per table) |
| **Authorization requirements** | Period/version/assignment/publication operations capability-gated via SPEC-06; publish is a distinct capability from edit; allow AND deny per surface; freshness (I-19) proven on the publish path (a mid-flight revocation denies publication) |
| **Audit requirements** | Every state transition, manual assignment, override, publication, supersession audited in the same unit of work; **no `assignment_audit` table is created (CAR-020)** — history is snapshots-by-identity + audit events; chain verifies clean after the full harness |
| **Migration requirements** | up/down/up clean; down documented incl. trigger/constraint removal order; renumber-on-second-merge discipline |
| **UI / Accessibility** | none this packet (M3-004/005/006); axe gate stays green |
| **Tests** | Every state-machine transition (legal and illegal); D-1a incl. midnight-spanning; D-14; credits independence (credit moves without touching the assignment; voiding; reassignment); period overlap exclusion; timezone semantics of period boundaries; idempotency-key replay; concurrency (advisory-lock contention, V-12/V-15b shapes); crash recovery |
| **Red-case tests** | (1) direct `UPDATE` on a published snapshot raises (D-15a); (2) `DELETE` on a published version row raises (D-15c); (3) forbidden-column change on a published version raises (D-15b); (4) permitted column outside its transition raises (V-19 shape); (5) cross-period double-booking aborts the publishing transaction (D-1b); (6) outbox-write failure injected → publication rolls back atomically BUT notification-delivery failure after commit never unwinds it (I-11, both halves); (7) `current_published_assignments` drift (manual tamper) detected by the V-15c equality check; (8) with a D-15 trigger dropped in a rolled-back probe transaction, the V harness demonstrably fails (falsifiability) |
| **Named-condition proofs** | (1) the complete V-01..V-19 harness; (2) SBX-018; (3) I-11 non-rollback; (4) D-1b cross-period abort; (5) byte-identity of published history (V-04/08/11 shape). Standalone + package + full battery, all three reported |
| **Standalone verification** | The V harness file(s), SBX-018, every named proof, every new test file |
| **Acceptance criteria** | check 12/12 · red-cases all proven (incl. new) · fixture-regression green · SBX battery green, sweep extended, zero wrong-tenant rows · **V-01..V-19 all green, individually listed** · SBX-018 PASS with falsifiability · I-11 proof filed · audit chain clean · migration cycle clean · evidence complete · tree clean |
| **Commands to run** | install · check · red-cases · fixture-regression · SBX battery · V harness standalone · migration cycle · named proofs standalone |
| **Required evidence** | `docs/evidence/EV-M3-PUBLICATION/` — the V-row table with per-row results, SBX-018 artifacts, immutability trigger outputs, hash-comparison records, concurrency and crash-recovery logs, I-11 fault-injection record, INDEX.md |
| **Escalation conditions** | Any SPEC-05 row that cannot be implemented as written · any doc-06 schedule-table field that conflicts with SPEC-05 · the report/calendar-feed regeneration rows (V-02/09) if no report/feed generator exists yet — **pre-ruled: a minimal deterministic version-scoped rendering (not a product feature) is in scope solely to satisfy byte-identity; if even that seems to grow product surface, escalate** · schedule-requirements shape ambiguity against FAD-16 · timezone backfill default doubt · any isolation or immutability doubt (stop-and-report) |
| **Rollback instructions** | Single squash-merge revert; migration down documented; no earlier migration touched |
| **Fable acceptance checklist** | Re-run everything from clean · diff within globs · run the V harness myself and diff per-row results against the claim · attempt my own published-row mutation via psql as each application role (must raise) · replay a publication with a reused idempotency key myself · kill a publication mid-flight myself and verify recovery · verify V-17/18 covers literally every column (schema-diff cross-check) · verify no UI and no solver code · fixture-regression fresh seed · standalone sweep · verify the ten schedule concepts stayed distinct (naming review) |
| **Independent-review checklist** | Reviewer-authored probes: race two publications with hand-built clients; attempt D-15 bypass via every role, via triggers-disabled-in-transaction, via `TRUNCATE`, via FK cascade paths; attempt snapshot mutation through the credits path (V-23 keying — does a credit edit reach the snapshot?); verify D-1a midnight case with a crafted overnight pair; tamper `current_published_assignments` directly and check V-15c catches it; inject outbox failure at both points (pre-commit and post-commit) and verify the I-11 boundary; mutation-test the byte-identity check (flip one byte — caught?); verify the reconciler cannot resurrect a cancelled publication; attempt cross-group period/version access as every role |

**Independent second review: MANDATORY** (immutability/publication critical class).

---

## 6. Shared integration surfaces — pre-assigned before any parallel issuance

Binding on M3-002 and M3-003 (M3-001 runs alone). Any need to cross these lines mid-task is an **escalation, never an edit**; the orchestrator routes it to the integration packet. Substantive production-code composition never happens through unreviewed edits on main.

| Surface | Ownership |
|---|---|
| **Migrations** | M3-001 → `0007`. M3-002 → `0008`. M3-003 → `0009` (+`0010` if split). The parallel pair merges serialized; the **second merger rebases, renumbers its migration(s) to follow the first's, and re-runs the full battery + migration cycle before its merge is accepted** |
| **Contracts** | `packages/contracts/src/authn/**` (001) · `src/rules/**` (002) · `src/schedule/**` (003). No shared contract file is edited by two packets; a genuinely shared type = escalation → integration packet |
| **Fixtures** | MULTI baseline enforced-immutable (digest-controlled) — nobody touches it. Per-file `ownedMulti` everywhere. Support files: `test/support/authn.ts` (001) · `test/support/rules.ts` (002) · `test/support/schedule.ts` + **`test/support/staffing.ts`** (003). M3-002 needing staffing fixtures = escalation |
| **Build inputs** | Neither 002 nor 003 creates `build_runs` or any solver-invocation surface (M4). Compiled-rule artifacts (002) and period/requirement rows (003) become joint inputs only in M4 and in M3-004's validation display — any earlier join = integration packet |
| **Audit events** | Namespaces: `authn.*` (001) · `rules.*` (002) · `schedule.*` (003). The audit module itself is frozen to all three |
| **Outbox events** | `authn.*` (001) · `schedule.*` (003) · none (002). Outbox infrastructure frozen to all three |
| **UI** | `apps/web/src/authn/**` (001) · `src/rules/**` (002) · none (003). Shared shell/routing touchpoints: minimal diffs, individually justified; a conflicting shell edit is resolved by the integration packet, not by merge improvisation |
| **Evidence** | `docs/evidence/EV-M3-AUTHN/` (001) · `EV-M3-RULES/` (002) · `EV-M3-PUBLICATION/` (003); later packets get `EV-M3-<NAME>/` each |
| **Serialized merge order** | 001 alone. Then 002/003: first-accepted merges first; second rebases + renumbers + full battery re-run; orchestrator runs complete validation after **each** merge |
| **Integration-packet trigger (OPUS-M3-008 is mandatory regardless)** | Fires early if: a shared file need appears · a cross-packet type/fixture need appears · post-merge composition reveals cross-cutting behaviour (as M2-004 did) · any deliverable requires both packets' tables in one surface |

## 7. Revalidation gate before issuing M3-002 and M3-003

Reserved. Before those two packets issue, a dated note here records: M3-001's actual outcome and any packet deltas it forces; re-verification that the two allowed-glob sets are disjoint (`git`-level check, recorded); confirmation the fixture-regression and SBX baselines are green on the post-001 main. Issued only after that note exists.

> **Revalidation note — 2026-08-04, binding on M3-002 and M3-003 (post-OPUS-M3-001 acceptance at `6a01196`):**
>
> **M3-001 outcome and packet deltas it forces.** M3-001 accepted and merged; authn/sessions/invitation/activation are on main with migration `0007`, `TENANT_TABLES` now 31 (30 tenant + `users`), the `authn.*` audit namespace, two grant-only action keys (`identity.reset_mfa`, `identity.administer_sessions`), the `preauth` route-policy class, and FAD-18/19/20. Deltas the two follow-on packets inherit:
> - **Migration numbers advance by one.** M3-002 → `0008`, M3-003 → `0009` (+`0010` if split) as §6 already assigns — no change, but now confirmed against real state (`0007` is taken).
> - **Real sessions exist.** Both packets test against real authenticated sessions (the M2 injected-context surrogate is retired for HTTP-surface tests); the authn support seam is `apps/api/test/support/authn.ts` with `provisionAccount` (idempotent) and `FIXTURE_PASSWORD` — reuse it, do not fork it.
> - **The pre-auth policy class and the `authn.*`/`security.*` audit namespaces are frozen** to both packets exactly as the audit module is (§6). Rules use `rules.*`, schedule uses `schedule.*`.
> - No M3-001 change narrows or reshapes M3-002's rule scope or M3-003's SPEC-05 scope; the packets stand as written in §§4–5.
>
> **Disjointness re-verified (git-level, recorded).** M3-002 allowed globs (`apps/api/src/rules/**`, `packages/domain/src/rules/**`, `packages/contracts/src/rules/**`, `apps/web/src/rules/**`, `solver/corpus/**`, migration `0008`, `test/support/rules.ts`, additive gate for the unmapped-node check) and M3-003 allowed globs (`apps/api/src/schedule/**`, `packages/contracts/src/schedule/**`, migrations `0009`/`0010`, `test/support/schedule.ts` + `test/support/staffing.ts`) share **no path**. `schema.ts` and `packages/contracts/src/index.ts` are additive-only barrels each edits under its own namespace — a genuine collision there is an escalation to the integration packet, not a merge. `test/support/staffing.ts` is M3-003-owned for the window (§6); M3-002 needing it is an escalation. Confirmed no overlap by inspection of §4/§5 against the merged tree at `6a01196`.
>
> **Baselines green on post-001 main.** At `6a01196`: `corepack pnpm check` 12/12 + 965 tests, `corepack pnpm red-cases` 14/14, all three documentation validators green, fixture-regression 75/75 and the SBX battery 5/5 (0 blocked / 0 vacuous) verified on the accepted branch and re-confirmed on main. The two packets branch from this commit.
>
> **Both issued 2026-08-04 in parallel** under this note: `opus/m3-002-rules` (worktree `.worktrees/m3-002`) and `opus/m3-003-publication` (worktree `.worktrees/m3-003`), derived DB ports, independent second review mandatory for each, serialized merge with the second merger rebasing + renumbering its migration.

## 8. Remaining M3 packet families — pre-declared, to be finalized here before issuance

M3 does not close after §§3–5: the roadmap outcome requires the experiences below, each a full packet in this document (same field set, same critical-class review) finalized after the first three tasks are accepted and their real surfaces are known. Planned seams (subject to refinement at finalization, never to scope reduction):

- **OPUS-M3-004 — Scheduler schedule-authoring experience:** period administration; demand/requirement authoring; virtualized scheduling grid **plus accessible tabular/list alternative**; cell editor; manual assignment/removal/reassignment; credits; provenance; pins; validation display (incl. compiled-rule results — first sanctioned rules×schedule join); eligibility (owns the absent-vs-empty ruling, §2.11); conflicts; incomplete-demand indicators; stale-edit handling; I-13 explicit Save; loading/empty/error/denial/narrow-viewport (320px) states; **and the remaining 27 per-node rule parameter editors** (assigned 2026-08-04 at M3-002 acceptance — the API/model/DB accept all 30 kinds; the UI ships 3, disclosed on-page).
- **OPUS-M3-005 — Publication and version-management experience:** publication review with blocking validation results; confirmation friction; published-schedule view; version history; version comparison; affected-staff diff display; forward correction; revert-by-publishing-forward; immutable-history display; publication audit display where authorized.
- **OPUS-M3-006 — Staff-facing schedule views:** personal published schedule; daily schedule/assignment sheet; accessible calendar **and** tabular alternatives; timezone-correct rendering incl. overnight shifts; mobile layout; keyboard navigation; programmatic status and error messages; owns the calendar-key edge ruling (§2.10).
- **OPUS-M3-007 — Group settings and site attribute:** request-until, picklist-access, timezone administration; `locations` + `site_label` under the PO-DEC-01 default with both migration directions modelled as fixtures (§2.4–7).
- **OPUS-M3-008 — Integration and hardening (mandatory):** composition of rules, periods, assignments, profiles, qualifications, catalogue, audit, authorization, entitlements, outbox, and UI; **SPEC-05 §6 step 06 HARD-rule re-validation at publication** (carried from M3-003 — the rules×publication join §6 forbade mid-parallel; recorded 2026-08-04); current-qualification checks and qualification expiry against assignment date; entitlement/authorization freshness; cross-group and cross-tenant denial at the composed surfaces; publication concurrency re-proof on the composed system; duplicate-request handling; request-budget evidence; migration and rollback verification across `0007`+; NR-14 redesign (§2.9); **the audit-verification group-scope false-alarm** (M3-003 review N-8: `app_verify_audit_chain` from a group-scoped context reports spurious gaps — the audit module was frozen to M3 packets; recorded 2026-08-04); SPEC-14 automatable cells (§2.12); applicable QA-SCH manual-path battery; independent adversarial probes. Standing note (M3-003 review N-6, pre-existing): `pnpm red-cases` requires a built `apps/web/dist` for the two network-guard GREEN arms — run after `gate:build` in fresh worktrees.

## 10. Finalized packets M3-004..M3-008 (2026-08-04, after the first three acceptances; supersedes the §8 outlines)

**Execution waves.** Wave 1 in parallel: **M3-004** (scheduler authoring experience) and **M3-007** (settings + site) — globs disjoint per §10a. Wave 2 in parallel after wave 1 merges: **M3-005** (publication/version-management experience) and **M3-006** (staff views) — globs disjoint per §10a. **M3-008** last, alone, after everything else is merged. All five are critical class: independent second review with reviewer-authored probes mandatory. Serialized merges per wave; second merger rebases and re-runs the full battery. Base for wave 1: `a616cec`.

### 10a. Shared-surface pre-assignment for the waves

| Surface | Ownership |
|---|---|
| Migrations | **Only M3-007 migrates** (`0010_group_settings_locations.sql`). M3-004/005/006 have NO migration; a schema need in those packets is an escalation |
| `apps/api/src/schedule/**` (M3-003's service) | **Frozen to all five UX packets.** Routes call the existing service; a missing service function is an **escalation** (routed to M3-008 or ruled) — never an in-packet edit |
| API routes | `schedule-authoring.route.ts` (004) · `settings.route.ts` (007) · `schedule-publication.route.ts` (005) · `schedule-views.route.ts` (006) — one new file each; `server.ts` registration lines minimal and composed at merge |
| Contracts | `packages/contracts/src/schedule/authoring.ts` (004) · `src/settings/**` (007) · `src/schedule/versions.ts` (005) · `src/schedule/views.ts` (006); barrel adds additive |
| Web | `apps/web/src/schedule/**` + `apps/web/src/rules/**` (004) · `src/settings/**` (007) · `src/publication/**` (005) · `src/my-schedule/**` (006); router/shell touchpoints minimal, individually justified, composed at merge |
| Domain vocabulary (`event-names.ts`, `authz/catalogue.ts`) | 007 may add `settings.*` events + settings action keys; 005/006 may add read-action keys **only if missing** — every addition additive, proposed in the return report, FAD-ratified at acceptance (the established class). 004 adds **nothing** (it drives existing `schedule.*`/`rules.*` actions); needing one is an escalation |
| Fixtures | 007 creates `test/support/settings.ts`; the others use existing support files read-only (extension = escalation). MULTI baseline stays enforced-immutable |
| Sweep floor | Only 007 adds tables (`locations`); it raises the floor. Wave-2 packets raise nothing |
| Budgets | `budgets.json` additive rows only, every interaction recorded |
| Evidence | EV-M3-AUTHORING-UX (004) · EV-M3-SETTINGS (007) · EV-M3-PUBLICATION-UX (005) · EV-M3-STAFF-VIEWS (006) · EV-M3-INTEGRATION (008) |

### 10b. OPUS-M3-004 — Scheduler schedule-authoring experience (wave 1) — **ACCEPTED & MERGED 2026-08-05** (20c0c13; REVISE on 4 reviewer-probed concurrency/enforcement blockers → closed; FAD-23; task log)

| Field | Content |
|---|---|
| **Objective** | The scheduler-facing authoring surface over M3-003's service: schedule-period administration; demand/requirement authoring; a **virtualized scheduling grid plus a first-class accessible tabular/list alternative** (SP-E; the alternative is not a degraded mode); cell editor; manual assignment/removal/reassignment; credits (draft-only moves); provenance display; pins; conflicts display; incomplete-demand indicators (requirements vs assignments); stale-edit handling (server-authoritative CAS per PO-DEC-18 — a stale edit is refused and re-fetched, never silently merged); I-13 explicit Save everywhere; loading/empty/error/denial/narrow-viewport (320px) states; **the 27 remaining per-node rule parameter editors** (every one of the 30 kinds authorable end-to-end via UI, each with an e2e author→list→edit round trip at least at smoke level; the on-page quantification updated); **validation display = recorded conflicts + structural validation only** (no rule execution against drafts — that is the step-06 class, M3-008; displaying it here would fake a capability) |
| **Owns rulings** | Eligibility absent-vs-empty (§2.11): propose with rationale in the return report; Fable ratifies as a FAD before merge |
| **Allowed** | `apps/api/src/http/routes/schedule-authoring.route.ts` (new) + minimal `server.ts` registration; `packages/contracts/src/schedule/authoring.ts` (new) + barrel line; `apps/web/src/schedule/**` (new), `apps/web/src/rules/**` (extend); router/shell minimal; `apps/api/test/**`, `apps/web/e2e/**`; `budgets.json` additive; `scripts/red-cases/**` additions; `docs/evidence/EV-M3-AUTHORING-UX/**` |
| **Prohibited** | Everything else — expressly: migrations, `apps/api/src/schedule/**`, `apps/api/src/{authn,rules,catalogue,profiles,audit}/**`, domain vocabulary, `scripts/gates/*.mjs`, other packets' surfaces. Existing tests/gates never weakened |
| **Key proofs** | I-13 (fill-everything-don't-save = zero requests, red-cased); stale-edit CAS (concurrent editor loses explicitly, refetch shown); grid virtualization does not break the accessible alternative (both render the same data — equality asserted); every draft mutation lands audited via the existing service; denial states for a member without `schedule.version.edit`; axe both viewports; keyboard end-to-end journey (create period → author requirement → assign → save); budgets recorded |
| **Acceptance** | check 13/13 · red-cases all proven (incl. new) · fixture-regression green · SBX battery green · e2e journeys green · evidence with INDEX · tree clean. Standalone + package + battery for named proofs. Escalation conditions per the standing list plus: any service-function gap; any schema need; the eligibility ruling contested by evidence |

### 10c. OPUS-M3-007 — Group settings and site attribute (wave 1) — **ACCEPTED & MERGED 2026-08-04** (c3e1baa; review clean of blockers; FAD-24; task log)

| Field | Content |
|---|---|
| **Objective** | Migration `0010`: `locations` per doc 06 §3.2 row 237 (group tenant, `name`, **`site_label?` free-form attribute per CAR-021 — no FK, no site table**, `address?`, `timezone?`, FORCE RLS, composite FKs) and **typed, CHECK-constrained group-settings columns** (no free-form JSON): request-until configuration (storage + authoring + validation; **enforcement recorded at M5**), picklist-access configuration (storage + authoring; **enforcement recorded at M9**), timezone administration (audited change; a change while published schedules exist is permitted with an explicit audited warning — the display-semantics question is recorded to the exit report). Authoring API + accessible UI for all of it; **both site-migration directions modelled as fixtures** (CAR-021 §3.2a: attribute→entity and entity→attribute expressed as test fixtures proving the data can move — the alternative migration is NOT built); **no site administration surface, no site-scoped API, no site-specific workflow** (non-bypass rule 12) |
| **Allowed** | `apps/api/migrations/0010_group_settings_locations.sql` (new); `apps/api/src/settings/**` (new) + `settings.route.ts` + minimal `server.ts` line; `apps/api/src/db/schema.ts` additive; `packages/contracts/src/settings/**`; `apps/web/src/settings/**`; router/shell minimal; `packages/domain/src/audit/event-names.ts` (`settings.*` additive) + `authz/catalogue.ts` (settings action keys, additive, FAD-ratified); `test/support/settings.ts` (new); tests/e2e; budgets additive; red-cases additions; `docs/evidence/EV-M3-SETTINGS/**` |
| **Prohibited** | Everything else; migrations 0001–0009 immutable; no schedule/rules/authn surfaces; existing tests/gates never weakened |
| **Key proofs** | Settings mutations capability-gated (allow AND deny), audited, group-scoped (cross-group denial); locations swept by SBX-004 (floor raised); both-direction site fixtures round-trip; timezone change audited with the warning surfaced; migration 0010 up/down/up clean; I-13/I-10/axe/keyboard/320px on the settings UI |
| **Acceptance** | Standing battery + the proofs above + evidence with INDEX + tree clean. Escalations: any settings shape doc 06 cannot sanction; any temptation toward a site entity; any enforcement semantics that cannot wait for M5/M9 |

### 10d. OPUS-M3-005 — Publication and version-management experience (wave 2) — **ACCEPTED & MERGED 2026-08-05** (112b8c4; review zero-blocking; FAD-26; task log)

| Field | Content |
|---|---|
| **Objective** | Publication review (diff preview before publish; blocking items = recorded `hard-breach` conflicts + structural validation; graduated type-to-confirm friction per SP-E); publication confirmation invoking the real CAS + idempotency-key flow (client generates and retains the key; a retry replays, red-cased); published-schedule view; version history; version comparison (identity-keyed diff via the existing pure diff module); affected-staff diff display; forward correction (clone→draft handoff to the authoring surface); revert-by-publishing-forward with its own friction; immutable-history display (superseded versions readable, marked, never editable); publication audit display **where authorized** (if no audit read API exists, that is an escalation, not an in-packet audit-module edit) |
| **Allowed** | `schedule-publication.route.ts` (new) + minimal registration; `packages/contracts/src/schedule/versions.ts` (new) + barrel; `apps/web/src/publication/**` (new); router/shell minimal; read-action key additions only if missing (additive, FAD-ratified); tests/e2e; budgets additive; red-cases additions; `docs/evidence/EV-M3-PUBLICATION-UX/**` |
| **Prohibited** | Everything else; no service edits; no migrations; `schedule.publish`/`schedule.revert` remain grant-only — the UI must render the deny state for a scheduler without the grant (proven) |
| **Key proofs** | Publish requires the grant (deny rendered + server-denied, both asserted); the CAS loser's explicit failure rendered; idempotent replay (double-click, retry-after-timeout) publishes once — client-side red case; history immutability rendered truthfully (no edit affordance on published/superseded); diff display equals the pure-function diff (equality asserted); axe/keyboard/320px/budgets/I-13 |

### 10e. OPUS-M3-006 — Staff-facing schedule views (wave 2) — **ACCEPTED & MERGED 2026-08-05** (57685ab; review zero-blocking; FAD-25 closes carried item §2.10; task log)

| Field | Content |
|---|---|
| **Objective** | Personal published schedule (current version only, self-scoped); daily schedule/assignment sheet **per doc 07's distinction** (a read of the published master schedule for a date — NOT the picklist `daily_assignments` module, which is M9); accessible calendar view AND tabular alternative (both first-class); timezone-correct rendering against the group timezone including overnight shifts (a shift crossing midnight appears on both days, labelled); mobile layout; keyboard navigation; programmatic status and error messages (SP-HR); loading/empty/error/denial states. **Owns the calendar-key edge ruling** (§2.10): propose with rationale; Fable ratifies before merge; surfaced in the exit report. In-app views only — feed/token export is out of scope (recorded, owned by the notification/calendar milestone) |
| **Allowed** | `schedule-views.route.ts` (new) + minimal registration; `packages/contracts/src/schedule/views.ts` (new) + barrel; `apps/web/src/my-schedule/**` (new); router/shell minimal; self-scoped read-action key only if missing (additive, FAD-ratified); tests/e2e; budgets additive; red-cases additions; `docs/evidence/EV-M3-STAFF-VIEWS/**` |
| **Prohibited** | Everything else; no service edits; no migrations; a staff member must never see another member's non-published or cross-group data (proven both at API and UI) |
| **Key proofs** | Self-scoping (member A cannot fetch member B's view; cross-group denial byte-identical); published-only (a draft version is invisible to staff); overnight rendering correctness (fixture with a 22:00→06:00 shift asserted on both days); timezone correctness under a non-UTC group timezone fixture; axe/keyboard/320px/mobile/budgets |

### 10f. OPUS-M3-008 — Integration and hardening (last, alone) — **ACCEPTED & MERGED 2026-08-05** (dfa717f; REVISE closed B-1/B-2 + 8 fold-ins + O-1; FAD-27; task log). Originally issued 2026-08-05 (branch `opus/m3-008-integration`, worktree `.worktrees/m3-008`; base = the wave-2 close-out commit). **Docket accumulated during the milestone (binding, in addition to the table below):** the audit READ API + publication audit display completion (FAD-26) · the holiday-calendar read/write key split (FAD-25 / D-10) · the period-length DB CHECK (M3-004 fold-in N-4's database half, migration 0011 pre-approved for these two schema items ONLY — anything further is an escalation) · the settings-page consolidation (M3-007 note) · the preview-port worktree derivation (E-2 class) · the SBX-004 stale "coverage gap" line refresh (M3-007 NB-6) · the PostgresError→404 mapping follow-up (M3-007 NB-3) · the plain-anchor section nav (M3-005 N4)

| Field | Content |
|---|---|
| **Objective** | The composed system proven: **SPEC-05 §6 step 06 HARD-rule re-validation at publication** (the sanctioned rules×publication join — compiled HARD rules evaluated against the version content at publish time, a breach blocks publication; this is validation of manual content, NOT solver work); current-qualification checks and **qualification expiry against assignment date** at the authoring and publication surfaces; entitlement/authorization freshness re-proofs on composed surfaces; cross-group/cross-tenant denial at every composed surface (sweep re-run, floor confirmed); publication concurrency re-proof on the composed system; duplicate-request handling; request-budget evidence complete; migration and rollback verification across `0001..0010`; **NR-14 redesign** (§2.9 — evidence writes to scratch by default, `--refresh` to update tracked artifacts; the restore discipline retires); **the audit-verification group-scope false-alarm fix** (the audit module is UNFROZEN for exactly this: `app_verify_audit_chain` verifying correctly from any context or refusing group-scoped invocation explicitly — never a spurious gap report); SPEC-14 automatable cells executed (real-AT cells stay honestly unclaimed → M6); **applicable QA-SCH manual-path battery** executed and filed; consolidated real-browser evidence for the M3 exit (authoring, publication, staff views across desktop/mobile/320px/keyboard/states); independent adversarial probes on the composed system |
| **Allowed** | Broad by necessity, still bounded: `apps/api/src/**` (composition seams, each edit justified per file in the report), `apps/web/src/**` (composition only), contracts, tests/e2e, `scripts/sbx/**` + `scripts/red-cases/**` (NR-14 redesign + additions), audit module **only** for the named fix, budgets, `docs/evidence/EV-M3-INTEGRATION/**`. Migrations only if the step-06 join requires storage (escalate first) |
| **Prohibited** | No solver work (step 06 evaluates declared HARD rules against existing content — bounded, deterministic, no search); no capability scope change; no gate weakened; no UX regression |
| **Key proofs** | Step-06: a draft violating a compiled HARD rule cannot publish (blocked, explained, red-cased with the rule disabled = publishes — falsifiability); qualification-expiry-vs-assignment-date (a holding expiring mid-period blocks exactly the assignments on dates after expiry); composed concurrency (publication race re-run with the full UI stack live); QA-SCH filed; NR-14 redesign red-cased (a tracked artifact no longer changes on a plain battery run; `--refresh` updates it) |

## 11. Entry gate, branches, review, evidence (standing; applies to every packet above)

Branches `opus/m3-00X-<slug>` from current main in worktrees `.worktrees/m3-00X`, derived DB ports (standing discipline), squash-merge by Fable only, separate commit per accepted task, complete validation (validators + check + red-cases + fixture-regression + SBX battery) after each merge. Every packet: independent second review by a separate sub-agent with reviewer-authored probes; blocking findings repaired then delta-reviewed; the orchestrator re-runs every acceptance command from clean before accepting. Escalation is success behaviour. A passing test without a demonstrated red or mutation case is not sufficient evidence for a load-bearing invariant. Nothing here weakens any existing test, invariant, gate, accessibility requirement, tenant control, authorization control, audit requirement, or privacy restriction.
